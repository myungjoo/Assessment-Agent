# Assessment-Agent — Master Plan

> 이 파일은 planner sub-agent가 점진적으로 채우고 정련한다. 부트스트랩 시점에는 phase 골격만 존재한다.
> 자세한 단위 작업은 [docs/tasks/](tasks/) 의 T-NNNN 파일을 참조.
> 각 phase 의 bullet 은 [README.md](../README.md) 의 지시사항과 [docs/requirements.md](requirements.md) 의 REQ-NNN 매핑을 cover 해야 한다.
> 완료된 phase 의 본문은 [PLAN_archive.md](PLAN_archive.md) 로 분리 보관 — planner hot read 절감.

상태는 [STATE.json](STATE.json) 의 `phase` 필드와 동기화되어야 한다.

---

## Phase P0 — Bootstrap

**완료 (2026-05-24 00:34 KST)** — 자동 루프 골격 + main CI lint/build/test 검증. 본문은 [PLAN_archive.md](PLAN_archive.md#phase-p0--bootstrap) 참조.

---

## Phase P0.5 — Test·CI infra hardening (README 110-114 / §3.2 강제층)

**완료 (2026-05-24 19:02 KST)** — T-0007 spec-presence (+ T-0012 patch) / T-0008 coverage threshold 50% / T-0009 smoke / T-0010 e2e 모두 main 진입. CI 8 step 으로 R-110~R-114 multi-layer 강제. 본문은 [PLAN_archive.md](PLAN_archive.md#phase-p05--testci-infra-hardening-readme-110-114--§32-강제층) 참조.

---

## Phase P1 — Architecture (MVA)

**완료** — 4 architecture document (requirements.md kind 컬럼 / deployment.md / components.md / modules.md) 모두 main 진입 + ADR-0001 / ADR-0002 / ADR-0003 ACCEPTED. 본문은 [PLAN_archive.md](PLAN_archive.md#phase-p1--architecture-mva) 참조.

---

## Phase P2 — Use case decomposition

목표: README + P1 architecture 를 기반으로 각 use case 를 1 파일씩 분해. 이후 phase 들의 task 가 use case 를 cover 하는 형태로 진행.

- [x] Use case 발굴: README 각 섹션 → [docs/use-cases/](use-cases/) 의 `UC-NN-*.md`. 각 use case 는 actor (SuperAdmin / Admin / User / Scheduler / Reviewer Agent) / 트리거 / 흐름 / 데이터 / NFR (성능·보안) / 관련 REQ 명시. **P2-Entry 진행: [docs/use-cases/INDEX.md](use-cases/INDEX.md) 8 UC backbone 박제 완료 (T-0019, PR-18)**. UC-01 본문 분해 ([UC-01-evaluation-execution.md](use-cases/UC-01-evaluation-execution.md), T-0020) 완료. UC-02 본문 분해 ([UC-02-evaluation-query.md](use-cases/UC-02-evaluation-query.md), T-0022) 완료. UC-03 본문 분해 ([UC-03-person-crud.md](use-cases/UC-03-person-crud.md), T-0023) 완료. UC-04 본문 분해 ([UC-04-account-auth.md](use-cases/UC-04-account-auth.md), T-0024) 완료. UC-05 본문 분해 ([UC-05-llm-config.md](use-cases/UC-05-llm-config.md), T-0025) 완료. UC-06 본문 분해 ([UC-06-evaluation-delete-reeval.md](use-cases/UC-06-evaluation-delete-reeval.md), T-0026) 완료. UC-07 본문 분해 ([UC-07-export-import.md](use-cases/UC-07-export-import.md), T-0027) 완료. UC-08 본문 분해 ([UC-08-permission-denied.md](use-cases/UC-08-permission-denied.md), T-0028) 완료. **P2 UC 본문 분해 8/8 closure (UC-01 ~ UC-08, T-0020 ~ T-0028).**
- [x] 각 use case 가 P1 component view 의 어느 component 를 거치는지 매핑 (sequence diagram 또는 텍스트). UC-01 cover ([UC-01](use-cases/UC-01-evaluation-execution.md) §5 sequence + §9 component/module mapping). UC-02 cover ([UC-02](use-cases/UC-02-evaluation-query.md) §5 sequence + §9 component/module mapping). UC-03 cover ([UC-03](use-cases/UC-03-person-crud.md) §5 sequence + §9 component/module mapping). UC-04 cover ([UC-04](use-cases/UC-04-account-auth.md) §5 sequence + §9 component/module mapping). UC-05 cover ([UC-05](use-cases/UC-05-llm-config.md) §5 sequence + §9 component/module mapping). UC-06 cover ([UC-06](use-cases/UC-06-evaluation-delete-reeval.md) §5 sequence + §9 component/module mapping). UC-07 cover ([UC-07](use-cases/UC-07-export-import.md) §5 sequence + §9 component/module mapping). UC-08 cover ([UC-08](use-cases/UC-08-permission-denied.md) §5 sequence + §9 component/module mapping). **P2 UC 매핑 8/8 closure.**
- [x] **Use case 인벤토리 검증**: requirements.md 의 모든 functional REQ 가 1+ use case 로 cover 되는지 확인. 빠지면 use case 추가. **[REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) ([T-0029](tasks/T-0029-uc-inventory-audit.md)) 완료 — gap 1 건 (REQ-004 사용자 지정 기간 임의 평가문, UC-09 신설 또는 UC-01 확장 권장 — follow-up task T-0030+ 책임; 2026-08-03 재판정: T-0030 · T-0031 은 각각 api.md · data-model.md 산출로 status DONE 이고 두 task 본문이 REQ-004 을 Out of Scope 로 재이관해 이 지목은 소진 상태이며 승계 task 는 미생성 — 근거는 [REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) §12.10 참조)**. uc-covered 48 / cross-cutting 4 / infrastructure 13 / gap 1 = 66. 2026-08-03 재판정: 재판정 후보 17 row 전건 (T-1405 설계 + T-1406 ~ T-1408 실판정) 결과가 유지 12 / 기록만 5 / 분류 변경 0 이라 위 4 값 · 합 66 은 무변 — 대조 실측값은 [REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) §12.9 참조. 2026-08-03 재분류 (T-1413): REQ-004 는 UC-09 신설 (T-1411) + INDEX 등록 (T-1412) 으로 gap → uc-covered 전이해 위 4 값이 uc-covered 49 / cross-cutting 4 / infrastructure 13 / gap 0 = 66 으로 갱신 — 근거는 [REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) §12.11 · §12.12 참조 (앞부분의 옛 4 값 · gap 1 건 서술은 T-0029 시점 기록이라 보존).
- [x] **API contract 초안** — [docs/architecture/api.md](architecture/api.md). use case 흐름 기반으로 HTTP endpoint 목록. 구체 schema 는 P3 에서. **T-0030 으로 박제 완료 — [api.md](architecture/api.md) (8 UC × 약 35 endpoint × 9 resource prefix, MVA 수준 — METHOD/path/UC/description/auth tier 5 컬럼 표 + 표준 status code policy + UC §5 step cross-reference).**
- [x] **데이터 모델 초안** — [docs/architecture/data-model.md](architecture/data-model.md). 핵심 entity (Person / ServiceIdentity / Assessment / Contribution / Summary / Group / Part / LlmProviderConfig 등) 의 conceptual model. 테이블 컬럼은 P3. **T-0031 으로 박제 완료 — [data-model.md](architecture/data-model.md) (10 entity × 5 컬럼 표 + 10 관계 mermaid ER + raw 미저장 invariant § 4 + cross-cutting field § 5 + REQ coverage 20/20 § 6). 본 task 머지로 Phase P2 fully complete.**
- [x] **디렉토리 구조 정의** — [docs/architecture/directory.md](architecture/directory.md). NestJS 표준 + module view 와 mapping. T-0021 으로 박제 완료.

각 항목은 planner가 1~3개의 T-NNNN task로 분할한다. 모두 commitMode: direct 또는 pr (use case 는 doc → direct; api/data-model 은 ADR 동반 시 pr).

**Phase P2 fully complete** — T-0031 머지 시점. 5 entry artifact (UC backbone INDEX.md / 8 UC 본문 / api.md / data-model.md / directory.md) + REQ coverage audit 모두 박제. 다음 자연 phase 진입은 **P3 Domain core** (Persistence layer / Auth/RBAC / 인원 관리 / 평가 결과 저장 모델). P3 의 첫 task 는 planner 가 다음 호출에서 결정.

---

## Phase P3 — Domain core

목표: 외부 통합 없이 자체적으로 돌릴 수 있는 도메인 핵심.

**P3 entry document**: [p3-implementation-plan.md](architecture/p3-implementation-plan.md) ([T-0032](tasks/T-0032-p3-entry-implementation-plan.md)) — 10 bullet ↔ 약 8 T-NNNN task 시퀀스 (T-0033 ~ T-0040) 매핑 + 의존성 graph + ADR 후보 5 항목 + 인간 승인 게이트 (T-0033 `pnpm add prisma @prisma/client pg`).

- [x] 평가 대상 인원 관리 (CRUD, group, deactivate/activate — 휴직 시 숨김). **implemented-on-main**: [`person.service.ts`](../src/user/person.service.ts) `PersonService` CRUD(create/findAll/findOne/update/remove) + deactivate/reactivate(휴직 숨김, activeOnly 조회) 를 [`person.controller.ts`](../src/user/person.controller.ts) 5 endpoint 가 forward. [ADR-0002](decisions/ADR-0002-db.md).
- [x] **서비스별 ID 매핑** — github.com / github.sec.samsung.net / github.ecodesamsung.com / confluence.sec.samsung.net 등 각 서비스의 ID 보유, 일부 NULL 허용 (R-48). **implemented-on-main**: [`schema.prisma`](../prisma/schema.prisma) `model ServiceIdentity`(1 Person ↔ N `service`/`externalId` 매핑, `@@unique([personId, service])` 로 서비스당 1 매핑 invariant, 미등록 서비스는 row 부재로 NULL 표현). [ADR-0002](decisions/ADR-0002-db.md).
- [x] **Primary key 역할 ID 지정** — 서비스 중 1개의 ID 를 기준 식별자로 (예: confluence.sec.samsung.net ID) (R-47). **implemented-on-main**: [`schema.prisma`](../prisma/schema.prisma) `ServiceIdentity.isPrimary Boolean @default(false)`(REQ-024 — 1 Person 당 정확히 1 primary 의 schema-level 표식, service-layer 강제). [ADR-0002](decisions/ADR-0002-db.md).
- [x] **Group 정책** — 한 인원은 임의 group 다중 소속 가능, 단 조직도 파트는 정확히 1개 (R-51). **implemented-on-main**: [`group.service.ts`](../src/user/group.service.ts) `GroupService`(N:M 다중 group 소속 middle-table) + [`add-member.dto.ts`](../src/user/dto/add-member.dto.ts) via [`group.controller.ts`](../src/user/group.controller.ts); 조직도 파트 정확히 1개는 `Person.partId` 직접 FK([`part.service.ts`](../src/user/part.service.ts) `PartService.findPersonsByPartId` 1 query navigation). [ADR-0002](decisions/ADR-0002-db.md).
- [x] 평가 결과 저장 모델 (commit/document 단위, 일/주/월 요약). **implemented-on-main**: [`schema.prisma`](../prisma/schema.prisma) `model Assessment`(Person × period(일/주/월) × scope(commit/document/aggregate) cross product, 평가 결과값만 보유) + `model Contribution`(단위 기여) + `model Summary`(일/주/월 요약). [ADR-0006](decisions/ADR-0006-assessment-data-model.md).
- [x] **🔥 Raw data 저장 금지 (R-59)** — code commit 본문·문서 변경 본문 등 raw 는 저장하지 않고 평가된 결과 (난이도/기여도/양/평가문) 만 보유. ADR-필수 항목. **implemented-on-main**: [`schema.prisma`](../prisma/schema.prisma) `model Assessment` 이 commit message 본문/diff/문서 본문/Confluence page 본문 컬럼을 **정의하지 않는다** — "저장 안 함"을 application-layer 검증이 아닌 schema 차원(컬럼 부재)으로 강제(자리가 없으면 저장 불가). [ADR-0006](decisions/ADR-0006-assessment-data-model.md) §Decision 4 schema-level 강제.
- [x] **상대 비교 가능 데이터 구조** — 개발자 간 동일 metric 비교가 가능한 형태 (R-63). **implemented-on-main**: [`schema.prisma`](../prisma/schema.prisma) `Assessment.contributionScore Decimal`(정규화 수치 — REQ-036 상대 비교 가능 형태, Float 부동소수 오차 회피; `Contribution.contributionScore`/`Summary.metricScore` 동형). [ADR-0006](decisions/ADR-0006-assessment-data-model.md).
- [x] Persistence layer (DB 는 ADR-0002 에서 이미 결정됨 — 본 phase 에서 구현). **implemented-on-main**: [`prisma.service.ts`](../src/persistence/prisma.service.ts) `PrismaService` + [`persistence.module.ts`](../src/persistence/persistence.module.ts)(PostgreSQL persistence layer, 각 domain service 의 repository 주입 경로). [ADR-0002](decisions/ADR-0002-db.md).
- [x] Auth/RBAC 모델 (SuperAdmin/Admin/User) — 첫 로그인 SuperAdmin 지정, Admin→User 변경은 SuperAdmin만, 본인 self-demote 금지 (R-84). **implemented-on-main**: [`user.service.ts`](../src/user/user.service.ts) `signup`(첫 등록 user `countAll === 0` 분기 SuperAdmin 자동 지정) + `updateRole`(REQ-044 5 invariant, self-demote 차단 `ForbiddenException`) + [`roles.guard.ts`](../src/auth/roles.guard.ts) `RolesGuard`/[`roles.decorator.ts`](../src/auth/roles.decorator.ts) `@Roles`. [ADR-0002](decisions/ADR-0002-db.md).
- [x] User read-only 권한 범위 명시 — 조회·sort·filter 만 (R-86). **implemented-on-main**: [`user-response.dto.ts`](../src/user/dto/user-response.dto.ts) 응답 whitelist(User 는 조회 축만 노출, mutation 필드 미노출) + [`roles.guard.ts`](../src/auth/roles.guard.ts) `RolesGuard` 가 write endpoint 를 Admin 이상으로 제한. [ADR-0002](decisions/ADR-0002-db.md).
- [x] **[테스트 품질] unit branch coverage 완성** — `person.service.ts` L120 `update()` P2002 발생 시 `patch.email` 이 undefined 인 케이스 unit test 추가 (현재 branch 96.66% → 100% 목표). R-112 negative case 충분 cover 의무 이행. **(완료 — `src/user/person.service.spec.ts` L407~428 에 dedicated test 존재, branch 100% 도달. T-0153 doc-sync 확인.)**
- [x] **[테스트 품질] smoke test domain endpoint 확장** — 현재 smoke 가 `GET /` 만 커버. `/api/persons` CRUD (POST·GET·PATCH·DELETE) + 향후 Group/Part endpoint 에 대한 bootstrap smoke 추가. AppModule mock-DB 방식으로 실 DB 없이 supertest 실행. **(완료 — `test/smoke/{persons,groups,parts}.smoke-spec.ts` 등 domain smoke spec 존재, T-0043→T-0053 real DB cutover 로 진화. T-0153 doc-sync 확인.)**
- [x] **[테스트 품질] e2e test domain endpoint 확장** — 현재 e2e 가 `GET /` HTTP contract 만 검증. `/api/persons` 의 status code + response body shape (DTO contract) + 4xx error shape 를 e2e-spec 으로 커버. R-113 e2e 의무 이행. **(완료 — `test/e2e/` 에 persons/groups/parts/users/auth/assessments/contributions/summaries 등 9 e2e spec 존재. T-0153 doc-sync 확인.)**
- [x] **[테스트 품질] CI smoke/e2e real PostgreSQL 전환** — 현재 T-0043 smoke / T-0044 e2e 는 [test/helpers/prisma-mock.ts](../test/helpers/prisma-mock.ts) 의 PrismaService override 로 mock-DB 사용. 사용자 정책 변경 (2026-05-26): mock 이 아닌 real PostgreSQL 을 CI 안에서 직접 띄워서 통합 검증. 구현 방향: `.github/workflows/ci.yml` 에 `services: postgres:` container (또는 `apt install postgresql` + `pg_ctlcluster start`) + `DATABASE_URL` env var 셋업 + `pnpm prisma migrate deploy` step 추가 + smoke/e2e 가 real DB 에 query 발행하도록 PrismaService override 제거 (또는 mock 과 real 양쪽 mode 병행 — `TEST_DB_MODE` env var 분기). ADR 동반 — mock vs real 의 trade-off (CI 속도 vs 통합 정확도) 박제 + 선택 사유 + 후속 e2e cleanup (`afterEach` truncate) 정책. 본 task 후 mock-DB helper 의 위상 (unit-only 보조 vs deprecated) 도 결정. **[ADR-0004](decisions/ADR-0004-smoke-e2e-db-mode.md) 박제 완료 (T-0051) — 후속 T-0052 가 본 ADR 의 결정에 따라 .github/workflows/ci.yml services.postgres + DATABASE_URL env + migrate deploy step + smoke/e2e override 제거 + afterEach truncate helper 추가.**

### P3 → P4 전이 trigger

P3 진행 중 발견된 진척 status quo + P4 진입 trigger 의사결정 가능 형태를 별도 doc 으로 박제: [docs/architecture/p3-to-p4-transition.md](architecture/p3-to-p4-transition.md) (T-0063 doc-only direct).

- **현 status (T-0075 closure 시점, session #22 turn 1 refresh)**: entity 박제 layer-progress 5/11 → **8/11 (45% → 73%)** — Person / ServiceIdentity / Group (CRUD-U 4-layer fully closed) / Part (CRUD-U 4-layer fully closed) / PersonGroupMembership / module 2/5 유지 (PersistenceModule / UserModule) / ADR 1/4 유지 (ADR-0004 ACCEPTED) / test-quality 4/4 + 9-cell closure 유지 (backbone 3 도메인 × 3 layer, mock 시대 종결). Group + Part CRUD-U 4-layer 박제 milestone 추가 (T-0066+T-0067+T-0068 + T-0069+T-0071+T-0075).
- **권장 trigger option**: **(c) hybrid-parallel** — 핵심 backbone (User + AuthModule + ADR-0008 + Assessment + Contribution + Summary + ADR-0005 cross-cutting + raw 미저장 R-59 schema-level 강제) 완성 후 P4 진입. LlmProviderConfig + DifficultyMapping + PermissionDeniedRecord 는 P4 와 병행. ~9 task estimate (T-0063 박제 시점) → **~5~6 task** 로 축소 (T-0076 session #22 refresh, Group + Part CRUD-U 박제 후). 권장 강화 (선택 강제 안 함, 권장만 박제 — [p3-to-p4-transition.md §4.1](architecture/p3-to-p4-transition.md) 박제).
- **전이 시점의 실 의사결정은 다음 planner dispatch 또는 humanQuestion 발화의 책임** — 본 doc 은 trigger option (a) eager-transition / (b) strict-completion / (c) hybrid-parallel 의 trade-off 만 박제, T-0063 머지로 STATE.phase 변경 0 (P3-in-progress 유지).
- **binding decision (T-0133)**: option (c) hybrid-parallel 채택, STATE.phase P4-in-progress 전환 — entity 9/11 + module 3/5 + ADR 8 ACCEPTED + RBAC chain 3/3 완결 (T-0125/T-0132 closure) 후 위임된 binding-decision 박제. 박제 위치 [p3-to-p4-transition.md §7](architecture/p3-to-p4-transition.md). LlmProviderConfig / DifficultyMapping / PermissionDeniedRecord + ADR-0005 cross-cutting + ADR-0007 audit log 는 P4 와 병행 (본 task 머지 commit 이 전환 박제).

---

## Phase P4 — External integrations

- [x] GitHub 통합 — 3 instance 모두: **github.com / github.sec.samsung.net / github.ecodesamsung.com**. 각 instance 의 URL·org·token 설정 분리. **(완료)** — GithubModule 단일 GithubAdapter + instance-keyed config (`GITHUB_INSTANCES` + per-key `_HOST` / `_ORG` / `_TOKEN_ENC`) 박제 (ADR-0016 transport 계약 + ADR-0017 config source). 3 host variant (github.com / github.sec.samsung.net / github.ecodesamsung.com) 분리 + token JIT decrypt (ADR-0014) + 4xx → PermissionDeniedEvent emit.
- [x] **GitHub Issue 평가** (R-30) — Repo 내 Issue 작성을 문서 기여로 평가. 단 **본인이 본인 follow-up 을 남기고 본인이 소비하는 경우 카운트 제외**. **(완료)** — 수집 leg: `github-collection.service.ts` 의 `{ endpoint: "issues", suffix: "issues" }` 로 issue list endpoint 수집. 평가 leg: `evaluation-input.mapper.ts` 가 `issue → "document"`(Issue 는 문서 기여) 로 정규화 (ADR-0032 §1), `evaluation-dedup.ts` 의 `excludeSelfFollowUps` 가 같은 issue 단위 동일 author 후속 활동을 카운트 제외 (ADR-0032 §4 (b)-2) + `evaluation-orchestrator.service.ts:148` 이 `excludeSelfFollowUps(dedupTemporalDuplicates(inputs))` 로 실제 배선.
- [x] Confluence 통합 — 지정 주소의 Confluence Service 내 **지정 SPACE들** 다중 관리. **(완료)** — ConfluenceModule 단일 ConfluenceAdapter + instance-keyed config (`CONFLUENCE_INSTANCES` + per-key `_BASE_URL` / `_AUTH_USER` / `_TOKEN_ENC` / `_SPACE_ALLOWLIST`) 박제 (ADR-0018 transport 계약 + ADR-0019 same-host auth restriction). 지정 SPACE 다중 관리는 per-instance allowlist env + ConfluenceSpaceTraversalService 의 allowlist 순회 로 박제 + token JIT decrypt (ADR-0014) + 4xx → PermissionDeniedEvent emit.
- [x] **Confluence SPACE 탐색 정책** (R-34) — Crawling 또는 page List/Hierarchy 기반 탐색 중 택. ADR 로 결정. **(완료)** — ADR-0013 ACCEPTED 가 셋 (crawling / page List / Hierarchy) 중 **page List 기반 allowlist 순회** 를 default 로 박제 (Crawling 미선택). multi-SPACE 경계 + 4xx skip-and-continue 정책 동반. ConfluenceSpaceTraversalService 가 per-instance `_SPACE_ALLOWLIST` env 의 allowlist 를 순회 + 4xx → PermissionDeniedEvent emit + 다음 SPACE 진행 으로 in-code 박제.
- [x] **LLM provider 추상화** — 5 provider: **custom (OpenAI 호환 / 내부 자체 서버 / proxy 가능 / 3 model 슬롯을 모두 custom 으로 채울 수도 있음)** / Azure OpenAI / Anthropic / Google Gemini / OpenAI (R-99~103) **(완료)** — 5 provider adapter (openai-compatible/azure_openai/anthropic/google_gemini) + LlmHttpGateway dispatch 머지. T-0157 (custom/openai) / T-0158 (routing) / T-0160 (anthropic) / T-0162 (gemini).
- [x] **3가지 난이도 모델 할당** (R-97) — 평가 항목별 난이도 분류 + 어떤 항목이 어떤 난이도 모델로 처리될지 구현 과정에서 결정. ADR 로 박제. **(완료)** — ADR-0011 (3 난이도 모델 할당) ACCEPTED + T-0165 PR-153 LlmHttpGateway 난이도 routing wiring + T-0166/T-0167 doc-sync.
- [x] **Admin 이 LLM 모델 지정** UI (R-96) **(backend 완결, UI 는 P6)** — GET /api/llm/providers (T-0140~T-0142) + POST/PATCH/DELETE (T-0149/T-0151/T-0150) + api.md (T-0152). UI 는 P6 frontend phase 잔여.
- [x] 자격증명 관리 + **권한 부족 감지·통지** (사용자 + 관리자 모두 인식 가능, R-20·33) **(완료)** — 권한 부족 감지·통지는 `src/permission-denied/` 의 PermissionDeniedRecord chain(controller/service/repository/module + GitHub·Confluence persisting emitter, ADR-0022 data-model + ADR-0023 audit RBAC)으로 adapter 4xx → `PermissionDeniedEvent` emit → 영속 → `GET /api/permission-denied-records`(`@Roles("User")`) 조회로 박제. R-33 "사용자 + 관리자 모두 인식 가능" 은 service-layer audience 차등(Admin 전체 / non-Admin binding-scoped)으로 충족. 자격증명 관리는 `src/user-instance-access/` 의 UserInstanceAccess binding(ADR-0024 data-model + ADR-0027 grant RBAC) + instance-keyed `_TOKEN_ENC` JIT decrypt(ADR-0014)로 박제.
- [x] **[credential-prep / 운영 공백] 토큰 암호화 CLI** — GitHub/Confluence env 의 `GITHUB_<KEY>_TOKEN_ENC` / `CONFLUENCE_<KEY>_TOKEN_ENC` 는 **암호화된 ciphertext** 를 요구하나, 평문 토큰 → ciphertext 변환 도구가 아직 없다. `src/llm/llm-apikey-cipher.service.ts`(LlmApiKeyCipher, ADR-0014)를 재사용하는 작은 CLI/script (예: `scripts/encrypt-token.ts` — stdin 평문 토큰 + `LLM_APIKEY_ENC_KEY` → base64 AES-256-GCM envelope 출력)로 박제. **사용자 GitHub/Confluence token 주입의 선행 조건**. dependency 0(Node 내장 crypto), 실 token·실 key 0(test 는 test key). 사용자 대화 결정(2026-06-03)으로 backlog 박제. **(완료)** — `scripts/encrypt-token.ts` (T-0206 박제) 가 LlmApiKeyCipher (ADR-0014 AES-256-GCM envelope) 를 재사용해 stdin 평문 토큰 + `LLM_APIKEY_ENC_KEY` env → base64 ciphertext envelope 출력 으로 박제. argv 경로 fallback + TTY 분기 + 평문 토큰·키 stdout/stderr echo 0 (§9 보안). colocated spec (`encrypt-token.spec.ts`) R-112 4 종 cover. 새 외부 dependency 0 (Node 내장 crypto).
- [x] **[credential-prep / 운영 공백] LLM provider apiKey encryption-at-rest 완결** — `POST/PATCH /api/llm/providers` write 입력 시 LlmApiKeyCipher(ADR-0014)로 `apiKey` 를 암호화 저장 + read never-back + JIT decrypt 배선을 완결했다. **DB schema migration 동반 가능 → §5 schema 게이트 그 task 진입 시 재확인**. 실 LLM key 주입의 선행 조건. 사용자 대화 결정(2026-06-03)으로 backlog 박제. **(완료)** — write 경로 `LlmProviderConfigService.create/update` 가 `LlmApiKeyCipher.encrypt`(AES-256-GCM envelope)로 ciphertext 영속(service line 159/221), read 경로는 `LlmProviderConfigView = Omit<…,"apiKey">` sanitize 로 never-read-back, `LlmHttpGatewayService` 가 LLM 호출 시점에만 `cipher.decrypt`(line 143)로 JIT 복호, `prisma/schema.prisma` 의 `apiKey` 컬럼도 ciphertext 주석(line 361~366)으로 amend 완료. 새 schema migration·외부 dependency 0(Node 내장 crypto).

---

## Phase P5 — Evaluation pipeline

- [x] 단위 commit/document 평가 (난이도·기여도·양) — **implemented-on-main**: [`evaluation-scoring.service.ts`](../src/assessment-evaluation/evaluation-scoring.service.ts) `EvaluationScoringService.score` 가 LLM narrative → [`classifyNarrative`](../src/assessment-evaluation/domain/evaluation-prompt.ts)(narrative → difficulty/contribution 분류) + [`calculateEvaluationVolume`](../src/assessment-evaluation/domain/evaluation-volume.ts)(metadata 기반 결정적 volume) 을 compose 해 [`EvaluationResult`](../src/assessment-evaluation/domain/evaluation-result.ts) 5필드(unitId/narrative/difficulty/contribution/volume, REQ-032)를 조립·반환하는 경로로 shipped ([ADR-0032](decisions/ADR-0032-p5-evaluation-contract.md) §2 LLM scoring 입력 shape + §3 난이도·기여도·양 output 산출).
- [x] 일/주/월 요약 평가 (LLM 정성 + Metric 수치). **당일 활동은 자정까지 평가 미실시** (R-61). 주간은 다음주 시작 시, 월간은 다음달 시작 시. **implemented-on-main**: [`isPeriodEvaluable`/`computePeriodEnd`](../src/assessment-evaluation/domain/period-evaluable.ts) 가 요약 대상 period 의 평가 허용 시점을 판정 — day = 해당 KST 달력일의 다음 KST 자정, week = 다음 KST 월요일 00:00, month = 다음 달 1 일 KST 00:00 (모두 반열림 `[start, end)`, [ADR-0039](decisions/ADR-0039-timezone-kst-boundary-policy.md) §Decision 3 KST(Asia/Seoul) 경계) 이후에만 Summary 생성을 허용 → "당일 활동은 자정까지 평가 미실시 / 주간은 다음주 시작 / 월간은 다음달 시작"(R-61)을 KST 경계로 cover. [`enumerateSummaryDueCoordinates`](../src/assessment-evaluation/domain/summary-due-coordinates.ts) 가 평가 대상 (personId·period·periodStart) 좌표를 enumeration 하고, [`SummaryBatchOrchestratorService`](../src/assessment-evaluation/summary-batch-orchestrator.service.ts) 가 이를 batch 로 소비해 [`evaluateAndPersist`](../src/assessment-evaluation/summary-aggregate-orchestrator.service.ts)(aggregate orchestrator, LLM 정성 narrative + metric 수치 산출·영속) chain 으로 배선 ([ADR-0035](decisions/ADR-0035-aggregate-summary-evaluation.md) §Decision 3 aggregate summary 평가).
- [x] **사용자 지정 기간** 임의 평가문 생성 (R-9) — Admin/User 가 임의 기간을 지정해 LLM 평가문 요청. **implemented-on-main**: `@Post("period")` endpoint ([`assessment-evaluation.controller.ts`](../src/assessment-evaluation/assessment-evaluation.controller.ts)) 가 요청 role 을 dispatch — Admin 은 full-persist 경로 [`period-bridge-admin-persist.service.ts`](../src/assessment-evaluation/period-bridge-admin-persist.service.ts) (`generateAndPersist`, first-write-wins read-through + `reevaluate` 명시 mode opt-out 분기, ADR-0037 §Decision 1/2/3 + [ADR-0038](decisions/ADR-0038-overwrite-reevaluate-persisted-assessment.md)), User 는 self-only ephemeral 경로 [`period-bridge-ephemeral.service.ts`](../src/assessment-evaluation/period-bridge-ephemeral.service.ts) (collect(in-memory)→evaluate→return, DB write 0, ADR-0037 §Decision 1) 로 배선. 임의 기간 경계는 KST 로 해석(위 line 110 timezone=KST + [`period-boundary.ts`](../src/common/period-boundary.ts)). e2e: [period-bridge-admin-persist.e2e-spec.ts](../test/e2e/period-bridge-admin-persist.e2e-spec.ts) + [period-bridge-ephemeral.e2e-spec.ts](../test/e2e/period-bridge-ephemeral.e2e-spec.ts) + [period-bridge-reevaluate.e2e-spec.ts](../test/e2e/period-bridge-reevaluate.e2e-spec.ts). canonical ADR = [ADR-0037](decisions/ADR-0037-period-collection-evaluate-bridge.md) (ACCEPTED, §Decision 1~5 전량 resolve).
- [x] **중복 제거** — fork/rebase/meld 로 인한 중복 + **시간적 중복** (earlier date 우선 — 2월 결과물이 3월 timestamp 일 때 2월 기여로 판단, R-21). **implemented-on-main**: T-0289 [`dedupTemporalDuplicates`](../src/assessment-evaluation/domain/evaluation-dedup.ts) (PR #241, earlier-date 우선 시간적 중복 제거) + collection-side 구조적 중복 [commit-dedup.ts](../src/assessment-collection/domain/commit-dedup.ts) (fork/rebase/meld). detection-signals pipeline([evaluation-adjustments-pipeline.ts](../src/assessment-evaluation/domain/evaluation-adjustments-pipeline.ts) `applyEvaluationAdjustments`) + orchestrator([evaluation-orchestrator.service.ts](../src/assessment-evaluation/evaluation-orchestrator.service.ts)) 배선.
- [x] **재수집 정책** — 평가 자료 재수집 시 저장 부분 중복 방지. **최근 1주 는 재수집·중복 제거 OK** (data sync 보호, R-58). **implemented-on-main**: [`applyRecollectionWindow`](../src/assessment-collection/domain/recollection-window.ts) (`RECOLLECTION_WINDOW_DAYS=7` 재수집·중복 방지 window) + scheduling-side T-0424 [`buildRecentDeletionWindow`](../src/scheduling/recent-deletion-window.ts) (PR #341, 최근 N일 window 산출).
- [x] **Abusing 방지 metric** — 코드 abusing (commit/PR 숫자만 늘리기, R-26) + **문서 abusing** (의미 없는 기여 단순 반복, R-40). **implemented-on-main**: [`evaluation-abuse-signal.ts`](../src/assessment-evaluation/domain/evaluation-abuse-signal.ts) (신호) + T-0522 [`applyAbuseSignalToVolume`](../src/assessment-evaluation/domain/evaluation-abuse-adjust.ts) (PR #435, volume 중립화/감점). detection-signals pipeline `applyEvaluationAdjustments` v1 고정 순서(abuse→update-count→quality→underperformer→notable) 첫 adjuster 로 배선.
- [x] **문서 update 횟수 중립화** (R-41) — 습관적 중간 저장으로 update 횟수만 늘어나는 경우 advantage/disadvantage **둘 다 없어야**. **implemented-on-main**: T-0524 [`computeUpdateCountNeutralization`](../src/assessment-evaluation/domain/evaluation-update-count-neutral.ts) (PR #437, version 임계 detection) + T-0525 [`applyUpdateCountNeutralizationToVolume`](../src/assessment-evaluation/domain/evaluation-update-count-adjust.ts) (PR #438, volume net-0 보존). volume 산출([evaluation-volume.ts](../src/assessment-evaluation/domain/evaluation-volume.ts))이 version 을 애초에 미사용 → advantage 0; 중립 보존이 abuse 감점 공식 미적용 → disadvantage 0. [ADR-0049](decisions/ADR-0049-doc-update-count-neutralization.md) 의 group-by-collapse 안은 동일 REQ 의 중복 메커니즘으로 판명되어 SUPERSEDED (미채택 alternative 박제) — Q-0046 옵션1.
- [x] **품질 분류** (R-37·38) — 단순 보고·copy-paste 로그 = **zero-contribution** / 새 알고리즘 설계·외부 연구 도입 소개자료 = **높은 contribution**. **implemented-on-main**: [`evaluation-quality-signal.ts`](../src/assessment-evaluation/domain/evaluation-quality-signal.ts) (신호) + T-0528 [`applyContributionQualityFloor`](../src/assessment-evaluation/domain/evaluation-quality-adjust.ts) (contribution floor 강등). detection-signals pipeline `applyEvaluationAdjustments` v1 순서 quality adjuster 로 배선.
- [x] **"어렵고 남이 못할 일" 정성 평가** (R-25) — 중요한 기여 / 난이도 높은 기여 식별. **implemented-on-main**: [`evaluation-notable-contribution-signal.ts`](../src/assessment-evaluation/domain/evaluation-notable-contribution-signal.ts) (신호) + T-0534 [`applyNotableContributionAnnotation`](../src/assessment-evaluation/domain/evaluation-notable-contribution-adjust.ts) (notable annotation). detection-signals pipeline `applyEvaluationAdjustments` v1 순서 마지막 notable adjuster 로 배선.
- [x] **저성과자 식별** (R-27) — 코드 기여 현격히 떨어지는 인원 식별. **implemented-on-main**: [`evaluation-underperformer-signal.ts`](../src/assessment-evaluation/domain/evaluation-underperformer-signal.ts) (신호) + T-0531 [`applyUnderPerformerAnnotation`](../src/assessment-evaluation/domain/evaluation-underperformer-adjust.ts) (PR #445, underperformer annotation). detection-signals pipeline `applyEvaluationAdjustments` v1 순서 underperformer adjuster 로 배선.
- [ ] 평가 재실행·부분 reset (R-64) — **(부분 완료)** Q-0045 옵션1 run-side 사슬이 `POST /api/assessment-evaluation/unevaluated-fill-run`(평가 없는 부분 일괄 평가, REQ-037)까지 닫혔다. chain T-0556~T-0570(순수 조각→`@Injectable` orchestrator(T-0564)→controller route(T-0565)→e2e(T-0566)→[ADR-0048](decisions/ADR-0048-default-model-id-source.md)(T-0567)→resolver(T-0568)→controller resolver wiring(T-0569)→request body `defaultModelId` 필드 제거(T-0570, PR #484 squash c2e7c0c 머지)). default modelId 의 source 가 server-side `LlmProviderConfigResolver`(단일-row 운용 가정, ADR-0048 §Decision 1)로 단일화돼 caller-free 로 닫혔다. **checkbox `[ ]` 유지** — 본 사슬은 R-64 의 "재실행·부분 reset" 중 unevaluated-fill-run(부분 재실행)만 cover 했고, overwrite/reset 잔여는 bullet 107(DEFERRED, Q-0032 first-write-wins 유지)이 별도. REQ-051(custom 3 model 슬롯) 진입 시 다중-row default 선택 정책은 [ADR-0048](decisions/ADR-0048-default-model-id-source.md) §Decision 2 후속 ADR 이 prerequisite(deferred).
- [x] **overwrite / 이미 영속화된 평가문 재평가** (replace existing — ADR-0033 reeval/reset-and-recreate 경로) — Q-0032 v1 first-write-wins read-through(ADR-0037 §Decision3) 를 명시 mode 시에만 opt-out 하는 재평가 경로가 완결됐다. **implemented-on-main**: canonical source = [ADR-0038](decisions/ADR-0038-overwrite-reevaluate-persisted-assessment.md) (ACCEPTED 2026-06-10), 구현 chain T-0333~T-0337 — DTO `reevaluate` flag ([period-bridge.dto.ts](../src/assessment-evaluation/dto/period-bridge.dto.ts), T-0333) + controller mode dispatch ([assessment-evaluation.controller.ts](../src/assessment-evaluation/assessment-evaluation.controller.ts) `dto.mode === "reeval" ? "reeval" : "fill"` → `persist(context, results, mode)`, T-0336) + orchestration first-write-wins opt-out (T-0335) + e2e ([period-bridge-reevaluate.e2e-spec.ts](../test/e2e/period-bridge-reevaluate.e2e-spec.ts), T-0337). 무플래그 default 는 first-write-wins 보존(ADR-0037 §D3), 새 schema migration 0. [ADR-0053](decisions/ADR-0053-overwrite-reeval-mechanism.md) 은 ADR-0038 중복 재결정으로 **SUPERSEDED** (T-0807, supersededBy: ADR-0038) — canonical = ADR-0038, ADR-0053 §Follow-ups chain 은 착수 대상 아님. `(DEFERRED)` 해제(오너 승인 2026-07-07 Q-0051); R-64 잔여 = line 106 unevaluated-fill-run 부분과 별개로 본 overwrite/재평가 mechanism 은 완결.
- [ ] **🔴 live-LLM bridge 검증 — 사용자 승인 (2026-06-11), ⏰ credential 만료 2026-06-30 전 실행 필수** — [ADR-0037](decisions/ADR-0037-period-collection-evaluate-bridge.md) §Decision5: 머지된 mocked-only bridge(POST /period)를 **실 네트워크 LLM 1회 round-trip** 으로 검증 — 평가문 품질·scoring·narrative 경로 확인. credential = Q-0022 와 동일 경로 재사용(로컬 `secrets.env` repo 밖, `AZURE_OPENAI_*` → `LLM_LIVE_*` 매핑, **만료 2026-06-30**). env-gated live spec(env 부재 시 skip → CI green), 새 외부 dependency 0, §9 실값 파일 금지. **§5 credential 게이트는 본 사용자 승인으로 충족** (Q-0034 권고 ③ 채택 — 사용자 conversation 결정). T-0333 (ADR-0038 chain) 과 독립 — chain 진행 중 병행 또는 직후 큐잉 가능. 만료일 임박 고려해 우선 처리 권장. — **오너 결정 2026-07-07 (Q-0051): azure credential 미주입. 로컬 Ollama(custom OpenAI-호환 경로, ADR-0045 §D3)를 canonical live-LLM 경로로 계속 진행. azure 전용 wire(period-bridge-live.smoke-spec.ts) 검증은 불요/보류 — Q-0048 이 로컬 Ollama 로 대표 실증 완료.**
- [ ] **🟢 실 평가 e2e 테스트 데이터 = github.com `myungjoo` + `leemgs` 공개 활동 — 사용자 지정 (2026-06-22)** — 합성 데이터 대신 **실 github.com 공개 활동**을 실 평가 e2e 입력으로 사용한다. 단계: ① 두 사용자를 테스트 Person 으로 seed (github.com `ServiceIdentity` = username, ADR-0006 person/service-identity 모델) ② `assessment-collection` 이 그들의 public 활동(commit/PR/issue, R-30 self-follow-up 제외) 수집 ③ 로컬 Ollama(`deploy/seed-llm-config.sh` 로 `LlmProviderConfig` 배선)로 실 LLM 평가 → 평가문·난이도·기여도 산출 ④ `deploy/daily-test.sh` 에 `step_eval` 추가(`LLM_LIVE_*`→Ollama endpoint + `test/smoke/period-bridge-live.smoke-spec.ts` 실행 또는 평가 endpoint 실호출) → 결과를 daily-test result/rolling 이슈에 박제 = **자율 nightly 실 평가 e2e**. **검증됨**: gh 로 두 사용자 데이터 수집 가능(myungjoo public_repo 89 / leemgs 366, rate limit 여유). 운영 전제(코드 외): deploy 환경에 github.com **read PAT** 주입(`public_repo`/read scope 면 충분, 토큰 암호화 CLI 로 `GITHUB_<KEY>_TOKEN_ENC`, 만료 무관) + PC Ollama LAN 노출. **raw 미저장(R-59)** — 평가 결과만 보관. 새 외부 dependency 0(내장 fetch). 직전 "daily-test live-eval 미트리거" gap 의 구체 데이터·배선을 본 bullet 이 확정. — **오너 승인 2026-07-07 (Q-0051): 실 github.com myungjoo/leemgs 평가 e2e 운영 환경 승인. 로컬 Ollama 로 실 LLM 평가. ✅ 오너 확인 2026-07-07: PC Ollama 는 이미 LAN 노출됨 → stage 배포 환경에서 사용 가능 실증. 잔여 운영 전제 = github read-scope PAT 주입뿐(암호화 `scripts/encrypt-token.ts` → `GITHUB_<KEY>_TOKEN_ENC`). credential 실값은 env/secret 주입만(§9), git/STATE/journal 절대 금지. 권장 착수 3순위.**
- [x] **timezone = KST(Asia/Seoul) 확정 반영 — 사용자 결정 (2026-06-11)** — 그동안 deferred 였던 timezone 쟁점(Asia/Seoul vs UTC, Q-0034 context (5) 참조)을 사용자가 **KST(Asia/Seoul)** 로 확정. 적용 대상: P5 일/주/월 요약 경계(위 R-61 자정 룰의 "자정" = KST 자정), 주간/월간 시작 시점 판정, 사용자 지정 기간(R-9) 해석, 시각화 표시. 세부(저장은 UTC timestamptz 유지 + 경계 계산·표시만 KST 등 표준 패턴)는 구현 진입 시 **ADR 로 박제** — 본 bullet 은 사용자 결정의 박제이며 ADR-first 로 처리. 새 dependency 0. **implemented-on-main**: (1) [ADR-0050](decisions/ADR-0050-timezone-kst-period-boundary.md) (KST 기간 경계 표준 — 저장=UTC `timestamptz` 유지 + 경계 계산·표시만 +09:00 KST 변환, 주 시작=월요일 ISO 8601, 반열림 `[start, end)`, ACCEPTED); (2) [ADR-0051](decisions/ADR-0051-user-configurable-timezone.md) (user-configurable timezone) + [ADR-0052](decisions/ADR-0052-user-timezone-storage.md) (per-user timezone 저장); (3) [`period-boundary.ts`](../src/common/period-boundary.ts) (KST(Asia/Seoul) 경계 계산 helper — `Intl.DateTimeFormat` 경유, hardcoded offset 산술 금지) + [`period-evaluable.ts`](../src/assessment-evaluation/domain/period-evaluable.ts) (일/주/월 요약 경계 KST 판정); (4) [`schema.prisma`](../prisma/schema.prisma) `User.timezone String @default("Asia/Seoul")` 컬럼 (migration `20260706000000_user_timezone`, ADR-0052). 배선 chain T-0798~T-0803 전량 머지. R-9 요청 해석·display 축까지 shipped.

---

## Phase P6 — Web UI

> **오너 승인 (2026-07-07, Q-0051)**: frontend stack = **React + Vite** 확정. Vite 는 React 를 대체하는 게 아니라 JSX/TSX transpile + 번들 + dev-server(HMR) 를 담당하는 하부 빌드 도구다(오너 질문 "Vite 왜 필요?" 에 대한 설명 후 채택). SSR 불요 정적 SPA(NestJS backend 뒤). deferred 잔여 배선(ReEvaluationTriggerPanel·SchedulePanel·polling 등) 재개 승인 — P6 진입 시 frontend stack ADR(React+Vite) 선행. 단 우선순위상 P6 는 옵션 5·4·2 뒤.
>
> **편집 주 (2026-07-31, T-1351)**: 위 인용문의 `deferred 잔여 배선(...)` 열거는 **승인 시점(2026-07-07)의 서술**이다. 이후 `SchedulePanel` 은 [T-0885](tasks/T-0885-wire-schedule-panel-adminview.md), `ReEvaluationTriggerPanel` 은 [T-0886](tasks/T-0886-wire-reevaluation-trigger-panel-adminview.md) 로 **배선 완료**돼 두 패널은 더 이상 defer 대상이 아니다(AdminView 마운트 실측). 현재 실 defer 잔여는 **`EvaluationGuardBanner` 자동 polling 1 항목**뿐 — 근거는 아래 120 · 123 행. 승인 자체(P6 재개 승인 · React+Vite 확정 · 우선순위 서술)는 그대로 유효하다.

- [x] 로그인 / SuperAdmin 초기 셋업 흐름 **(완료)** — composition-wiring ②(AuthGate, T-0379)·⑥(SuperAdminSetupForm 배선, T-0394)로 조립 완료. 인증 게이트 + 최초 SuperAdmin 셋업 폼이 AppShell 에 배선됨(REQ-044 3 권한 로그인 UI / REQ-038).
- [x] 시각화 대시보드 (정렬·필터·시계열) **(완료)** — DashboardView(③a~③b-3, T-0381~T-0384) + presentational(필터바·시계열·분포·페이지네이션 등, T-0361~T-0375) 조립 완료(REQ-038).
- [x] Admin 패널 (인원·그룹·재평가·import/export·스케줄) **(완료)** — AdminView(④a~④h, T-0385~T-0392)로 GroupMemberList 조회·DifficultyModelSelector·export/import·scope·RBAC gating 조립 완료(REQ-049). 여기에 더해 PersonList·GroupList·PartList·UserList·LlmProviderConfigList 패널도 같은 view 에 마운트돼 있고(구별 패널 **10 종**, `PersonList` 는 인원 목록·파트 소속 인원 목록 두 자리), 인원·그룹·파트·사용자 CRUD 와 멤버 add/remove([T-1130](tasks/T-1130-adminview-member-remove.md)·[T-1131](tasks/T-1131-adminview-member-add.md))·provider CRUD([T-1133](tasks/T-1133-llm-provider-list.md))·역할 변경·인스턴스 접근 grant/revoke 를 포함한 **mutation 러너 26 개** 가 배선돼 있다 — 이 bullet 소관의 defer 잔여가 0 이라 마커를 **(완료)** 로 승격했다(EvaluationGuardBanner 자동 갱신 잔여는 121 행 R-78 bullet 소관). 단 **재평가(ReEvaluationTriggerPanel)·스케줄(SchedulePanel) 도 AdminView 에 마운트 완료** — [T-0885](tasks/T-0885-wire-schedule-panel-adminview.md)(SchedulePanel↔`PUT`·`GET /api/schedules` + `POST /api/schedules/trigger`)·[T-0886](tasks/T-0886-wire-reevaluation-trigger-panel-adminview.md)(ReEvaluationTriggerPanel↔`POST /api/schedules/recent-deletion/:personId`) 배선으로 defer 사유였던 backend 계약 미shipped 상태가 소멸했다(P7 133~135 행 shipped 서술이 반증).
- [x] **평가 진행 중 시각화 보호** (R-78) — 평가 자료 수집/평가 중에는 기존 자료만 표시 + 상단 경고 배너 **(완료 — 배선)** — EvaluationGuardBanner DashboardView 배선(⑤, T-0393) 완료(REQ-078). 단 **자동 polling 은 backend status 계약 미shipped 로 defer**(아래 deferred 잔여 참조).
- **composition-wiring 전환 (완결)** — presentational 분해 완료(15 컴포넌트, T-0361~T-0375). [ADR-0041](decisions/ADR-0041-frontend-composition-wiring.md)(ACCEPTED, T-0377 flip) 이 App.tsx 조립 구조(AppShell→인증 게이트→화면 컨테이너→presentational, controlled lift-up)·무라우터 view 전환 라우팅·native fetch hook data-fetch 경계·non-parallel single-claim shape 를 박제했고, 후속 wiring chain ①~⑥(T-0378~T-0394)이 `independentStream: p6-frontend-composition` 단일-claim 순차로 **완결**됐다(②AuthGate→③DashboardView→④AdminView→⑤EvaluationGuardBanner→⑥SuperAdminSetupForm, PR #325 squash b152181 로 마무리).
  - **deferred 잔여 (backend 계약 확정 후 배선)** — 다음은 make-work 가 아니라 backend-contract 미shipped 로 의도적 defer: EvaluationGuardBanner 자동 polling(assessments rows status 필드 부재). backend 계약 확정 후 배선한다. **목록에서 내린 항목 (배선 완료)** — 멤버 add·remove mutation([T-1130](tasks/T-1130-adminview-member-remove.md)·[T-1131](tasks/T-1131-adminview-member-add.md)·[T-1238](tasks/T-1238-group-member-add-container-wire.md)) 과 import 결과 상세([T-1132](tasks/T-1132-adminview-import-result-detail.md)·[T-1296](tasks/T-1296-import-response-restore-summary.md)·[T-1310](tasks/T-1310-import-result-restore-summary-detail.md)) 는 shipped 라 본 잔여 목록에서 제거했고([T-1313](tasks/T-1313-p6-deferred-residual-list-resync.md)), ReEvaluationTriggerPanel·SchedulePanel 배선 대기 항목도 [T-0885](tasks/T-0885-wire-schedule-panel-adminview.md)·[T-0886](tasks/T-0886-wire-reevaluation-trigger-panel-adminview.md) 배선 완료로 내렸다 — 남은 1 항목(EvaluationGuardBanner 자동 polling)만이 실제 defer 다.
  - **게이트된 backlog** — web vitest CI 배선(T-0355)은 `onHold: credential-workflow-scope`(token workflow scope 부재)로 게이트됨 — 진입 시 게이트 상태 재확인 필요.
  - **게이트된 backlog — web coverage threshold (T-1165 reviewer MINOR-2)** — `web/package.json` 의 vitest 에 `coverageThreshold` 가 없어 web 쪽은 R-112 의 line/function ≥ 80% 가 기계적으로 강제되지 않는다(`vitest run` 만 존재, coverage provider 미설치). 도입하려면 `@vitest/coverage-v8` **새 외부 dependency** 가 필요해 CLAUDE.md §5 새-dep 게이트 대상 — 사용자 승인 후 ADR 없이도 진행 가능한 devDependency 인지 판단 필요. P6 web 코드가 계속 늘고 있어 승인 시 우선 처리 권장.

- [x] **🔴 [오너 지시 2026-08-26] 계정 생성 UX — 아이디/암호 조건 사전 안내 + 구체적 실패 사유 표시 (R-158~R-160)** — 계정 생성(첫 로긴 SuperAdmin 셋업 · Admin 의 사용자 추가) 화면에서 아이디·암호 조건(허용 문자·형식·길이)을 입력 전에 안내하고, 실패 시 위반 조건별 구체 사유를 표시한다. "이미 등록된 사용자이거나 입력이 올바르지 않습니다" 류 포괄 문구 금지, 아이디 중복 오류와 형식/길이 위반 오류 구분. backend 오류 응답 계약(필드별 사유)과 frontend 표시 양쪽 배선 필요. planner: README 신규 R-158~R-160 을 requirements.md REQ row 로 동기 후 task 분해. **2026-08-30 승격** — [docs/requirements.md](requirements.md) `86~88 행` 의 REQ-067 · REQ-068 · REQ-069 가 모두 `DONE` 이고, 그 shipped 를 만든 slice 가 조건 사전 안내 축 [T-1710](tasks/T-1710-setup-form-credential-policy-hint.md) · [T-1711](tasks/T-1711-admin-user-create-credential-hint.md) 부터 구체 사유 · 중복 구분 축 [T-1712](tasks/T-1712-signup-failure-reason-classifier.md) ~ [T-1716](tasks/T-1716-signup-failure-body-contract-e2e.md) 까지 `T-1710 ~ T-1716` chain 전량이라 본 bullet 을 `[x]` 로 승격한다 (T-1787). 단 여러 사유 줄의 **줄 단위 구분 표시 (REQ-084) 는 본 bullet 소관이 아니라 `133 행` (R-187~R-191) 소관** 이므로 승격 근거에서 제외한다.
- [x] **🔴 [오너 지시 2026-08-26] 평가 대상 추가·편집 인터페이스 (R-164~R-168)** — 로그인 직후 "평가 대상을 선택하면 결과가 표시됩니다" 빈 상태에서 막히지 않도록, 평가 대상(① 대상자 인원 ② 대상 시스템 — GitHub org/repo · Confluence 주소/SPACE)을 추가·편집하는 인터페이스를 제공한다. 인원 축은 **2026-08-30 재판정 결과 5/5 shipped** — 추가 · 삭제 · 변경 · Deactivate · Activate 5 동작이 모두 AdminView 인원 관리 섹션에서 실제 발사된다(생성 폼 → `handleCreatePerson` / 인라인 수정 폼 + 활성·휴직 `<select>` → `buildPersonPatch` → `handleUpdatePerson` / `PersonList` `onDelete` → `handleDeletePerson`, 근거 slice 는 목록 [T-1142](tasks/T-1142-adminview-person-list-mount-fetch.md) · 생성 [T-1143](tasks/T-1143-adminview-person-create.md) · 삭제 [T-1144](tasks/T-1144-adminview-person-delete-mutation.md) · 수정/active [T-1145](tasks/T-1145-adminview-person-edit.md) 이고 backend 는 `T-0036` · `T-0037` 5 route + `UpdatePersonDto.active`). T-1802 시점의 유일한 잔여였던 **Activate(재활성) UI 진입점 1 건** 은 backend query 축 [T-1803](tasks/T-1803-person-list-include-inactive-query.md)(`GET /api/persons?includeInactive=true` → `service.findAll()`, PR #1417) 과 web 소비 축 [T-1804](tasks/T-1804-adminview-include-inactive-toggle.md)(AdminView "휴직 인원 포함" 토글 → `buildPersonsPath(nonce, true)`, PR #1418) 이 연속으로 닫았다 — 토글을 켜면 휴직 인원이 목록에 다시 노출되고 기존 인라인 수정 폼의 활성·휴직 `<select>` → `PATCH /api/persons/:id` `{active:true}` 가 곧 재활성 경로다(재활성 전용 route 는 없다). 근거는 [requirements.md](requirements.md) `90 행` REQ-071 `DONE` 재판정(T-1805). **2026-09-01 승격** — T-1802 시점에 잔여로 남아 있던 `89 행` REQ-070(빈 상태 우산) · `91 행` REQ-072(시스템 등록 · 편집) · `92 행` REQ-073(RBAC) 세 row 가 실측 재판정으로 **모두 `DONE`** 이 되어(T-1833) 마커를 `[x]` 로 승격한다. **시스템 축은 더 이상 부재가 아니다** — 옛 서술("시스템 축은 수집 대상 등록 모델 · API · UI 가 부재하면 신설")은 거짓이 됐다: [ADR-0059](decisions/ADR-0059-collection-target-registration.md) 가 `CollectionTarget` 단일 model + `type` discriminator + flat `/api/collection-targets` 5 route 를 결정했고, backend 축([T-1810](tasks/T-1810-collection-target-service-read-create.md) · [T-1811](tasks/T-1811-collection-target-service-update-delete-module.md) service, [T-1814](tasks/T-1814-collection-target-controller-get-routes.md) ~ [T-1817](tasks/T-1817-collection-target-controller-delete-route.md) 5 route, [T-1823](tasks/T-1823-collection-targets-error-contract-e2e.md) 오류 계약 e2e)과 web 축([T-1825](tasks/T-1825-admin-collection-target-list-panel.md) 목록 · [T-1826](tasks/T-1826-admin-collection-target-create-form.md) 등록 · [T-1828](tasks/T-1828-admin-collection-target-delete.md) 삭제 · [T-1829](tasks/T-1829-admin-collection-target-active-toggle.md) 활성 토글 · [T-1831](tasks/T-1831-admin-collection-target-endpoint-edit.md) endpoint 편집 · [T-1832](tasks/T-1832-admin-collection-target-scope-edit.md) 범위 3 축)이 모두 main 에 머지돼 AdminView `5985 행` 수집 대상 섹션에서 GitHub organization/repository 와 Confluence base URL · SPACE 를 실제로 등록 · 편집한다. 정체성 축(`type` · `instanceKey`)은 미구현 잔여가 아니라 ADR-0059 `§Decision 5` 의 **DELETE + POST 계약**이다. 빈 상태 우산(REQ-070)은 대시보드 미선택 분기의 인원 선택 컨트롤([T-1723](tasks/T-1723-dashboard-person-selector-wiring.md))과 인증 후 '관리' 내비게이션([T-1717](tasks/T-1717-appshell-authed-view-nav.md))이 함께 닫았고, Admin 편집 / User 조회(RBAC 일관, REQ-073)는 backend `@Roles` tier 분리와 web `isAdmin` gating 두 층으로 shipped 다([T-1720](tasks/T-1720-appshell-role-nav-gating.md) 등급별 내비게이션 필터 포함). planner: README R-164~R-168 동기는 완료 — 본 bullet 의 후속 작업은 없다.
- [x] **🔴 [오너 지시 2026-08-26] 대시보드 실동작 (R-175~R-178)** — ① 대시보드 안에서 평가 대상 인원 선택 UI — **2026-08-30 shipped**: 옛 서술("현재 AppShell 이 DashboardView 를 무-prop 마운트해 personId 부재 → 안내문만 렌더, 선택 수단 0")은 더 이상 사실이 아니다. 컴포넌트 축 [T-1722](tasks/T-1722-dashboard-person-selector-component.md) 가 순수 presentational 선택 컨트롤(label + select + placeholder option)을 신설하고, 컨테이너 배선 축 [T-1723](tasks/T-1723-dashboard-person-selector-wiring.md) 이 GET /api/persons 조회 → 후보 매핑 → 마운트를 이어 붙여 AppShell 의 무-prop 마운트는 그대로여도 DashboardView 가 선택 personId state 를 소유하고 미선택·정상 두 분기 모두에서 선택 수단을 렌더한다 — 근거는 [requirements.md](requirements.md) `93 행` REQ-074 `DONE` 재판정 (T-1796) ② 표시 계약 정합 — **2026-08-30 shipped**: 옛 서술("프런트 EvaluationResultRow/ContributionRow 가 backend Assessment/Contribution 응답과 id 외 불일치해 실데이터 렌더 0")은 더 이상 사실이 아니다. 표시 4 축(테이블 [T-1724](tasks/T-1724-assessment-display-row-mapper.md)~T-1727 · 점수 분포 T-1728~T-1731 · 시계열 T-1788, T-1789 · 기여 상세 패널 T-1790, T-1791)에 이어 마지막 잔여였던 narrative 축(패널 optional 슬롯 [T-1793](tasks/T-1793-evaluation-detail-panel-narrative-slot.md) → 컨테이너 배선 [T-1794](tasks/T-1794-dashboardview-narrative-wire.md), PR #1415)까지 main 에 머지돼 backend 필드 5 종(volume · difficulty · contributionScore · narrative · period/periodStart)이 표시 컬럼 재설계 + 계약 고정 test 와 함께 화면까지 닿는다 — 근거는 [requirements.md](requirements.md) `94 행` REQ-075 `DONE` 재판정 (T-1795) ③ 점수 분포 축 실 metricScore 스케일 정합 — **2026-08-30 shipped**: 실 스케일(0–3, 폭 0.5 6 등분) 순수 집계 모듈 [T-1728](tasks/T-1728-contribution-score-scale-module.md) 신설 이후 분포 축 배선 [T-1729](tasks/T-1729-dashboard-score-distribution-real-scale-wire.md) · 요약 지표 축 배선 [T-1730](tasks/T-1730-dashboard-metric-summary-real-scale-wire.md) · 옛 0–100 가정 임시 브리지 제거 [T-1731](tasks/T-1731-dashboard-remove-legacy-score-bridge.md) 까지 chain 전량이 main 에 머지돼 분포 구간과 요약 카드 만점이 모두 실 스케일 상수에서만 오고, 0–100 회귀는 drift guard spec 이 막는다 — 근거는 [requirements.md](requirements.md) `95 행` REQ-076 `DONE` 재판정 (T-1797) ④ 기간(일/주/월+시작) 지정 UI + POST /api/assessment-evaluation/period 호출 경로 배선 — **2026-08-30 shipped**: 요청 조립 계약 [T-1732](tasks/T-1732-evaluation-period-request-module.md) · 선택 컨트롤 [T-1733](tasks/T-1733-dashboard-period-selector-component.md) · 실행/정규화 [T-1734](tasks/T-1734-period-evaluation-submit-module.md) · 컨테이너 배선 [T-1735](tasks/T-1735-dashboard-period-evaluation-wire.md) · 성공 후 재조회 [T-1736](tasks/T-1736-use-api-resource-reload.md), [T-1737](tasks/T-1737-dashboard-period-evaluation-reload.md) 6 slice 가 모두 main 에 머지돼 대시보드에서 기간 종류(일/주/월)와 시작일을 실제 `<select name="period">` · `<input type="date" name="periodStart">` 로 고른 뒤 POST 호출까지 끊김 없이 이어지고 성공 시 결과 표·추이가 재조회된다. 마지막 잔여였던 조회(GET) 축도 [T-1799](tasks/T-1799-dashboardview-period-query-wire.md)(PR #1416, main `a6e44a94`)가 닫아, 화면에서 고른 기간이 `GET /api/assessments` · `GET /api/summaries` 조회 query 에 그대로 실린다(`web/src/views/DashboardView.tsx` `197 행` `deriveQueryPeriod` 가 컨테이너 state 우선 · prop fallback 으로 파생 → `565 행` `queryPeriod` → `570 행` · `585 행` 두 조회 path 조립. `web/src/AppShell.tsx` `315 행` 무-prop 마운트는 그대로지만 state 우선 규칙이라 무해). 시작 시점(`periodStart`)이 조회 query 에 없는 것은 GET 계약([api.md](architecture/api.md) `97 행` · `116 행` 이 `?personId=&period=` 뿐)의 범위 문제이지 지정 UI 제공 의무의 잔여가 아니다 — 근거는 [requirements.md](requirements.md) `96 행` REQ-077 `DONE` 재판정 (T-1800). planner: R-175~R-178 REQ row 동기 후 task 분해. **2026-08-30 승격** — [requirements.md](requirements.md) `93~96 행` 의 REQ-074 · REQ-075 · REQ-076 · REQ-077 네 row 가 **모두** `DONE` 이고, 그 판정을 만든 재판정 slice 는 ①인원 선택 UI [T-1796](tasks/T-1796-requirements-req074-person-selector-rejudge.md) · ②표시 계약 정합 [T-1795](tasks/T-1795-requirements-req075-narrative-rejudge.md) · ③점수 스케일 정합 [T-1797](tasks/T-1797-requirements-req076-score-scale-rejudge.md) · ④기간 지정 UI + POST /api/assessment-evaluation/period 호출 경로 [T-1800](tasks/T-1800-requirements-req077-query-axis-rejudge.md) 다. 마커를 `[ ]` 로 묶어 두었던 보류 사유(④ 의 `DONE` 판정을 만드는 것이 그 slice 자신이라는 자기-근거 순환)는 T-1800 이 main 에 머지되며 소멸했으므로, 네 row 가 모두 `DONE` 이라는 사실만을 근거로 본 slice 가 이 bullet 을 `[x]` 로 승격한다 (T-1801).
- [x] **🔴 [오너 지시 2026-08-26] 인원별 서비스 ID 매핑(ServiceIdentity) 관리 API·UI (R-182~R-183)** — schema 의 ServiceIdentity(서비스별 ID + isPrimary)는 수집이 소비하나 CRUD endpoint 0 · Admin 패널 0 — 인원 추가 폼이 이름/email 만 받아 UI 로 추가한 인원이 GitHub/Confluence 수집 대상으로 연결되지 않는다. 조회/추가/수정/삭제 API(예: /api/persons/:id/identities) + AdminView 편집 UI + primary 지정(1 인원 1 primary invariant). backend+web 양쪽 — API 설계는 architect ADR 동반. planner: R-182~R-183 REQ row 동기 후 task 분해. **2026-08-30 승격** — [docs/requirements.md](requirements.md) `97~98 행` 의 REQ-078 · REQ-079 가 두 축(API · Admin UI) 모두 shipped 로 `DONE` 이고, [ADR-0058](decisions/ADR-0058-service-identity-management-api.md) `§Follow-ups` 의 (a) ~ (e) chain 이 전량 shipped 되어 본 bullet 을 `[x]` 로 승격한다 (T-1785).
- [ ] **🔴 [오너 지시 2026-08-26] UI 기본기 — CSS·로그아웃·세션 복원·R-78 polling (R-187~R-191)** — ① 전역 스타일 도입 + AdminView 다수 섹션 탭/구획 내비게이션 — **2026-09-03 앞 축(전역 CSS 도입) shipped**: 옛 서술("현재 web 에 CSS 파일 0, className 만 존재")은 더 이상 사실이 아니다. [T-1858](tasks/T-1858-web-global-stylesheet-adr-wire.md)(PR #1459, main `5ae7e13d`)이 [ADR-0061](decisions/ADR-0061-frontend-global-stylesheet.md)(D1 순수 CSS 단일 파일 · 새 dependency 0 · D2 진입점 side-effect import 1 곳 · D3 `:root` 토큰 전용 + 기존 className anchor 만 · D4 계약 guard CI 게이트)을 박제하고, `web/src/styles/global.css`(토큰 15 종 + reset + element 기본 + 기존 anchor 8 종)와 진입점 배선(`web/src/main.tsx` `6 행` `import './styles/global.css';` 1 줄), 계약 guard(`web/src/styles/globalCssContract.ts` 의 `findMissingTokens` · `hasGlobalStylesheetImport` + colocated `globalCssContract.test.ts`)를 머지했다. **뒤 축(AdminView 다수 섹션 탭/구획 내비게이션)은 미shipped** — 근거는 [requirements.md](requirements.md) `99 행` REQ-080 `IN_PROGRESS` 재판정 (T-1859) ② 로그아웃(backend 쿠키 무효화 endpoint 포함) — **2026-09-02 shipped**: 이미 있던 backend `POST /api/auth/logout`(`src/auth/auth.controller.ts` `194 행` — access · refresh cookie clear 2 종 후 204)에 web 축이 이어졌다. [T-1837](tasks/T-1837-web-logout-wiring.md)(PR #1443, main `57ab7e41`)이 호출 helper `logout()`(`web/src/api/auth.ts` `76 행` — 실패를 `false` 로 흡수해 throw 0)과 AppShell 진입점(`126 행` `shouldShowLogout` 게이트 · `369 행` `handleLogout` 의 등급 초기화 · `'login'` 복귀 · `sessionEpoch` 증가 AuthGate remount)을 배선해 화면에서 세션이 실제로 끝난다 — 근거는 [requirements.md](requirements.md) `100 행` REQ-081 `DONE` 재판정 (T-1839) ③ 새로고침 시 GET /api/auth/me 부트 hydration 으로 인증 상태 복원(T-1718 helper 재사용) — **2026-09-02 shipped**: [T-1838](tasks/T-1838-web-session-restore-hydration.md)(PR #1444, main `cd071b25`)이 순수 helper 2 개(`web/src/AppShell.tsx` `142 행` `shouldRestoreSession` — 미인증 진입 view · 미시도 · 미적재 3 조건, `160 행` `restoredView` — 유효 사용자면 인증 후 view, 아니면 미인증 진입 view)와 부트 effect(`303 행` — `fetchCurrentUser()` 1 회 호출 · 실패 조용 흡수 · 시도 플래그 확정으로 재시도 루프 차단 · `cancelled` 언마운트 방어)를 배선해 유효한 쿠키 세션이면 새로고침 후 로그인 화면으로 되돌아가지 않는다 — 근거는 [requirements.md](requirements.md) `101 행` REQ-082 `DONE` 재판정 (T-1839) ④ R-78 평가 진행 배너 자동 polling — **2026-09-02 shipped**: 옛 서술("실행 상태 조회 endpoint 신설 포함 — 기존 P6 deferred 잔여 해소")은 더 이상 미해소가 아니다. [ADR-0060](decisions/ADR-0060-evaluation-run-status-endpoint.md) 계약대로 backend → e2e → web 순서로 닫혔다 — **backend** 는 축별 카운터 service(`src/run-status/run-status.service.ts` `66 행` `begin` · `80 행` `end` · `98 행` `snapshot`)와 `try` / `finally` 로 감싼 실행 진입점 4/4 배선([T-1841](tasks/T-1841-run-status-service-module.md) PR #1446 ~ [T-1845](tasks/T-1845-run-status-collection-wiring.md) PR #1450, 비용 없는 `unevaluated-fill-plan` 은 `§Decision 4` 대로 제외) 위에 조회 route `GET /api/run-status` 를 얹었고([T-1846](tasks/T-1846-run-status-query-route.md) PR #1451, main `8ac8cb62` — `src/run-status/run-status.controller.ts` `44 행` · `51~53 행` `@Get()` + `User+` guard + AppModule 등록), **e2e** 는 [T-1847](tasks/T-1847-run-status-e2e-contract.md)(PR #1452) 가 `test/e2e/run-status.e2e-spec.ts` 로 미인증 401 · 인증 200 + 8 필드 shape · 비실행 `active: false` 를 고정했다. **web** 은 [T-1848](tasks/T-1848-web-run-status-api-client.md)(PR #1453) 의 조회 helper(`web/src/api/runStatus.ts` `77 행` `fetchRunStatus()` — 실패를 `false` 로 흡수해 throw 0)와 [T-1849](tasks/T-1849-appshell-run-status-polling.md)(PR #1454, main `58ce46f3`) 의 polling 배선(`web/src/AppShell.tsx` `184 행` 5 초 주기 상수 · `392~422 행` 즉시 1 회 + interval + `visibilitychange` 로 탭 비가시 중단 + unmount cleanup)이 `486 행` 에서 기존 배너의 `active` prop 까지 이었다 — 근거는 [requirements.md](requirements.md) `102 행` REQ-083 `DONE` 재판정 (T-1851) ⑤ 여러 줄 오류 안내 줄 단위 표시 — **2026-09-01 shipped**: 옛 서술("SETUP_ERROR_SEPARATOR 한 줄 합침 해소")은 더 이상 미해소가 아니다. 셋업 폼 축 [T-1834](tasks/T-1834-setup-form-error-lines.md) 과 Admin 사용자 추가 폼 축 [T-1835](tasks/T-1835-admin-create-user-error-lines.md) 이 머지돼 두 화면 모두 실패 사유를 줄 배열로 내려보내고 role="alert" 영역에서 줄마다 별도 element 로 렌더한다(구분자 상수 `SETUP_ERROR_SEPARATOR` · `CREATE_USER_ERROR_SEPARATOR` 는 join 파생 export 전용으로만 남아 화면에 나타나지 않는다) — 근거는 [requirements.md](requirements.md) `103 행` REQ-084 `DONE` 재판정 (T-1836). 본 bullet 의 잔여는 ① 후반부의 **AdminView 다수 섹션 탭/구획 내비게이션 1 건**뿐이며(① 앞 축인 전역 CSS 도입과 ② · ③ · ④ · ⑤ 는 shipped), 그 1 건이 아직 미shipped 라 마커는 `[ ]` 로 유지한다. 옛 지시 두 건은 이행 완료라 다음 행동으로 대체한다 — "CSS 방식(순수 CSS vs 라이브러리 새 dep)은 architect 판단 — 새 dep 시 §5 게이트" 는 ADR-0061 이 순수 CSS · 새 dependency 0(D1)으로 확정해 [CLAUDE.md](../CLAUDE.md) `§5` 새-dep 게이트 미해당으로 종결됐고, "planner: R-187~R-191 REQ row 동기 후 task 분해" 는 REQ-080 ~ REQ-084 row 동기와 T-1834 ~ T-1858 slice 분해로 완료됐다. **다음 행동** = AdminView 섹션 탭/구획 내비게이션 slice(`pr`) — 마크업 anchor(섹션 탭 className) 추가와 그 className 을 받는 전역 CSS 규칙을 **한 slice 에 함께** 넣는다([CLAUDE.md](../CLAUDE.md) `§3` 소비처 동반 의무 — CSS 규칙만 넣고 마크업을 미루면 동작 변화가 0 이다). ADR-0061 `D3` 이 "신규 className 대량 도입은 본 ADR 범위 밖" 이라 한 것은 그 마크업 변경이 **별도 slice 의 책임**이라는 범위 선언이지 금지가 아니므로, 그 slice 는 D3 의 `:root` 토큰 전용 규약 안에서 새 anchor 를 자기 책임으로 도입한다.

---

## Phase P7 — Scheduling & operations

> **오너 승인 (2026-07-07, Q-0051)**: P7 진입 + `@nestjs/schedule` 새 외부 dependency 승인(§5 새-dep 게이트 해소). ADR 선행 후 SchedulerModule 구현 — cron 주기 지정(R-72)·manual trigger(R-73)·재수집(R-74). 이 backend 계약이 P6 SchedulePanel defer 도 연쇄 해소. 권장 착수 순위 = 옵션 5 다음(2순위).

- [x] Admin이 cron 주기 지정 (예: KST 02:00) (R-72). **implemented-on-main**: [`cron-schedule.controller.ts`](../src/scheduling/cron-schedule.controller.ts) `@Get()`/`@Put()`/`@Delete(":name")`/`@Post("trigger")` route (cron 조회·upsert·삭제·즉시 트리거). [ADR-0042](decisions/ADR-0042-nestjs-schedule-adoption.md) (`@nestjs/schedule` 도입 ACCEPTED).
- [x] Manual trigger (R-73). **implemented-on-main**: cron-schedule [`@Post("trigger")`](../src/scheduling/cron-schedule.controller.ts) (스케줄 즉시 실행) + backfill [`@Post("backfill/:personId")`](../src/scheduling/backfill.controller.ts) (신규 인원 backfill 수동 실행) 두 경로가 각각 다른 대상의 수동 트리거를 cover (중복 아님). [ADR-0042](decisions/ADR-0042-nestjs-schedule-adoption.md).
- [x] 최근 N일 결과 manual delete → 재수집 (예: 1일/7일/30일, R-74). **implemented-on-main**: [`recent-deletion.controller.ts`](../src/scheduling/recent-deletion.controller.ts) `@Post("recent-deletion/:personId")` route (최근 N일 window 산출 후 삭제→재수집). [ADR-0042](decisions/ADR-0042-nestjs-schedule-adoption.md).
- [x] **신규 인원 추가 시 1년치 평가 1회** (R-50) — 일반 인원의 매일 1주일 단위 평가와 분리. **implemented-on-main**: [`buildBackfillPlan`](../src/scheduling/backfill-plan.ts) (`DEFAULT_WEEKS=52`≈1년 주 단위 window 산출, 매주 1회 평가와 분리된 신규 인원 1회 backfill) + [`runBackfill`](../src/scheduling/backfill-runner.service.ts) (`BackfillRunnerService` 1회 실행). [ADR-0042](decisions/ADR-0042-nestjs-schedule-adoption.md).
- [x] Import / export / restore (R-57) — 평가 자료 backup/restore. **implemented-on-main**: export 경로 [`export.controller.ts`](../src/export/export.controller.ts) (`@Post()` 생성 / `@Get("running")` / `@Post("describe-scope")` / `@Post("preview-selection")` / `@Get(":id/download")` streaming / `@Get(":id/status-view")` / `@Get(":id")` 조회) + [`export-job.service.ts`](../src/export/export-job.service.ts) 영속화 (dump materialization·chunk streaming·resume) + import 경로 [`import.controller.ts`](../src/import/import.controller.ts) (`@Post()` / `@Get("running")` / `@Get("modes")` / `@Get(":id")`) + [`import-job.service.ts`](../src/import/import-job.service.ts) + restore 경로 [`import-restore-plan.ts`](../src/export/import-restore-plan.ts)/[`import-restore-preview.ts`](../src/export/import-restore-preview.ts)/[`import-restore-result.ts`](../src/export/import-restore-result.ts). canonical [ADR-0044](decisions/ADR-0044-export-import-job-persistence.md)/[ADR-0046](decisions/ADR-0046-export-dump-materialization-storage.md)/[ADR-0047](decisions/ADR-0047-export-dump-db-read-scope.md).
- [ ] **성능 검증**:
  - 100~200명 / 50~100 repo / ~1000 confluence page / **1h 이내** (R-91) — **baseline 실측 S1 16 회 · S2 6 회 · S3 5 회 완료(S1 중 실 scale 표본 133 은 13 회 dispatch · 수치 회수는 12 회 — 7 회차만 seed step fail 로 k6 미실행) · 실 수집 축 미검증**: S1 스크립트 [`test/load/s1-batch.js`](../test/load/s1-batch.js) + [`load-k6.yml`](../.github/workflows/load-k6.yml) 의 "k6 S1 평가 배치 부하 시나리오 실행" step(`LOAD_TEST_STUB=1` 주입) + `package.json` `test:load:s1` + ADR-0057 `D1` stub 배선이 모두 main 에 있다(T-1627 `f6052cf3` · T-1628 `b6d4d6d9` · T-1629 `8338572e` stub, T-1631 `fa0aad91` · T-1632 `9099d99f` 스크립트, T-1633 `63555e09` step, T-1634 `971c716c` 표본 env 방어). **표본 10 축 baseline 은 2 회 회수**됐다 (T-1637 1 회차 · T-1639 2 회차 run **32503914467**) — `load-k6.yml` run **32459501970**(2026-08-21, conclusion `success`, 12 step 전부 success)에서 `http_req_duration{route:batch}` p95 **99.29ms**(임계 `p(95)<270677ms` = 1h × 10/133 통과) · `http_req_failed` **0.00%(0/26)** · `iteration_duration` **99.72ms** 를 얻었고, 환경 메타와 한계는 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 에 박제했다 (도구 [ADR-0054](decisions/ADR-0054-load-resilience-harness-tool.md) 는 **ACCEPTED** — frontmatter date 2026-07-08, 2026-07-30 owner k6 승인 flip, 같은 문서 §5 item 1·3·5 가 그 현행 상태를 가리킨다). 2 회차는 p95 **96.98ms** · `http_req_failed` **0.00%(0/26)** 로 같은 임계를 통과했고(회차 간 Δ 약 −2.3%), T-1638 `tee -a` 배선 덕에 커널·vCPU·메모리 등 환경 메타 7 항목을 `gh run view --log` 만으로 회수했다. **3 회차(T-1641)** 는 T-1640 이 연 `s1_persons` input 을 `133` 으로 dispatch 해 run **32524618230**(head sha `8b9a9bfe`, conclusion `success`, 12 step 전부 success, 표본 인원 로그 행 `133` 확인)에서 `http_req_duration{route:batch}` p95 **760.91ms** · `http_req_failed` **0.00%(0/272)** · `iteration_duration` **761.86ms** 를 얻었다 — 표본이 외삽 기준과 같아 임계가 1h 예산 전체(`p(95)<3600000ms`)로 넓어진, **선형 외삽 없이 예산과 직접 대조된 첫 실측**이다(예산의 약 0.02%). **4 회차(T-1642)** 는 같은 `s1_persons=133` 으로 1 회 dispatch 해 run **32533779832**(head sha `1236a880`, conclusion `success`, 12 step 전부 success, 표본 인원 로그 행 `133` 확인)에서 p95 **730.81ms** · `http_req_failed` **0.00%(0/272)** · `iteration_duration` **731.89ms** 를 얻어 **실 scale 축의 첫 run-to-run 쌍**(760.91ms → 730.81ms, Δ −30.10ms · 약 −3.96%)을 만들었다. **5 회차(T-1643)** 는 같은 `s1_persons=133` 으로 1 회 dispatch 해 run **32540981922**(head sha `a9a08e43`, conclusion `success`, 전 step success, 표본 인원 로그 행 `133` 확인)에서 p95 **711.23ms** · `http_req_failed` **0.00%(0/272)** · `iteration_duration` **712.30ms** 를 얻어 실 scale 축 표본을 **3 개**로 만들었다 — 3·4·5 회차 batch p95 760.91ms · 730.81ms · 711.23ms 의 **평균 734.32ms · 범위 49.68ms · 표본표준편차 25.02ms(변동계수 3.41%)** 로 처음으로 분산을 추정했다. **그럼에도 140 행 checkbox 는 `[ ]` 유지** — 인원 축만 실 scale 이고 LLM 은 여전히 stub(ADR-0057 `D1`) · 표본 person 에 `ServiceIdentity` 가 없어 외부 수집 왕복 0(50~100 repo · ~1000 page 축 미검증) · 단일 iteration 이라 REQ-047 완료 조건에 미달한다. 잔여는 실 dataset seed 기반 수집 왕복 실측이며 임계 fix 축은 아래대로 해소됐다 — 실 scale 축 표본 3 개로 분산 추정이 가능해져 **임계 확정은 별도 slice 로 착수 가능하다고 판정**했지만(산포가 평균의 3~7% · 세 값 모두 1h 예산의 약 0.02% · 남은 불확실성은 분산이 아니라 stub 조건의 대표성), 그 확정 slice 는 **T-1644 가 문서 축에서 집행 완료**했다 — `§3` 표에 S1 **관찰용** `http_req_duration{route:batch}` p95 임계 **≤ 900ms(stub 조건 baseline, 표본 133)** 를 추가하고(평균 734.32ms + 3 × 표본표준편차 25.02ms = 809.38ms 를 100ms 단위 올림, 실측 max 760.91ms 보다 큼), S1 배치 실패·재시도율은 5 회 run 전부 `http_req_failed` 0.00% 라 `(baseline 후 fix)` 태그를 떼고 **error rate < 1%** 로 확정했다. 이 900ms 는 **REQ-047 판정 임계가 아니라** stub 조건(LLM stub · 수집 왕복 0 · 단일 iteration) 회귀 관찰용 기준선이고, REQ-047 판정은 1h 예산(`3,600,000ms`, 스크립트 `FULL_RUN_BUDGET_MS`) 그대로다. S2 · S3 의 `baseline 후 fix` 는 해당 축 실측 0 회라 무변경이다. 확정된 900ms 를 스크립트 `thresholds` 게이트로 태우는 **배선은 T-1645(PR #1316 → main `874297ca`)로 완료**됐다 — [`s1-batch.js`](../test/load/s1-batch.js) 가 `STUB_BASELINE_PERSONS = 133` · `STUB_BASELINE_P95_MS = 900` 두 상수를 두고 `http_req_duration{route:batch}` 임계 배열에 `p(95)<900` 을 **표본 133 일 때만** 얹는 조건부 활성이라 기본 표본 10 run 영향은 0 이고, 판정 임계는 여전히 1h 예산 외삽(`BATCH_P95_MS`)이다. 그리고 그 배선이 **실 run 에서 실제로 활성·판정되는지**를 **6 회차(T-1647)** 가 실측했다 — 같은 `s1_persons=133` 으로 1 회 dispatch 한 run **32562811133**(head sha `07deff81`, conclusion `success`, 전 step success, 표본 인원 로그 행 `133` 확인)의 k6 `THRESHOLDS` 출력에 `http_req_duration{route:batch}` 임계가 `✓ 'p(95)<3600000'` · `✓ 'p(95)<900'` **2 개로 등장하고 둘 다 통과**해 조건부 게이트가 런타임에서 활성됨이 확인됐다(**결함 0**). 그 회차 수치는 p95 **792.27ms**(900ms 까지 여유 **107.73ms** · 약 11.97%) · `http_req_failed` **0.00%(0/272)** · `iteration_duration` **794.05ms** 이고, 실 scale 표본이 **4 개**가 되어 batch p95 760.91 · 730.81 · 711.23 · 792.27ms 의 **평균 748.81ms · 범위 81.04ms · 표본표준편차 35.46ms(변동계수 4.74%)**, **평균 + 3σ = 855.19ms 로 900ms 안**이라 `§3` 표 임계 숫자는 재확정 없이 무변경이다 — 3~5 회차의 단조 감소가 6 회차에서 반전한 것이 T-1644 의 max 대신 평균 + 3σ 마진 선택을 사후 검증했다. 여전히 LLM stub · 수집 왕복 0 · 단일 iteration 이라 `140 행` checkbox 는 `[ ]` 유지다. 잔여인 **실 dataset seed 축**도 입력 데이터와 안전장치까지는 확보됐다 — T-1648(main `c95b7dec`, PR #1317)이 [realdata-scale-devset.md](ops/realdata-scale-devset.md) `§A` 33 명 + `§B` 100 명 = **133 로그인**을 기계 판독 fixture [`test/load/realdata-devset-logins.json`](../test/load/realdata-devset-logins.json) + 검증 로더 [`test/helpers/realdata-devset-logins.ts`](../test/helpers/realdata-devset-logins.ts) 로 박제했고, T-1649(main `87cdb828`, PR #1318)가 정본 markdown 표 ↔ fixture **drift guard**([`realdata-devset-logins-doc-consistency.ts`](../test/helpers/realdata-devset-logins-doc-consistency.ts), `pnpm test` 에서 CI 게이트)를 신설했다 — 잔여는 그 133 로그인으로 `Person` + github `ServiceIdentity` 를 적재해 실 수집 왕복을 태우는 **seed 실행 경로** 1 개다(실행 0 회). 그 seed 실행 경로의 **배선 4 축은 T-1651~T-1661 로 닫혔다** — helper chain 7 종(`test/helpers/realdata-devset-seed-*.ts`; T-1651 `4e0697c6` · T-1652 `bcce5516` · T-1653 `26a9e8f9` · T-1654 `53ebe0aa` · T-1655 `7bc054a7` · T-1656 `1a7ace68` · T-1657 `1e44f562`), 실행 진입점 [`scripts/seed-devset-logins.ts`](../scripts/seed-devset-logins.ts) + `pnpm seed:devset-logins`(T-1658 `609c937b`), workflow 툴체인 3 step(T-1659 `f9da3e7f`) 과 [load-k6.yml](../.github/workflows/load-k6.yml) `114 행` `133 로그인 실 dataset seed 적재` step(T-1660 `73100c77`), 그리고 [`s1-batch.js`](../test/load/s1-batch.js) `setup()` 이 합성 생성 대신 `GET /api/persons` + `@load.devset.test` 접미사 필터로 적재분을 겨냥하도록 바꾼 소비 경로(T-1661 `499df531`) 다. **그 배선을 태운 첫 실 run 은 T-1663 의 run `32652307813`**(2026-08-23T16:38:45Z dispatch, head sha `0046e366`, `-f s1_persons=133`, 1 회 한정)이며 **conclusion `failure`** 였다 — `133 로그인 실 dataset seed 적재` step 이 ServiceIdentity leg 첫 upsert 에서 ``Argument `person` is missing.`` 로 죽었고(원인: `ServiceIdentityUpsertArgs.create` 가 Person 관계를 비워둔 채 `where` 의 `personId` 만 치환), 그 exit 1 로 k6 5 step 이 전부 skipped 라 S1 수치·`setup()` 소비 경로 검증은 **회수 실패**다. 따라서 잔여 ① 은 **미해소 유지**이고 내용이 `seed 실행 성공 0 회 + 결함 1 건 수정 대기` 로 구체화됐을 뿐이며(수정은 별도 pr-mode slice — T-1663 은 측정·기록 전용), LLM stub · 수집 왕복 0 · 단일 iteration 조건도 그대로라 `140 행` checkbox 는 `[ ]` 유지다. 경로 상세는 [realdata-scale-devset.md](ops/realdata-scale-devset.md) `## seed 실행 경로` 절에 있다. **그 결함은 T-1664(PR #1330 → main `61f616a1`)가 `resolveRealDataPersonId` 로 `create.personId` 를 배선해 닫았고, 재 dispatch 인 T-1665 의 run `32665014391`(2026-08-23T20:38:13Z dispatch, head sha `3319ac41`, `-f s1_persons=133`, 1 회 한정)이 conclusion `success` 로 시나리오 step 전부를 통과했다** — seed step 이 `devset seed 완료 — person 133 건 / serviceIdentity 133 건 적재` 를 찍어 **seed 실행 성공이 처음으로 1 회** 확보됐고, `http_reqs` 가 272 → **7** 로 떨어져 `setup()` 이 합성 생성 없이 적재분을 조회해 소비했음이 확인됐으며, `http_req_duration{route:batch}` 임계 2 개(`✓ 'p(95)<3600000'` · `✓ 'p(95)<900'`)가 측정 p95 **757.65ms** 로 둘 다 통과했다(실 scale 표본 5 개 평균 750.57ms · 표본표준편차 30.96ms · 평균+3σ 843.45ms ≤ 900ms → `§3` 임계 재확정 불요, 근거는 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 8 회차). 그럼에도 잔여 ① 은 **미해소 유지** — `ServiceIdentity` 가 붙었어도 job 에 수집 자격증명이 없어 **실 수집 왕복은 여전히 0** 이고 LLM stub · 단일 iteration 조건도 그대로라 `140 행` checkbox 는 `[ ]` 유지다. **9 회차인 T-1667 의 run `32677333740`(2026-08-24T00:39:02Z dispatch, head sha `7063584f`, `-f s1_persons=133`, 1 회 한정)은 conclusion `success` 로 step 21 개를 전부 통과했고, T-1666 이 배선한 표본 로그 `[s1-batch] devset 표본 취득 133명 / 요청 133명` 을 처음 회수해 8 회차가 간접 증거 3 종으로 추론했던 표본 수 133 을 직접 확정했다** (seed step 도 `person` 133 · `serviceIdentity` 133 으로 연속 2 회 성공). batch p95 는 **824.71ms** 로 임계 2 개(`✓ 'p(95)<3600000'` · `✓ 'p(95)<900'`)를 통과했고 `http_req_failed` 0.00%(0/7) · `http_reqs` 7 로 합성 생성 왕복 0 이 유지됐다(실 scale 표본 6 개 평균 762.93ms · 표본표준편차 41.02ms · 평균+3σ 886.00ms ≤ 900ms → `§3` 임계 재확정 불요이나 여유가 56.55 → 14.01ms 로 좁아져 다음 표본에서 재판단, 근거는 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 9 회차). 그럼에도 잔여 ① 은 **미해소 유지** — 확정된 것은 표본 수·seed 재현성이지 수집 왕복이 아니라 실 수집 왕복 0 · LLM stub · 단일 iteration 조건이 그대로이므로 `140 행` checkbox 는 `[ ]` 유지다. 이어 T-1668 은 실측 · 코드 변경 0 으로 `p(95)<900` stub baseline 게이트의 **재확정 규칙을 다음 표본 이전에 사전 박제**했다 — ① 트리거(실 run `✗` 또는 실 scale 표본 전량의 평균+3σ 가 현 임계 초과) · ② 상향 폭(T-1644 도출식 재적용 — 평균+3σ 의 100ms 올림) · ③ 표본 취급(outlier 제거 · 표본 10 혼합 · 하향 금지) · ④ 집행 경로(코드 `pr` + doc `direct` 2 task split) 4 항목을 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3` 에 굳혔다(현 표본 6 개의 평균+3σ 886.00ms 는 트리거 미충족이라 임계 숫자 변경 0). 따라서 **다음 실측 회차부터는 상향 여부를 표본 수치 대입만으로 기계적으로 판정**하며, 사후 정당화(over-fitting) 여지를 남기지 않는다. **10 회차**(T-1669, run **32690756666** `success`, head sha `cd411817`)는 그 규칙을 **처음 기계 적용**한 회차로, batch p95 **743.96ms** · 표본 취득 133 명 == 요청 133 명 · seed 연속 3 회 성공 · `http_reqs` 7 유지를 회수했다. 실 scale 표본 **7 개** 전량의 평균 **760.22ms** · 표본표준편차 **38.13ms** · 평균+3σ **874.60ms**(올림 후 900ms)가 트리거 ①-(b) 를 넘지 않고 실 run 의 `p(95)<900` 도 `✓` 라 ①-(a) 도 미충족이어서 **임계 무변경**(평균+3σ 여유 14.01 → **25.40ms**)으로 결론했고, 코드 · 상수 · `§3` 표 숫자 변경은 0 이다(규칙 ④ 의 2 task split 불요). 이어 [T-1671](tasks/T-1671-s2-devset-dataset-swap-design.md) 은 같은 chain 의 잔여 후보였던 **S2 조회 부하의 dataset 교체**를 코드 착수 이전에 설계로 고정했다 — [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) 의 `#### S2 dataset 교체 설계 (사전 박제)` 소절에 ① person leg 만 devset 조회로 전환(group / part 는 합성 seed 유지 — devset 은 `Person` · `ServiceIdentity` 두 leg 만 적재) ② `teardown()` 의 person DELETE 제거로 공유 dataset 보존 ③ `K6_SEED_PERSONS` 를 "표본 상한" 으로 재정의하되 숫자는 `30` 무변경 + parity 유지 ④ 같은 commit 에서 갱신할 drift-guard 단언 대체 목록 (a)~(g) ⑤ `§3` 표 S2 p95 3000ms 무변경 ⑥ pr(코드+spec) → direct(문서) 2 task split 을 박제했다. 본 회차는 문서 2 개만 바꿔 코드 · 워크플로 · spec · 임계 변경과 실 run 이 모두 0 이므로 `140 행` checkbox 는 `[ ]` 그대로다. 이어 [T-1672](tasks/T-1672-s2-devset-dataset-swap-exec.md) 가 그 설계 ⑥ split 의 **1 번(pr 교체 집행)** 을 PR **#1333** → main **`27953b24`**(2 파일 `+267/-33`) 로 끝냈다 — `s2-read.js` `setup()` 의 person leg 가 합성 `POST /api/persons` 반복 생성에서 `GET /api/persons` 1 회 + `@load.devset.test` 필터 → `slice(0, SEED_PERSONS)` → `map` **단일 식**으로 바뀌었고(group / part leg 는 합성 seed 유지), `teardown()` 의 person DELETE 루프 제거로 **공유 dataset 이 보존**되며(group / part 회수는 유지), `K6_SEED_PERSONS` 는 숫자 `30` · 정규화 표현 · 3 자 parity 무변경인 채 의미만 "표본 상한" 으로 재정의됐고, drift-guard 단언 (a)~(g) 는 같은 commit 에서 갱신됐다. 이번 회차 [T-1673](tasks/T-1673-s2-devset-dataset-swap-doc-sync.md) 은 그 split 의 **2 번(direct 문서 반영)** 이라 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§5` item 5 와 이 줄에 집행 **사실**만 박제할 뿐, 코드 · spec · 워크플로 · `§3` 표 임계 변경이 0 이고 **S2 축 실측도 여전히 0 회**(실 dispatch 는 설계 ⑥ 의 세 번째 task 소관)라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1674](tasks/T-1674-load-k6-s2-first-measurement.md) 가 그 **세 번째 task(S2 첫 실측 dispatch)** 로 `load-k6.yml` 을 **정확히 1 회**(`-f s1_persons=133`, head sha `7788552a`) 발화했으나 run **32746598803** 의 conclusion 은 **`failure`** 였다 — S1 leg 의 stub 조건 관찰용 게이트 `p(95)<900` 이 실 run 에서 처음 `✗`(p95 **967.52ms**)로 crossed 돼 k6 가 exit 99 를 냈고, S2 step 에 `if: always()` 가 없어 `k6 S2 조회 부하 시나리오 실행` 이 **`skipped`** 됐다. 그래서 **S2 축 실측 수치는 여전히 0 개**이고 `§3` 표의 S2 임계(p95 `< 3s` · p50 / throughput `baseline 후 fix` · error rate `< 1%`)와 코드 · spec · 워크플로는 **모두 무변경**이며 재 dispatch 도 0 이다(경위 · 원문 인용은 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 의 `S2 1 회차` 소절). `140 행` checkbox 는 LLM stub · 실 수집 왕복 0 조건 그대로라 계속 `[ ]` 다. 이어 [T-1675](tasks/T-1675-load-k6-s1-11th-run-recovery.md) 가 **재 dispatch 0** 으로 같은 run 로그(`gh run view 32746598803 --log`)만 재독해 그 job 의 S1 leg 수치를 회수했다 — batch p95 **967.52ms** 로 **S1 실측이 11 회**가 됐고, stub 조건 관찰용 게이트 `p(95)<900` 이 실 run 에서 처음 `✗` 가 돼 T-1668 재확정 규칙의 트리거 **①-(a) 가 첫 충족**됐다. 규칙 ② 를 대입한 산정 결과는 실 scale 표본 **8 개**의 평균 **786.13ms** · 표본표준편차 **81.35ms** · 평균+3σ **1030.18ms** → 100ms 올림 **1100ms** 이지만, 숫자 실제 변경은 규칙 ④ 가 못 박은 **2 task split**(코드 `pr` 상수 동기 → 문서 `direct` 수치 갱신)로 이월돼 본 회차에서는 `§3` 표 임계 · `STUB_BASELINE_P95_MS` · smoke spec 상수 · 워크플로가 **전부 무변경**이다. `140 행` checkbox 는 LLM stub · 실 수집 왕복 0 조건 그대로라 계속 `[ ]` 다. 이어 세 slice 가 그 이월분과 skip 구조를 닫았다 — (a) [T-1676](tasks/T-1676-s1-stub-baseline-threshold-code-sync.md) 가 규칙 ④ split 앞단(코드 `pr`)으로 [`s1-batch.js`](../test/load/s1-batch.js) 의 관찰용 게이트 상수 `STUB_BASELINE_P95_MS` 를 `900` → **`1100`** 으로 동기하고 drift guard smoke 의 대조 리터럴을 같은 commit 에서 갱신했으며(PR **#1334** → main **`ebe6d8f8`**), (b) [T-1677](tasks/T-1677-s1-stub-baseline-threshold-doc-sync.md) 이 split 뒷단(문서 `direct`)으로 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3` 임계 표 row · 도출식 각주 · 성격 구분 각주 · 규칙 소절 pointer 를 `1100ms` 로 맞췄고(main **`24b2d3f5`**), (c) [T-1678](tasks/T-1678-load-k6-s2-s3-step-not-cancelled-gate.md) 이 [`load-k6.yml`](../.github/workflows/load-k6.yml) 의 S2 · S3 step 에 `if: ${{ !cancelled() }}` 를 얹어(`always()` 미사용 — 취소 시 부하 발생기 미실행 불변식과 "정리 step 만 `always()`" 관행 보존) **S1 leg red 가 두 leg 를 통째로 skip 시키던 구조 결함**을 닫고 drift guard smoke 에 `T-1678` describe **12 test**(negative 5 종)를 더했다(PR **#1335** → main **`8af5b06d`**). 그럼에도 `140 행` checkbox 는 계속 `[ ]` 다 — 세 slice 는 임계 표현과 실행 게이트만 고쳤을 뿐 LLM 은 여전히 stub(ADR-0057 `D1`) · **실 수집 왕복 0**(50~100 repo · ~1000 page 축 미검증) · 단일 iteration 조건이 그대로라 REQ-047 완료 조건에 미달한다. 본 회차 [T-1679](tasks/T-1679-not-cancelled-gate-doc-sync.md) 도 그 배선 사실을 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` `S2 1 회차` (d) 무효화 표기 · `§5` item 5 문단과 이 줄에 반영하는 `direct` 문서 slice 라 코드 · 워크플로 · spec · 임계 숫자 · 실 dispatch 가 전부 0 이므로 **마커 승격은 없다**. 그 배선 뒤 첫 dispatch 는 [T-1680](tasks/T-1680-load-k6-s2-s3-first-measurement.md) 이 **정확히 1 회** 집행했고(run **32780975839**, head sha **`013f3f10`**, conclusion `success`, 재 dispatch · rerun 0), **S2 축 2 회차 · S3 축 첫 실측**이 회수됐다 — S2 임계 **6 종** · S3 임계 **4 종**이 모두 `✓`(전역 p95 **7.42ms** · **20.91ms**, `http_req_failed` 양쪽 **0.00%**)이고 수치는 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 의 `S2 2 회차` · `S3 1 회차` 에 박제했다. 표본이 축마다 **1 회**뿐이라 `§3` 표의 S2 · S3 임계(`< 3s` · `baseline 후 fix` · `< 1%`)는 **무변경**이고, LLM stub · 실 수집 왕복 0 조건도 그대로라 `140 행` checkbox 는 계속 `[ ]` 다. 그 첫 실측이 정황 추정으로만 답했던 두 물음(공유 dataset 보존 · iteration 자기 정리)을 [T-1682](tasks/T-1682-s3-persons-row-count-log.md) 가 직접 카운트로 바꿨다 — [`s3-concurrent.js`](../test/load/s3-concurrent.js) 의 `setup()` · `teardown()` 에 `GET /api/persons` 행 수 로그 2 줄(수치만 · 분기 0)을 얇고 drift-guard smoke 에 `T-1682` describe **12 케이스**(R-112 4 종)를 더했으며(PR **#1336** → main **`a5f84cb1`**, 2 파일), 준비 / 정리 왕복은 `seed` · `teardown` route tag 라 판정 tag `read` · `write` 의 p95 오염이 **0** 이고 임계 4 종 · stages 는 무변경이다(단 `http_reqs` 항등식은 `3 × iterations` → **`3 × iterations + 2`** 로 바뀜다). 본 회차 [T-1683](tasks/T-1683-s3-row-count-log-doc-sync.md) 은 그 배선 사실을 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§2` S3 계약 · `§3.1` 두 회차 pointer 표기 · `§5` item 5 문단과 이 줄에 반영하는 `direct` 문서 slice 라 **새 dispatch · 수치 재산정이 전부 0** 이며, 실측 회차도 S1 **12 회** · S2 **2 회** · S3 **1 회** 그대로라 `140 행` checkbox 는 여전히 `[ ]` 다. 본 회차 [T-1684](tasks/T-1684-s3-rowcount-dispatch-record.md) 가 그 배선 후 **첫 dispatch 를 정확히 1 회** 태워(run **32798553930**, 2026-08-25T01:42:00Z, head sha `3193d68d`, `-f s1_persons=133`, conclusion `success`, rerun 0) S3 leg 수치를 회수했고 `§3.1` 에 `#### S3 2 회차` 를 신설했다 — 임계 **4 종 전부 `✓`**(전역 p95 **51.53ms** · `http_req_failed` **0.00%** `0 out of 14867`)이고, 무엇보다 행 수 로그가 **시작 133행 · 종료 133행 / 시작 133행** 으로 찍혀 차이가 **0 행** 이라 공유 dataset 보존과 iteration 자기 정리 잔여가 정황이 아닌 **직접 카운트**로 닫혔다(`3 × iterations + 2` = 14867 은 확인만). 실측 회차는 S1 **12 회** · S2 **2 회** · S3 **2 회** 이고 같은 run 의 S1 · S2 leg 수치는 새 dispatch 0 으로 다음 slice 가 재독해 회수하며, `§3` 표 임계는 표본 2 회라 무변경이고 실 수집 왕복도 여전히 0 이라 `140 행` checkbox 는 계속 `[ ]` 다. 본 회차 [T-1685](tasks/T-1685-s1-13-s2-3-log-reread.md) 는 **그 같은 run 의 로그를 재독만** 해(`gh run view 32798553930 --log`, 새 `workflow_dispatch` · rerun · 재시도 **0**) S1 · S2 leg 수치를 회수하고 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 에 `#### 13 회차` 와 `#### S2 3 회차` 를 신설했다 — S1 은 임계 **3 종 전부 `✓`**(batch p95 **858.42ms** · `http_req_failed` **0.00%** `0 out of 7`), S2 는 임계 **6 종 전부 `✓`**(전역 p95 **5.83ms** · route 별 5.17~6.19ms · `http_req_failed` **0.00%** `0 out of 25971`) 이며 두 leg 모두 `level=error` **0 줄** 이고 `p99` 는 **미확보** 다. T-1668 재확정 규칙을 기계 재계수하면 실 scale(표본 133) 표본이 **10 개**(평균 **797.16ms** · 표본표준편차 **75.85ms** · 평균+3σ **1024.70ms** → 100ms 올림 **1100ms** = 현행 임계) 라 트리거 ①-(a) · ①-(b) 가 **모두 미발화** 이고, `§3` 표 · `STUB_BASELINE_P95_MS` · drift-guard spec 은 **문자 단위 무변경** 이다. 실측 회차는 S1 **13 회** · S2 **3 회** · S3 **2 회**(같은 run 이라 S3 는 올리지 않는다) 이고, 행 수 로그가 `s3-concurrent.js` 한정이라 S1 · S2 축 직접 행 수 카운트는 **미확보** 이며 실 수집 왕복도 여전히 0 이라 `140 행` checkbox 는 계속 `[ ]` 다. 본 회차 [T-1686](tasks/T-1686-k6-seed-persons-cap-raise-judgment.md) 은 그 3 회차를 근거로 `§2` 조항 ③ 가 유예했던 `K6_SEED_PERSONS` 상한 상향(30 → 133) 판단을 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 의 `#### K6_SEED_PERSONS 상한 상향 판단 (T-1686)` 소절로 닫은 `direct` 문서 slice 라 **새 dispatch · rerun · 값 변경이 전부 0** 이다 — 결론은 **유지(`30`)**으로, S2 2 · 3 회차 모두 `N` **30** == 상한 < `M` **133** 이라 binding 이지만 `default()` 가 `personIds` 를 소비하지 않아 상향의 측정 이득이 **0** 이기 때문이고(전역 p95 **7.42ms** · **5.83ms** 는 임계 `3000ms` 대비 여유를 보일 뿐 근거가 되지 못한다), 재판단 트리거 4 종만 열거했다. 실측 회차도 S1 **13 회** · S2 **3 회** · S3 **2 회** 그대로라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1687](tasks/T-1687-stagewise-percentile-export-design.md) 은 회차마다 이월돼 온 `p99` 미확보와 S3 저하 곡선의 단계 분해 불가 두 공백의 회수 설계(k6 내장 후보 2 종 `summaryTrendStats` · `handleSummary` · 판정 임계 불변 · 단계 tag key 분리 원칙 · run log 1 줄 출력 규약 · `pr`/`direct` split)를 코드 배선 전에 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3` 의 `#### 단계별 percentile export 설계 (사전 박제, T-1687)` 소절로 박제한 `direct` 문서 slice 라 **새 dispatch · rerun · 코드 변경 · 새 수치가 전부 0** 이고, 실측 회차도 S1 **13 회** · S2 **3 회** · S3 **2 회** 그대로라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1690](tasks/T-1690-stage-value-recovery-path-reconfirm.md) 은 T-1689 가 배선한 `stage` tag 만으로는 k6 종료 요약이 tag 를 자동 분해하지 않아 단계별 값 자체가 생기지 않는다는 caveat 를 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3` 같은 소절의 조항 **⑥**(`349~375 행`)으로 굳혀 경로 α(`thresholds` 에 관찰용 selector 선언)를 배제하고 경로 β(k6 런타임 내장 `k6/metrics` 의 `Trend` custom metric — 새 외부 dependency 0, 판정면 변화 0)를 채택한 `direct` 문서 slice 라 **새 dispatch · rerun · 코드 변경 · 새 수치가 전부 0** 이고, 실측 회차도 S1 **13 회** · S2 **3 회** · S3 **2 회** 그대로라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1692](tasks/T-1692-s3-stage-trend-first-dispatch-record.md) 는 T-1688 · T-1689 · T-1691 배선 후 **첫 dispatch 1 회**(run **32833365988**, head sha `4c6eaac6`, rerun · 재 dispatch 0, conclusion `success`)로 S3 leg 만 회수해 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 에 `#### S3 3 회차` 를 신설한 `direct` 문서 slice 다 — 단계별 custom Trend **3 행이 전부** 출력돼 단계 1 p95 **5.01ms** · p99 **6.55ms** → 단계 2 **21.33ms** · **26.25ms** → 단계 3 **21.4ms** · **26.12ms** 로 단계 분해가 처음 가능해졌고 `p(99)` 열(전역 **25.02ms**)도 실 run 에서 처음 확보됐으며 행 수 잔여도 **0 행**(시작 133행 → 종료 133행)으로 재확인됐지만, 그 단계 표본이 **1 회**뿐이라 `latency cliff` 유무는 단정하지 않고 `§3` 표 임계는 문자 단위 무변경이며 코드 변경도 **0** 이다. 실측 회차는 S3 **2 회 → 3 회** 로만 오르고 S1 **13 회** · S2 **3 회** 는 그대로여서(같은 run 의 S1 · S2 leg 회수는 다음 slice 이월) `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1693](tasks/T-1693-s1-14-s2-4-log-reread.md) 은 그 **같은 run `32833365988` 의 로그를 재독만** 해(새 dispatch · rerun · 재시도 **0** · 코드 변경 **0**) `§3.1` 에 `#### 14 회차`(S1 leg step 12) 와 `#### S2 4 회차`(S2 leg step 14) 를 신설한 `direct` 문서 slice 다 — S1 은 임계 **3 종 전부 `✓`**(batch p95 **770.66ms**), S2 는 **6 종 전부 `✓`**(전역 p95 **6.94ms**) 이고 T-1688 의 `p(99)` 열이 **S1 · S2 두 축에서 처음** 출력돼(S1 전역 **732.45ms** · S2 전역 **8.58ms**) 회차마다 이월돼 온 `p99` 공백이 닫혔으며, T-1668 규칙 기계 재계수(실 scale 표본 **11 개** · 평균 **794.75ms** · 표본표준편차 **72.40ms** · 평균+3σ **1011.94ms** → 올림 후 **1100ms** = 현행 임계)로 트리거 ①-(a) · ①-(b) 가 **모두 미발화**라 `§3` 표 · `STUB_BASELINE_P95_MS` · drift-guard spec 은 문자 단위 무변경이다. 실측 회차는 S1 **13 → 14 회** · S2 **3 → 4 회** 로 오르고 S3 **3 회** 는 그대로이며, 실 수집 왕복이 여전히 **0**(LLM stub) 이라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 [T-1694](tasks/T-1694-s3-stage-trend-second-dispatch-record.md) 는 [`load-k6.yml`](../.github/workflows/load-k6.yml) 을 **정확히 1 회** dispatch(run **32843613484**, 2026-08-25T11:41:27Z, head sha `640f5fe3`, `-f s1_persons=133`, rerun · 재시도 **0**) 해 그 run 의 **S3 leg 만** 회수하고 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` 에 `#### S3 4 회차` 를 신설한 `direct` 문서 slice 다 — run conclusion 은 step 12 S1 leg 가 관찰용 게이트 `✗ 'p(95)<1100' p(95)=1.15s` 를 크로스해(exit 99) **`failure`** 였으나 S3 step 15 는 `if: ${{ !cancelled() }}` 로 그대로 돌아 **`success`** 였고 임계 **4 종 전부 `✓`**(전역 p95 **17.79ms** · `http_req_failed` **0.00%** `0 out of 25205`), 단계별 custom Trend **3 행이 다시 전부** 출력돼(단계 1 p95 **4.36ms** · p99 **7.34ms** → 단계 2 **20.01ms** · **64.93ms** → 단계 3 **18.24ms** · **23.08ms**) **단계 표본이 1 개 → 2 개**가 됐으며 `p(99)` 열(전역 **33.94ms**)도 **연속 2 회** 확보되고 행 수 잔여도 **0 행**(시작 133행 → 종료 133행)으로 재확인됐다. 그럼에도 `latency cliff` 유무 판정과 `§3` 표 임계는 **문자 단위 무변경**이고 코드 변경도 **0** 이다(2 표본에 규칙을 적용하는 판정 · 조항 ⑥ 표시 수단 결정은 별도 slice 소관). 실측 회차는 S3 **3 회 → 4 회** 로만 오르고 S1 **14 회** · S2 **4 회** 는 그대로여서(같은 run 의 S1 · S2 leg 회수는 다음 slice 이월) `140 행` checkbox 는 계속 `[ ]` 다 — 실 수집 왕복은 여전히 **0**(LLM stub). 이어 [T-1695](tasks/T-1695-s1-15-s2-5-log-reread.md) 는 그 **같은 run `32843613484` 의 로그를 재독만** 해(새 `workflow_dispatch` · rerun · 재시도 **0** · 코드 변경 **0**) `§3.1` 에 `#### 15 회차`(S1 leg step 12) 와 `#### S2 5 회차`(S2 leg step 14) 를 신설한 `direct` 문서 slice 다 — S1 은 관찰용 게이트를 **처음 크로스**해 `✗ 'p(95)<1100' p(95)=1.15s` · exit **99** 였고(ms 원값 **1154.508883ms** 는 step 13 summary JSON 에서만 확보 — 콘솔은 `1.15s` 요약 단위라 없는 정밀도를 추정하지 않았다), S2 는 임계 **6 종 전부 `✓`**(전역 p95 **6ms** · `http_req_failed` **0.00%** `0 out of 24963`) 이면서 S1 이 red 인데도 step 14 가 `if: ${{ !cancelled() }}` 로 돌아 **"S1 red 여도 S2 가 돈다" 의 실 run 첫 실증**을 남겼다. T-1668 규칙 기계 재계수(실 scale 표본 **12 개** · 평균 **824.73ms** · 표본표준편차 **124.70ms** · 평균+3σ **1198.83ms** → 올림 후 **1200ms**)로 트리거 **①-(a) · ①-(b) 가 둘 다 발화**했으나, 규칙 ④ 가 코드 `pr` 과 문서 `direct` 를 한 task 로 합치지 말라고 명시해 본 slice 는 `§3` 표 · `STUB_BASELINE_P95_MS` · drift-guard spec 을 **문자 단위 무변경**으로 두고 숫자 집행을 **2 task split 로 이월**했다. 실측 회차는 S1 **14 → 15 회** · S2 **4 → 5 회** 로 오르고 S3 **4 회** 는 그대로이며, 실 수집 왕복이 여전히 **0**(LLM stub) 이라 `140 행` checkbox 는 계속 `[ ]` 다. 그 이월분의 **문서 축은 본 T-1697 이 닫았다** — 코드 축 T-1696(PR **#1340** → main **`5fb0931c`**) 이 `STUB_BASELINE_P95_MS` 를 `1200` 으로 선행 동기한 뒤, 부하계획 `§3` 임계 표 · 도출식 각주 · 성격 구분 각주 · T-1668 규칙 소절 · `§5` item 5 의 관찰용 p95 표기를 `1100` → **`1200`** 으로 맞춘 것이다(새 실측 **0** — 회차 개수 S1 15 · S2 5 · S3 4 와 `140 행` checkbox `[ ]` 는 무변경). 이어 [T-1698](tasks/T-1698-s3-latency-cliff-judgment-rule.md) 은 그 S3 단계 분해 2 표본에 적용할 **판정 규칙만** 부하계획 `§3` 에 `#### S3 latency cliff 판정 규칙 (사전 박제, T-1698)` 소절로 사전 박제한 `direct` 문서 slice 다 — 6 조항(① 판정 입력: 단계별 Trend p95 1 차 · 정황 4 종은 보조 근거로 강등 / ② 판정식: `R(1) = p95(단계 2)/p95(단계 1)` 배수 임계 **8.0**(= 단계 간 VU 증가 비 4 배의 2.0 배) + 절대 하한 가드 **`Δp95 ≥ 10ms`** / ③ 표본 요건: 최소 **2 개** · 최근 연속 2 표본 동일 결론, 미달 시 **판정 보류** / ④ 결론 3 값의 `§3` 표 귀결 / ⑤ 집행 split: 규칙 적용은 별도 `direct` slice / ⑥ 판정면 불변)을 굳히되 **2 표본에 규칙을 대입하지는 않았고**, 새 dispatch · 실측 **0** · 코드 **0 LOC** 이라 `§3` 표 S3 행과 회차 개수(S1 **15 회** · S2 **5 회** · S3 **4 회**) 도 무변경이며, 실 수집 왕복이 여전히 **0**(LLM stub) 이라 `140 행` checkbox 는 계속 `[ ]` 다. 그 규칙의 **2 표본 기계 대입은 T-1699 가 집행**해 `§3.1` S3 3 · 4 회차 꼬리에 산출값을 박제했다 — 표본 1 `R(1) = 21.33 / 5.01 = 4.26` · `Δp95 = 16.32ms`, 표본 2 `R(1) = 20.01 / 4.36 = 4.59` · `Δp95 = 15.65ms` 로 둘 다 배수 임계 `8.0` **불만족** · 가드 `10ms` **만족**이라 조항 ㉱ 상 각 표본 결론이 `cliff 부재` 이고, 조항 ③ 표본 요건(최소 2 개 · 최근 연속 2 표본 동일 결론) 충족으로 종합 결론도 **`cliff 부재`** 이며, 조항 ④ 대로 `§3` 표 S3 행과 규칙 소절(임계 `8.0` · 가드 `10ms`) 은 **문자 단위 무변경**(표 diff **0 줄** · 규칙 재조정 **0**) 이고 새 dispatch · 실측 **0** · 코드 **0 LOC** 이라 회차 개수(S1 **15 회** · S2 **5 회** · S3 **4 회**) 도 그대로이며 `140 행` checkbox 는 계속 `[ ]` 다. 이어 **T-1700 이 그 재확정 임계 `p(95)<1200` 의 실 run 첫 적용을 회수**했다 — `load-k6.yml` 을 `-f s1_persons=133` 으로 **정확히 1 회** dispatch(run **32879776505**, head sha `45c7376a`, conclusion **`success`** · step 21 개 전부 success)해 S1 leg 만 `§3.1` `#### 16 회차` 로 신설했고, THRESHOLDS 로그 원문에 **`✓ 'p(95)<1200' p(95)=824.89ms`** 가 등장해 T-1696(코드) · T-1697(문서) 이 올린 관찰용 게이트가 실 run 에서 처음 **통과**했음이 박제됐다(3 종 전부 `✓` · `level=error` **0 줄** · 표본 `133명 / 133명` 일치 · seed 연속 **9 회** 성공). T-1668 규칙을 실 scale 표본 **13 개**에 기계 재계수한 값은 평균 **824.74ms** · 표본표준편차 **119.39ms** · 평균+3σ **1182.92ms**(올림 **1200ms** = 현행) 로 트리거 **①-(a) · ①-(b) 둘 다 미발화** 라 `§3` 표 · 규칙 소절 · `s1-batch.js` · drift-guard spec 은 **문자 단위 무변경**(코드 **0 LOC** · rerun **0**) 이며, 같은 run 의 **S2 · S3 leg 는 다음 slice 소관**이라 회분 표기는 S1 만 **16 회**(S2 **5 회** · S3 **4 회** 유지) 로 올랐고 실 수집 왕복이 여전히 **0** 이라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 **T-1701 이 같은 run 32879776505 의 로그를 재독만 해**(새 `workflow_dispatch` · `gh run rerun` · 재시도 **0**) T-1700 이 이월한 S2 · S3 leg 를 회수했다 — `§3.1` 에 `#### S2 6 회차`(임계 **6/6 `✓`** · 전역 p95 **6.88ms** · `http_reqs` **22023** · error rate **0.00%** · 표본 `30명 / 133건 / 상한 30명` 무변경 · S1 leg 가 `success` 인 run 에서 얻은 첫 S2 표본) 와 `#### S3 5 회차`(임계 **4/4 `✓`** · 전역 p95 **20.26ms** · `http_reqs` **23510** · 단계별 Trend 3 행 전부 출력 · persons 행 수 시작 **133** = 종료 **133** 으로 잔여 **0**) 를 add-only 로 신설했고, 이로써 그 run 의 미회수 leg 는 **0** 이며 회분 표기는 S2 **6 회** · S3 **5 회**(S1 **16 회** 유지) 로 올랐다. 단계 표본이 **3 개**가 됐다는 사실만 확정하고 T-1698 판정 규칙 조항 ② 의 **3 번째 표본 기계 대입**은 별도 slice 로 미뤘으므로 `§3` 표 S3 행 · 규칙 소절 · S2 행은 **문자 단위 무변경**이고, 코드 **0 LOC** · 실측 **0** 이라 `140 행` checkbox 는 계속 `[ ]` 다. 이어 **T-1702 가 그 판정 규칙 조항 ② 를 3 번째 표본에 기계 대입**해(새 `workflow_dispatch` · rerun · 실측 **0** · 코드 **0 LOC**) `#### S3 5 회차` 꼬리에 표본 3 의 `R(1) = 21.41 / 4.92 = 4.35`(배수 임계 **8.0** 불만족) · `Δp95 = 16.49ms`(절대 가드 **10ms** 만족) 산출값과, 표본 **3 개**로 조항 ③ 을 재평가해도 최소 2 개 · 최근 연속 2 표본(S3 4 · 5 회차) 동일 결론 요건이 충족돼 종합 결론이 **`cliff 부재` 유지**임을 add-only 로 박제했으며, 조항 ④ 대로 `§3` 표 S3 행 · 규칙 소절은 **문자 단위 무변경**이고 회분 표기(S1 **16 회** · S2 **6 회** · S3 **5 회**) 와 `140 행` checkbox `[ ]` 도 그대로다. 이어 **T-1703 이 그 단계 축의 S2 확대 여부를 배선 전에 판단**해(새 `workflow_dispatch` · rerun · 실측 **0** · 코드 **0 LOC**) 부하계획 `§3` 에 `#### S2 단계 분해 확대 판단 (사전 박제, T-1703)` 소절을 add-only 로 신설했다 — `s2-read.js` 가 `vus: 5` · `duration: "20s"` 상수 VU 이고 `options.stages` 가 **0** 이라는 기박제 사실 위에 축 후보 3 종(시간 등분 tag · ramping `stages` 도입 · 확대 안 함)을 조건 ⓐ(판정면 이동) · ⓑ(`§3` 표 대응 칸)로 기계 대입한 결과 3 후보 모두 ⓑ false 라 결론은 **`확대 불요`** 이며, S2 확대 배선 `pr` slice 는 만들지 않고 재개 트리거 T1~T3 만 남겼다. `§3` 표 S2 · S3 행과 규칙 소절은 **문자 단위 무변경**이고 회분 표기(S1 **16 회** · S2 **6 회** · S3 **5 회**) 와 `140 행` checkbox `[ ]` 도 그대로다. 이어 **T-1704 가 `§3` 표에 남은 `baseline 후 fix` 태그 2 칸(S2 `p50 latency / throughput` 관찰용 · S3 `error rate`)의 해제 요건 · 판정 규칙 · 결론을 집행 전에 박제**했다(새 `workflow_dispatch` · rerun · 실측 **0** · 코드 **0 LOC**) — 부하계획 `§3` 에 `#### S2 · S3 baseline 후 fix 표기 해제 판단 (사전 박제, T-1704)` 소절을 add-only 로 신설해 S1 선례에서만 끌어온 요건 3 항(표본 수 — 관찰용 수치 칸 **3 개** · error rate 칸 **5 개** · 조건 동일성 · 산정식)과 조건 2 개(ⓐ 요건 충족 · ⓑ 판정용/관찰용) · 분기 결론 3 값(`해제 채택` · `해제 유예` · `해제 불요`)을 굳힌 뒤 기계 대입해 **S2 칸은 `해제 불요`**(ⓐ true 이지만 관찰용이라 REQ 판정 임계 이동 **0**) · **S3 칸은 `해제 채택`**(표본 5 회 전량 `http_req_failed` **0.00%** — 집행은 코드 0 LOC · 문서 `direct` 별도 slice)으로 결론을 칸마다 1 값씩 박았고, `§3` 의 "실측 아직 **0 회**" bullet 에는 무효화 표기를 add-only 로 덧붙였다(원문 무변경). `§3` 표 · T-1644 각주 · T-1668 규칙 소절은 **문자 단위 무변경**이고 회분 표기(S1 **16 회** · S2 **6 회** · S3 **5 회**) 와 `140 행` checkbox `[ ]` 도 그대로다. 이어 **T-1705 가 그 결론 중 `해제 채택` 인 S3 칸을 집행**해(새 `workflow_dispatch` · rerun · 실측 **0** · 코드 **0 파일 · 0 LOC**) `§3` 표 `200 행` S3 행의 pass 임계를 **`< 1%` 확정**으로 만들고 S1 서식을 승계한 **S3 확정 근거 bullet 1 개**를 add-only 로 신설했으며(표본 **5 개** 전량 `http_req_failed` **0.00%** 인용 — 표 숫자와 [`s3-concurrent.js`](../test/load/s3-concurrent.js) 의 `rate<0.01` 이 이미 같은 값이라 코드 변경 0), 이로써 표에 `baseline 후 fix` 태그가 남은 칸은 **S2 `p50 latency / throughput` 1 개**(`해제 불요`)뿐이고 T-1701 이 이월한 세 항목이 전부 닫혔다 — 정본은 부하계획 [`§3`](ops/load-resilience-test-plan.md) 의 `S3 error rate < 1% 확정 근거` bullet 과 `§5` item 5 꼬리이며, 회분 표기(S1 **16 회** · S2 **6 회** · S3 **5 회**) 와 `140 행` checkbox `[ ]` 는 무변경이다. 이어 **T-1706 이 그 세 항목이 닫힌 뒤 남은 잔여 ①(실 수집 왕복 0)의 해소 경로를 배선 전에 판단**해(새 `workflow_dispatch` · rerun · 실측 **0** · 코드 **0 LOC**) 부하계획 [`§3`](ops/load-resilience-test-plan.md) 에 `#### 실 수집 왕복(§5 잔여 ①) 해소 경로 판단 (사전 박제, T-1706)` 소절을 add-only 로 신설했고, 후보 3 종(㉠ 실 자격증명 주입 · ㉡ 왕복 재현 대체안 · ㉢ 해소 불요)에 조건 2 개(ⓐ 판정면 이동 · ⓑ 자율 집행 가능)를 기계 대입한 결론은 **㉠ `사람 승인 대기` · ㉡ · ㉢ `해소 불요`** 로 `자율 집행 채택` 이 **0 건**이라 배선 slice 도 `humanQuestion` 도 만들지 않으며(재개 트리거 T1~T3 만 남긴다), 잔여 개수 **1 개** 유지 · 회분 표기(S1 **16 회** · S2 **6 회** · S3 **5 회**) 와 `140 행` checkbox `[ ]` 는 무변경이다.
  - **조회·시각화 3초 이내** (R-92) — 이미 저장된 결과 조회 시. 임계는 p95 **3000ms**(REQ-048). **harness shipped(부분)** 이며 아래는 요약본이다 — slice 별 상세·구조 축·계수 셈법의 **정본** 은 [`test/perf/README.md`](../test/perf/README.md) 다.
    - **harness 규모** — [`test/perf/`](../test/perf/README.md) 의 `*.perf-spec.ts` **63 개**(그중 read 경로 **51 개**) + 순수 primitive **4 파일**(`latency-metrics.ts` · `latency-collector.ts` · `latency-baseline.ts` · `latency-baseline-io.ts`).
    - **CI 강제 경로** — [`ci.yml`](../.github/workflows/ci.yml) 의 `perf test` step 이 `pnpm test:perf` 를 돌린다(T-0878). 별도 job 신설 0.
    - **실 DB round-trip baseline** — mock override 0 부트스트랩 + 실 Prisma 왕복 측정이 **slice 29 까지** 도달했고 모두 p95 **< 3000ms** 다(실 DB spec **29 개** · endpoint 도메인 **15** · 조회 route **31**). slice 1~29 각각의 구조 축(index 형태 · guard tier · 404/403 발생 layer · payload 축 · 규모 표본)과 glob 셈법은 [`test/perf/README.md`](../test/perf/README.md) `## 실 DB round-trip baseline (slice 목록)` 이 정본이다([T-1354](tasks/T-1354-plan-p7-perf-verification-evidence.md) · T-1500~T-1557 slice 축 · T-1536~T-1556 doc-sync 축).
    - **체크인 baseline 게이트** — 근거는 [ADR-0056](decisions/ADR-0056-perf-baseline-checkin-ci.md), CI 는 `perf test` step 에 `PERF_CHECKIN_BASELINE: "1"` 로 토글 ON([T-1584](tasks/T-1584-ci-perf-checkin-baseline-toggle.md)), `test/perf/baselines/` 체크인 JSON 은 `measure→confirm` **5 route** 로 확정돼(`§Follow-ups (a)` 완결) 경로가 `absent` 아닌 `compared` 로 돈다. 다만 **상대 회귀는 관찰만 하고 exit code 를 바꾸지 않으며**(`§Decision 3 (b)`) `§Follow-ups (c)` 임계 fix 는 축적 run 20 미만이라 **미완** 이다. 계약 정본은 [`test/perf/README.md`](../test/perf/README.md) `## 체크인 baseline 게이트`.
    - **잔여 4 축** — ① 나머지 read perf-spec **30 개** 는 여전히 service mock + guard override(배선 latency)라 cutover·retire 판단 미착수, ② baseline 파일 확정·임계 fix 미완(잔여 route 인벤토리 (A) 30 / (B) 0 / (C) 0 의 정본은 [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§ 5` item 5), ③ REQ-047 실 scale(100~200명 / 50~100 repo) 부하 미검증 — 위 소규모 표본은 상대 비교용이라 실 scale 이 아니고 두 표본의 대소 관계도 미단언, ④ 시각화(web) 렌더 측정 축 부재.
    - **오너 결정 승계** — 신규 per-route baseline slice 큐잉은 **금지**(아래 `151 행` 상한). 잔여 4 축이 살아 있으므로 위 `140 행` checkbox 는 `[ ]` 로 유지한다.

- [ ] **🔴🔴 [오너 결정 2026-07-30 · ADR-0054 ACCEPTED] R-91 k6 부하검증 — 최우선·즉시 착수, R-92 churn 중단** — [ADR-0054](decisions/ADR-0054-load-resilience-harness-tool.md) **ACCEPTED flip 완료**(owner k6 dependency 승인). **다음 planner turn 은 R-92 per-route perf-spec churn 을 멈추고 본 chain 을 착수한다**: ① k6 도입 pr-mode task(`package.json` + `ci.yml` 별도 job, 상시 PR CI 무영향) ② REQ-047 실 scale 부하 harness — 실데이터 133명([docs/ops/realdata-scale-devset.md](ops/realdata-scale-devset.md)) seed → 100~200명/50~100 repo/~1000 page **1h 이내** + p95 threshold 게이트. **신규 R-92 per-route baseline slice 큐잉 금지(perf-spec 63개 충분, 실 scale 아님).**
- [ ] **🟡 [오너 결정 2026-07-30] R-92 per-route perf baseline churn 중단(상한)** — `test/perf/*.perf-spec.ts` 이미 63개(read 51). route 하나당 slice 1개 반복은 한계효용 ~0(전부 `p95<3000ms` 소규모 표본=실 scale 아님). **신규 per-route baseline slice 큐잉 금지** — R-92 read-latency 축은 충분 입증됨. 잔여 route 는 필요 시 기존 spec 에 route 추가로 흡수(신규 slice task 금지). 자원을 위 R-91 로 전환.
- [x] **🟡 [오너 결정 2026-07-30] PLAN R-92 bullet prune (문서 smell)** — 현 R-92 sub-bullet 이 단일 줄 ~67,000자 mega-narrative(slice 1~19+ 누적)로 비대화. 요약본(≤10줄: 구조 축·p95 3000ms 임계·정본 pointer `test/perf/README.md`)으로 압축하고 slice별 상세는 README 로 외화. direct doc-only. **집행 완료(T-1707)** — 교체 전 단일 줄 `46,156 자` 를 요약본 **7 줄**(위 R-92 bullet)로 압축하고 slice 별 상세·계수 셈법·체크인 게이트 계약은 정본 [`test/perf/README.md`](../test/perf/README.md) 로 pointer 화했다.

- [ ] **🟢 [오너 지시 2026-07-30] R-91 실데이터 규모 dataset 준비 = nnstreamer+nntrainer+Samsung 활성 개발자 133명** — 정본 [docs/ops/realdata-scale-devset.md](ops/realdata-scale-devset.md). 구성: nnstreamer+nntrainer 2026 PR 작성자 33 + Samsung org 최근1년 활동 top100(중복 0) = **133명 (R-91 100~200 범위 충족)**. Person+github `ServiceIdentity` seed → `assessment-collection` 실 수집 → 평가 → R-91 규모 검증(k6, ADR-0054) 실 표본. github 수집=배포기기 PAT. raw 미저장(R-59). **진척(정본 pointer)** — dataset 구성 · seed 배선 · 실 run 성공(첫 성공 run `32665014391` 의 `person 133 건 / serviceIdentity 133 건 적재`, 표본 로그 run `32677333740` 의 `표본 취득 133명 / 요청 133명`, [load-resilience-test-plan.md](ops/load-resilience-test-plan.md) `§3.1` `#### 16 회차` 기준 seed step **연속 9 회 성공**)의 정본은 [realdata-scale-devset.md](ops/realdata-scale-devset.md) `## seed 실행 경로` 의 **실측 상태** bullet 이다(T-1708). 다만 위 arrow chain 중 `실 수집 → 평가` 단계는 LLM `LOAD_TEST_STUB=1` stub · 자격증명 0 이라 **미발화**이므로 본 checkbox 는 `[ ]` 로 유지한다.
---

## Phase P8 — Hardening & launch

- [x] E2E 시나리오 커버리지. **implemented-on-main**: `test/e2e/` 에 19 e2e-spec 시나리오 커버리지가 shipped — 인증 [`auth.e2e-spec.ts`](../test/e2e/auth.e2e-spec.ts)·평가 [`assessments.e2e-spec.ts`](../test/e2e/assessments.e2e-spec.ts)·기여 [`contributions.e2e-spec.ts`](../test/e2e/contributions.e2e-spec.ts)·그룹/파트/인원 [`groups`](../test/e2e/groups.e2e-spec.ts)/[`parts`](../test/e2e/parts.e2e-spec.ts)/[`persons.e2e-spec.ts`](../test/e2e/persons.e2e-spec.ts)·요약 [`summaries.e2e-spec.ts`](../test/e2e/summaries.e2e-spec.ts)·임의기간 평가 [`period-bridge-admin-persist`](../test/e2e/period-bridge-admin-persist.e2e-spec.ts)/[`ephemeral`](../test/e2e/period-bridge-ephemeral.e2e-spec.ts)/[`reevaluate.e2e-spec.ts`](../test/e2e/period-bridge-reevaluate.e2e-spec.ts)·권한거부 audit [`permission-denied-records.e2e-spec.ts`](../test/e2e/permission-denied-records.e2e-spec.ts)·export streaming [`export-download.e2e-spec.ts`](../test/e2e/export-download.e2e-spec.ts)·미평가 채움 [`unevaluated-fill-plan`](../test/e2e/unevaluated-fill-plan.e2e-spec.ts)/[`run.e2e-spec.ts`](../test/e2e/unevaluated-fill-run.e2e-spec.ts)·인스턴스 접근 [`user-instance-access`](../test/e2e/user-instance-access.e2e-spec.ts)/[`audit-roundtrip.e2e-spec.ts`](../test/e2e/user-instance-access-audit-roundtrip.e2e-spec.ts)·수집 트리거 [`assessment-collection-trigger.e2e-spec.ts`](../test/e2e/assessment-collection-trigger.e2e-spec.ts) 등(대표 나열, 전체 shipped). [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) 의 `pnpm test:e2e` step 으로 CI 에서 자동 수행(R-113). [T-0825](tasks/T-0825-plan-p8-e2e-scenario-coverage-implemented-on-main.md).
- [x] 보안 점검 (secret 처리, 인증 흐름, RBAC). **보안 감사 완료**: [docs/ops/security-review.md](ops/security-review.md) 가 secret 처리(§1 JWT secret 해석 `resolve-jwt-secret.ts`·access↔refresh 분리·env 주입 정책·실값 금지 규율)·인증 흐름(§2 발급 `auth.controller.ts`·검증 `jwt.strategy.ts`·guard `jwt-auth.guard.ts`·미인증 차단 401, REQ-043)·RBAC(§3 `roles.decorator.ts`+`roles.guard.ts` escalation·Admin 게이트 REQ-045·permission-denied audit ADR-0023 REQ-008/016) 4 섹션 + 감사 결과 표(통제/실증파일/REQ/상태)로 실증. 심각 미비 통제 0(gap G1~G4 모두 known-deferral 낮음), 각 gap 은 별도 pr-mode task 로 tracking. [T-0824](tasks/T-0824-p8-security-review-audit.md).
- [x] 운영 문서 (배포·복구·trouble-shoot). **implemented-on-main**: [`docs/ops/runbook.md`](ops/runbook.md) 가 배포(§1 재배포 실행/migration 적용 순서/배포 성공 확인)·복구(§2 롤백/DB restore/migration 실패 대응)·trouble-shoot(§3 증상별 진단 + 알려진 장애 유형)·운영 전제(§4 체크리스트) 4 섹션을 실행 절차 중심으로 cover, 정책은 [`deployment.md`](architecture/deployment.md)·[`daily-deploy-test.md`](ops/daily-deploy-test.md) cross-link. [T-0821](tasks/T-0821-p8-ops-runbook.md).
- [ ] 부하·내성 테스트 (계획: [docs/ops/load-resilience-test-plan.md](ops/load-resilience-test-plan.md) — 시나리오 3종(평가 배치 1h / 조회 p95 3s / 동시성 내성)·측정 임계·도구 후보 문서 확정. harness 구현·도구 도입은 신규 dependency ADR 후 follow-up 이므로 checkbox 는 미완료 유지. [T-0826](tasks/T-0826-p8-load-resilience-test-plan.md))

---

## 운영 정책 review backlog

phase 진행과 별개로, driver / loop / cron 등 운영 정책 자체의 완화·강화를 정기 검토 대상으로 박제. 본 절은 결정이 아니라 **future ADR 의 출발점 인덱스** — 트리거 조건이 충족되면 planner 가 해당 항목을 ADR 작성 task 로 변환한다.

- [x] **cron 1-fire 1-task 정책 완화 검토 — 완결** — 현 [docs/LOOP.md](LOOP.md) §3 의 cron 매 발화 1-task 종료 정책의 완화 가능성을 [ADR-0020](decisions/ADR-0020-multi-task-fire-cron-n2-activation.md) (ACCEPTED) 으로 검토·결정 완결했다. 4-step rollout (T-0197 ADR / T-0198 STATE `flags.multiTaskFire` 필드 / T-0199 LOOP.md §1 `[7.5]` cron chain 분기 / T-0200 §10 재조정 + 토글) 으로 **N=2 cron-fire 한정 활성** 완료 — 한 cron fire 안 task 최대 2 개 chain (`/loop`·human 진입점은 1-task/turn 불변). `STATE.json` `flags.multiTaskFire: true` 토글로 활성화됐으며, 활성 후 30 일 dogfood 관찰 window 진행 중 (위반/context 누적/race/push-contention 관찰, 1 회 재발 시 flag `false` rollback). N≥3 는 ADR-0020 이 금지.
- [x] **CLAUDE.md / LOOP.md 길이 mitigation 검토 — 완결(후보 a 채택)** — 두 문서 후반부 hard rule (CLAUDE §11 trail / §12 언어 / LOOP §4 push hard rule / [.claude/agents/reviewer.md](../.claude/agents/reviewer.md) (4) sub-check 등) attention drift 누락 위험을, 후보 (a) **CLAUDE.md 앞단 §0.5 "Hard rule 인덱스" 1 페이지 cheat sheet** (핵심 8 룰 요약 + 본문 § navigation) 로 mitigate 완료. 트리거 평가 (session #49, [Q-0018](STATE.json) (4)(a) 사용자 결정): LOC 합 = CLAUDE 461 + LOOP 444 = **905 ≥ 800 MET**, 그러나 "룰 누락 사고 1건 재발" 트리거는 §0.5 도입 후 **0 건** — cheat sheet 가 attention-drift 를 실효 차단 중. 후보 (b) LOOP.md §1 표준 prompt 의 `docs/DRIVER_PROMPT.md` 분리는 single-source doc-drift 유지비용 > 현 가치라 **미채택** (사고 1건 재발 시 ADR 로 재검토 — 재검토 트리거는 살아있음). → 본 항목 완결, §0.5 cheat sheet 가 표준 mitigation.
- [x] **cron@cloud `refs/locks/*` 403 자율성 해소 — 완결(2026-06-05)** — cloud sandbox credential proxy 가 `claude/*` 접두 브랜치로만 push 허용([공식](https://code.claude.com/docs/en/claude-code-on-the-web))해 [ADR-0009](decisions/ADR-0009-strong-ref-cas-lock.md) 의 `refs/locks/driver`(브랜치 아님) lock CAS push 가 **HTTP 403** → cron@cloud 가 ref-CAS lock 미획득·pr-mode stand down (로컬 `/loop` 의존, 진정한 cron 자율 long-horizon 불가). GitHub 권한 grant(2026-06-02) 후에도 **403 잔존 실증** (`B-credential-2026-06-04T16:04Z` + 06-05 cron@cloud no-op journal) → proxy 제한이 GitHub App scope 와 별개 층임 확정. **해법(설계 기존재, 현 SUPERSEDED = [T-0154](tasks/T-0154-cloud-proxy-branch-lock-adr.md))**: lock 저장소를 `refs/locks/driver`(blob ref) → `claude/*` 네임스페이스 **브랜치**(예: `refs/heads/claude/lock-driver`, commit ref)로 이전 — proxy 기본 허용이라 **PAT·토글 없이** cron@cloud 자율 lock 가능, `--force-with-lease` CAS 원자성(강한 mutex)은 ref 종류 무관 불변. **planner 처리**: T-0154 refresh 활성화 — ① premise-gate 충족 박제(403 잔존 실증 → ADR 진행 분기) ② T-0154 계획의 ADR-0015 번호 선점됐으므로 다음 free `ADR-NNNN` 재배정 → ADR(pr) 신설 → 후속 direct: LOOP.md §1[1]·§4 + CLAUDE.md §10 의 lock 명령을 브랜치-lock 프로토콜로 동기 + feature-branch cleanup 이 lock 브랜치 오삭제 안 하게 가드 → ACCEPTED flip + 다음 cron@cloud fire 자율 lock 검증. 대안 기각: PAT 주입(§5 외부 credential 부담) / "unrestricted branch pushes" 토글(`refs/locks/*` 는 브랜치 아니라 여전히 403, 불충분). dependency 0. **해소 결과**: ADR-0028 (T-0242 PR-209 c5926fd PROPOSED → T-0243 414a6e2 ACCEPTED, 2026-06-05) 가 lock 저장소를 `refs/locks/driver` (blob) → `refs/heads/claude/lock-driver` (commit ref) 로 이전. 첫 cron@cloud 자율 lock CAS 성공 = cron@vm-454c 8195047 (16:08 KST, `claude/lock-driver` zero-sha 생성 → tip 0472be7, credential 0, 옛 9 회 403 패턴 종결). T-0154 SUPERSEDED (T-0244 22dd70f, `supersededBy: [ADR-0028, T-0242, T-0243]`). ADR-0028 Follow-up §3 운영 관찰 검증 완료.
- [x] **realdata live spec timeout 120s 상향 — 완결(2026-07-30, T-1314)** — 배포 기기 gating 활성화(§E/§G, `.env.realdata`) 이후 daily-test 가 live leg 4종을 실제로 돌린다. 현 timeout 은 `realdata-e2e-live` 30s / `realdata-e2e-github-collection-live` 30s / `realdata-e2e-eval-chain-live` 45s 인데, 로컬 Ollama **콜드 로드가 실측 64.5s**(warm 8.8s)라 예열이 빠지는 경우(예: docker 재빌드로 redeploy 가 keep_alive 5분을 넘김) 첫 LLM 호출이 그대로 타임아웃난다. **세 spec 의 `jest.setTimeout` 을 120000 으로 상향** — 값은 이미 `rediscovery-search-live`·`publish-live` 가 쓰는 컨벤션이고 콜드 64.5s 대비 2배 여유다. 5분은 hang 감지가 과도하게 둔해져(live it 6개 × 5분 = 최악 30분) 기각. 본 항목은 ADR 불요 — planner 가 **`commitMode: pr` task 1개**(3 파일 1줄씩)로 바로 변환하면 된다. 해당 리터럴을 감시하는 drift-guard spec 은 없음(확인 완료). 예열(`start-llm.ps1` 의 `-NoWarm` 제거)은 루틴 쪽 별건이며 본 상향은 그 보험이다. **shipped**: [T-1314](tasks/T-1314-realdata-live-spec-timeout-120s.md) → PR #1196 squash `6b0b2aee` — 세 spec `jest.setTimeout` 을 30s/30s/45s → `120000` 으로 상향 + 근거 주석 + [docs/ops/daily-deploy-test.md](ops/daily-deploy-test.md) §G-4 인용 리터럴 정합. gating 구조 변경 0. 예열(`start-llm.ps1 -NoWarm` 제거)은 예고대로 별건으로 남는다.
- [x] **`_common.ps1` 폴백 정리의 stale 런너 프로세스명 수정 — 완결(2026-07-30, T-1315)** — [`deploy/local-llm-example/_common.ps1`](../deploy/local-llm-example/_common.ps1) 의 `Start-OllamaServerIfNeeded` 폴백 경로가 멈춘 인스턴스를 정리할 때 종료 대상을 `'ollama app', 'ollama', 'ollama_llama_server'` 로 잡는데, **현재 Ollama 는 모델을 자식 프로세스 `llama-server`**(`%LOCALAPPDATA%\Programs\Ollama\lib\ollama\llama-server.exe`)**에 적재**한다 — 마지막 이름이 구버전이라 매칭되지 않는다. 실측(2026-07-30, RTX 4070 12GB): 부모(`ollama app`+`ollama`)만 강제 종료하면 `llama-server` 가 **고아로 남아 VRAM 10,588 MiB 를 계속 점유**했고, 그 프로세스를 직접 종료한 뒤 1,357 MiB 로 복귀했다. 영향 — 서버가 멈춰 폴백을 타는 상황에서 이전 런너가 8GB 를 물고 있으면 새 서버의 모델 적재가 VRAM 부족으로 CPU offload 되거나 실패해 **daily-test 의 live leg 타임아웃/FAIL 의 잠재 원인**이 된다. 수정 = 종료 목록에 `'llama-server'` 추가(구버전 호환을 위해 기존 이름 유지 — 1줄). `commitMode: pr`, ADR 불요. 동반 확인: 이 목록을 검증하는 parity spec(`realdata-e2e-local-llm-example-common-ps1-*` / `install-ps1-*` / `stop-llm-ps1-*` contract 3종)에 프로세스명 assert 가 있으면 함께 갱신하고, 없으면 R-112 대로 목록 assert 를 1개 추가한다. 참고: 게임 등 다른 GPU 소비자를 위해 VRAM 을 즉시 비우는 정상 경로는 `stop-llm.ps1`(`keep_alive=0`, 서버 자신이 언로드 — 고아 0, 실측 1,405 MiB 복귀)이며 부모 강제 종료가 아니다. **shipped**: [T-1315](tasks/T-1315-local-llm-killlist-llama-server.md) → PR #1197 squash `f4cd5da2` — kill 목록이 `_common.ps1:116` 외에 `expose-lan.ps1:64` · `install.ps1:89` 에도 글자-동일 복제돼 있어 **세 곳 모두**에 `'llama-server'` 를 추가(레거시 3 이름·`-ErrorAction SilentlyContinue` 유지)했고, 동반 확인 지시대로 기존 contract 3종에 프로세스명 assert 가 없음을 확인해 R-112 대로 정적 parity smoke 1 파일(15 test)을 신설했다 — 목록 drift 가 이제 red 로 잡힌다. `stop-llm.ps1` 정상 경로(keep_alive=0 자기 언로드)는 예고대로 무변경.
- [ ] **[오너 지시 2026-08-31] 슬라이스 과분할 차단 — 소비처 동반 의무** — 3 일(08-29~08-31) 실측에서 PR 43 건의 제품 코드 합이 2,725 줄(**PR 당 평균 63 줄**)이었고 같은 기간 test 8,269 줄 · 문서 5,358 줄이 붙었다. 극단 사례가 [ADR-0058](decisions/ADR-0058-service-identity-management-api.md) `§Follow-ups (d)` 의 T-1770~T-1777 — "AdminView 에 ServiceIdentity 행별 액션 버튼 마운트" 하나에 **PR 8 건**(러너 → 플래그 helper → 어댑터 → props factory → slot → slot factory → 편집 진입 helper → 마운트). 각 slice 의 코드 품질은 문제가 아니고, **절단면이 기능이 아니라 diff 크기**인 것이 문제다 — §3 cap 이 상한만 정하고 "무엇이 한 slice 인가" 의 하한을 정하지 않아 cap 에 닿지 않는 한 계속 잘게 자를 유인이 남는다. slice 당 고정비는 task 문서 ~100 줄 + spec ~190 줄 + bookkeeping commit 2 건. **처리** = [T-1820](tasks/T-1820-slice-granularity-rule.md) (`direct`, CLAUDE.md §3 에 "소비처 동반 의무" bullet 추가 — 소비처 없는 helper 단독 PR 금지, cap 초과가 수치로 제시된 경우만 분리 허용).
- [ ] **[오너 지시 2026-08-31] REQ 재판정 왕복 제거 — 구현 후 1 회만** — 3 일 commit 183 건 중 **48 건이 재판정 · 승격 · 마커 · doc-sync · 정정**. 그 중 기계적 왕복이 있다: `REQ-075 재판정(T-1792) → 슬롯(T-1793) → 배선(T-1794) → REQ-075 재판정(T-1795)`, `REQ-077 재판정(T-1798) → 배선(T-1799) → REQ-077 재판정(T-1800)`. 같은 REQ 를 구현 직전·직후로 두 번 판정하며 각각이 별도 task + 별도 commit + 별도 fire 를 쓴다. **구현 전 재판정은 새 정보를 만들지 않는다** — 직후 재판정이 같은 REQ 를 다시 실측해 덮어쓰고, 구현 전에 필요한 "아직 유효한가" 판단은 planner 의 issue-still-relevant pre-check 가 이미 수행해 `plannerNote` 에 박제하고 있다. **처리** = [T-1821](tasks/T-1821-req-readjudication-once-rule.md) (`direct`, CLAUDE.md §3.1 에 판정 규칙 6 추가 + 예외 2 종 — arc 무관 status drift 정정 / 다중 REQ arc 는 REQ 당 1 회 기준).
- [ ] **[오너 지시 2026-08-31] AdminView.tsx god component 부채 — 3,630 줄** — [web/src/views/AdminView.tsx](../web/src/views/AdminView.tsx) 가 **3,630 줄 · top-level 선언 39 개**(2026-09-04 head `06074b68` 실측)이며, 최초 기록(2026-08-31 · 6,087 줄 · 선언 149 개) 대비 **-2,457 줄**이다. **측정 방법**(다음 갱신이 비교 가능하도록 박제) — LOC 은 `wc -l web/src/views/AdminView.tsx`, 선언 수는 `grep -cE '^(export )?(async )?(function|const|let|type|interface|class|enum) ' web/src/views/AdminView.tsx`. 최초 값 149 는 측정 방법이 기록되지 않았으므로 **선언 수 증감은 지표(indicative)일 뿐 동일 방법 비교가 아니다** — 동일 방법 비교가 성립하는 지표는 LOC 뿐이다. 본 bullet 이 이 부채의 **최초 추적 지점**이다(그 전까지 PLAN · ADR · task 어디에도 없었음). 추적 부재는 구조적 유인의 결과다 — §3 cap(`diff ≤ 300 LOC`)이 순수 추출 리팩터에서는 이동량을 그대로 diff 로 계상해 400 줄 helper 군 추출이 즉시 cap 초과가 되는 반면, 같은 helper 를 AdminView 에 **덧붙이는** slice 는 60~120 줄이라 언제나 cap 안이다. 즉 cap 이 **append 를 싸게, extract 를 비싸게** 만들어 god component 를 키우는 방향으로 작동했다. **선행 처리** = [T-1822](tasks/T-1822-pure-extraction-cap-bend-category.md) (`direct`, [.claude/agents/planner.md](../.claude/agents/planner.md) cap-bend 표에 "순수 추출 리팩터" 카테고리 + `sizeExempt` 직행 규칙 추가 — 파일 수 cap 5 는 유지, LOC 만 면제). **진척 = 순수 추출 16 슬라이스** — [adminServiceIdentityRowActions.tsx](../web/src/views/adminServiceIdentityRowActions.tsx)(행 액션 helper 군, 선행) · [T-1830](tasks/T-1830-adminview-collection-target-runners-extract.md) [adminCollectionTargetRunners.ts](../web/src/views/adminCollectionTargetRunners.ts)(수집 대상 러너 군) · [T-1852](tasks/T-1852-adminview-service-identity-runners-extract.md) [adminServiceIdentityRunners.ts](../web/src/views/adminServiceIdentityRunners.ts)(ServiceIdentity 러너 군, `-200 줄` + 역방향 import 해소) · [T-1854](tasks/T-1854-adminview-group-part-mutation-runners-extract.md) [adminGroupPartMutationRunners.ts](../web/src/views/adminGroupPartMutationRunners.ts)(그룹·파트 mutation 러너 군 14 심볼, `-484 줄`) · [T-1856](tasks/T-1856-adminview-person-mutation-runners-extract.md) [adminPersonMutationRunners.ts](../web/src/views/adminPersonMutationRunners.ts)(인원 mutation 러너 군 11 심볼, `-287 줄`) · [T-1857](tasks/T-1857-adminview-llm-provider-mutation-runners-extract.md) [adminLlmProviderMutationRunners.ts](../web/src/views/adminLlmProviderMutationRunners.ts)(LLM provider mutation 러너 군, `-238 줄`) · [T-1860](tasks/T-1860-adminview-import-export-runners-extract.md) [adminImportExportRunners.ts](../web/src/views/adminImportExportRunners.ts)(import/export 러너 군 21 심볼, `-356 줄`) · [T-1869](tasks/T-1869-adminview-schedule-apply-runner-extract.md) [adminScheduleRunners.ts](../web/src/views/adminScheduleRunners.ts)(스케줄 apply 러너 · 안내 문구 helper 3 심볼 + 상수 6, `-91 줄`) · [T-1870](tasks/T-1870-adminview-schedule-trigger-reeval-runners-extract.md) 같은 모듈(trigger · 재평가 러너 4 심볼 + 상수 2, `-100 줄`, **스케줄 축 마감**) · [T-1872](tasks/T-1872-adminview-create-user-runners-extract.md) [adminUserMutationRunners.ts](../web/src/views/adminUserMutationRunners.ts)(사용자 생성 축 9 선언, `-105 줄`) · [T-1873](tasks/T-1873-adminview-user-access-role-runners-extract.md) 같은 모듈(인스턴스 접근 · 역할 변경 축 10 심볼 + 상수 4, `-194 줄`, **사용자 관리 축 마감**) · [T-1874](tasks/T-1874-adminview-membership-mutation-runners-extract.md) [adminMembershipRunners.ts](../web/src/views/adminMembershipRunners.ts)(그룹 멤버십 add · remove 러너 4 심볼, `-126 줄`) · [T-1876](tasks/T-1876-adminview-membership-derivations-extract.md) [adminMembershipDerivations.ts](../web/src/views/adminMembershipDerivations.ts)(멤버십 파생 helper 5 + row 타입 3 + 상수 1, `-151 줄`) · [T-1877](tasks/T-1877-adminview-difficulty-assign-runner-extract.md) [adminLlmProviderMutationRunners.ts](../web/src/views/adminLlmProviderMutationRunners.ts)(난이도 매핑 assign 축 2 심볼 + 상수 1, `-59 줄`, **난이도 매핑 mutation 축 마감**) · [T-1879](tasks/T-1879-adminview-resource-path-builders-extract.md) [adminResourcePathBuilders.ts](../web/src/views/adminResourcePathBuilders.ts)(경로 빌더 순수 helper 8 심볼, `-127 줄`) · [T-1880](tasks/T-1880-adminview-provider-difficulty-derivations-extract.md) [adminProviderDifficultyDerivations.ts](../web/src/views/adminProviderDifficultyDerivations.ts)(provider · 난이도 파생 helper 4 심볼 + 상수 1, `-105 줄`, **파생 helper 축 마감**). **넷째 슬라이스에서 처음으로 큰 순 감소가 나왔다** — 3 슬라이스 시점의 순 감소는 34 줄에 그쳤고 선언 수는 되레 늘어 **append 속도가 extract 속도를 앞섰다**고 적었는데, T-1854 한 건이 `-484 줄`을 냈고 선언 수도 167 → 150 으로 처음 감소했다. **그 페이스가 6 슬라이스 연속 이어졌다** — T-1856 `-287 줄` · T-1857 `-238 줄` · T-1860 `-356 줄` · T-1869 `-91 줄` · T-1870 `-100 줄` 로 다섯 건이 더해졌고, 이어 T-1872 `-105 줄` · T-1873 `-194 줄` · T-1874 `-126 줄` 세 건이 더해졌으며, 다시 T-1876 `-151 줄` · T-1877 `-59 줄` · T-1879 `-127 줄` · T-1880 `-105 줄` 네 건이 더해져 누적 감소는 최초 기록 대비 **-2,457 줄**(선언 149 → 39)이 됐다. 다만 잔여는 여전히 크다 — 목표선 ≤ 2,000 줄 까지 **-1,630 줄**이 남아 선언된 슬라이스당 -250~500 줄 밴드 상단(-500 줄)으로도 산술 4 회 이상이 필요하고, **최근 3 슬라이스 실측은 `-59` · `-127` · `-105`(평균 `-97`) 로 그 밴드 하단에도 못 미친다** — 그 페이스가 이어지면 산술 17 회가 필요하다. **구조 산술**(head `06074b68` 실측) — 파일은 4 구역이다: 헤더 주석 + import 블록 `1 행` ~ `319 행`(319 줄) · top-level 상수 · 타입 · 순수 helper `321 행` ~ `591 행`(**271 줄** — 직전 측정 514 줄에서 `-243`) · `AdminView` 컴포넌트 본문 `593 행` ~ `3496 행`(2,904 줄 — 직전 측정과 **동일**) · 배럴 재수출 `3498 행` ~ `3630 행`(133 줄). 즉 **잔여 helper 표면 271 줄을 전량 추출해도 약 3,360 줄**이라 목표선까지 1,360 줄이 남는다 — 지금까지의 순수 추출 경로만으로는 **목표 도달이 산술적으로 불가능**하며, 목표선은 **컴포넌트 본문 2,904 줄의 섹션 단위 하위 컴포넌트 분리** 없이는 닿지 않는다. 그 작업은 markup + state 를 함께 옮기므로 [.claude/agents/planner.md](../.claude/agents/planner.md) 의 **순수 추출 3 조건 (a)(b)(c) 를 그대로 충족하지 못할 수 있어**(`sizeExempt` 직행 카테고리와 성격이 다르다) 착수 전 별도 판단이 필요하다. **본문 내부 분해**(head `06074b68` 실측, 본 갱신에서 처음 박제) — 컴포넌트 본문 2,904 줄은 `593 행` ~ `2589 행` 의 hooks · state · 핸들러 prelude(**1,997 줄**)와 `2590 행` ~ `3496 행` 의 JSX return(**907 줄**)으로 나뉘며, 본문 mass 의 약 **69% 가 markup 이 아니라 prelude** 다. 따라서 "JSX 섹션 하위 컴포넌트화" 만으로는 최대 907 줄에만 닿고, 목표선은 **prelude 의 state · 핸들러 군집을 custom hook 모듈로 옮기는 경로** 없이는 도달하지 않는다. 그 경로 역시 markup 이 아닌 state 를 옮기므로 위 순수 추출 3 조건 (a)(b)(c) 충족 여부가 **착수 전 별도 판단** 대상이라는 점은 같다. 또 **순 감소가 이동량보다 작게 나오는 이유** — 추출마다 import 블록과 배럴 재수출이 커져(현재 319 + 133 = **452 줄**, 파일의 약 12%) 이동분을 상쇄하기 때문이며, 이동 62 줄이 순 `-59` 로 상쇄된 T-1877 이 그 실례다. **후속** = 실제 분할 `pr` task — 직전 지목(경로 빌더 helper 축 8 심볼)은 [T-1879](tasks/T-1879-adminview-resource-path-builders-extract.md) 가 [adminResourcePathBuilders.ts](../web/src/views/adminResourcePathBuilders.ts) 로 전량 추출해 마감했고, 더 작은 후속 후보로 적어 둔 provider · 난이도 파생 helper 축도 [T-1880](tasks/T-1880-adminview-provider-difficulty-derivations-extract.md) 이 [adminProviderDifficultyDerivations.ts](../web/src/views/adminProviderDifficultyDerivations.ts) 로 이미 옮겨 좌표가 통째로 무효이므로 교체한다. 다음 대상은 **잔여 순수 표면 일괄 소진 슬라이스** 다 — `321 행` ~ `591 행` 의 **잔여 top-level 상수 · 타입 · 순수 helper 전량**(271 줄)이며 세 묶음이다: ① 화면 문구 · DOM id 상수 22 개(`PERSON_HEADING`(`323 행`) ~ `FALLBACK_GROUP_NAME`(`505 행`), `AUTH_ME_PATH`(`416 행`) 포함) · ② 수집 대상 범위 편집 축 4 심볼(`EMPTY_COLLECTION_TARGET_SCOPE_INPUT`(`354 행`) · `SCOPE_EDIT_SEPARATOR`(`361 행`) · `foldScopeForEdit`(`366 행`) · `buildScopePatch`(`375 행`)) · ③ 폼 옵션 · 게이트 축 8 심볼(`ScopeOption`(`434 행`) · `EXPORT_SCOPE_OPTIONS`(`440 행`) · `LlmProviderOption`(`455 행`) · `LLM_PROVIDER_OPTIONS`(`461 행`) · `resolveProviderSelectValue`(`479 행`) · `REEVAL_WINDOW_OPTIONS`(`490 행`) · `InFlightIdGate`(`563 행`) · `createInFlightIdGate`(`575 행`)). **잔류 허용** — 컴포넌트 자신의 props · 응답 row 타입(`MeRow`(`511 행`) · `AdminViewProps`(`515 행`))과 `isAdminRole`(`557 행`)은 잔류해도 무방하다(컴포넌트 계약에 밀착 — 옮기면 역방향 import 위험). **목적지** — ② 는 이미 수집 대상 축을 소유한 [adminCollectionTargetRunners.ts](../web/src/views/adminCollectionTargetRunners.ts) 로(같은 모듈이 `parseScopeInput` · `scopeFieldsForCollectionTargetType` 를 이미 쓴다), ① · ③ 은 신규 모듈 1 개로 모은다 — 즉 **한 슬라이스 = AdminView + 기존 모듈 1 + 신규 모듈 1(+ 신규 spec 1)** 로 파일 cap(≤ 5) 안이며 LOC 은 순수 추출 면제 대상이다. **소비처 동반 의무 충족** — 잔류 컨테이너가 세 묶음을 계속 소비하므로(예: `foldScopeForEdit` `2044 행` · `EMPTY_COLLECTION_TARGET_SCOPE_INPUT` `2028 행` · 문구 상수는 JSX return 전역) AdminView 가 새 모듈에서 import 로 되돌려 쓰는 방향이며 helper 단독 slice 가 아니다. 기대 순 감소는 **`-230 줄` 안팎**(이동 271 줄 − 신규 import 블록 증가분)이고, 이 한 건으로 **잔여 helper 표면이 소진**되어 이후 남는 것은 컴포넌트 본문뿐이다. **파일 cap 주의** — 배럴(`3498 행` ~ `3630 행`) 재수출을 유지하면 심볼 import 방식 spec 은 무수정 통과하지만, AdminView **소스 텍스트**를 `readFileSync` 로 읽는 drift-guard 가 15 개 있으므로 그 anchor 가 이동 대상 문구 상수인지 **슬라이스 착수 시 재확인**해 파일 cap(≤ 5, LOC 만 면제) 산술에 반영한다. 분할 진행에 따라 본 bullet 의 실측 LOC 을 갱신하고, 목표선(≤ 2,000 줄) 도달 시 `[x]`.
- [ ] **PLAN.md 단계별 분리 검토** — 현재 PLAN.md 1 파일 + [PLAN_archive.md](PLAN_archive.md) 완료-phase 분리로 충분. 트리거: phase 별 평균 LOC ≥ 30 또는 PLAN.md 합계 LOC ≥ 350 도달 시 phase 별 (`PLAN_PN.md`) 파일 분리 ADR-NNNN 검토. 트리거 미달 시 현 구조 유지.
- [x] **ADR-0036 fine-grained concurrency staged rollout 추적 — stage 5b ON (2026-06-10)** — [ADR-0036](decisions/ADR-0036-fine-grained-concurrency.md) (**ACCEPTED 2026-06-10**) 의 critical-section-only lock + claim 기반 task 소유 (driver=N 동시 진행) 를 `flags.fineGrainedConcurrency` 토글 기반 staged rollout 으로 채택 완료. stage 1 (T-0326: `flags` 자리 박제 + [planner.md](../.claude/agents/planner.md) 독립-stream 분해 정책 §) → **break-even gate build-through 승인** (2026-06-10 사용자 결정 — 한 시점 독립 task ≥ 2 가 cron + multi-machine `/loop` 상시 진입점으로 이미 실증돼 stage 2~5 인프라 구축을 막지 않음, ADR-0036 §Decision 0) → stage 2~5 인프라 전량 shipped (claim registry schema + select+claim / §1 loop 재작성 + CLAUDE §10 · LOOP §4 동기 / per-PR CI concurrency group / T-0341 §Decision 8 안전장치 5종 — fail-safe 강등·claim-시점 재검증·merge 직전 rebase+CI green·`concurrencyIncidents` 회로 차단기·5a/5b/5c 이행). 현재 **stage 5b ON**: T-0348 (5a `maxConcurrentClaims=1` 메커니즘 검증) → T-0349 (5a 게이트 무사고 실측) → T-0350 (5b, `STATE.json.flags.fineGrainedConcurrency=true` + `maxConcurrentClaims=2`, `commitMode: direct` task 끼리만 동시 claim 2 — pr-mode 는 활성 claim 0 일 때만 단독). 상세는 CLAUDE.md §10 line 341~343 + [concurrency.md](architecture/concurrency.md). 잔여: stage 5c (전면 병렬 + 30일 dogfood) 는 5b 정확성 게이트(이중 claim 0 + bookkeeping 충돌 0) 통과 후 별도 task — 5c 잔여 있으나 rollout 이 실효 활성(stage 5b) 됐으므로 `[x]`. §D8 (d) 회로 차단기 강등 시에만 토글 OFF 복귀.

---

## 의존성

P0 → P0.5 → **P1 (Architecture)** → **P2 (Use case decomposition)** → (P3, P4 병행) → P5 → P6 → P7 → P8

각 phase 내부 task 순서는 planner가 결정한다.

P1 의 architecture document 는 living document — 이후 task 진행 중 architect 가 ADR 와 함께 갱신한다 ([.claude/agents/architect.md](../.claude/agents/architect.md)).
