# Requirements Traceability — README → REQ-NNN

본 문서는 [README.md](../README.md) 의 모든 지시사항을 **추적 가능한 REQ-NNN ID** 로 박제하고, 각 REQ 가 어느 phase / task / code / test 에서 구현·검증되는지 1:1 매핑하는 표다.

## 운영 룰

- **단일 source of truth**: README 의 새 지시 / 수정 / 삭제는 본 문서의 매핑에도 즉시 반영 (planner 가 README 변경을 감지하면 REQ row 갱신).
- **kind enum**: `FR` (Functional Requirement — 사용자 시나리오 / 기능 / 행동) / `NFR` (Non-Functional Requirement — 성능·보안·가용성·확장성·UX 품질) / `Constraint` (외부 제약 — 사용 가능 stack / 외부 시스템 / 정책 / 법적 / 운영).
- **상태 enum**: `PLANNED` (PLAN.md 에 bullet 으로 등록) / `IN_PROGRESS` (대응 task 진행 중) / `DONE` (대응 PR merge 됨) / `BLOCKED` (humanQuestion 발생) / `SUPERSEDED` (해당 REQ 가 다른 REQ 로 대체됨).
- **검증 위치 enum**: `unit` / `smoke` / `e2e` / `perf` / `policy` (정책 / 문서 / agent rule) / `manual` (사람 검증 필요) / `n/a`.
- **하나의 REQ 가 여러 task 에 분포 가능**: "구현 위치" 컬럼에 phase / task 목록을 comma 로.
- 본 표의 본문은 **P1-Entry (T-0013) 가 채웠다** — 66 REQ row 모두 `kind` (FR / NFR / Constraint) 분류 완료, 7 컬럼 schema 로 확장. 이후 README 변경 시 planner 가 본 표를 동기화.

## 매핑 표

7 컬럼 schema (REQ / README 행 / 요약 / kind / 구현 위치 / 검증 위치 / 상태). kind 값: `FR` / `NFR` / `Constraint`.

| REQ | README 행 | 요약 | kind | 구현 위치 (phase/task) | 검증 위치 | 상태 |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-001 | 1 | 본 문서는 Use Case 문서의 기본 | Constraint | P2 | policy | DONE (implemented-on-main — 4 축 모두 실측 충족. **UC 문서 실재 축**: `ls docs/use-cases/` 결과 `UC-NN-*.md` 8 건 (`UC-01-evaluation-execution.md` · `UC-02-evaluation-query.md` · `UC-03-person-crud.md` · `UC-04-account-auth.md` · `UC-05-llm-config.md` · `UC-06-evaluation-delete-reeval.md` · `UC-07-export-import.md` · `UC-08-permission-denied.md`) 와 부속 문서 2 건 (`docs/use-cases/INDEX.md` 106 행 · `docs/use-cases/REQ-COVERAGE-AUDIT.md` 177 행) 이 모두 실재한다. INDEX.md 는 3 행에서 자신을 "P2 entry task T-0019 의 산출물" 이자 "각 UC 본문의 목차" 로 규정하고 17 행 `## 2. UC 목록 표` 절이 UC ID · title · actor · 주요 component · 주요 module · 관련 REQ · status 컬럼으로 8 UC 를 파일 링크와 함께 열거한다. REQ-COVERAGE-AUDIT.md 는 상단 결론부 18 행에서 "8 UC 의 `coversReq` 합집합이 functional REQ 의 거의 전부를 cover, gap 1 건 (REQ-004) 검출, cross-cutting 4 건 · infrastructure 13 건 · uc-covered 48 건" 으로 판정한다. **README 역추적 축**: `grep -c "README" docs/use-cases/UC-*.md` 는 8/8 파일이 README 를 인용 (UC-01 9 · UC-02 6 · UC-03 7 · UC-04 9 · UC-05 9 · UC-06 6 · UC-07 6 · UC-08 4 건) 하고 `grep -c "REQ-0"` 도 8/8 파일이 REQ 를 인용 (UC-01 56 · UC-02 32 · UC-03 49 · UC-04 31 · UC-05 46 · UC-06 42 · UC-07 36 · UC-08 41 건) 해 0 건 파일이 없다. **사용 안내 축**: `README.md` 4~9 행 (`# Assessment-Agent` — 무엇을 평가 · 저장 · 표시하는 시스템인지 소개) · 66~74 행 (`# 평가 자료의 시각화와 UI` — 조회 · 이름/ID/지표 sorting · filtering · 기간별 지표 추이 · Admin 의 주기 지정 · manual trigger · 최근 구간 삭제) · 136~140 행 (`# 로컬 빌드 / 테스트` — `pnpm install` 부터의 실행 순서) 이 사용자가 할 수 있는 동작을 열거한다. **정책 박제 축**: `.claude/agents/planner.md` 88 행 `## P2 (Use case decomposition) entry sequence` 아래 92 행 "**P2-Entry**: Use case 인벤토리 — README → `docs/use-cases/UC-NN-*.md` 1개씩 (한 호출당 1 use case task). 모든 functional REQ 가 1+ use case 로 cover 되는지 검증." 과 본 문서 7 행 "**단일 source of truth**: README 의 새 지시 / 수정 / 삭제는 본 문서의 매핑에도 즉시 반영 (planner 가 README 변경을 감지하면 REQ row 갱신)." 이 README → UC 파생을 planner 운영 규칙으로 박제한다. 검증 위치 `policy` 는 이 두 문장으로 충족되고, 구현 위치 `P2` 는 `docs/PLAN.md` 34 행 (`[x]` UC 발굴 — INDEX.md 8 UC backbone 박제 + UC-01 ~ UC-08 본문 분해 8/8 closure) · 35 행 (`[x]` UC ↔ component 매핑 8/8 closure) · 43 행 ("Phase P2 fully complete — T-0031 머지 시점") 의 완료 마킹으로 충족된다. 한계 — "Description 역할" 의 충분성은 정성 판정이라 정적 실측으로 확인 불가이고, README 본문과 8 UC 본문의 내용 정합은 문장 단위 전수 대조 없이는 판정 불가라 본 재판정은 파일 실재 · 인용 건수 · index/audit 실재 수준에서 멈춘다. 정책 축의 두 문장은 문서 서술일 뿐 자동 강제가 아니다 — `.github/workflows/` 와 `scripts/` 전수에서 `use-cases` 를 참조하는 것은 `scripts/check-doc-only-pr.sh` 30 행의 doc-only 경로 allowlist 뿐이라, README 변경 시 UC 갱신 누락을 막는 CI step 은 0 건이다) |
| REQ-002 | 6 | Web Interface 를 제공하는 Agent System | FR | P6 / P3 | smoke + e2e | IN_PROGRESS |
| REQ-003 | 7 | 개발자 기여 양·질 평가 / 저장 / 표시 | FR | P3 + P5 + P6 | unit + smoke | IN_PROGRESS (평가 축 · 저장 축 · 표시 축 API 실재 / 표시 축 프런트 렌더 부재: **평가 축** 의 orchestration 진입은 `src/assessment-evaluation/evaluation-orchestrator.service.ts` 139~142 행 `async evaluateActivities(activities: Activity[], options: ScoringOptions): Promise<EvaluationResult[]>` 이며, 158 행 `computeEvaluationAdjustmentSignals(deduped)` 로 detection 신호 container 를 모은 뒤 164 행 `results.push(await this.scoringService.scoreUnit(input, options))` 를 dedup 후 단위마다 순차 호출하고 177 행 `applyEvaluationAdjustments(entries, signals)` 로 후처리해 반환한다. **양 축과 질 축은 각각 별도 심볼로 실재** 하며 분기점은 `src/assessment-evaluation/evaluation-scoring.service.ts` 90 행 `async scoreUnit` 안이다 — 양 축은 107 행 `const volume = calculateEvaluationVolume(input)` (정의 `src/assessment-evaluation/domain/evaluation-volume.ts` 30 행 `export function calculateEvaluationVolume(input: EvaluationInput): number`) 이 내는 정수 1 종이고, 질 축은 99~101 행 `this.gateway.generate(prompt, { modelId })` 가 낸 `narrative` 본문 + 104 행 `const { difficulty, contribution } = classifyNarrative(narrative)` (정의 `src/assessment-evaluation/domain/evaluation-prompt.ts` 155 행 `export function classifyNarrative`) 가 내는 등급 2 종이다 — 한쪽 축만 실재하는 상태가 아니다. **저장 축** 의 진입은 `src/assessment-evaluation/evaluation-result-persist.service.ts` 89 행 `export class EvaluationResultPersistService` 의 103 행 `async persist(context, results, mode)` 이고, 108 행 `mapEvaluationResultsToAssessment(context, results)` → `$transaction` → 209 행 `createAssessment` 의 213 행 `tx.assessment.create({ data: { ...mapped.assessment, contributions: { create: mapped.contributions } } })` nested create 로 도달한다. upsert 가 아니라 **reset-and-recreate insert** 다 — 좌표 row 가 이미 있으면 fill 모드는 no-op 이고 reeval 모드만 기존 row 를 delete 후 재생성하며, 실패 경계는 P2002 와 reeval delete 경합의 P2025 를 ConflictException 으로 좁혀 변환하고 그 외 Prisma error 는 전파한다. 컬럼 대조 결과 **양 축 값** 은 단위 차원 `model Contribution` (`prisma/schema.prisma` 329 행) 의 337 행 `volume Int` 와 좌표 차원 `model Assessment` (294 행) 의 302 행 `volume Int` 두 곳에 담긴다 (매퍼 `src/assessment-evaluation/domain/evaluation-result.persist.mapper.ts` 148 행 `volume: result.volume` / 183 행 Σ 합산 → 202 행 전사). **질 축 값** 은 셋으로 갈린다 — 난이도는 146 행 `difficulty: result.difficulty` → Contribution 335 행 `difficulty String` 및 200 행 `aggregateDifficulty(contributions)` → Assessment 300 행에 담기고, 기여도 등급은 원 등급 문자열 그대로가 아니라 147 행 `contributionScore: contributionLevelToScore(result.contribution)` 로 **수치 변환된 뒤** Contribution 336 행 · Assessment 301 행 `contributionScore Decimal` 에 담기며, 정성 평가문은 192 행 `results.map((r) => r.narrative).join("\n\n")` → Assessment 303 행 `narrative String` **1 컬럼에만** 담긴다 — `model Contribution` 329~349 행에는 narrative 컬럼이 아예 없어 단위별 평가문이 개별 row 로 보존되지 않는다. 좌표 요약 저장은 별도 경로다: `src/assessment-evaluation/summary-persist.service.ts` 98~103 행 `async persistSummary(context, results, mode, options)` 가 105 행 `aggregateMetricScore(results)` 와 110~114 행 `generateBatchNarrative` 를 합쳐 192 행 `tx.summary.create` 로 `model Summary` (361 행) 의 366 행 `narrative String` · 367 행 `metricScore Decimal` 2 컬럼에 쓴다. **표시 축 (a) API 는 실재** 한다 — `src/user/assessment.controller.ts` 71 행 `@Controller("api/assessments")` 의 93~96 행 `@Get()` + `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles("User")` `findByPerson` 이 103 행에서 service 로 위임하고, `src/user/assessment.service.ts` 99~106 행이 다시 `src/user/assessment.repository.ts` 134~148 행 `findByPerson` 의 `prisma.assessment.findMany({ where, orderBy: { periodStart: "desc" } })` 로 위임하는데 `select` 절이 없어 Assessment 10 컬럼 (295~304 행) 전체 — 즉 양 축 `volume` 과 질 축 `difficulty` · `contributionScore` · `narrative` 4 필드 — 가 응답 row 에 그대로 실린다 (단건은 114~118 행 `@Get(":id")` `findOne`). `src/user/contribution.controller.ts` 는 75 행 `@Controller("api/contributions")` 의 97~100 행 `findByAssessment` 로 **선택된 1 Assessment 의 component 단위 목록** 을 내는 하위 조회라, 사람 좌표 목록을 내는 assessments route 와 역할이 갈린다. **표시 축 (b) 프런트 렌더는 미충족** 이다 — `web/src/views/DashboardView.tsx` 103 행 `buildAssessmentsPath` 가 114 행 `/api/assessments?...` path 를 조립하고 368~369 행 `useApiResource<EvaluationResultRow[]>(path)` 로 소비하지만, 그 `EvaluationResultRow` 는 `web/src/components/EvaluationResultTable.tsx` 9~18 행의 `{ id, subjectName, metricLabel, score }` 4 필드이고 22~26 행 `COLUMNS` 가 렌더하는 컬럼도 `subjectName` · `metricLabel` · `score` 3 종이라 backend row 필드명과 `id` 외 **한 개도 일치하지 않는다**. 특히 양 축 `volume` 과 난이도 `difficulty` 는 655 행 분량의 `DashboardView.tsx` 전수에서 참조가 **0** 이고, 240 행 `deriveScoreBuckets` 의 점수 히스토그램도 존재하지 않는 필드인 248 행 `row.score` 를 읽어 250 행 clamp 후 0 bucket 으로 수렴한다. **wiring 축** 은 dead code 가 아니다 — spec 제외 전수 참조에서 `EvaluationOrchestratorService` 는 정의 (orchestrator 85 행) 외에 `src/assessment-evaluation/assessment-evaluation.controller.ts` 148 행 · `src/assessment-evaluation/period-bridge-ephemeral.service.ts` 69 행 · `src/assessment-evaluation/period-bridge-admin-persist.service.ts` 83 행 3 곳에서 생성자 주입되고, `EvaluationResultPersistService` 는 정의 (persist service 89 행) 외에 같은 controller 149 행 · admin bridge 84 행 2 곳에서 주입된다 (나머지 hit 은 `assessment-evaluation.module.ts` 78 · 83 · 156 · 157 행의 provider / export 등록과 주석뿐). 평가→저장 chain 은 controller 208~211 행 `@Post("evaluate")` + `@Roles("Admin")` → 220 행 `this.orchestrator.evaluateActivities(activities, {...})` → orchestrator 164 행 `scoreUnit` → 243 행 `this.persistService.persist(context, results, mode)` → persist 103 행 → 213 행 `tx.assessment.create` 이고, 표시 chain 은 assessments controller 93 행 → service 99 행 → repository 134 행이다 — 정의만 있고 호출 0 인 심볼은 없다. 검증 위치 `unit + smoke` 의 unit 실 근거는 행두 `it(` 기준 3 spec 172 it 이다 (`src/assessment-evaluation/evaluation-orchestrator.service.spec.ts` 119 it · `src/assessment-evaluation/evaluation-result-persist.service.spec.ts` 18 it · `src/user/assessment.controller.spec.ts` 35 it). smoke 는 REQ-003 축을 사실상 cover 하지 않는다 — `ls test/smoke/` 150 파일 중 `api/assessments` · `api/contributions` · `EvaluationResultPersistService` 를 참조하는 spec 이 0 이고, 평가 축만 `test/smoke/period-bridge-live.smoke-spec.ts` 가 2 it (185 행 실 LLM round-trip happy · 219 행 격리) 로 닿는데 67~69 행 `resolveLiveTestGating` 가 env 부재 시 `describe.skip` 이라 공개 CI 기본 조건에서는 발화조차 하지 않으며, 그 격리 it 은 오히려 persist 심볼 주입 0 을 검증한다. 한계 — 단위 평가문이 Contribution 에 컬럼 없이 Assessment.narrative 로 join 되어 단위별 정성 근거의 개별 조회 경로가 없고 기여도 등급도 수치 변환 후에만 저장돼 원 등급 문자열이 복원되지 않는다. 프런트는 필드명 계약이 backend 와 어긋나 양 축·질 축 어느 값도 실렌더되지 않고, smoke 는 저장 축·표시 축을 0 건 cover 한다. 양 축 수치의 산출 입력 신호 구성과 그 calibration 근거는 본 재판정에서 확인하지 않았다) |
| REQ-004 | 9 | 수치 지표 + LLM 평가 코멘트 (사용자 지정 기간) | FR | P5 | unit + e2e | IN_PROGRESS (수치 축 · 기간 축 · LLM 코멘트 축 · API 노출 축 실재 / 프런트 노출 축 · 기간 종료 경계 · 좌표 종합 코멘트의 HTTP 진입점 부재: **수치 축** 은 `src/assessment-evaluation/evaluation-scoring.service.ts` 90~93 행 `async scoreUnit(input: EvaluationInput, options: ScoringOptions): Promise<EvaluationResult>` 이 담당하고, 옵션 타입은 46~49 행 `export interface ScoringOptions` 의 `modelId: string` 단일 필드다. 산출은 110~116 행 단일 return 의 5 필드 (`unitId` / `narrative` / `difficulty` / `contribution` / `volume`) 이며 그 중 지표는 **단일 합산 점수가 아니라 3 종 병렬 값** 이다 — 104 행 `const { difficulty, contribution } = classifyNarrative(narrative)` 가 LLM 평가문에서 뽑는 난이도 (easy / medium / hard) · 기여도 (zero / low / medium / high) 2 종 등급, 107 행 `const volume = calculateEvaluationVolume(input)` 이 내는 정수 1 종. 단일 점수로의 합성은 **좌표 차원에서만** 일어난다 — `src/assessment-evaluation/domain/summary-aggregate.ts` 84 행 `export function aggregateMetricScore(results: EvaluationResult[]): number` 가 79~83 행 주석의 규칙 (난이도 평균 + 기여도 평균 + `log1p(Σ volume)`) 을 57~59 행 `DIFFICULTY_WEIGHT` / `CONTRIBUTION_WEIGHT` / `VOLUME_WEIGHT` 각 1 로 가중 합산한 뒤 63 행 `METRIC_SCORE_PRECISION = 6` 자리로 round 해 단일 `metricScore` 를 만들고, 빈 입력은 86~88 행에서 결정적 0 이다. 즉 "다양한 지표" 는 단위 3 종 + 좌표 합성 1 종으로 충족하되, 양 축 입력 신호는 `src/assessment-evaluation/domain/evaluation-volume.ts` 30~31 행 `input.metadata.titleLength` **단일** 이다 (33~35 행 비-number → 0, 37~39 행 비유한수 → 0, 41~44 행 `Math.floor` 후 음수 절하로 ≥ 0 정수). **기간 축** 의 입력 계약은 `src/assessment-evaluation/dto/period-bridge.dto.ts` 36 행 `export class PeriodBridgeDto` 의 5 키다 — 41~43 행 `personId` (`@IsString` + `@IsNotEmpty`), 47~49 행 `period`, 53~55 행 `scope`, 61~64 행 `periodStart` (`@IsString` + `@IsNotEmpty` + **`@IsISO8601`**), 82~84 행 `reevaluate?` (`@IsOptional` + `@IsBoolean`). 45~46 행 주석대로 `period` 의 허용 literal (day / week / month) 은 `@IsIn` 미적용이라 DTO 가 아니라 service 책임이고, DTO 는 형식만 강제한다. route 는 `src/assessment-evaluation/assessment-evaluation.controller.ts` 133 행 `@Controller("api/assessment-evaluation")` 아래 339~342 행 `@Post("period")` + `@HttpCode(200)` + `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles("User")` 이며, 352~355 행이 `isAdminRole(actor?.role)` 로 Admin 분기 (`persistForAdmin`) 와 User 분기 (`ephemeralForUser`) 를 dispatch 한다 (같은 route 의 role 분기 — 208~211 행 `@Post("evaluate")` 는 `@Roles("Admin")` 로 이미 수집된 activities 를 body 로 받아 평가·영속하는 별개 경로이고, `period` 는 기간 좌표로 **fresh collect 부터** 수행한다는 점이 역할 차이다). 기간은 **임의 시작 + 고정 granularity** 다 — 277~287 행 `normalizeKstPeriodStart` 가 `getKstPeriodRangeByPeriod(period, parseKstPeriodInput(periodStart, timeZone), timeZone).start` 로 입력 instant 를 요청 period 의 canonical 경계로 snap 하므로 시작 입력은 임의 ISO 이되 좌표는 일/주/월 경계로 수렴한다. 종료 경계 입력 필드는 계약에 **없다** — 430~443 행 User 분기와 474~501 행 Admin 분기가 모두 `{ since: <snap 된 boundary>.toISOString() }` 만 넘기고, `src/assessment-collection/collection-spec.service.ts` 39~42 행 `async buildCollectionSpec(person: GithubCollectionSpecInput, since?: string): Promise<CollectionSpec>` 도 시작만 받는다. 두 실행 경로 차이는 영속화다 — `src/assessment-evaluation/period-bridge-ephemeral.service.ts` 100~123 행 `generateEphemeral` 은 106~109 행 spec 조립 → 113 행 `collectActivities(spec)` → 116~119 행 `filterActivitiesByAuthor` → 122 행 `evaluateActivities(filtered, options)` 로 끝나 **DB write 0** 이고, `src/assessment-evaluation/period-bridge-admin-persist.service.ts` 130~158 행 `generateAndPersist` 는 같은 4 단계 (138~141 / 145 / 148~151 / 154 행) 뒤 157 행 `persistAndReadThrough(context, results, reevaluate)` 로 영속화한다. **LLM 코멘트 축** 의 단위 차원은 scoring service 99~101 행 `this.gateway.generate(prompt, { modelId })` **1 회** 결과 `narrative` 가 그대로 `src/assessment-evaluation/domain/evaluation-result.ts` 54~70 행 `export interface EvaluationResult` 의 2 번째 필드로 실리는 구조다. 그 prompt 는 `src/assessment-evaluation/domain/evaluation-prompt.ts` 91~113 행 `buildEvaluationPrompt` 가 92~97 행 `"다음 기여 활동을 평가하라."` + `contributionKind` / `sourceType` / `timestamp`, 102~105 행 조건부 `titleLength`, 108~110 행 난이도·기여도 형식 지시로 조립하므로 **단위 1 건 평가문 + 등급 마커** 이지 활동 목록 요약이 아니다. README 9 행의 "어떠한 주요 활동이 있었는지" 에 대응하는 **구간 종합** 코멘트는 별도로 실재한다 — `src/assessment-evaluation/summary-narrative.service.ts` 96~112 행 `generateBatchNarrative(context, results, options)` 가 102 행 `buildSummaryBatchPrompt` 로 조립해 106~108 행 generate 1 회로 좌표당 1 문단을 만들고, 그 prompt 는 `src/assessment-evaluation/domain/summary-batch-prompt.ts` 61 행 `"다음은 한 사람의 한 평가 구간에 속한 단위 기여 평가 묶음이다. 이 묶음을 종합해 해당 구간의 요약 평가문을 한 단락으로 작성하라."` + 62~65 행 좌표 (personId / period / periodStart / unitCount) + 77~82 행 단위별 `difficulty` · `contribution` · `volume` · `narrative` 직렬화다 (빈 묶음은 68~73 행에서 "평가할 단위 기여가 없음" prompt 로 흡수). fallback 경계는 두 service 가 같다 — gateway reject 는 swallow 0 으로 전파 (scoring 78~80 행 · narrative 83~86 행 주석), 빈/malformed narrative 는 `evaluation-prompt.ts` 155~173 행 `classifyNarrative` 가 marker 부재·미인식 값에서 안전 default (medium / low) 로 환원해 throw 0 이며, narrative 문자열 자체는 narrative service 110~111 행에서 위장 없이 그대로 반환된다. **wiring 축** 은 dead code 가 아니다 (spec 제외 전수 참조): `EvaluationScoringService` 의 실 호출부는 `src/assessment-evaluation/evaluation-orchestrator.service.ts` 90 행 생성자 주입 + 164 행 `results.push(await this.scoringService.scoreUnit(input, options))` 1 곳이고, 두 bridge service 는 controller 154 행 / 161 행 생성자 주입 후 439 행 `this.ephemeralBridge.generateEphemeral(...)` / 495 행 `this.adminBridge.generateAndPersist(...)` 로 호출된다 — chain 은 controller 339 행 `@Post("period")` → 353 / 355 행 role dispatch → bridge → `evaluateActivities` → orchestrator 164 행 `scoreUnit` → scoring service 95~107 행 domain 순수 함수 3 종이다. 반면 좌표 종합 코멘트 chain 은 `SummaryNarrativeService` → `src/assessment-evaluation/summary-persist.service.ts` 110 행 `generateBatchNarrative` 호출 → `SummaryAggregateOrchestratorService` → `SummaryBatchOrchestratorService` 로 이어지지만 **HTTP caller 가 0** 이다 (전수 참조에서 controller 측 참조 0, `assessment-evaluation.module.ts` 의 provider / export 등록뿐). **노출 축** 은 3 항목이 갈린다 — (a) API 응답에 수치와 코멘트가 함께 실린다: User 분기 반환은 `EvaluationResult[]` 라 한 객체 안에 narrative 와 3 지표가 병존하고, 읽기 경로도 `src/user/assessment.repository.ts` 55~62 행 `difficulty` · `contributionScore` · `volume` · `narrative` 4 필드가 같은 Assessment row 이며 134~146 행 `findByPerson` 이 `select` 절 없이 `findMany` 하므로 `src/user/assessment.controller.ts` 71 행 `@Controller("api/assessments")` 응답에 그대로 실린다. 단 Admin 분기 응답 (controller 505~512 행) 은 `assessmentId` / `personId` / `period` / `scope` / `periodStart` / `created` 좌표 6 키만이라 수치·코멘트가 0 이다. (b) 프런트 렌더는 미충족이다: `web/src/views/DashboardView.tsx` 198~218 행 `deriveContributionMetrics` 가 206 행 `row.score ?? row.contribution` 과 209 행 `row.rationale ?? row.narrative` 로 점수·정성 근거를 파생해 616~623 행 `EvaluationDetailPanel` 로 넘기지만, 그 source 는 185~191 행 `buildContributionsPath` 의 `GET /api/contributions` 이고 `prisma/schema.prisma` 329~349 행 `model Contribution` 에는 narrative 컬럼이 없으며 수치 컬럼명도 336 행 `contributionScore` 라, 163~177 행 `ContributionRow` 가 기대하는 `score` / `contribution` / `rationale` / `narrative` 어느 키와도 일치하지 않아 실제 렌더는 207~208 행 점수 0 fallback + 209 행 근거 undefined 로 수렴한다. (c) 기간 지정 UI 는 부재다: `period` 는 73~98 행 `DashboardViewProps` 의 optional prop (78~79 행) 일 뿐 화면 입력 요소가 없고 (파일의 유일한 입력 컨트롤은 597~600 행 `<select aria-label="평가 결과 선택">`), `web/src/AppShell.tsx` 141 행이 `<DashboardView />` 를 무-prop 로 마운트하며, `web/src` 전수에서 `assessment-evaluation` 참조가 0 이라 `POST /period` 를 호출하는 프런트 경로 자체가 없다. 검증 위치 `unit + e2e` 의 실 근거는 unit 2 spec 24 it (`src/assessment-evaluation/evaluation-scoring.service.spec.ts` 14 it · `src/assessment-evaluation/summary-narrative.service.spec.ts` 10 it) + e2e 2 spec 18 it (`test/e2e/period-bridge-ephemeral.e2e-spec.ts` 9 it · `test/e2e/period-bridge-admin-persist.e2e-spec.ts` 9 it) 이다. e2e 는 **기간 축과 노출 축 (HTTP 계약) 만** cover 한다 — ephemeral spec 은 self-only 200 + DB write 0 · actor 재존재 회귀 · 403 / 401 / 404 / 400 negative 를, admin spec 은 full-persist row 0→1 · 같은 KST 주의 서로 다른 입력 2 건이 동일 좌표로 수렴 · first-write-wins 재호출 시 row 증가 0 · RBAC negative 를 검증하며, happy case 의 평가 결과가 빈 배열이라 **수치 값과 LLM 코멘트 본문은 e2e 가 검증하지 않는다**. 한계 — 사용자 지정 "기간" 이 시작 (since) 만 갖고 종료 경계 입력이 계약에 없어 수집이 open-ended 이며, 좌표 종합 코멘트는 HTTP 진입점이 0 이라 "지정 기간의 주요 활동 요약" 이 API 로 도달하지 않고 단위별 평가문만 노출된다. 프런트는 기간 지정 UI 가 없고 상세 패널 파생 필드명이 backend Contribution row 와 불일치해 수치·코멘트가 실질 렌더되지 않으며, e2e 는 수치·코멘트 값 축을 cover 하지 않는다. `volume` 이 `titleLength` 단일 신호 기반인 점, `aggregateMetricScore` 의 가중치 3 종이 모두 1 인 baseline 인 점의 calibration 근거도 본 재판정에서 확인하지 않았다) |
| REQ-005 | 16 | github.com 평가 | FR | P4 | unit (provider) + e2e | DONE (단일 GithubAdapter + instance-keyed env config 배선, public 은 api.github.com 라우팅 특례 — live 호출은 env-gated) |
| REQ-006 | 17 | github.sec.samsung.net 평가 | FR | P4 | unit + e2e | DONE (같은 GithubAdapter 의 sec instance 경로, GITHUB_LIVE_HOST_SPECS 사양 박제 — live 호출은 env-gated) |
| REQ-007 | 18 | github.ecodesamsung.com 평가 | FR | P4 | unit + e2e | DONE (같은 GithubAdapter 의 ecode instance 경로, GITHUB_LIVE_HOST_SPECS 사양 박제 — live 호출은 env-gated) |
| REQ-008 | 20 | 접근 권한(read) 부족 시 인식·통지 | FR | P4 | unit + smoke | DONE (implemented-on-main — 감지 · 기록·영속 · 인식 경로 3 축이 모두 실재하고 프런트 배선까지 이어진다. 감지 축: `src/github/github-adapter.service.ts` 411 행 `private mapNon2xx(status: number, input: GithubRequestInput): GithubDomainError` 이 status 401/403 에서 417 행 `this.permissionDeniedEmitter.emit({ host: input.host, path: input.path, status })` 를 호출한 뒤 `GithubDomainError("permission-denied", ...)` 를 throw 하고, 이 매핑은 384 행 `throw this.mapNon2xx(response.status, input)` 로 실 요청 경로에서 호출된다. 정의만 있고 호출 참조 0 인 심볼이 아니다 — 수집 경로는 `src/assessment-collection/github-collection.service.ts` 71 행 `constructor(private readonly client: GithubInstanceClient)` + 90 행 `await this.client.requestAllPagesForInstance(...)` 이고 (org/repo 열거도 동형: `github-org-repo-enumerate.service.ts` 28 행 주입 + 43 행 호출), `src/github/github-instance-client.service.ts` 55 행 `private readonly adapter: GithubAdapter` 가 70 행 `this.adapter.request(input)` · 82 행 `this.adapter.requestAllPages(input)` 로 위임한다. 주입되는 emitter 도 no-op 이 아니다 — `src/github/github.module.ts` 62 행이 `PERMISSION_DENIED_EMITTER` token 에 `useClass: PersistingPermissionDeniedEmitter` 를 바인딩한다. 기록·영속 축: `src/permission-denied/persisting-permission-denied-emitter.ts` 46 행 `emit(event: PermissionDeniedEvent): void` 이 49~55 행에서 `recordService.record({ provider: "github", instanceRef: event.host, resourceRef: event.path, httpStatus: event.status })` 로 정규화하며 (fire-and-forget — 56~63 행 `.catch` 가 DB 실패를 흡수해 adapter throw 흐름을 보존), 저장 진입 메서드는 `permission-denied-record.service.ts` 109 행 `async record(event: RecordPermissionDeniedInput): Promise<PermissionDeniedRecord>` 이고 82 행 `deriveReason` 이 401/403 → `"permission-denied"`, 404 → `"not-found-or-hidden"`, 그 외 → null 로 도출한다. 실 write 는 `permission-denied-record.repository.ts` 69 행 `this.prisma.permissionDeniedRecord.create({ data: input })` 이며 저장 대상 모델은 `prisma/schema.prisma` 513 행 `model PermissionDeniedRecord` (provider / instanceRef / resourceRef / principal? / httpStatus / reason? / createdAt + 523~524 행 index 2 개) 로 실재한다. 인식 경로 축: `permission-denied-record.controller.ts` 78 행 `@Controller("api/permission-denied-records")` + 98~100 행 `@Get()` · `@UseGuards(JwtAuthGuard, RolesGuard)` · `@Roles("User")` 이라 authenticated User 이상 (User / Admin / SuperAdmin) 이 GET 으로 조회한다. audience 차등은 controller 분기 0 이고 service 가 단일 강제 지점 — 159~164 행 `async list(actor, query)` 가 `isAdminBypass(actor?.role)` (53 행, `ROLE_HIERARCHY.Admin` 재사용) 이면 `repository.findMany(query)` 로 전체를 반환하고 (관리자 축), non-Admin 은 172 행 `findInstanceRefsByUserId(actor?.sub ?? "")` allowlist 로 좁혀 197 행 `findMany({ ...query, instanceRefIn: allowlist })` 한다 (사용자 축). 즉 사용자와 관리자 모두 조회 가능하되 사용자는 자기 instance binding 범위로 제한된다. 프런트 노출 축: `web/src/views/DashboardView.tsx` 38 행 `import PermissionDeniedRecordList from '../components/PermissionDeniedRecordList'` + 632~636 행 JSX 사용 (records / loading / error props) 로 배선돼 있고 조회 endpoint 는 같은 파일 66 행 상수 `PERMISSION_DENIED_RECORDS_PATH = '/api/permission-denied-records'` 다 — 컴포넌트 실체는 `web/src/components/PermissionDeniedRecordList.tsx` 52 행. 검증 실측: unit 2 spec 24 it (`src/permission-denied/permission-denied-record.controller.spec.ts` 16 it — 그중 434 행이 `it.each(["User", "Admin", "SuperAdmin"])`, `persisting-permission-denied-emitter.spec.ts` 8 it), smoke `test/smoke/permission-denied-record.smoke-spec.ts` 6 it (`package.json` 20 행 `test:smoke` 대상), e2e `test/e2e/permission-denied-records.e2e-spec.ts` 10 it. 한계 — (1) 감지는 401/403 만 emit 하고 404 는 415~428 행에서 `not-found` 로만 분류해 record 가 남지 않으며 (service 의 `deriveReason` 은 404 분기를 갖지만 GitHub 축에서 그 입력이 도달하지 않는다) 429/5xx 도 대상 밖이다. (2) "통지" 는 pull 조회뿐 — push 알림 (mail / webhook) 경로는 `src/` 에 부재하고, 영속 실패는 emitter 60 행 `logger.warn` 으로 swallow 돼 사용자에게 노출되지 않는다. (3) non-Admin 은 UserInstanceAccess binding 이 0 이면 176 행에서 빈 배열이라 사용자 인식이 binding 데이터 존재에 의존한다. (4) 프런트 배선은 DashboardView 1 곳뿐이다 (`web/src/views/AdminView.tsx` 매칭 0). (5) 검증 위치 컬럼은 `unit + smoke` 인데 실측 spec 에는 e2e 10 it 도 포함돼 컬럼이 실재와 어긋난다 — 본 slice 는 컬럼 값을 수정하지 않고 사실만 부기한다. (6) Confluence 측 `src/permission-denied/persisting-confluence-permission-denied-emitter.ts` 축은 REQ-016 소관이라 본 재판정 밖이다) |
| REQ-009 | 21 | Fork/Rebase/Meld 중복 제거 + 시간적 중복 (earlier date 우선) | FR | P5 | unit | IN_PROGRESS (시간적 중복 earlier-date 우선 축 · fork (SHA 보존) 중복 제거 축 · pipeline wiring 축 실재 / rebase·meld 로 commit ID 가 갈리는 "내용물" 중복 제거 축 부재: 시간적 중복 축은 수집-side · 평가-side 양쪽에 실재한다 — 수집-side 는 `src/assessment-collection/domain/commit-dedup.ts` 51 행 `export function dedupGithubActivities(activities: GithubActivity[]): GithubActivity[]`, 평가-side 는 `src/assessment-evaluation/domain/evaluation-dedup.ts` 118 행 `export function dedupTemporalDuplicates(inputs: EvaluationInput[]): EvaluationInput[]` 이고 (후자는 121 행에서 53 행 내부 헬퍼 `dedupByEarliest(inputs, (input) => input.unitId)` 로 위임한다), 두 파일 모두 동형 `isEarlier(a: string, b: string): boolean` (commit-dedup 26 행 / evaluation-dedup 36 행) 이 `Date.parse` 수치 비교를 우선하고 둘 중 하나라도 `Number.isNaN` 이면 사전식 `a < b` 로 fallback 한다. 판정식은 commit-dedup 72 행 `isEarlier(activity.timestamp, current.timestamp)` / evaluation-dedup 84 행 `isEarlier(input.timestamp, current.timestamp)` 로 **엄격히 더 이른** 항목만 기존 winner 를 교체하므로 동일 timestamp tie 는 **먼저 등장한 항목 유지** (입력 순서 보존) 이고, 반환 순서도 각 키의 최초 등장 위치 기준 (commit-dedup 79~81 행 `firstSeenOrder` 정렬 / evaluation-dedup 90~94 행 slot 직렬화) 으로 결정적이다 — README 21 행의 "2 월 결과물이 3 월 timestamp 로 중복되면 2 월 기여로 판단" 은 충족한다. 구조적 중복 축은 **SHA 동일 전제에서만 동작** 한다: 식별 키는 commit-dedup 40 행 `dedupKey` 가 산출하며 commit 은 42 행 `commit:${activity.externalId}` 단일 키, pr/issue 는 44 행 `${activity.kind}:${activity.repoRef}:${activity.externalId}` 합성 키다. `externalId` 는 `src/assessment-collection/domain/activity.ts` 48~50 행 주석대로 GitHub 에서 commit SHA / PR·issue number 이므로 fork 로 **같은 SHA** 가 여러 repo·instance 에 나타나는 경우는 repoRef 를 키에서 뺀 덕에 잡히지만, README 21 행이 명시한 "commit ID 는 다르지만 중복된 내용물" (rebase / meld 로 SHA 가 재작성되는 경우) 은 키가 갈려 **잡히지 않는다**. 내용 hash / diff 유사도 기반 dedup 은 `src/` 전체에 부재다 (`similarity` · `contentHash` · `diffHash` 어느 심볼도 `src` 이하 `*.ts` 에서 매칭 0). 평가-side 키도 성격이 같다 — `unitId` 는 `evaluation-input.ts` 10 행 주석대로 `sourceType:instanceKey:externalId` 합성이라 역시 식별자 기반이며, evaluation-dedup.ts 17~19 행 주석 자체가 "fork/rebase/meld 구조적 중복(R-9)은 수집-side commit-dedup 책임이라 본 함수 범위 밖" 이라고 경계를 박제한다. wiring 축은 dead code 가 아니다: 수집 flow 는 `src/assessment-collection/github-collection.service.ts` 32 행 import + 115 행 `return dedupGithubActivities(collected)` (전 source 누적 직후의 반환 경로), 평가 flow 는 `src/assessment-evaluation/evaluation-orchestrator.service.ts` 73 행 import + 148 행 `const deduped = excludeSelfFollowUps(dedupTemporalDuplicates(inputs))` 로 정규화 직후 · detection 이전에 호출된다. spec 제외 전수 참조에서 그 밖의 호출부는 0 이다 (`evaluation-unevaluated-period-select.ts` 112 행은 주석 언급일 뿐 호출 아님). 검증 위치 `unit` 의 근거는 domain unit 2 spec 60 it — `src/assessment-collection/domain/commit-dedup.spec.ts` 31 it · `src/assessment-evaluation/domain/evaluation-dedup.spec.ts` 29 it. 한계 — dedup 축을 직접 cover 하는 e2e / smoke 는 0 이다 (`test/e2e/` 에 dedup spec 파일 부재, smoke 2 spec 은 "deduped input" 을 주석으로 언급만 한다). 또 commit-dedup.ts 4~6 행이 선언한 "DB unique constraint 와는 별개의 in-memory 연산" 경계에서 두 layer 가 중복 적용될 때의 상호작용, Confluence 측 `src/assessment-collection/domain/page-dedup.ts` (page-id + version latest-wins) 와 commit 측 earliest-wins 의 정책 방향이 반대인 이유, dedup 오탐 시 사람이 제거된 활동을 되살리는 개입 경로는 본 재판정에서 확인하지 않았다) |
| REQ-010 | 24 | 코드 기여 양·질 평가 | FR | P5 | unit | DONE (implemented-on-main — 양 축 · 질 축 · 두 축 병존 · pipeline wiring · 영속 5 축 모두 실재: **양 축** 은 `src/assessment-evaluation/domain/evaluation-volume.ts` 30 행 `export function calculateEvaluationVolume(input: EvaluationInput): number` 다. 산출식은 31 행 `const titleLength = input.metadata.titleLength` **단일 신호** 이고, 33~35 행에서 number 가 아니면 0, 37~39 행에서 `Number.isFinite` 위반(NaN / Infinity / -Infinity)이면 0, 41~42 행 `Math.floor` 후 음수 절하로 ≥ 0 정수를 보장한다(throw 0 · 부수효과 0 · 1~7 행 주석대로 LLM 무관 결정적). 즉 여기서 세는 "양" 은 **제목 길이** 이며 diff 라인 수 · 변경 파일 수 · 추가/삭제량 같은 **코드 규모 지표는 전혀 보지 않는다** — 14~16 행 주석이 `titleLength` 를 "현 collection mapper 가 박제하는 유일 정량 metric" 으로 자인하고 23~25 행이 v1 baseline 임을 명시한다. **질 축** 은 두 층으로 나뉘어 실재한다: (a) LLM 산출 등급 층 — `src/assessment-evaluation/domain/evaluation-prompt.ts` 155 행 `export function classifyNarrative(narrative: string)` 이 50~51 행 `DIFFICULTY_MARKER` / `CONTRIBUTION_MARKER`(`/\bdifficulty[^\S\n]*:[^\S\n]*([^\s,\n]+)/i` 형태, case-insensitive)로 LLM narrative 에서 등급 토큰을 추출하고, marker 부재 · 미인식 값이면 32 행 `const DEFAULT_DIFFICULTY: Difficulty = "medium"` · 33 행 `const DEFAULT_CONTRIBUTION: ContributionLevel = "low"` 로 환원한다(throw 0, 항상 허용 집합 멤버). (b) 결정적 코드 신호 층 — `evaluation-quality-signal.ts` 115~117 행 `export function computeContributionQualitySignal(inputs: EvaluationInput[]): ContributionQualitySignal` 이 137 행 `if (titleLength <= CONTRIBUTION_QUALITY_TITLE_FLOOR)` 로 zero-contribution 후보를 식별하며 그 상수는 52 행 `export const CONTRIBUTION_QUALITY_TITLE_FLOOR = 1`(제목 부재 0 또는 단일 글자 1 만 후보) 이고 118~122 행 한국어 `TypeError` 가 파일의 유일한 throw 경로다. 소비측은 `evaluation-quality-adjust.ts` 119~122 행 `export function applyContributionQualityFloor(entries: ContributionQualityAdjustEntry[], signal: ContributionQualitySignal): ContributionQualityAdjustEntry[]` 로, 138~141 행에서 author 신호 + `authorSignal.zeroContributionUnitIds.includes(entry.result.unitId)` 로 대상을 단위 단위로 좁혀 143~145 행 `nextContribution` 을 62 행 `export const CONTRIBUTION_QUALITY_FLOOR_LEVEL: ContributionLevel = "zero"` 로 강등하고, 151 행 `result: { ...entry.result, contribution: nextContribution }` 로 **`contribution` 필드 하나만** 바꾼다(나머지 전사 · 새 객체 복제로 비파괴). **비대칭은 그대로다** — 결정적 신호는 강등(floor) 방향뿐이고 등급을 올리는 결정적 경로는 `src/` 에 없다. **둘 모두 축** 은 충족한다: `evaluation-result.ts` 54 행 `export interface EvaluationResult` 가 57 행 `unitId` · 60 행 `narrative` · 63 행 `difficulty: Difficulty` · 66 행 `contribution: ContributionLevel` · 69 행 `volume: number` 5 필드를 가져 질 축 2 필드와 양 축 1 필드가 **한 객체에 병존** 하며, 조립 지점은 `evaluation-scoring.service.ts` 104 행 `const { difficulty, contribution } = classifyNarrative(narrative)` + 107 행 `const volume = calculateEvaluationVolume(input)` 직후 110~116 행의 단일 return 이다(LLM 질 축과 결정적 양 축을 한 함수에서 compose). 영속도 병존한다 — `prisma/schema.prisma` 329 행 `model Contribution` 의 335 행 `difficulty String` · 336 행 `contributionScore Decimal` · 337 행 `volume Int` 3 컬럼이고 REQ-010 전용 컬럼 · 플래그는 없다. 다만 **두 축을 하나의 최종 점수로 합성하는 지점은 없다** — `evaluation-result.persist.mapper.ts` 146~148 행이 `difficulty: result.difficulty` · `contributionScore: contributionLevelToScore(result.contribution)` · `volume: result.volume` 로 축별 전사하고, 상위 집계도 183 행 `const volume = contributions.reduce((sum, c) => sum + c.volume, 0)`(합) · 190 행 `contributionScore`(component score 평균) · 200 행 `difficulty: aggregateDifficulty(contributions)`(`DIFFICULTY_ORDER` 최대) 로 **축별로 따로** 집계할 뿐 축 간 가중치가 0 이다(합성 없이 병렬 보존). 등급→점수 변환만 결정적이다 — 74~78 행 `CONTRIBUTION_SCORE_BY_LEVEL` 이 `zero: 0` / `low: 1` / `medium: 2` / `high: 3` 등간격 ordinal 이고 104~110 행 `contributionLevelToScore` 가 미지 등급을 throw 로 거른다. **wiring 축** 도 정의만 있는 dead code 가 아니다(spec 제외 전수 참조): 양 축 `calculateEvaluationVolume` 은 (a) 본류 산출 = `evaluation-scoring.service.ts` 35 행 import + 107 행 호출, (b) detection 재사용 = `evaluation-abuse-signal.ts` 22 행 import + 137 행 `const volume = calculateEvaluationVolume(input)` 2 곳이고, 질 축은 (a) detection = `evaluation-detection-signals-pipeline.ts` 51 행 import + 108 행 `quality: computeContributionQualitySignal(deduped)`(5-detection container 의 **3 번째** 필드), (b) 소비 = `evaluation-adjustments-pipeline.ts` 74 행 import + 211~214 행 `applyContributionQualityFloor(updateCountAdjusted, signals.quality)` — 5-adjuster(abuse → update-count → **quality** → underperformer → notable) 중 **3 번째**, (c) 본류 = `evaluation-orchestrator.service.ts` 76 행 / 71 행 import 후 158 행 `computeEvaluationAdjustmentSignals(deduped)` → 164 행 `results.push(await this.scoringService.scoreUnit(input, options))` 순차 호출 → 177 행 `applyEvaluationAdjustments(entries, signals)` 다. 양 축도 후처리 대상이라 199 행 `applyAbuseSignalToVolume` · 203 행 `applyUpdateCountNeutralizationToVolume` 2 adjuster 가 `volume` 을, quality 가 `contribution` 을 손대 필드 직교가 유지된다. 검증 위치 `unit` 의 근거는 domain unit 5 spec 123 it — `src/assessment-evaluation/domain/evaluation-volume.spec.ts` 21 it · `evaluation-quality-signal.spec.ts` 20 it · `evaluation-quality-adjust.spec.ts` 24 it · `evaluation-prompt.spec.ts` 37 it · `evaluation-result.persist.mapper.spec.ts` 21 it. 한계 — 양 축이 코드 규모를 보지 않아 "코드 기여의 양" 을 제목 길이로 근사하며(변경 라인 수 metric 은 `ActivityMetadata` 자체에 부재), 질 축의 결정적 층도 같은 `titleLength` 1 신호에 의존해 두 축이 사실상 동일 입력 신호를 공유한다. `CONTRIBUTION_QUALITY_TITLE_FLOOR = 1` 의 튜닝 근거는 44~51 행 주석의 보수적 baseline 선언뿐이고 dogfood 실측 calibration 은 아직 없으며, 두 축 합성 가중치는 존재 자체가 없어 calibration 대상도 아직 아니다. 질 축 오탐(정상 기여가 zero 로 강등)을 사람이 되살리는 개입 경로도 `src/` 에서 확인되지 않았다. 한계 — e2e 는 **노출 축만** cover 한다: `test/e2e/contributions.e2e-spec.ts` 19 it 가 58~67 행 `CONTRIBUTION_DTO_FIELDS` 에 64~66 행 `difficulty` · `contributionScore` · `volume` 을 포함시켜 두 축의 API 병존을 검증하지만 DB fixture 를 직접 심으므로 산출 helper 는 발화하지 않는다. 산출 경로를 실제로 발화시키는 것은 env-gated live smoke 2 종(`test/smoke/period-bridge-live.smoke-spec.ts` 24 행 · `test/smoke/realdata-e2e-live.smoke-spec.ts` 23 행이 주석으로 "classifyNarrative → volume 산출" 경로를 명시)뿐이고, gating env 부재인 public CI 기본 조건에서는 `describe.skip` 되어 실행되지 않는다) |
| REQ-011 | 25 | 중요·어려운 기여 → 높은 점수 ("어렵고 남이 못할 일") | FR | P5 | manual + unit | IN_PROGRESS (중요·어려운 기여 식별 축 · pipeline wiring 축 실재 / README 25 행 뒷절의 "더 높은 점수" 부여 축 부재: 식별 로직은 `src/assessment-evaluation/domain/evaluation-notable-contribution-signal.ts` 128~130 행 `export function computeNotableContributionSignal(inputs: EvaluationInput[]): NotableContributionSignal` 이다. 141~153 행에서 author 별로 단위를 순회하되 150 행 `if (input.contributionKind === "code")` 조건을 통과한 단위만 `codeUnitCount` 로 누적하고 (document 등 그 밖의 kind 는 카운트에서 단순 제외 — `contributionKind` 필터가 실재한다), 158~163 행에서 전 author 평균 `meanCodeUnitCount = totalCodeUnitCount / totalAuthorCount` 를 낸 뒤 173 행 `const notable = comparable && acc.codeUnitCount > ceiling` 로 판정한다. `ceiling` 은 168 행 `meanCodeUnitCount * NOTABLE_RELATIVE_CEILING` 이고 그 상수는 71 행 `export const NOTABLE_RELATIVE_CEILING = 1.5` — 즉 **비교 모집단은 조직 전체가 아니라 그 batch 에 함께 들어온 동료 author 집합** 이며 그 평균의 1.5 배 **초과(strictly greater)** 만 notable 이다 (정확히 1.5 배인 경계값은 제외 — REQ-013 저성과자 `UNDERPERFORMER_RELATIVE_FLOOR = 0.5` 의 대칭). 경계 가드는 3 종 — 167 행 `comparable = totalAuthorCount >= 2 && meanCodeUnitCount > 0` 이 단독 author batch 와 전원 code 기여 0 batch 를 보수적으로 notable 0 으로 흡수하고, 131~135 행이 `inputs` null/undefined 를 한국어 `TypeError` 로 차단하며 (파일의 유일한 throw 경로), 빈 배열은 totalAuthorCount 0 · byAuthor 빈 배열 · notableDetected false 로 흡수된다. 산출 타입은 85~94 행 `NotableContributionSignal` (`totalAuthorCount` / `meanCodeUnitCount` / 74~81 행 `NotableContributionEntry` 배열 `byAuthor` / batch 차원 `notableDetected`) 이고 138~139 행 Map + authorOrder 병행으로 author 최초 등장 순서로 결정적 정렬된다. **높은 점수 부여 축은 코드에 없다** — 소비측 `src/assessment-evaluation/domain/evaluation-notable-contribution-adjust.ts` 130~133 행 `export function applyNotableContributionAnnotation(entries: NotableContributionAdjustEntry[], signal: NotableContributionSignal): NotableContributionAdjustEntry[]` 가 실제로 바꾸는 필드는 `result.narrative` **하나뿐** 이다: 147~161 행이 `{ ...entry.result, narrative: nextNarrative }` 로 새 객체를 만들며 점수 필드 `difficulty` / `contribution` / `volume` 은 손대지 않고 그대로 전사한다 (85~92 행 `NotableContributionAdjustEntry` 주석도 "narrative 만 검토 / annotation 하고 나머지 필드는 전사" 로 자인). narrative 변경조차 169~174 행 `annotateNarrative` 가 78 행 `export const NOTABLE_CONTRIBUTION_NARRATIVE_MARKER = "[중요기여] "` 를 접두하는 것뿐이라, README 25 행 뒷절의 "더 높은 점수" 는 미구현이고 현재는 중요기여 **사실의 외화** 까지만 도달했다 (파일 30~32 행 주석이 "volume / contribution 같은 정량·등급 필드를 손대지 않고 narrative 에 marker 만 접두 — 점수 반영은 별도 task" 로 명시). 적용 성질은 170 행 `narrative.startsWith(marker)` 검사로 멱등, 157~160 행 새 객체 복제로 비파괴 (입력 entries 길이·순서 보존), marker 해제 역방향 0 으로 단조 상향이며, 148~149 행 판정이 author-level 이라 notable author 의 **모든** 단위가 대상이다 (unitId 매칭 없음 — 저성과자 annotation 과 동형). 134~139 행 두 `TypeError` 외 throw 0. 다른 adjuster 와의 필드 직교는 abuse · update-count 가 `volume`, quality 가 `contribution` 을 손대므로 충돌 0 이지만, underperformer 와는 **같은 `narrative` 필드** 를 공유한다 — adjustments pipeline 이 underperformer(4) → notable(5) 순으로 thread 하므로 두 marker 가 겹치면 `evaluation-adjustments-pipeline.ts` 224~229 행 주석대로 저성과자 marker 가 접두된 narrative 위에 중요기여 marker 가 다시 접두된다 (같은 batch 에서 한 author 가 평균의 0.5 배 미만이면서 1.5 배 초과일 수는 없어, 실 중첩은 이미 marker 가 붙은 narrative 가 재입력될 때만 성립). wiring 축은 정의만 있는 dead code 가 아니다 — spec 제외 전수 참조에서 실 호출부는 (a) detection = `src/assessment-evaluation/domain/evaluation-detection-signals-pipeline.ts` 50 행 import + 110 행 `notableContribution: computeNotableContributionSignal(deduped)` (5-detection container 의 마지막 필드), (b) 소비 = `src/assessment-evaluation/domain/evaluation-adjustments-pipeline.ts` 72 행 import + 230~233 행 `applyNotableContributionAnnotation(underPerformerAnnotated, signals.notableContribution).map((entry) => entry.result)` — 5-adjuster (abuse → update-count → quality → underperformer → **notable**) 중 **5 번째** 이자 flatten 직전, (c) 본류 = `src/assessment-evaluation/evaluation-orchestrator.service.ts` 76 행 import + 158 행 `computeEvaluationAdjustmentSignals(deduped)` 와 71 행 import + 177 행 `applyEvaluationAdjustments(entries, signals)` 다. 결과 노출·영속 경로에는 **notable 전용 컬럼이 없다** — `prisma/schema.prisma` 전체에 notable 관련 필드가 0 이고, 중요기여 사실은 marker 문자열로만 남아 `src/assessment-evaluation/domain/evaluation-result.persist.mapper.ts` 192 행 `const narrative = results.map((r) => r.narrative).join("\n\n")` 결합을 거쳐 schema 294 행 `model Assessment` 의 303 행 `narrative String` 로 영속된다. 단위 차원 `model Contribution` (329~349 행) 은 `difficulty` String · `contributionScore` Decimal · `volume` Int 만 갖고 narrative 컬럼이 없어, 단위별 marker 는 Assessment 의 결합 narrative 안에서만 확인된다. 검증 위치 `unit` 의 근거는 domain unit 2 spec 38 it — `src/assessment-evaluation/domain/evaluation-notable-contribution-signal.spec.ts` 19 it (happy path · error path · branch coverage · negative cases · 결정성/비변형 5 describe) · `src/assessment-evaluation/domain/evaluation-notable-contribution-adjust.spec.ts` 19 it (happy path · error path · flow/branch · negative cases 충분 cover · 결정성/비변형 5 describe). 한계 — 표의 검증 위치 `manual` 축은 실 근거를 찾지 못했다: 문서화된 수동 검증 절차가 `docs/` 에 부재하고 (REQ-011 언급은 task 정의서 · journal 뿐), 본 축을 직접 cover 하는 e2e / smoke 도 0 이다 (`test/` 전수에서 `NotableContribution` · `applyEvaluationAdjustments` · `중요기여` 참조 0, `test/helpers/realdata-e2e-evaluation-inputs.ts` 40 행이 notable 보정을 상위 orchestrator 책임으로 명시해 helper 범위 밖에 둔다). 한계 — `NOTABLE_RELATIVE_CEILING = 1.5` 의 튜닝 근거는 64~70 행 주석의 "평균의 1.5 배 초과는 현격히 높은 의 직관과 부합" 이라는 v1 baseline 선언뿐이고 dogfood 실측 calibration 은 아직 없다. "어렵고 남이 못할 일" 의 난이도 의미도 코드 단위 **개수** 로만 근사하며 (변경 라인 수 · 난이도 메타 미사용 — 파일 11~17 행 주석이 "본 신호 자체는 양 축" 이라고 자인), 상대 비교 모집단이 batch 구성에 따라 흔들려 같은 author 도 함께 평가되는 동료에 따라 판정이 바뀔 수 있고, 오탐으로 marker 가 붙은 단위를 사람이 해제하는 개입 경로는 `src/` 에서 확인되지 않았다) |
| REQ-012 | 26 | 코드 abusing 방지 (commit/PR 숫자만 늘리기) | FR | P5 | unit | DONE (computeAbuseSignal 반복 부풀리기 신호 + applyAbuseSignalToVolume volume 감점, adjustments pipeline 1 순위 배선) |
| REQ-013 | 27 | 저성과자 식별 | FR | P5 | unit | DONE (implemented-on-main — 식별 로직 · pipeline wiring · 결과 노출 3 축 모두 실재: 식별 로직은 `src/assessment-evaluation/domain/evaluation-underperformer-signal.ts` 124 행 `export function computeUnderPerformerSignal(inputs: EvaluationInput[]): UnderPerformerSignal` 로, 137~149 행에서 author 별로 `input.contributionKind === "code"` 인 단위만 세어 `codeUnitCount` 를 누적하고 (document 등 다른 kind 제외), 158 행에서 전 author 평균 `meanCodeUnitCount` 를 낸 뒤 169 행 `const underPerformer = comparable && acc.codeUnitCount < floor` 로 판정한다. 여기서 `floor` 는 164 행 `meanCodeUnitCount * UNDERPERFORMER_RELATIVE_FLOOR` 이고 그 상수는 67 행 `export const UNDERPERFORMER_RELATIVE_FLOOR = 0.5` — 즉 **비교 대상은 조직 전체가 아니라 그 batch 안 동료 author 들의 code 단위 수 평균** 이며, 그 평균의 절반 **미만** 이면 저성과 후보다. 163 행 `comparable = totalAuthorCount >= 2 && meanCodeUnitCount > 0` 가드로 단독 author / 전원 code 기여 0 인 batch 는 보수적으로 저성과 0 (false-positive 회피), 127 행은 `inputs` null/undefined 를 한국어 `TypeError` 로 차단한다. 산출 타입은 81 행 `UnderPerformerSignal` (`totalAuthorCount` / `meanCodeUnitCount` / 70 행 `UnderPerformerEntry` 배열 `byAuthor` / batch 차원 `underPerformerDetected`) 다. wiring 축은 정의만 있는 dead code 가 아니라 실 평가 flow 에 배선돼 있다: detection 측은 `evaluation-detection-signals-pipeline.ts` 52 행 import + 109 행 `underPerformer: computeUnderPerformerSignal(deduped)`, 소비 측은 `evaluation-adjustments-pipeline.ts` 77 행 import + 219 행 `applyUnderPerformerAnnotation(qualityAdjusted, signals.underPerformer)` (5-adjuster 중 4 순위) 이고, 두 composer 는 `evaluation-orchestrator.service.ts` 76/71 행 import 후 158 행 `computeEvaluationAdjustmentSignals(deduped)` → 177 행 `applyEvaluationAdjustments(entries, signals)` 로 orchestrator 본류에서 호출된다. 결과 노출 경로는 점수 조정이 아니라 **narrative 문자열 접두** 다: `evaluation-underperformer-adjust.ts` 120 행 `applyUnderPerformerAnnotation(entries, signal)` 이 저성과 author 의 **모든** 단위 `result.narrative` 앞에 68 행 `export const UNDERPERFORMER_NARRATIVE_MARKER = "[저성과자] "` 를 159 행 `annotateNarrative` 로 접두하며, 160 행 `narrative.startsWith(marker)` 검사로 멱등 · 148~151 행 새 객체 복제로 비파괴 · marker 해제 역방향 없음 (단조). `difficulty` / `contribution` / `volume` 은 손대지 않아 앞 3 adjuster 와 직교한다. 영속 측에는 **저성과 전용 컬럼이 없다** — `prisma/schema.prisma` 전체에 underPerformer 관련 필드가 0 이고, 저성과 사실은 294 행 `model Assessment` 의 303 행 `narrative String` (및 361 행 `model Summary` 의 366 행 `narrative String`) 문자열의 prefix 로만 남는다. 검증 위치 `unit` 의 근거는 domain unit 2 spec 38 it — `evaluation-underperformer-signal.spec.ts` 18 it · `evaluation-underperformer-adjust.spec.ts` 20 it. 한계 — `UNDERPERFORMER_RELATIVE_FLOOR = 0.5` 의 튜닝 근거는 60~66 행 주석의 "평균의 절반 미만은 현격히 떨어지는 의 직관과 부합" 이라는 v1 baseline 선언뿐이고 dogfood 실측 calibration 은 아직 없다. 상대 비교의 모집단도 조직/그룹 정의가 아니라 **한 평가 batch 에 함께 들어온 author 집합** 이라 batch 구성에 따라 판정이 흔들릴 수 있고, 기여의 난이도 · 질은 보지 않고 code 단위 **개수** 만 센다. 또 저성과 전용 조회 endpoint · UI 노출 · 오탐 시 사람이 marker 를 해제하는 개입 경로는 실재를 확인하지 못했으며, 저성과 축을 직접 cover 하는 e2e 는 `test/` 아래에 0 이다) |
| REQ-014 | 30 | Issue 평가 (본인 follow-up 소비 제외) | FR | P4 + P5 | unit | DONE (implemented-on-main — self-follow-up 제외 축 · Issue 평가 대상 축 · pipeline wiring 축 3 개 모두 실재: 제외 로직은 `src/assessment-evaluation/domain/evaluation-dedup.ts` 153~155 행 `export function excludeSelfFollowUps(inputs: EvaluationInput[]): EvaluationInput[]` 이고, 156 행에서 같은 파일 53 행 내부 헬퍼 `dedupByEarliest(inputs, keyOf)` 에 키 산출 함수를 넘겨 위임한다. 제외 판정 순서는 세 단계다 — (1) 158~159 행 `if (input.contributionKind !== "document") return undefined` 로 code 기여를 먼저 배제한다 (self-follow-up 부적용). 키가 `undefined` 인 활동은 67~70 행 passthrough 슬롯을 타 원래 위치에 그대로 보존된다. (2) 163 행 그룹 키 `${input.unitId} ${input.author}` — **동일 기여 단위 식별 키는 `unitId`** (`src/assessment-evaluation/domain/evaluation-input.mapper.ts` 46~47 행 `buildUnitId` 가 `<sourceType>:<instanceKey>:<externalId>` 로 합성) 이고 **author 동일성 비교 대상 필드는 `EvaluationInput.author`** 이며, 둘을 공백 1 칸으로 이어 붙인다 (`unitId` 는 `:` 합성이라 공백을 포함하지 않아 author 경계와 충돌 0). (3) 같은 그룹 키가 2 건 이상이면 73~86 행 earliest-wins 로 정리한다 — 84 행 `isEarlier(input.timestamp, current.timestamp)` (Date.parse 수치 비교 우선 · NaN 시 사전식 fallback) 가 더 이른 timestamp 1 건, 즉 **최초 기여(issue 생성) 1 건만 유지** 하고 동일 timestamp tie 는 먼저 등장한 항목을 남긴다. 결과적으로 동일 author 후속 활동만 제외되고, 다른 author 의 동일 단위는 그룹 키가 달라 전부 보존된다 (입력 비변형 · 반환 순서 안정). Issue 평가 대상 축은 수집-side 와 평가-side 를 나눠 확인했다 — 수집 layer 는 `src/assessment-collection/domain/activity.ts` 36 행 `GithubActivityKind` 가 commit / pr / issue 3 종 union 으로 선언되고 69~70 행 `GithubActivity.kind` 로 노출되며, `src/assessment-collection/domain/github-activity.mapper.ts` 72~75 행이 number 만 있고 `pull_request` 가 부재인 raw item 을 `issue` kind 로 판정해 164~173 행에서 `sourceType: "github"` typed `GithubActivity` 로 조립한다 (수집 endpoint 자체도 `src/assessment-collection/github-collection.service.ts` 37 행 `GithubActivityEndpoint` 3 종과 47 행 `{ endpoint: "issues", suffix: "issues" }` 로 실재). 평가-side 는 `evaluation-input.mapper.ts` 35~39 행 `resolveGithubContributionKind` 의 38 행 `return activity.kind === "issue" ? "document" : "code"` 가 Issue 를 `contributionKind: "document"` 평가 입력으로 매핑하고 (55 행 `mapActivityToEvaluationInput`), 그 입력이 `src/assessment-evaluation/evaluation-orchestrator.service.ts` 144 행 `activities.map(mapActivityToEvaluationInput)` → 163~164 행 `results.push(await this.scoringService.scoreUnit(input, options))` 로 실제 단위 scoring 대상이 된다 — 수집만 되고 평가 입력 매핑이 없는 상태가 아니다. wiring 축은 `grep -rn "excludeSelfFollowUps" src --include=*.ts` 비-spec 전수로 확인했고 참조는 정의 파일 (`evaluation-dedup.ts` 11 · 52 행 주석 + 153 행 정의) 과 orchestrator (44 · 74 · 98 · 148 행) 뿐이며, 148 행 `const deduped = excludeSelfFollowUps(dedupTemporalDuplicates(inputs))` 가 evaluate flow 본류에서 호출된다 (정의만 있고 호출 0 인 dead code 아님). 검증 위치 `unit` 의 근거는 `src/assessment-evaluation/domain/evaluation-dedup.spec.ts` 203 행 `describe("excludeSelfFollowUps (self-follow-up 제외, R-30)")` 블록의 **14 it** 이다 (204 행 happy-path · 227 행 branch/negative · 388 행 immutability/determinism 3 하위 describe 합산, 파일 전체 29 it 중 블록 범위 203~491 행 실측). 한계 — `evaluation-dedup.ts` 20~22 행과 145~148 행이 자인하듯 comment thread 가 수집되지 않아 self-follow-up 검출은 issue 단위 author 동일성 휴리스틱이다: 같은 issue 에 남긴 본인 comment 가 별도 `unitId` 로 들어오면 그룹 키가 갈려 제외되지 않고, 반대로 동일 issue 가 동일 author 로 재등장하면 정상 갱신이어도 후속이 제외될 수 있다 (comment-level 정밀 검출은 수집 mapper 확장 ADR-0029 경계 별도 slice 로 deferred). 한계 — README 30 행 본절의 "다른 개발자들의 활동에 도움이 된 경우" 라는 유용성 조건이 점수 산정에 반영되는지는 확인하지 못했다 (`src/` 비-spec 전수에서 해당 판정 로직 0 — 현재는 Issue 작성 사실이 document 기여로 평가될 뿐 타인에게 도움이 됐는지는 판정하지 않는다). 오탐으로 제외된 활동을 사람이 되살리는 개입 경로도 확인하지 못했고, Issue 평가 축 · self-follow-up 제외 축을 cover 하는 e2e / smoke 는 `test/` 아래에 0 이다 (`test/helpers/realdata-e2e-evaluation-inputs.ts` 40 행이 self-follow-up 제외를 상위 orchestrator 책임으로 명시해 그 helper 범위 밖에 둔다)) |
| REQ-015 | 31 | Confluence 지정 SPACE 평가 | FR | P4 | unit + e2e | DONE (implemented-on-main — 지정 축 · 수집 축 · 평가 배선 축 3 개가 모두 실재한다. 지정 축: `src/confluence/confluence-instance-config.ts` 94~96 행 `export function resolveConfluenceInstances(env: NodeJS.ProcessEnv): ConfluenceInstanceResolution` 이 부수효과 0 순수 함수로 env 를 읽어 활성 instance 배열을 만든다. 값 출처는 **env 전용** 이다 — 29 행 `CONFLUENCE_INSTANCES_ENV = "CONFLUENCE_INSTANCES"` 가 활성 key list 이고 (100~112 행 comma/whitespace split + 빈 토큰 제거, 117~122 행 대문자 dedupe), key 별 변수 이름은 80~82 행 `confluenceEnvName(key, suffix)` 가 `CONFLUENCE_${key.toUpperCase()}${suffix}` 로 조립하며 suffix 상수는 34~37 행 `_BASE_URL` · `_AUTH_USER` · `_TOKEN_ENC` · `_SPACE_ALLOWLIST` 다. README 31 행의 "지정된 주소" 는 46 행 `baseUrl: string` (풀 REST base URL, 166 행 trim 전사) 이며 필수라 138~148 행이 `_BASE_URL` 또는 `_TOKEN_ENC` 부재 instance 를 reject 한다 (평문/빈 URL fallback 0). "지정된 SPACE들" 은 57 행 `spaceAllowlist: string[]` 로 **복수 지원 (배열)** 이고 156~161 행이 `_SPACE_ALLOWLIST` 를 comma split + trim + 빈 entry 제거로 채우며 부재/빈이면 빈 배열이다 (reject 사유 아님). 수집 spec 조립은 `src/assessment-collection/collection-spec.service.ts` 51 행 `const { instances } = resolveConfluenceInstances(this.env)` → 54 행 `return { github, confluence: { instances } }` 이고 그 env 출처는 32 행 `@Optional() private readonly env: NodeJS.ProcessEnv = process.env` 하나뿐이다. 수집 축: `src/confluence/confluence-space-traversal.service.ts` 101~103 행 `async traverseInstance(config: ConfluenceInstanceConfig): Promise<SpaceTraversalResult[]>` 이 110 행에서 token 을 1 회 JIT 복호한 뒤 115 행 `for (const spaceKey of config.spaceAllowlist)` 로 순회한다 — **순회 대상이 allowlist 자체** 라 allowlist 밖 SPACE 는 전체 조회 후 사후 필터되는 게 아니라 애초에 요청되지 않는다 (빈 allowlist 면 loop 미진입 · adapter 호출 0). SPACE 별 호출은 119~125 행에서 `path: CONFLUENCE_CONTENT_PATH` (65 행 `/content`) + `query: { [SPACE_KEY_QUERY_PARAM]: spaceKey }` (68 행 `spaceKey`) 를 조립해 129 행 `const pages = await this.adapter.requestAllPages(input)` 로 나가고, 결과는 74~79 행 `SpaceTraversalResult` 의 `spaceKey` · `pages` 로 SPACE 식별을 유지한다. 활동 단위 매핑은 `src/assessment-collection/confluence-collection.service.ts` 60~62 행 `collectConfluenceActivities` 의 65 행 instance loop → 70 행 `await this.traversal.traverseInstance(config)` → 74~79 행 page 별 `mapConfluenceActivity(raw, config.key, spaceKey)` (null 은 skip) 이고 90 행 `dedupConfluenceActivities` (page-id + version latest-wins) 로 마감한다. 문서 작성 / 업데이트 구분은 **구분 없음 (활동 단위 통합 수집)** 이다 — `src/assessment-collection/domain/activity.ts` 74~80 행 `ConfluenceActivity` 는 `spaceRef` 와 `version: number` 만 더할 뿐 GitHub 의 36 행 `GithubActivityKind` 같은 활동 종류 필드가 없고, `src/assessment-collection/domain/confluence-activity.mapper.ts` 는 83~89 행 `resolveTimestamp` 가 89 행 `readString(raw.version.when)` (마지막 수정 시각) 만, 64~72 행 `resolveAuthor` 가 71 행 `raw.version.by` (마지막 수정자) 만 읽는다 — createdAt / createdBy 계열 추출 0 · version history 전개 0 이라 page 1 건당 최신 version 1 활동만 생기고 109~143 행 `mapConfluenceActivity` 어디에도 작성/업데이트를 가르는 분기가 없다. 평가 배선 축: orchestration 호출 지점은 `src/assessment-collection/collection-orchestrator.service.ts` 67 행 `async collectActivities(spec: CollectionSpec)` 안의 71~72 행 `await this.collectSafely(() => this.confluence.collectConfluenceActivities(spec.confluence))` 이며 77 행에서 GitHub 결과와 concat 된다. 그 orchestrator 도 caller 0 이 아니다 — `src/assessment-collection/collection-entry.service.ts` 52~53 행 (`buildCollectionSpec` → `this.orchestrator.collectActivities(spec)`) 과 `src/assessment-collection/collection-persistence.service.ts` 62 행이 호출한다. 평가 입력 변환은 `src/assessment-evaluation/domain/evaluation-input.mapper.ts` 55~57 행 `mapActivityToEvaluationInput` 의 60~63 행 `activity.sourceType === "github" ? resolveGithubContributionKind(activity) : "document"` 로, Confluence 활동은 항상 `contributionKind: "document"` 가 되고 68 행 `sourceType: activity.sourceType` 로 출처가 보존되며 unitId 는 46~47 행 `${activity.sourceType}:${activity.instanceKey}:${activity.externalId}` 다 (GitHub 전용 분기만 있는 상태 아님). 검증 실측: unit 4 spec 58 it — `src/confluence/confluence-instance-config.spec.ts` 25 it, `src/confluence/confluence-space-traversal.service.spec.ts` 14 it, `src/assessment-collection/confluence-collection.service.spec.ts` 11 it, `src/assessment-collection/collection-spec.service.spec.ts` 8 it (`it.each` 0). 한계 — (1) 검증 위치 컬럼은 `unit + e2e` 인데 e2e 의 SPACE cover 는 0 이다: `test/e2e/assessment-collection-trigger.e2e-spec.ts` 는 8 it 중 Confluence 언급이 14 행 주석 1 줄 (adapter fetch 미호출 서술) 뿐이고 `test/e2e/` 전체에서 `spaceKey` · `spaceRef` · `SPACE_ALLOWLIST` grep 결과가 0 이다 — 본 slice 는 컬럼 값을 수정하지 않고 사실만 부기한다. (2) 작성/업데이트 구분이 부재해 README 31 행의 두 활동은 page 단위 최신 version 1 건으로 합쳐져 수집된다. (3) SPACE 지정 경로가 env 전용이라 Person / 평가 대상별 SPACE 지정이나 DB · 요청 파라미터 설정 경로가 없다 (collection-spec.service.ts 49~50 행 주석이 "활성 instance 전체" 를 명시). (4) 수집된 `spaceRef` 와 `version` 이 평가 입력까지 전사되지 않는다 — evaluation-input.mapper 65~75 행이 두 필드를 옮기지 않고 metadata 만 전달하는데 `confluence-activity.mapper.ts` 95~102 행 `buildMetadata` 는 `titleLength` 만 담으므로 `src/assessment-evaluation/domain/evaluation-update-count-neutral.ts` 171~174 행 `resolveUpdateCount` 의 `input.metadata.version` 이 production 경로에서 항상 부재해 0 fallback 이다) |
| REQ-016 | 33 | Confluence 접근 권한 부족 인식·통지 | FR | P4 | unit | DONE (implemented-on-main — Confluence 축의 감지 · 기록·영속 · 인식 경로 3 축이 모두 실재하고 프런트 배선까지 이어진다. 감지 축: `src/confluence/confluence-adapter.service.ts` 491 행 `private mapNon2xx(status: number, input: ConfluenceRequestInput): ConfluenceDomainError` 이 497 행의 status 401 또는 403 분기에서 498 행 `this.permissionDeniedEmitter.emit({ baseUrl: input.baseUrl, path: input.path, status })` 를 호출한 뒤 `ConfluenceDomainError("permission-denied", status, ...)` 를 반환하고, 이 매핑은 469 행 `throw this.mapNon2xx(response.status, input)` 로 실 요청 경로 (326 행 `async request` · 353 행 `async requestAllPages`) 에서 호출된다. 정의만 있고 호출 참조 0 인 심볼이 아니다 — 수집 경로는 `src/assessment-collection/collection-orchestrator.service.ts` 59 행 `private readonly confluence: ConfluenceCollectionService` → `src/assessment-collection/confluence-collection.service.ts` 54 행 `constructor(private readonly traversal: ConfluenceSpaceTraversalService)` + 70 행 `await this.traversal.traverseInstance(config)` → `src/confluence/confluence-space-traversal.service.ts` 129 행 `const pages = await this.adapter.requestAllPages(input)` 로 이어진다. 권한 부족 SPACE 의 진행 정책은 skip-and-continue 다 — traversal 132~161 행 catch 가 `ConfluenceDomainError` 만 흡수하고 (135~137 행 비-도메인 error 는 재 throw) 148~155 행 `if (error.kind === "permission-denied")` 에서 동일 event 를 emit 한 뒤 161 행 `continue` 로 다음 SPACE 를 진행한다 (전체 abort 0). 즉 skip 이 조용하지 않고 emit 을 동반한다. 기록·영속 축: `src/permission-denied/persisting-confluence-permission-denied-emitter.ts` 52 행 `emit(event: PermissionDeniedEvent): void` 이 58~64 행에서 `this.recordService.record({ provider: "confluence", instanceRef: event.baseUrl, resourceRef: event.path, httpStatus: event.status })` 로 정규화하며 저장 provider 리터럴은 60 행 `provider: "confluence"` 다 (GitHub 은 host→instanceRef, Confluence 는 baseUrl→instanceRef 비대칭 흡수). fire-and-forget 이라 65~70 행 `.catch` + 71~75 행 try/catch 가 DB 실패와 동기 throw 를 모두 흡수하고 80~84 행 `warnSwallow` 가 warn 으로만 남긴다. 이후 경로는 `permission-denied-record.service.ts` 109 행 `async record(...)` → `permission-denied-record.repository.ts` 69 행 `this.prisma.permissionDeniedRecord.create({ data: input })` → `prisma/schema.prisma` 513 행 `model PermissionDeniedRecord` 로 실재한다. 바인딩도 no-op 이 아니다 — `src/confluence/confluence.module.ts` 87~88 행이 `CONFLUENCE_PERMISSION_DENIED_EMITTER` token 에 `useClass: PersistingConfluencePermissionDeniedEmitter` 를 provide 하고 adapter 는 308 행 `@Optional() @Inject(CONFLUENCE_PERMISSION_DENIED_EMITTER)` 로 그것을 주입받는다. 인식 경로 축: 조회 route 는 provider 공용이다 — `permission-denied-record.controller.ts` 78 행 `@Controller("api/permission-denied-records")` + 98~100 행 `@Get()` · `@UseGuards(JwtAuthGuard, RolesGuard)` · `@Roles("User")` 이고, provider 는 104 행 `@Query("provider") provider?: string` 의 선택 필터라 111~112 행에서 값이 있을 때만 filter 에 실린다. repository 도 104~105 행 `if (filter?.provider !== undefined) where.provider = filter.provider` 로 미지정 시 where 절에 provider 조건이 없어 Confluence record 가 같은 route 로 노출된다. audience 는 service 159~163 행 `async list(actor, query)` 의 `isAdminBypass(actor?.role)` (53 행) 이면 전체 반환 (관리자 축), non-Admin 은 170 행 `findInstanceRefsByUserId` allowlist 로 좁혀 197 행 `findMany({ ...query, instanceRefIn: allowlist })` 한다 (사용자 축) — 사용자와 관리자 모두 조회 가능하되 사용자는 자기 instance binding 범위로 제한된다. 프런트 노출 축: `web/src/views/DashboardView.tsx` 38 행 `import PermissionDeniedRecordList from '../components/PermissionDeniedRecordList'` + 630~636 행 JSX (records / loading / error props) 로 배선돼 있고 조회 endpoint 는 같은 파일 66 행 `PERMISSION_DENIED_RECORDS_PATH = '/api/permission-denied-records'` 로 provider query 가 없다. 컴포넌트 `web/src/components/PermissionDeniedRecordList.tsx` 52 행 `function PermissionDeniedRecordList(...)` 는 77~81 행 `records.map` + `<span>{row.provider}</span>` 로 provider 를 주 라벨로 렌더할 뿐 GitHub 전용 분기가 없어 Confluence provider record 도 렌더 대상이다. 검증 실측: unit 3 spec 98 it — `src/permission-denied/persisting-confluence-permission-denied-emitter.spec.ts` 11 it, `src/confluence/confluence-space-traversal.service.spec.ts` 14 it, `src/confluence/confluence-adapter.service.spec.ts` 73 it (`it.each` 0). 한계 — (1) traversal 149 행 emit 은 production DI 에서 no-op 이다: 92 행 `@Optional() private readonly permissionDeniedEmitter: PermissionDeniedEmitter = NO_OP_PERMISSION_DENIED_EMITTER` 가 interface type 이라 token 바인딩이 없고 `confluence.module.ts` 77~82 행 주석도 traversal emitter wiring 을 Out of Scope 로 명시한다 — 실 영속 경로는 adapter emit 1 개뿐 (그 덕에 SPACE 당 중복 record 도 생기지 않는다). (2) 감지는 401/403 만 emit 하고 404 는 511~513 행에서 `not-found` 로만 분류해 record 가 남지 않으며 429 (520 행) · 5xx 도 대상 밖이라 권한상 비가시 SPACE 는 인식되지 않을 수 있다. (3) "통지" 는 pull 조회뿐 — push 알림 (mail / webhook) 경로는 `src/` 에 부재하고 영속 실패는 emitter 81 행 `logger.warn` 으로 swallow 돼 사용자에게 노출되지 않는다. (4) non-Admin 은 binding allowlist 가 0 이면 빈 결과라 사용자 인식이 binding 데이터 존재에 의존한다. (5) 프런트 배선은 DashboardView 1 곳뿐이며 provider 필터 UI 는 없다. (6) 검증 위치 컬럼은 `unit` 인데 실측상 smoke `test/smoke/permission-denied-record.smoke-spec.ts` 6 it (102 행이 provider "confluence" round-trip) 와 e2e `test/e2e/permission-denied-records.e2e-spec.ts` 10 it (182 행 provider 배열 assertion · 277 행 provider 필터 비매칭 검증) 도 Confluence 축을 cover 한다 — 본 slice 는 컬럼 값을 수정하지 않고 사실만 부기한다) |
| REQ-017 | 34 | Confluence SPACE crawling vs hierarchy 탐색 정책 (ADR) | Constraint | P4 (ADR 필수) | policy | DONE (implemented-on-main — ADR 실재 축 · 결정 내용 축 · 구현 정합 축 3 개가 모두 실재한다. ADR 실재 축: `docs/decisions/ADR-0013-confluence-space-traversal-policy.md` frontmatter 가 2 행 `id: ADR-0013` · 4 행 `status: ACCEPTED` · 5 행 `date: 2026-06-01` · 6 행 `relatedTask: T-0145` 로 박제돼 있어 구현 위치 "P4 (ADR 필수)" 가 실 문서로 충족된다 (status 는 원문 값 `ACCEPTED` 를 그대로 인용 — 해석·승격 없음). 결정 내용 축: README 34 행이 허용한 셋 (crawling / page List / Hierarchy) 중 47 행 소제목 "Decision §1 — 탐색 메커니즘: page List 기반 (SPACE content list API 순회)" 와 49 행 "ConfluenceAdapter 의 default 탐색 메커니즘은 **지정 SPACE 의 content list API 순회** 다" 가 page List 기반 **1 개를 명시 default 로 택** 했고, 50 행이 full crawling 을 (page body link 파싱 의존 → raw 미저장 invariant 마찰 · SPACE 경계 이탈 위험 · 재귀 depth/cycle 복잡) 사유로, 51 행이 Hierarchy tree 순회를 (orphan/루트 직속 page 누락 · ancestor·child round-trip 증가) 사유로 각각 미채택 처리해 택 1 이 배타적으로 확정된다 (98~104 행 Alternatives 표도 (1) 채택 / (2) crawling · (3) Hierarchy 기각으로 같은 결론을 재박제). 인접 결정으로 54 행 Decision §2 가 SPACE key allowlist 순회 + (page, version) raw-transient 경계를, 60 행 Decision §3 이 권한 부족 4xx 의 SPACE 단위 skip-and-continue 를 박제한다. 구현 정합 축: `src/confluence/confluence-space-traversal.service.ts` 가 그 결정을 그대로 구현한다 — 65 행 `const CONFLUENCE_CONTENT_PATH = "/content"` 와 68 행 `const SPACE_KEY_QUERY_PARAM = "spaceKey"` 로 SPACE-scoped content list 의 path·query param 을 상수 고정하고, 119~125 행이 `path: CONFLUENCE_CONTENT_PATH` + `query: { [SPACE_KEY_QUERY_PARAM]: spaceKey }` 로 요청을 조립하며, 129 행 `await this.adapter.requestAllPages(input)` 가 `src/confluence/confluence-adapter.service.ts` 353 행 `async requestAllPages(input: ConfluenceRequestInput): Promise<unknown[]>` 로 위임돼 응답 body 의 `_links.next` cursor 를 끝까지 따라가는 평면 page 순회를 수행한다 (같은 파일 17~20 행 주석이 "첫 page 는 start=0 + limit 최대화 query 로 조립 → parseNextCursor 로 body `_links.next` 를 절대 URL 로 정규화 → next 부재 시 종료" 로 순회 계약을 박제). 순회 대상은 115 행 `for (const spaceKey of config.spaceAllowlist)` 로 allowlist 에 한정돼 전수 SPACE 자동 발견 경로가 없고 (Decision §2 정합), 131~161 행 catch 는 `ConfluenceDomainError` 만 흡수해 (135~137 행은 비-도메인 error 를 전파) 148~156 행에서 permission-denied 시 `permissionDeniedEmitter.emit` 후 161 행 `continue` 로 다음 SPACE 를 계속한다 (Decision §3 skip-and-continue 정합). 따라서 코드는 ADR 결정 (List 기반 · allowlist 순회 · skip-and-continue) 과 **일치** 하며 crawling 재귀나 hierarchy tree 순회 경로는 코드에 존재하지 않는다. 소스측 ADR 역참조: `grep -rn "ADR-0013" src` = 3 파일 12 행 — `src/confluence/confluence-space-traversal.service.ts` 61 행 ("ADR-0013 §1 의 SPACE content list API 순회 default") · 67 행 (spaceKey query param) · 108 행 · 141 행, `src/confluence/confluence-instance-config.ts` 16 행 · 25 행 · 54 행 ("평가 대상 SPACE key 목록(ADR-0013 §2 allowlist)"), `src/assessment-collection/domain/page-dedup.ts` 19 행 ("ADR-0013 §2 (page, version) 정합"). 검증 위치 `policy` 의 실 근거는 test 개수가 아니라 이 ADR 문서 실재 (status ACCEPTED) + 위 3 파일 12 행의 소스측 역참조이며, 문서측으로도 `docs/architecture/modules.md` 36 행 ConfluenceModule row 가 REQ 컬럼에 "REQ-017 (crawling vs hierarchy)" 를 걸고 ADR 컬럼에 `ADR-0013 (SPACE 탐색 정책)` 실링크를 달아 과거의 "정책은 P4 ADR" 미결정 톤이 해소돼 있다. 한계 — (i) `grep -rln "ADR-0013" test src --include=*spec.ts` = 0 으로 ADR-0013 결정 문자열을 직접 물고 있는 정책 drift-guard spec 은 실재하지 않는다 (traversal service 자체 spec `src/confluence/confluence-space-traversal.service.spec.ts` 는 있으나 ADR 참조 assert 는 없음) — 탐색 메커니즘이 코드에서 crawling/hierarchy 로 바뀌어도 CI 가 자동으로 잡지 못하며, 검증 위치 컬럼 값은 본 재판정에서 수정하지 않았다. (ii) ADR 51 행이 Hierarchy 를 default 에서 기각한 결과로 `grep -rn "ancestors" src` = 0 — Consequences 5 가 남긴 ancestors 메타 보강 경로는 미구현이고, Consequences 음의 1 이 지목한 content type 필터도 미구현이라 blog/draft 등 page type 누락 위험은 열려 있다.) |
| REQ-018 | 37 | 단순 보고·copy-paste 로그 = zero-contribution | FR | P5 | unit | DONE (implemented-on-main — 저품질 판정 축 · zero-contribution 강등 축 · pipeline wiring 축 3 개 모두 실재: 판정은 `src/assessment-evaluation/domain/evaluation-quality-signal.ts` 115~117 행 `export function computeContributionQualitySignal(inputs: EvaluationInput[]): ContributionQualitySignal` 이다. 판정식은 136~139 행 — 각 단위마다 182 행 내부 헬퍼 `resolveTitleLength(input)` 로 `input.metadata.titleLength` **단일 필드** 만 읽어 정규화한 뒤 52 행 상수 `CONTRIBUTION_QUALITY_TITLE_FLOOR = 1` 과 137 행 `titleLength <= CONTRIBUTION_QUALITY_TITLE_FLOOR` 로 비교해 zero-contribution 후보로 식별하고 해당 author 의 `zeroContributionUnitIds` 에 등장 순서대로 push 한다. `contributionKind` 필터는 없다 — 96 행 JSDoc 이 "contributionKind 무관 — code/document 둘 다 trivial title 은 후보" 로 명시한다. 반환 타입은 70~79 행 `ContributionQualitySignal` (`totalUnitCount` · `totalZeroContributionCount` · `byAuthor: ContributionQualityEntry[]` (55~66 행) · `zeroContributionDetected`) 이고 author 최초 등장 순서로 결정적 정렬된다. 가드는 3 종 — `inputs` 가 null/undefined 면 119~122 행에서 한국어 `TypeError` throw (파일의 유일한 throw 경로), 빈 배열이면 totalUnitCount 0 · byAuthor 빈 배열 · zeroContributionDetected false, `metadata.titleLength` 가 부재 / 비-number(string·boolean·null) / 비유한(NaN·Infinity) 이면 183~194 행 (185 행 `typeof` 검사 · 189 행 `Number.isFinite` 검사 · 193 행 `Math.floor` + 194 행 음수 0 절하) 이 모두 0 으로 흡수한다 (0 은 임계 이하라 후보로 식별되되 throw 는 없다). 강등 축은 `src/assessment-evaluation/domain/evaluation-quality-adjust.ts` 119~122 행 `export function applyContributionQualityFloor(entries: ContributionQualityAdjustEntry[], signal: ContributionQualitySignal): ContributionQualityAdjustEntry[]` 가 담당하며 (69~76 행 `ContributionQualityAdjustEntry` = `{ author, result }`), 강등 대상 필드는 `entry.result.contribution` **하나뿐** 이고 강등 값은 62 행 `export const CONTRIBUTION_QUALITY_FLOOR_LEVEL: ContributionLevel = "zero"` 다. 대상 판정은 138~141 행 3 조건 AND (author 매칭 + `zeroContribution === true` + `zeroContributionUnitIds.includes(entry.result.unitId)`) 라 같은 author 라도 후보 목록에 없는 단위는 passthrough 다. 149~152 행이 항상 `{ ...entry.result, contribution: nextContribution }` 로 **새 객체** 를 반환하므로 입력 비변형(비파괴) 이고, 이미 `"zero"` 인 단위는 같은 값이 재대입돼 **멱등** 이며 등급 상향 분기는 없다 (단조 비상향 — 123~127 행 두 `TypeError` 외 throw 0). 다른 adjuster 와는 필드 직교다: abuse · update-count 는 `volume`, underperformer · notable 은 `narrative` 를 손대고 본 helper 만 `contribution` 을 손댄다 (`evaluation-notable-contribution-adjust.ts` 24 행 · `evaluation-underperformer-adjust.ts` 18 행 주석이 같은 직교를 박제). **다만 "copy-paste 로그" 를 본문 유사도 · diff 로 실제 비교하는 로직은 부재하다** — 판정 입력은 `titleLength` 정수 1 개뿐이며 `evaluation-quality-signal.ts` 20~28 행 자체가 "현 collection mapper 가 박제하는 유일 정량 title 신호는 metadata.titleLength 하나뿐" 이라 v1 을 그 1 신호로 한정했다고 자인한다 (본문 텍스트 · diff · hash 를 읽는 경로는 두 파일 어디에도 없다). wiring 축은 두 심볼 (`computeContributionQualitySignal` · `applyContributionQualityFloor`) 의 `src` 이하 `*.ts` 전수 참조 (spec 제외) 로 확인했고, 정의 파일 · 주석 언급을 뺀 실 호출부는 (a) detection = `src/assessment-evaluation/domain/evaluation-detection-signals-pipeline.ts` 51 행 import + 108 행 `quality: computeContributionQualitySignal(deduped)` 로 5-detection container 의 `quality` 필드를 채우고, (b) adjustments = `src/assessment-evaluation/domain/evaluation-adjustments-pipeline.ts` 74 행 import + 211~214 행 `applyContributionQualityFloor(updateCountAdjusted, signals.quality)` 로 소비한다 (adjuster 순서 = abuse → update-count → **quality (3 번째)** → underperformer → notable → flatten). (c) 본류 호출은 `src/assessment-evaluation/evaluation-orchestrator.service.ts` 76 행 import + 158 행 `computeEvaluationAdjustmentSignals(deduped)` 와 71 행 import + 177 행 `applyEvaluationAdjustments(entries, signals)` 로, dedup 직후 detection → 단위 scoring → post-scoring adjust 순서로 evaluate flow 본류에서 실행된다 (정의만 있고 호출 0 인 dead code 아님). 결과 노출 경로는 narrative 문자열이 아니라 **점수 필드** 다 — `src/assessment-evaluation/domain/evaluation-result.persist.mapper.ts` 147 행 `contributionScore: contributionLevelToScore(result.contribution)` 가 104~111 행 결정적 변환 (74~79 행 `CONTRIBUTION_SCORE_BY_LEVEL` = zero 0 / low 1 / medium 2 / high 3) 을 거쳐 `prisma/schema.prisma` 336 행 `contributionScore Decimal` 로 영속되고, `src/assessment-evaluation/domain/summary-aggregate.ts` 101 행이 같은 변환으로 기여도 평균에 반영한다. 즉 강등은 0 점으로 영속되지만 **등급 문자열(zero/low/medium/high) 자체를 담는 영속 컬럼은 schema 에 없다** (`Contribution` model 329~349 행에 `difficulty` String · `contributionScore` Decimal · `volume` Int 만 존재). 검증 위치 `unit` 의 근거는 domain unit 2 spec 44 it — `src/assessment-evaluation/domain/evaluation-quality-signal.spec.ts` 20 it · `src/assessment-evaluation/domain/evaluation-quality-adjust.spec.ts` 24 it. 한계 — copy-paste 내용 비교 부재로 **제목이 정상 길이인 copy-paste 로그 · 단순 보고는 전혀 검출되지 않는다** (실 검출 범위는 사실상 "제목이 없거나 1 글자인 단위" 로 한정되며, README 37 행 앞절의 "단순 보고성 자료를 질적으로 낮게 평가" 중 제목이 멀쩡한 보고서는 여전히 LLM 정성 평가에만 의존한다). 한계 — 임계 `1` 의 튜닝 근거는 실측이 아니라 52 행 상수 위 주석의 보수적 추정이고 (dogfood 실측 · metadata enrich 후 조정은 Follow-up 으로만 남아 있다), 오탐으로 `"zero"` 강등된 단위를 사람이 해제하는 개입 경로는 `src/` 에서 확인되지 않았으며, 본 축을 cover 하는 e2e / smoke 는 0 이다 (`test/` 전수에서 `applyEvaluationAdjustments` 참조 0 — e2e 는 이미 영속된 `contributionScore` fixture 만 다룬다)) |
| REQ-019 | 38 | 새 알고리즘·외부 연구 소개 = 높은 contribution | FR | P5 | unit + manual | IN_PROGRESS (등급 정의 축 · LLM 산출 축 · 점수 영속 축 실재 / 상향을 결정하는 결정적 식별 축 · 부여 축 부재: 식별 축의 유일 후보는 `src/assessment-evaluation/domain/evaluation-quality-signal.ts` 115 행 `export function computeContributionQualitySignal(inputs: EvaluationInput[]): ContributionQualitySignal` 인데, 판정식 137 행 `titleLength <= CONTRIBUTION_QUALITY_TITLE_FLOOR` 가 52 행 상수 `CONTRIBUTION_QUALITY_TITLE_FLOOR = 1` 과 `input.metadata.titleLength` **단일 필드** 만 비교해 **임계 이하** 단위를 `zeroContributionUnitIds` 에 push 하므로 방향이 하향이다 — 즉 zero-contribution 후보 식별 (R-37 하향) 전용이며 high-contribution 후보 식별 (R-38 상향) 은 아니다. "새 알고리즘 설계 · 새 일거리 구상 · 외부 연구 소개 자료" 를 판별하는 필드 · 임계 · 분기는 domain 디렉토리 전수에서 0 이고, 파일 1~3 행 주석이 `R-37 / R-38` 을 함께 책임 범위로 적었을 뿐 상향 detection 은 구현돼 있지 않다. 부여 축의 유일한 결정적 등급 조정 경로는 `src/assessment-evaluation/domain/evaluation-quality-adjust.ts` 119 행 `applyContributionQualityFloor` 인데 대입 값이 62 행 `CONTRIBUTION_QUALITY_FLOOR_LEVEL: ContributionLevel = "zero"` 하나뿐이라 **floor 강등 전용 · 단조 비상향** 이고, 같은 파일 37 · 60 · 110~111 행 주석이 "등급 상향은 본 helper 의 책임이 아니다 — LLM 정성 평가 영역 + 별도 task" 로 자인한다. 따라서 `high` 부여는 결정적 코드가 아니라 LLM 산출에만 의존한다 — `src/assessment-evaluation/domain/evaluation-prompt.ts` 109 행 instruction line 이 난이도와 기여도를 각각 허용 등급 (기여도는 zero low medium high 4 종) 중 하나로 답하라고 지시하고, 155~170 행 `classifyNarrative` 가 51 행 `CONTRIBUTION_MARKER` 정규식으로 값 토큰을 추출해 `isContributionLevel` 통과 시에만 채택하며 미인식 · marker 부재는 33 행 `DEFAULT_CONTRIBUTION: ContributionLevel = "low"` 로 환원한다 (throw 0). `src/assessment-evaluation/domain/evaluation-result.ts` 28 행의 `"high" : 새 알고리즘 / 외부 연구 도입 등 높은 난이도·창의 기여` 는 **문서 주석뿐** 이며 실행 로직은 32 행 union 타입 · 37 행 `CONTRIBUTION_LEVELS` · 48 행 멤버십 검증까지다 (등급 문자열의 의미를 판별하는 코드는 없다). wiring 축은 두 심볼의 spec 제외 전수 참조로 확인했고 (a) detection = `src/assessment-evaluation/domain/evaluation-detection-signals-pipeline.ts` 51 행 import + 108 행 `quality: computeContributionQualitySignal(deduped)`, (b) adjustments = `src/assessment-evaluation/domain/evaluation-adjustments-pipeline.ts` 74 행 import + 211~214 행 `applyContributionQualityFloor(updateCountAdjusted, signals.quality)` (5-adjuster 중 3 번째), (c) 본류 = `src/assessment-evaluation/evaluation-orchestrator.service.ts` 158 행 `computeEvaluationAdjustmentSignals(deduped)` + 177 행 `applyEvaluationAdjustments(entries, signals)` 로 dead code 는 아니다 — 다만 이 배선이 실어 나르는 신호는 하향뿐이고, 상향의 실 배선은 `src/assessment-evaluation/evaluation-scoring.service.ts` 104 행 `const { difficulty, contribution } = classifyNarrative(narrative)` 라는 LLM 경유 경로다. 주석의 REQ 매핑은 어긋나 있다 — 두 quality 파일 2~3 행의 `REQ-037 / REQ-038` 표기는 본 표의 실제 REQ-037 (64 행 일괄 평가 + Reset & Reeval) · REQ-038 (68-71 행 UI 조회 / sort / filter / 시계열) 과 전혀 다른 대상을 가리키며, README 행 번호 `R-37 / R-38` 을 REQ ID 로 오기한 것이다 (본 task 는 doc-only 라 주석 수정은 Follow-up 으로만 남긴다). 영속 경로에 R-38 전용 컬럼 · 플래그는 없다 — `prisma/schema.prisma` 전수 확인 결과 `Contribution` model 335~336 행 `difficulty String` · `contributionScore Decimal` 과 300~303 행 동형 필드 · 366 행 `narrative String` 뿐이고, `high` 는 `src/assessment-evaluation/domain/evaluation-result.persist.mapper.ts` 147 행 `contributionScore: contributionLevelToScore(result.contribution)` 의 70~78 행 등간격 매핑 (zero 0 / low 1 / medium 2 / high 3) 을 거쳐 **숫자 3 으로만** 남는다. 검증 위치 `unit` 의 실 근거는 domain unit 3 spec — `src/assessment-evaluation/domain/evaluation-quality-signal.spec.ts` 20 it · `src/assessment-evaluation/domain/evaluation-quality-adjust.spec.ts` 24 it · `src/assessment-evaluation/domain/evaluation-prompt.spec.ts` 37 it. 한계 — 컬럼의 `manual` 축을 뒷받침하는 문서화된 수동 검증 절차는 `docs/` 전수에서 확인되지 않았다 (REQ-019 언급은 `docs/PLAN.md` · 본 표 · task 파일 · `docs/use-cases/REQ-COVERAGE-AUDIT.md` 53 행의 "uc-covered / UC-01 LLM 평가문" 뿐이라 검증 절차가 아니다). 한계 — 상향 판정이 전적으로 LLM 비결정성에 노출되고, marker 미인식 · 자유 산문 응답이면 `classifyNarrative` 가 경고 · 로그 · 재시도 없이 조용히 `"low"` default 로 환원해 실제 high 기여가 소실되며, 사람이 상향을 강제하거나 오판을 해제하는 개입 경로는 `src/` 에서 확인되지 않았다. 본 축을 cover 하는 e2e / smoke 는 0 이다 — `test/` 의 `"high"` 등장은 `test/helpers/realdata-e2e-result-issue-*-plan*.spec.ts` 의 이미 확정된 fixture 값뿐이고 `applyEvaluationAdjustments` 참조는 0 이다) |
| REQ-020 | 39 | 조직 기여 큰 인원 → 높은 점수 | FR | P5 | manual + unit | IN_PROGRESS (문서 기여의 결과 반영 축 · 등급→점수 영속 축 · LLM 정성 코멘트 산출 축 실재 / 문서 축 조직 기여 식별 축 · 결정적 점수 상향 축 · 문서 기반 코멘트 상향 축 부재: 식별 축부터 부재다 — `src` 이하 `*.ts` 전수 (spec 제외) 에서 `R-39` · `REQ-020` · "39 행" 세 문자열 검색 결과가 0 행이라 본 REQ 를 자기 책임으로 선언한 심볼은 `src/assessment-evaluation/domain` 전수에 없다. 최근접 후보 `src/assessment-evaluation/domain/evaluation-notable-contribution-signal.ts` 128~130 행 `export function computeNotableContributionSignal(inputs: EvaluationInput[]): NotableContributionSignal` 는 150~152 행 `if (input.contributionKind === "code") { acc.codeUnitCount += 1; }` 로 **code 단위만** 세며, 21~23 행 주석이 "document 단위는 코드 기여 정량에서 제외한다 … document 축은 별도 신호" 로, 77 행 필드 주석이 "(document 제외)" 로 각각 자인한다. 판정식은 167~173 행 — 167 행 `comparable = totalAuthorCount >= 2 && meanCodeUnitCount > 0`, 168 행 `ceiling = meanCodeUnitCount * NOTABLE_RELATIVE_CEILING` (71 행 상수 `NOTABLE_RELATIVE_CEILING = 1.5`), 173 행 `notable = comparable && acc.codeUnitCount > ceiling` (strictly greater) 이고, 기준값은 158~163 행 전 author codeUnitCount 의 평균 (author 0 명이면 0) 이다. 경계는 보수적 — 단독 author / 평균 0 batch 는 notable 0, 빈 배열은 totalAuthorCount 0 · byAuthor [] · notableDetected false, `inputs` 가 null/undefined 인 경우만 131~135 행 한국어 `TypeError` (유일 throw). 대칭 하향 축도 동형이다 — `src/assessment-evaluation/domain/evaluation-underperformer-signal.ts` 146 행 `if (input.contributionKind === "code")` + 67 행 `UNDERPERFORMER_RELATIVE_FLOOR = 0.5` 로 역시 document 를 제외한다. 즉 author 간 상대 비교 신호 2 종이 모두 code 축 전용이고 **문서 기여 단위 수를 author 간 비교하는 신호는 0** 이다. document kind 를 실제로 읽는 경로는 `src/assessment-evaluation/domain/evaluation-dedup.ts` 158 행 (document self-follow-up dedup) 과 `src/assessment-evaluation/domain/evaluation-update-count-neutral.ts` 120 행 (update 횟수 중립화 — REQ-022, 상향이 아니라 advantage 제거) 둘뿐이다. 점수 축은 두 층 모두 상향 결정 경로가 없다 — (a) 단위 차원: 5-adjuster 중 점수·등급을 **상향** 시키는 것이 0 이다 (abuse · update-count 는 `volume` 하향/중립, quality 는 `contribution` 을 `"zero"` 로 floor 강등, underperformer · notable 은 `narrative` marker 만 손댄다). 소비측 `src/assessment-evaluation/domain/evaluation-notable-contribution-adjust.ts` 30~32 행이 "`volume` / `contribution` 같은 정량/등급 필드를 손대지 않고 `narrative` 에 marker 만 접두한다 — 점수 반영은 별도 task" 로 자인한다. (b) 좌표 차원: `src/assessment-evaluation/domain/summary-aggregate.ts` 84 행 `export function aggregateMetricScore(results: EvaluationResult[]): number` 의 수식은 110~113 행 `DIFFICULTY_WEIGHT * difficultyAvg + CONTRIBUTION_WEIGHT * contributionAvg + VOLUME_WEIGHT * volumeSignal` 이고 57~59 행 세 가중치가 모두 `1`, 108 행이 `Math.log1p(volumeSum)` 압축이다. 입력 타입 `EvaluationResult` (`src/assessment-evaluation/domain/evaluation-result.ts` 54~71 행) 에는 **`contributionKind` 필드 자체가 없어** 집계 층이 code/document 를 구분할 수 없고 문서 전용 가중치도 0 이다. 따라서 문서 기여의 점수 상향은 adjuster 가 아니라 "문서 단위가 늘면 등급 평균과 Σvolume 을 통해 metricScore 가 자연히 커진다" 는 **간접 효과** 뿐이며, 그 volume 조차 `src/assessment-evaluation/domain/evaluation-volume.ts` 30~42 행 `calculateEvaluationVolume` 이 `metadata.titleLength` 정수 1 개로만 산출하는 kind 무관 값이다. 코멘트 축도 marker 접두까지다 — `applyNotableContributionAnnotation` (같은 adjust 파일 130~133 행) 은 147~161 행에서 `signal.byAuthor` 를 author 로 색인해 `notable === true` 인 author 의 **모든** 단위 `result.narrative` 앞에 78 행 상수 `NOTABLE_CONTRIBUTION_NARRATIVE_MARKER = "[중요기여] "` 를 접두하고 (169~174 행 `startsWith` 멱등, 비대상은 새 객체 복제 passthrough), 문장을 생성하지 않는다. 대상 판정이 code 단위 수 기반이므로 **문서 기여만 많은 인원은 marker 대상에서 구조적으로 제외된다**. 문서 기여 기반 코멘트 상향의 유일 경로는 LLM 산출이다 — `src/assessment-evaluation/domain/evaluation-prompt.ts` 109 행 instruction 이 난이도·기여도를 형식으로 답하라 지시하고 `classifyNarrative` 가 marker 를 파싱하며, 미인식·부재 시 170 행이 33 행 `DEFAULT_CONTRIBUTION: ContributionLevel = "low"` 로 (본 task 재실측값) 조용히 환원한다. wiring 축은 dead code 가 아니다 — (a) detection = `src/assessment-evaluation/domain/evaluation-detection-signals-pipeline.ts` 50 행 import + 110 행 `notableContribution: computeNotableContributionSignal(deduped)`, (b) adjustments = `src/assessment-evaluation/domain/evaluation-adjustments-pipeline.ts` 72 행 import + 230~233 행 `applyNotableContributionAnnotation(underPerformerAnnotated, signals.notableContribution).map((entry) => entry.result)` 로 5-adjuster 중 **5 번째(마지막)**, (c) 본류 = `src/assessment-evaluation/evaluation-orchestrator.service.ts` 158 행 `computeEvaluationAdjustmentSignals(deduped)` + 177 행 `applyEvaluationAdjustments(entries, signals)`, (d) summary 집계 = `src/assessment-evaluation/summary-persist.service.ts` 35 행 import + 105 행 `const metricScore = aggregateMetricScore(results)`. 다만 이 배선이 실어 나르는 상대 비교 신호가 code 축이라 REQ-020 문서 축은 그 위에 없다. 영속 경로에 REQ-020 전용 컬럼·플래그·랭킹 필드는 0 이다 — `prisma/schema.prisma` 329~349 행 `Contribution` 에는 332 행 `sourceType String` · 335 행 `difficulty String` · 336 행 `contributionScore Decimal` · 337 행 `volume Int` 만 있고 **`contributionKind` (code/document) 컬럼 자체가 없어** sourceType literal 로 간접 추정만 가능하며, 361~368 행 `Summary` 는 366 행 `narrative String` · 367 행 `metricScore Decimal` 뿐이다. 문서 기여의 크기는 `src/assessment-evaluation/domain/evaluation-result.persist.mapper.ts` 147 행 `contributionScore: contributionLevelToScore(result.contribution)` 가 74~79 행 등간격 매핑 (zero 0 / low 1 / medium 2 / high 3) 을 거쳐 만든 숫자와 metricScore 안에 code 기여와 섞여서만 남는다. 검증 위치 `unit` 의 실 근거는 domain unit 3 spec 53 it — `src/assessment-evaluation/domain/evaluation-notable-contribution-signal.spec.ts` 19 it · `src/assessment-evaluation/domain/evaluation-notable-contribution-adjust.spec.ts` 19 it · `src/assessment-evaluation/domain/summary-aggregate.spec.ts` 15 it 이며, 이 spec 들은 code 단위 수 기반 판정과 kind 무관 집계만 검증하고 문서 축 상향을 cover 하는 case 는 없다. 한계 — 컬럼의 `manual` 축을 뒷받침하는 문서화된 수동 검증 절차는 `docs/` 전수에서 확인되지 않았고, `docs/decisions/ADR-0003-deployment.md` 22 · 55 · 88 행과 `docs/tasks/T-0015-adr-0003-deployment-rest.md` 38 · 49 행의 `REQ-020` 언급은 전부 "권한 부족 통지 (README 20 행)" 를 가리키는 **옛 번호 체계 잔재** 라 본 행 (39 행 조직 기여) 의 근거가 아니다. 한계 — "조직에 큰 기여" 를 판정하는 결정적 기준 (문서 축 임계 · 상대 비교 · 조직 영향 메타) 이 전무해 상향이 LLM 비결정성과 간접 합산 효과에만 의존하고, 사람이 조직 기여 판단을 주입하거나 LLM 오판을 해제하는 개입 경로도 `src/` 에서 확인되지 않았다. 한계 — 본 축을 cover 하는 e2e / smoke 는 0 이다 (`test/` 전수에서 `NotableContribution` · `중요기여` · `applyEvaluationAdjustments` · `aggregateMetricScore` 참조 0 이고, `metricScore` 등장은 `test/e2e/summaries.e2e-spec.ts` 의 이미 영속된 값 조회뿐이다)) |
| REQ-021 | 40 | 문서 abusing 방지 (의미 없는 기여 단순 반복) | FR | P5 | unit | DONE (같은 computeAbuseSignal 의 R-40 문서 abusing 경로 — 코드 abusing 과 동일 심볼 쌍으로 cover) |
| REQ-022 | 41 | 문서 update 횟수 중립화 (advantage/disadvantage 둘 다 없음) | FR | P5 | unit | DONE (implemented-on-main — T-0524 `computeUpdateCountNeutralization` PR #437 + T-0525 `applyUpdateCountNeutralizationToVolume` PR #438; volume 산출이 version 미사용 → advantage 0, 중립 보존 → disadvantage 0. ADR-0049 group-by-collapse 안은 미채택 — Q-0046 옵션1) |
| REQ-023 | 45-46 | 서비스별 ID 매핑 (1 인물 ↔ N 서비스 ID) | FR | P3 | unit | DONE (implemented-on-main — `schema.prisma` `model ServiceIdentity` 의 1 Person ↔ N `service`/`externalId` 매핑 + `@@unique([personId, service])` 서비스당 1 매핑 invariant; 검증은 `service-identity.repository.spec.ts` 의 findByPersonId / create 케이스 — service·controller 미보유라 e2e 없음) |
| REQ-024 | 47 | Primary key 역할 ID 지정 (서비스 중 1개) | Constraint | P3 (ADR 필수) | policy + unit | DONE (implemented-on-main — `ServiceIdentity.isPrimary Boolean @default(false)` + repository `setPrimary` 의 `$transaction` updateMany(false) → update(true) 로 primary 1 row transition 보장, unit spec 3 종 cover. 한계 — 전용 ADR 없이 [ADR-0002](decisions/ADR-0002-db.md) 에 귀속돼 policy 축은 간접 cover 이고, "정확히 1 primary" service-layer 강제와 HTTP 노출(service·controller) 은 미보유) |
| REQ-025 | 48 | 일부 서비스 ID NULL 허용 | FR | P3 | unit | DONE (implemented-on-main — 미등록 서비스는 nullable 컬럼이 아니라 `ServiceIdentity` row 부재로 NULL 을 표현하는 설계; `findByPersonId` 가 row 부재 시 빈 배열 반환 spec 으로 검증 — service·controller 미보유라 e2e 없음) |
| REQ-026 | 49 | 인원 CRUD + Deactivate/Activate (휴직 시 숨김) | FR | P3 | unit + e2e | DONE (implemented-on-main — `PersonService` CRUD + `deactivate`/`reactivate`(softDelete/restore, 휴직 숨김) 를 `PersonController` 5 endpoint(`@Get()` · `@Get(":id")` · `@Post()` · `@Patch(":id")` · `@Delete(":id")`) 가 forward, `GET /api/persons` 는 `findActive` 로 비활성 숨김; unit spec + `test/e2e/persons.e2e-spec.ts` 11 it 로 검증. 한계 — 전용 `POST /:id/deactivate` endpoint 가 없어 활성 토글은 `PATCH /api/persons/:id` 의 `{active}` 경유이고, `deactivate`/`reactivate` 메서드 자체와 비활성 포함 조회(`findAll`) 는 HTTP 미노출) |
| REQ-027 | 50 | 신규 인원 1년치 평가 1회 (일반은 1주 단위) | FR | P7 + P5 | unit + e2e | DONE (POST /api/schedules/backfill/:personId · buildBackfillPlan DEFAULT_WEEKS=52 · unit spec 3종 — e2e 미보유) |
| REQ-028 | 51 | Group 정책 (다중 임의 group + 단일 조직도 파트) | FR | P3 | unit | DONE (implemented-on-main — 다중 group 소속은 `PersonGroupMembership` 중간 테이블 N:M(`@@unique([personId, groupId])` 로 중복 소속만 금지) 로, 조직도 파트 1 개는 `Person.partId` 단일 FK(`Restrict` 로 소속 인원 있는 Part 삭제 차단) 로 표현; `group.service.ts` · `part.service.ts` · `person-group-membership.repository.ts` + 각 unit spec 보유. 한계 — `partId String?` 가 **nullable** 이라 "정확히 1 파트" invariant 는 DB 제약이 아니며, schema 23~26 행 주석대로 service-layer 강제는 후속 과제로 defer) |
| REQ-029 | 56 | 평가 자료 non-volatile 저장 | NFR | P3 | unit | DONE (implemented-on-main — 저장소는 `schema.prisma` 44 행 `provider = "postgresql"` 의 PostgreSQL 영속 DB([ADR-0002](decisions/ADR-0002-db.md)), 평가 결과 write path 는 [ADR-0033](decisions/ADR-0033-evaluation-result-persistence.md) §Decision 1 매핑을 구현한 `src/assessment-evaluation/evaluation-result-persist.service.ts` → `src/user/assessment.repository.ts` 의 `create`/`delete` + `contribution.repository.ts` · `summary.repository.ts`; 검증은 `assessment.repository.spec.ts` 22 it · `summary.repository.spec.ts` 21 it 등 unit spec 과 `test/e2e/assessments.e2e-spec.ts` 20 it. 한계 — 프로세스 재기동 후 잔존을 직접 확인하는 durability 전용 test 는 없고, ADR-0002 의 single-operator 전제상 replication · PITR · 정기 backup 은 미도입) |
| REQ-030 | 57 | Export/backup + Restore | FR | P7 | e2e | DONE (implemented-on-main — 두 축 모두 실재: export 는 `src/export/export.controller.ts` 의 `@Controller("api/admin/export")` + `@Post()` / `@Get(":id/download")` (dump 다운로드) / `@Get(":id/status-view")`, restore 는 `src/import/import.controller.ts` 의 `@Controller("api/admin/import")` + `@Post()` / `@Post("preview")` / `@Get("modes")`; 영속 job record 는 `schema.prisma` 614 행 `ExportJob` · 649 행 `ImportJob` model, reset 축은 `ImportMode.REPLACE` (schema 571~574 행 enum, `ImportJob.mode` 기본값) 를 `import-restore-transaction.service.ts` 가 단일 `$transaction` 안에서 선삭제 후 재생성하는 형태로 구현. 검증은 e2e 5 spec 56 it — `test/e2e/export-download.e2e-spec.ts` 12 it · `export-scope-preview.e2e-spec.ts` 10 it · `import-restore-http.e2e-spec.ts` 21 it · `import-restore-rejection.e2e-spec.ts` 6 it · `import-restore-transaction.e2e-spec.ts` 7 it. 한계 — README 원문의 "reset" 이 DB 전체 초기화인지 dump 에 담긴 entity 범위 한정 선삭제인지는 본 재판정에서 확인하지 못했고 (`ImportMode` 는 destructive REPLACE 와 비파괴 MERGE 2 종 병존, `import-merge-conflict.ts` 가 후자의 충돌 처리 담당), 예약 backup · 외부 저장소 업로드 같은 운영 automation 과 비동기 job queue 는 부재해 runner 를 요청-응답 안에서 동기 await 한다) |
| REQ-031 | 58 | 재수집 중복 방지 + 최근 1주 재수집 OK | FR | P5 | unit | DONE (implemented-on-main — 두 축 모두 실재 + 수집 경로에 배선: 중복 방지 축은 `src/assessment-collection/domain/commit-dedup.ts` 의 `dedupGithubActivities()` (dedup 키 = commit 은 `commit:<externalId>`(=commit SHA) 단일 키 earliest-wins, pr/issue 는 `<kind>:<repoRef>:<externalId>` 합성 키) + `src/assessment-collection/domain/page-dedup.ts` 의 `dedupConfluenceActivities()` (dedup 키 = `externalId`(=page id), 최대 `version` latest-wins), 각각 `github-collection.service.ts` 32·115 행 · `confluence-collection.service.ts` 41·90 행에서 수집 누적 직후 호출되어 배선 확인. 최근 1주 축은 `src/assessment-collection/domain/recollection-window.ts` 의 `applyRecollectionWindow(since, windowDays)` + 기본 상수 `RECOLLECTION_WINDOW_DAYS = 7` 로, `since-derivation.service.ts` 25·59 행 `deriveSinceWithRecollectionWindow()` 를 거쳐 `collection-trigger.service.ts` 67 행에서 실제 수집 trigger 에 thread 된다 (`dto.windowDays` 미지정 시 7 일). 영속 layer 중복 차단은 `schema.prisma` 348 행 `@@unique([assessmentId, sourceRef])`. 검증은 unit 4 spec 76 it — `domain/commit-dedup.spec.ts` 31 it · `domain/page-dedup.spec.ts` 9 it · `domain/recollection-window.spec.ts` 17 it · `since-derivation.service.spec.ts` 19 it. 한계 — README 원문의 "뒤늦게 push 된 과거 자료를 놓치지 않는다" 축은 7 일 window 겹침으로만 부분 충족이고 window 밖 late-arriving 데이터의 별도 보정 경로는 부재하며, 주기 수집 scheduler 도 `src/scheduling/` 의 REST cron endpoint (`cron-schedule.controller.ts`) 까지만 존재하고 `scheduling.module.ts` 의 `CRON_TICK_HANDLER` 기본 provider 가 logging no-op stub 이라 자동 주기 재수집은 미결선 — 현재는 manual `backfill.controller.ts` 호출에 의존한다) |
| REQ-032 | 59 | 🔥 Raw data 저장 금지 — 평가 결과만 보유 | Constraint | P3 (ADR 필수) | policy + reviewer 점검 | DONE (implemented-on-main — 대응 ADR 실재: [ADR-0006](decisions/ADR-0006-assessment-data-model.md) §Decision 4 (raw 본문 컬럼 0 의 schema-level 강제) + [ADR-0033](decisions/ADR-0033-evaluation-result-persistence.md) §Decision 2 (영속화 path 의 raw payload 0 재확인, 새 컬럼 0), 둘 다 ACCEPTED. 실측 — `schema.prisma` 의 `Assessment` · `Contribution` · `Summary` 3 model 컬럼은 평가 파생 수치(difficulty · contributionScore · volume) · LLM narrative · 참조 식별자(sourceType · sourceUrl · sourceRef) · timestamp 뿐이고 commit diff · PR/issue body · 문서 원문 같은 raw 본문 컬럼은 0. 한계 — 검증 위치의 `reviewer 점검` 축에 자동화가 없다: `.claude/agents/reviewer.md` 의 raw 미저장 점검 항목 0 hit, `scripts/` 에도 대응 check script 부재라 사실상 ADR + schema 주석 policy 에만 의존) |
| REQ-033 | 60 | commit/문서 별 기여도·난이도·양 보유 | FR | P3 + P5 | unit | DONE (implemented-on-main — 3 축이 schema · 산정 · 영속 3 layer 모두에 실재: schema 축은 `schema.prisma` 329 행 `Contribution` model 의 `difficulty String` · `contributionScore Decimal` · `volume Int` 3 컬럼이고 건별 단위 키는 `sourceRef String` + 348 행 `@@unique([assessmentId, sourceRef])`. 산정 축은 `src/assessment-evaluation/evaluation-scoring.service.ts` 90 행 `EvaluationScoringService.scoreUnit()` 이 단위 1 건당 `gateway.generate` 1 회 → `classifyNarrative()` (`domain/evaluation-prompt.ts` 155 행) 로 난이도·기여도를, `calculateEvaluationVolume()` (`domain/evaluation-volume.ts` 30 행) 로 양을 산출해 `domain/evaluation-result.ts` 54 행 `EvaluationResult` 의 `difficulty` / `contribution` / `volume` 필드로 조립 — 3 축 모두 산정 경로 확인. 영속 축은 `domain/evaluation-result.persist.mapper.ts` 136 행 `mapEvaluationResultToContribution()` 이 결과 1 건 → Contribution create input 1 건으로 1:1 매핑 (`sourceRef = unitId`, `contributionScore = contributionLevelToScore()` 104 행) 하고 `evaluation-result-persist.service.ts` 213·216~217 행 `tx.assessment.create({ ... contributions: { create: mapped.contributions } })` nested create 로 건별 row 를 박제. 평가 단위 두 종은 persist.mapper 86 행 `KNOWN_SOURCE_TYPES = ["commit", "pr", "issue", "document"]` 로 code commit 과 문서 건 모두 정의. 검증은 unit 5 spec 107 it — `evaluation-scoring.service.spec.ts` 29 it · `domain/evaluation-result.persist.mapper.spec.ts` 21 it · `domain/evaluation-volume.spec.ts` 21 it · `evaluation-result-persist.service.spec.ts` 18 it · `domain/evaluation-result.spec.ts` 18 it. 한계 — 단위 종류 컬럼 `sourceType` 은 실 파이프라인에서 빈 문자열 placeholder 로 떨어진다: 유일한 unitId 합성 지점인 `domain/evaluation-input.mapper.ts` 46 행 `buildUnitId()` 가 prefix 로 `ActivitySourceType` (`github` / `confluence` 2 값, `assessment-collection/domain/activity.ts` 16 행) 을 쓰는데 `resolveSourceType()` (persist.mapper 117 행) 의 인정 집합과 미일치해 `""` fallback 이며 persist.mapper.spec 288 행이 그 동작을 그대로 고정 — 건별 3 축 보유 자체는 충족하나 commit/문서 구분은 `sourceRef` prefix 로만 남는다 (`sourceUrl` 도 도출 source 부재로 빈 문자열). 또 난이도 3 종 모델 (REQ-050) 과의 정합, 문서 건의 수집→평가 end-to-end 연결은 본 재판정에서 확인하지 않았다) |
| REQ-034 | 61 | 일별 활동 요약 평가문 (당일은 자정까지 안 함) | FR | P5 | unit | DONE (isPeriodEvaluable/computePeriodEnd 의 day 경로 — 다음 KST 자정 이후에만 Summary 생성 허용, enumerateSummaryDueCoordinates → SummaryBatchOrchestrator 배선) |
| REQ-035 | 62 | 주간/월간 요약 평가문 (다음주/다음달 시작 시) | FR | P5 | unit | DONE (같은 심볼의 week/month 경로 — 다음 KST 월요일 00:00 · 다음 달 1 일 00:00 이후 허용, ADR-0035 aggregate summary 평가로 cover) |
| REQ-036 | 63 | 상대 비교 가능 + LLM 정성 + Metric 수치 | FR | P3 + P5 | unit | IN_PROGRESS (LLM 정성 축 · Metric 수치 축 2 종 실재 + 한 row 결합 영속 확인 / 개발자 간 상대 비교 전용 산출 경로 부재: schema 축은 `prisma/schema.prisma` 361 행 `Summary` model 의 366 행 `narrative String` (LLM 정성) 과 367 행 `metricScore Decimal` (Metric 수치) 이 나란히 실재하고, 좌표 키는 363~365 행 `personId` / `period` / `periodStart` + 377 행 `@@unique([personId, period, periodStart])` · 379 행 `@@index([personId, period, periodStart])`. LLM 정성 축은 `src/assessment-evaluation/summary-narrative.service.ts` 54 행 `export class SummaryNarrativeService` 의 96 행 `generateBatchNarrative()` 가 106 행에서 `this.gateway.generate(prompt, { modelId })` 를 좌표당 **정확히 1 회** 호출해 narrative 를 만든다. Metric 수치 축은 `src/assessment-evaluation/domain/summary-aggregate.ts` 84 행 `aggregateMetricScore(results: EvaluationResult[]): number` — 입력은 그 좌표의 단위 `EvaluationResult[]` 이고 수식은 34~36 행 주석대로 `DIFFICULTY_WEIGHT × avg(난이도 ordinal 0~2) + CONTRIBUTION_WEIGHT × avg(기여도 ordinal 0~3) + VOLUME_WEIGHT × log1p(Σ volume)` 후 precision round 다. 즉 "개발된 Metric" 의 실체는 난이도 평균 · 기여도 평균 · 양의 log1p 압축 3 signal 의 **가중 합** 이며 57~59 행에서 세 가중치가 모두 `1` 이라 현재는 등가중 합이고, 빈 입력은 결정적 0. 두 축의 결합은 `src/assessment-evaluation/summary-persist.service.ts` 105 행 (metricScore 산출) → 110 행 (narrative 생성) → 119 행 `persistInTransaction` → 192 행 `tx.summary.create({ ... })` 로, 146 행 deleteMany 와 함께 단일 `$transaction` reset-and-recreate 안에서 **하나의 Summary row** 에 함께 영속됨을 확인. README 대상 범위인 주간/월간은 `src/assessment-evaluation/domain/summary-batch-outcome.ts` 43 행 `GRANULARITY_BUCKETS = ["day", "week", "month", "other"]` + 112 행 분류로 week · month 양쪽 모두 period 값에 존재. 검증은 unit 4 spec 110 it — `domain/summary-aggregate.spec.ts` 33 it · `summary-narrative.service.spec.ts` 24 it · `summary-persist.service.spec.ts` 15 it · `src/user/summary.controller.spec.ts` 38 it. 한계 — 상대 비교 축은 동일 척도 `metricScore` 컬럼과 좌표 index 제공까지만 충족이고 **비교 전용 산출 경로는 부재** 다: 순위 · 백분위 · person 간 비교 심볼이 `src/` 전체에 없고 `src/user/summary.controller.ts` 100~105 행 `@Get()` findAll 은 `personId` / `period` query filter 만 받으며 `src/user/summary.repository.ts` 111·116 행은 `orderBy: { periodStart: "desc" }` 시계열 정렬만 한다 (metricScore sort / 다중 person 집계 없음). 또 상대 비교의 UI 노출 여부, 위 등가중 수식이 README 가 의도한 "개발된 Metric" 에 부합하는지, 일별 요약 (day) 과 주간/월간 요약의 축 차이는 본 재판정에서 확인하지 않았다) |
| REQ-037 | 64 | 평가 없는 부분 일괄 평가 + Reset & Reeval | FR | P5 | e2e | IN_PROGRESS (일괄 평가 축 · Reeval 축 실재 / 디버깅용 명시적 Reset endpoint 부재: 일괄 평가 축은 `src/assessment-evaluation/assessment-evaluation.controller.ts` 538 행 `@Post("unevaluated-fill-plan")` (Admin+ `@Roles("Admin")`, handler `planUnevaluatedFill`) 과 599 행 `@Post("unevaluated-fill-run")` (handler `runUnevaluatedFill`) 의 plan → run 2 단계로 노출된다. plan 단계는 `src/assessment-evaluation/evaluation-unevaluated-fill-planner.service.ts` 38 행 `export class EvaluationUnevaluatedFillPlanner` 의 64 행 `async planUnevaluatedFill(intended: IntendedPeriodCoordinatesInput): Promise<UnevaluatedFillBatchPlan>` 가 `reader.readForPersons(personIds, options)` 로 영속 좌표를 읽고 `src/assessment-evaluation/domain/evaluation-unevaluated-fill-plan.ts` 90 행 `composeUnevaluatedFillPlan(input)` 에 위임하며, run 단계는 `src/assessment-evaluation/unevaluated-fill-run-orchestrator.service.ts` 61 행 `export class UnevaluatedFillRunOrchestratorService` 의 107 행 `async run(rawBridges, requestModelId, defaultModelId)` 가 person resolver 조립 + `generateAndPersist` bind 후 `runUnevaluatedFillRunCore` 로 좌표를 순차 처리한다 (좌표 1 개의 실패는 throw 없이 batch 가 흡수). "평가 결과가 없는 부분" 의 판별은 `src/assessment-evaluation/domain/evaluation-unevaluated-period-select.ts` 128 행 `selectUnevaluatedPeriods(intended: EvaluationPersistContext[], persisted: EvaluationPersistContext[]): EvaluationPersistContext[]` — 63 행 내부 헬퍼 `coordinateKey` 가 만드는 (personId, period, scope, periodStart) 4-tuple 키 기준 차집합 `intended \ persisted` 이며, "없음" 의 판정 기준은 어떤 값의 크기·null 여부가 아니라 **그 좌표의 영속 레코드 부재** 다 (periodStart 는 epoch ms 정규화라 동일 instant 의 다른 Date 객체가 같은 좌표로 매칭, personId/period/scope 는 미정규화 exact match, intended 내부 중복은 dedup 하지 않음). Reeval 축은 controller 241 행 `const mode: PersistMode = dto.mode === "reeval" ? "reeval" : "fill"` (208 행 `@Post("evaluate")`) 과 339 행 `@Post("period")` 의 500 행 `dto.reevaluate` pass-through (393 행 — 비-Admin 이 `reevaluate: true` 를 명시하면 403 fail-closed) 로 외부 노출되고, 실 덮어쓰기는 `src/assessment-evaluation/summary-persist.service.ts` 153 행 `persistInTransaction` 이 좌표로 `findUnique` 후 fill 이면 no-op (idempotent) / reeval 이면 177 행 `tx.summary.delete` → `createSummary` 재생성 하는 단일 `$transaction` reset-and-recreate 다 (`evaluation-result-persist.service.ts` 도 동형 모드 분기). 검증은 e2e 3 spec 27 it — `test/e2e/unevaluated-fill-plan.e2e-spec.ts` 9 it · `test/e2e/unevaluated-fill-run.e2e-spec.ts` 9 it · `test/e2e/period-bridge-reevaluate.e2e-spec.ts` 9 it, unit 은 `evaluation-unevaluated-fill-planner.service.spec.ts` 13 it · `unevaluated-fill-run-orchestrator.service.spec.ts` 12 it. 한계 — 디버깅 목적의 **명시적 Reset 은 외부에서 호출 불가** 다: partial-reset 구현체인 `summary-persist.service.ts` 146 행 `resetByPeriod(personId, period)` 와 `evaluation-result-persist.service.ts` 147 행 동명 메서드가 `prisma.*.deleteMany({ where: { personId, period } })` 로 실재하지만 두 심볼의 참조는 자기 service 와 자기 spec 뿐이라 controller wiring 이 0 이고 (spec cover 는 `summary-persist.service.spec.ts` 367 행 `describe("resetByPeriod (partial-reset)")` unit 3 it 뿐, e2e 0), 따라서 Reset 축은 재평가 시의 내부 overwrite (reeval 모드 delete→create) 로만 충족된다. 또 reset 의 운영 범위 (person+period 단위 위의 전체 초기화 가능 여부), Admin UI 노출 여부, 일괄 평가의 대상 기간 산정 방식 (`IntendedPeriodCoordinatesInput` 의 range enumerate 규칙) 은 본 재판정에서 확인하지 않았다) |
| REQ-038 | 68-71 | UI 조회 / sort / filter / 시계열 | FR | P6 | smoke + e2e | DONE |
| REQ-039 | 72 | Admin cron 주기 지정 | FR | P7 | unit + e2e | DONE (GET·PUT·DELETE /api/schedules 런타임 cron registry) |
| REQ-040 | 73 | Admin manual trigger | FR | P7 | e2e | DONE (POST /api/schedules/trigger 즉시 1회 발화) |
| REQ-041 | 74 | Admin 최근 N일 결과 manual delete → 재수집 | FR | P7 | unit + e2e | DONE (POST /api/schedules/recent-deletion/:personId delete→재수집) |
| REQ-042 | 78 | 평가 진행 중 시각화 보호 (기존 자료 + 경고 배너) | FR | P6 | smoke + e2e | DONE (배선 완료, 자동 polling defer) |
| REQ-043 | 83 | 모든 기능 ID/Password 보호 | NFR | P3 + P6 | e2e | IN_PROGRESS (자격증명 축 · guard wiring 축 · 프런트 진입 차단 축 실재 / 전 기능 보호 적용률 축 미완: **자격증명 축** 의 입력 계약은 `src/auth/dto/login.dto.ts` 28~30 행 `@IsEmail()` + `@IsNotEmpty()` 의 `email!: string` 와 36~38 행 `@IsString()` + `@IsNotEmpty()` 의 `password!: string` 2 필드다 — README 의 "ID" 는 별도 식별자 필드가 아니라 email 로 구현됐다. 검증 경로는 `src/auth/auth.controller.ts` 148 행 `@Post("login")` 이 `userRepository.findByEmail(dto.email)` 로 조회 후 `authService.verifyPassword(dto.password, ...)` 를 호출하는 것이며, 실 비교는 `src/auth/auth.service.ts` 84~85 행 `async verifyPassword(plain: string, hash: string): Promise<boolean>` 의 `bcrypt.compare` 다 (hash 산출은 75~76 행 `hashPassword` 의 `bcrypt.hash(plain, BCRYPT_ROUNDS)`, 상수 선언은 같은 파일 52 행). 인증 성공 시 발급물은 `auth.service.ts` 92 행 `issueAccessToken` · 106 행 `issueRefreshToken` 의 JWT 2 종이고 저장 위치는 controller 173~174 행 `res.cookie(ACCESS_TOKEN_COOKIE, accessToken, COOKIE_OPTIONS)` + `res.cookie(REFRESH_TOKEN_COOKIE, refreshToken, COOKIE_OPTIONS)` 의 cookie 다 (이름 상수 83~84 행, 90 행 `httpOnly: true`). **guard wiring 축** 은 `src/auth/jwt-auth.guard.ts` 22 행 `export class JwtAuthGuard extends AuthGuard("jwt") {}` → `src/auth/jwt.strategy.ts` 60 행 `jwtFromRequest: cookieExtractor` (36 행 `cookieExtractor` 가 cookie 의 access_token 을 read 하고 부재 시 null 반환 → 401) → 76~88 행 `validate(payload)` 의 `UnauthorizedException("Invalid token payload")` 3 분기로 이어지고, 권한 layer 는 `src/auth/roles.guard.ts` 51 행 `canActivate` 의 69 · 73 행 `UnauthorizedException("Authentication required")` + 86 행 `ForbiddenException("Insufficient role")` 다. 등록은 `src/auth/auth.module.ts` 81~82 행 providers / exports 이며 두 guard 모두 controller 에서 실제 참조된다 (정의만 있고 참조 0 인 심볼 없음). **프런트 진입 차단 축** 은 `web/src/AppShell.tsx` 136 행 `<AuthGate onLogin={onLogin} onAuthenticated={handleAuthenticated}>` (37 행 `const onLogin = authLogin` 이 `web/src/api/auth.ts` 25 행 `async function login(username: string, password: string): Promise<boolean>` = `POST /api/auth/login` 을 주입) 이고, 실 차단 지점은 `web/src/AuthGate.tsx` 81~83 행 `if (authenticated) return <>{children}</>` — 미인증이면 children 대신 LoginForm 만 렌더한다. 자격증명 입력 필드는 `web/src/components/LoginForm.tsx` 56~57 행 `<input type="text" name="username">` 와 66~68 행 `<input type="password" name="password">` 2 개다. **적용률 축은 미완** — `@Controller(` 보유 controller 는 20 개인데 그중 `@UseGuards` 를 하나라도 가진 것은 16 개이고, route decorator 기준으로는 74 개 중 49 개만 보호된다. `grep -rn "APP_GUARD" src --include=*.ts` 결과가 0 건이라 전역 guard 등록은 없으며, 따라서 guard 미참조 controller 4 개 — `src/app.controller.ts` (15 행 `@Get()` 1 route) · `src/user/group.controller.ts` (9 route) · `src/user/part.controller.ts` (6 route) · `src/user/person.controller.ts` (5 route) — 의 21 route 는 실제로 미보호다. 나머지 미보호 4 route 는 `src/auth/auth.controller.ts` 148 · 194 · 218 행 login / logout / refresh (자격증명 교환 경로) 와 `src/user/user.controller.ts` 156 행 `@Post()` signup 이며 후자는 142~144 행 주석이 "guard 없음 — Public tier (인증 없는 첫 user 진입 path 필수)" 로 의도를 명시한다. 반면 group · part · person 3 개는 주석이 미보호를 의도적 공개가 아니라 미완으로 적는다 (group 51 행 · part 28 행 "AuthGuard (Admin+ / User+) 적용 안 함 — 후속 task 책임", person 19 행 동문). 검증 위치 실 근거는 `test/e2e/auth.e2e-spec.ts` 의 `it(` 27 개 · `src/auth/jwt-auth.guard.spec.ts` 6 개 · `src/auth/roles.guard.spec.ts` 24 개이고, e2e 는 641 행 `negative — cookie 부재 시 401 (JwtAuthGuard reject)` 로 미인증 접근 거부 경로를 실제로 cover 한다. 한계 — 미보호 21 route 는 본 task 가 실측만 하고 조치하지 않았으며 (보안 코드 변경 Out of Scope), `src/app.controller.ts` 는 1~2 행 주석이 "sanity 용도" 라고만 적어 공개 의도의 명시 선언은 부재하다. 또 e2e 는 auth 도메인 route 만 cover 해 group · part · person 의 미보호를 fail 로 잡는 test 는 없다.) |
| REQ-044 | 84 | 첫 로그인 SuperAdmin / 3 등급 / 승급 / SuperAdmin 만 Admin→User | FR | P3 + P6 | unit + e2e | DONE |
| REQ-045 | 85 | Admin 권한 (재작성/Reset/Import/Export/인원편집/Group편집) | FR | P6 | e2e | IN_PROGRESS |
| REQ-046 | 86 | User read-only (조회/sort/filter) | FR | P6 | e2e | DONE |
| REQ-047 | 91 | 100~200명 / 50~100 repo / ~1000 confluence / 1h 이내 | NFR | P7 | manual + perf test | PLANNED 유지 (계획 축만 충족 · 측정 3 축 전부 부재: **계획 축** 은 충족이다 — `docs/ops/load-resilience-test-plan.md` 45 행 `### S1. 평가 배치 부하 (REQ-047)` 가 시나리오를 열고 47~48 행이 부하 규모 (100~200명 평가대상 × 50~100 repo × ~1000 confluence page 규모의 평가 작성 배치 1 회) 를, 49 행이 "목표: 배치 완료 시간 ≤ 1h. LLM 호출·수집(GitHub/Confluence)·저장 전 구간 포함" 을 서술하며, 81 행 임계 표 행 (S1 평가 배치 부하 / 배치 완료 시간 / ≤ 1h / REQ-047 (README line 91)) 이 같은 임계를 박제한다. 도구 결정은 `docs/decisions/ADR-0054-load-resilience-harness-tool.md` 4 행 `status: PROPOSED` · 7 행 `relatedReq: [REQ-047, REQ-048]` 로 **권고 단계이며 채택(ACCEPTED)이 아니다** — 같은 계획 문서 108~117 행이 k6 / artillery / autocannon 를 신규 dependency 로 분류해 CLAUDE.md §5 BLOCKED 규율에 걸어두었고 131 행 follow-up 3 "S1 / S3 부하 harness 구현" 이 미착수로 남아 harness 도구는 아직 미도입이다. **배치 측정 harness 축은 부재** 다 — `ls test/perf` 44 entry 를 batch · write · eval 3 패턴으로 필터한 결과가 0 건이고, 실재하는 perf-spec 은 `read.perf-spec.ts` 로 끝나는 조회 경로 30 개뿐이라 평가 작성(write) 배치의 소요시간을 재는 spec 이 전무하다. `grep -rn "REQ-047" test/ --include=*.ts` 는 2 건이지만 둘 다 `test/smoke/realdata-e2e-aggregator-collect-leg-...smoke-spec.ts` 43 행 · `test/smoke/realdata-e2e-aggregator-evaluation-leg-...smoke-spec.ts` 48 행의 author 귀속 key(externalId) 정합 주석이라 배치 성능과 무관해, 축을 충족으로 판정하지 않는다. **1 시간 절대 임계 축도 부재** 다 — `grep -rn "3600" test/perf --include=*.ts` 가 0 건이고 `test/smoke` 의 6 건은 전부 systemd `RandomizedDelaySec=3600` mutant 값 (redeploy timer bounded jitter spec 352 · 357 · 360 · 499 · 506 · 602 행) 이라 배치 임계와 무관하다 — 즉 판정(expect / throw)에 쓰이는 3600s 정의 0 건, **1h 절대 임계 assertion 부재**. **scale seed 축도 부재** 다 — `ls prisma/` 는 `migrations` · `schema.prisma` 2 entry 뿐이고 `ls scripts/` 19 entry 에 대규모 seed 스크립트가 없으며, `grep -rln "seed" prisma/ scripts/` 6 건 중 `prisma/schema.prisma` 는 25 · 433~434 행 주석 ("default Part seed migration 부재" · "3 row seed 자동화는 Follow-up") 이고 나머지 5 건은 `scripts/acquire-lock.test.sh` 등 lock / claim shell test fixture 라 전부 무관 — 100~200명 · 50~100 repo · ~1000 page 규모 fixture 는 0 건이다. 검증 위치 `manual + perf test` 의 실 근거도 미확보다 — `perf test` 축은 위 배치 spec 0 건 그대로이고, `manual` 축은 `docs/ops/runbook.md` 에 14 행 cross-link 한 줄만 있을 뿐 사람이 따라 할 배치 부하 실행 절차가 없고 계획 문서도 §4 도구 후보 · §5 follow-up 인덱스에 머물러 **manual 절차 미박제** 다. 한계 — 본 재판정은 doc-only 정적 실측 (파일 · 행 · 개수) 이라 `pnpm test:perf` 를 실행하지 않았고, full-scale 배치는 실 LLM 호출 비용과 외부 수집 I/O 의존 (계획 문서 51~52 행) 때문에 CI runner 로 1h 재현이 불가해, harness dependency 가 owner 승인으로 도입되기 전에는 실측 자체가 성립하지 않는다) |
| REQ-048 | 92 | 조회·시각화 3초 이내 | NFR | P6 + P7 | perf test | IN_PROGRESS (조회 측정 harness 축 · CI 강제 축 · 3 초 절대 임계 축 실재 / 시각화(web) 렌더 측정 축 부재: **조회 측정 harness 축** 은 충족이다 — `ls test/perf` 에서 `read.perf-spec.ts` 로 끝나는 파일이 30 개이고 (대표 2 개는 `test/perf/person-read.perf-spec.ts` 58 행 `describe("S2 조회 latency perf-spec — PersonController 배선 (REQ-048)")` 과 `test/perf/summary-read.perf-spec.ts` 50 행의 SummaryController 동형 서술), 측정 primitive 4 함수가 `test/perf/latency-metrics.ts` 에 모두 정의돼 있다 — 18 행 `percentile` · 62 행 `summarizeLatency` · 78 행 `errorRate` · 112 행 `throughput`, 그리고 48 행 `LatencySummary` 가 p50 / p95 / p99 필드를 보유한다. **CI 강제 축** 도 충족이다 — `.github/workflows/ci.yml` 234 행 `- name: perf test` + 243 행 `run: pnpm test:perf` 가 perf 스위트를 CI step 으로 강제하고, `package.json` 22 행 `"test:perf": "jest --config ./test/perf/jest-perf.json"` 이 그 script 를 정의하며 `test/perf/jest-perf.json` (278 bytes) 이 실재해 unit / smoke / e2e 와 격리된 독립 config 로 실행된다. **3 초 절대 임계 축** 도 충족이다 — 판정은 직전 baseline 상대 비교가 아니라 **리터럴 3000ms 절대 임계** 다: `test/perf/latency-collector.ts` 167 행 `const DEFAULT_P95_MAX_MS = 3000` 을 182 행 `assertS2Threshold` 가 기본 상한으로 쓰고 244~245 행이 `p95 임계 초과: p95=... >= 상한 ...ms (REQ-048)` 를 fail 사유로 push 하며, 대표 spec 2 개가 그 판정을 그대로 expect 한다 (`person-read` 117 행 `expect(assertion.pass).toBe(true)` · 156 행 `expect(assertS2Threshold(result).pass).toBe(true)`, `summary-read` 114 행 · 152 행 동형). baseline 경로 (`test/perf/latency-baseline.ts` 270 행 `compareBaselineReports`, `test/perf/latency-baseline-io.ts` 251 행 `compareBaselineFiles` · 331 행 `ConfirmOrCompareResult` · 381 행 `confirmOrCompareBaseline`) 는 **직전 baseline 대비 상대 회귀의 관찰·리포트 전용** 이며 같은 파일 362~364 행이 "S2 pass/fail 임계를 바꾸지 않는다 … 회귀 여부는 `comparison.regressed` 로 관찰만 노출, throw 하지 않음" 으로 못박아 절대 임계 판정과 독립이다. 저장소 실측 `grep -rn "3000" test/perf --include=*.ts` 는 73 건이고 그 중 주석을 제외한 8 건 안에서 판정에 실제로 쓰이는 정의는 위 167 행 1 건이다. 계획 축도 정합 — `docs/ops/load-resilience-test-plan.md` 58 행 "목표: p95 latency < 3s (README line 92 …)" 와 83 행 임계 표 행 (S2 조회 지연 / p95 latency / < 3s / REQ-048) 이 같은 임계를 서술한다. 검증 위치 `perf test` 의 실 근거는 행두 `it(` 기준 34 perf-spec 268 it 이다 — 이는 **정적 개수** 이며 본 slice 는 doc-only 라 `pnpm test:perf` 를 실행하지 않았다. **미충족은 시각화(web) 축** 이다 — README 92 행은 "로딩 및 시각화에 3초 이내" 로 렌더까지 포함하는데, `web/src` 에서 `performance.now` · `p95` · `latency` 3 패턴을 `grep -rn` 으로 찾은 결과가 0 건이라 렌더 latency 를 측정하는 경로가 전무하다 (`web/src` 디렉토리 자체는 10 entry 로 실재하므로 부재는 web 미구현이 아니라 측정 미도입이다). 한계 — **실 DB 축은 부분 해소** 다: slice 1 `test/perf/person-read-realdb.perf-spec.ts` (T-1500, main `0395c51e`) 가 mock override 0 으로 AppModule 을 부트스트랩하고 실 Prisma client 로 seed 한 뒤 `GET /api/persons` 를 반복 측정해 **실 Postgres round-trip 경로에서도 p95 < 3000ms** 임을 실측했고, slice 2 `test/perf/group-read-realdb.perf-spec.ts` (T-1502, main `97198504`) 가 같은 부트스트랩 · 정리 구조로 `GET /api/groups` 목록 · `:id` · `:id/persons` 3 route 를 측정했다 — slice 2 의 질적 차이는 **N+1 indirect navigation 측정** 이다 (`src/user/group.service.ts` 의 `findPersonsByGroupId` 가 membership 수에 비례해 `findById` 를 loop 호출하는 경로도 같은 임계를 충족함을 처음 증거화). 이어 slice 3 `test/perf/group-persons-scale-realdb.perf-spec.ts` (T-1504, main `a8bc1e28`, 7 test) 가 slice 2 와 **같은 route** (`GET /api/groups/:id/persons`) 를 membership **5 건 (소규모) vs 60 건 (대규모)** 두 표본으로 측정했다 — slice 3 의 질적 차이는 **규모 민감도 축** 이다 (membership 수 = 요청당 query 수이므로 규모가 커져도 p95 < 3000ms 가 유지되는지를 처음 증거화). 두 표본의 대소 관계 (`large.p95 > small.p95`) 는 latency 가 wall-clock 이라 비결정적이어서 **assert 하지 않는 관찰 성격** 이고, 여기서의 "대규모" 는 단일 route 의 **상대 비교용 표본** 이지 REQ-047 실 scale 부하 검증이 아니며, slice 3 은 route 를 늘리지 않았다. 이어 slice 4 `test/perf/assessment-read-realdb.perf-spec.ts` (T-1506, main `861add36`, 9 test) 가 세 번째 endpoint 도메인인 `AssessmentController` 의 조회 2 route (`GET /api/assessments?personId=&period=` · `GET /api/assessments/:id`) 를 측정했다 — slice 4 의 질적 차이는 **2 축** 이다: ① **인증·RBAC guard 를 실제로 통과하는 첫 실 DB perf slice** (실 JWT 로 `JwtAuthGuard` + `RolesGuard` 를 통과하므로 인증 layer + DB round-trip 을 모두 포함한 경로에서도 p95 < 3000ms 임을 처음 증거화했고, **401 negative 분기로 guard 가 살아 있음도 확인**), ② **index 필터 경로** (`@@index([personId, period, periodStart])` 를 타는 필터 + 다중 row 조회라 slice 1 의 flat 목록 · slice 2·3 의 N+1 loop 와 구조가 다름). 이어 slice 5 `test/perf/contribution-read-realdb.perf-spec.ts` (T-1508, main `b15ffb0e`, 10 test) 가 네 번째 endpoint 도메인인 `ContributionController` 의 조회 2 route (`GET /api/contributions?assessmentId=` · `GET /api/contributions/:id`) 를 측정했다 — slice 5 의 질적 차이도 **2 축** 이다: ① **부모→자식 FK fan-out** (`findByAssessment` 가 부모 `Assessment` id 로 자식 다중 row 를 `createdAt` 오름차순으로 긁고, 그 필터는 명시 `@@index` 가 아니라 **`@@unique([assessmentId, sourceRef])` composite unique index 의 prefix** 를 탄다), ② **3-level FK chain seed** (`Person → Assessment → Contribution` 으로 앞 slice 의 2-level 보다 한 단계 깊다). guard 는 slice 4 에 이어 **실 JWT 로 `JwtAuthGuard` + `RolesGuard` 를 통과** 했고 **401 negative 분기도 살아 있다**. slice 5 의 seed (`PRIMARY_CHILDREN = 5` · `OTHER_CHILDREN = 3`) 역시 상대 비교용 **소규모 표본** 이라 REQ-047 의 실 scale 부하 검증이 아니고, 규모 민감도는 slice 3 의 `:id/persons` 한 route 에 한정돼 **contribution fan-out 을 포함한 다른 endpoint 는 여전히 미측정** 이다. slice 4·5 가 route 폭을 늘렸으므로 **endpoint 수는 3 개 → 4 개 (조회 8 route)** 다 (정본 서술은 `test/perf/README.md` 508 행 `## 실 DB round-trip baseline (slice 목록)`). 그러나 나머지 read perf-spec 30 개 (read 34 개 − 실 DB read 4 개 — slice 5 도 파일명에 `read` 가 있어 피감수와 감수가 함께 1 씩 늘어 차이 30 은 불변) 는 여전히 mock service 기반이고 (`person-read` 115 행 · `summary-read` 112 행 주석 "mock service 는 즉시 반환 → p95 는 임계(3000ms) 훨씬 아래"), **실 scale (REQ-047 의 100~200명 / 50~100 repo) 부하 하의 3 초 충족은 여전히 미검증** 이며 baseline 파일 확정 · 임계 fix (`docs/ops/load-resilience-test-plan.md` `§ 5` item 5) 도 미완이다. **시각화(web) 렌더 측정 축 부재는 불변** 이고, CI runner 성능 편차와 p95 표본 수 의존성도 절대 임계 판정의 잔여 불확실로 남는다) |
| REQ-049 | 96 | Admin 이 LLM 모델 지정 | FR | P4 + P6 | e2e | DONE |
| REQ-050 | 97 | 3가지 난이도 모델 + 어떤 항목이 어떤 난이도인지 결정 | Constraint | P4 (ADR 필수) | policy + unit | IN_PROGRESS (ADR 축 · 결정 내용 축 · 슬롯 매핑 구현 축 실재 / 항목→난이도 결정 규칙 축 부재: **ADR 실재 축** 은 충족이다 — `docs/decisions/ADR-0011-difficulty-model-assignment.md` frontmatter 2 행 `id: ADR-0011` · 4 행 `status: ACCEPTED` · 5 행 `date: 2026-06-01` · 6 행 `relatedTask: T-0136` (승격 해석 없이 파일 값 그대로). **결정 내용 축** 3 항목도 모두 명시돼 있다 — (1) 슬롯 cardinality 는 Decision §1 (47~51 행) 의 49 행 "3 row 고정 모델 채택 … 3 개 (easy / medium / hard) 에 대해 정확히 3 row" + 50 행 `@@unique([difficulty])` 로 중복 슬롯 schema-level 차단, (2) 매핑 의미는 Decision §2 (53~58 행) 의 55 행 "FK (`llmProviderConfigId`) 참조 채택" + modelId 문자열 복제 금지 + 56 행 resolve 순서 (슬롯 → `LlmProviderConfig` row → 그 row 의 provider · modelId) 이고 51 행 · 57 행이 슬롯 N:1 config 를 확정, (3) fallback 은 Decision §3 (60~64 행) 의 62 행 "평가 거부 (fail-fast) 채택" — 슬롯 row 부재 · FK 부재 시 4xx 로 거부하고 기본 provider silent-fallback 은 63 행이 명시 기각. **슬롯 매핑 구현 축** 도 4 지점 모두 실재한다 — 난이도 값 집합은 `src/llm/difficulty.ts` 20 행 `Difficulty` String literal union (3 값 easy · medium · hard) + 26~30 행 `DIFFICULTIES` 배열의 `as const satisfies readonly Difficulty[]` + 36 행 `isDifficulty` type guard, 슬롯 재지정은 `src/llm/difficulty-mapping.service.ts` 153 행 `async assignProviderConfig(difficulty, llmProviderConfigId)` (사전 config 존재 검증 후 `src/llm/difficulty-mapping.repository.ts` 89 행 `updateProviderConfig` 호출, P2025 를 NotFoundException 으로 변환) 이고 resolve 는 같은 service 93 행 `async resolveModel(difficulty)` 이 95~123 행에서 미지원 난이도 · 슬롯 부재 · FK null · 가리킨 config 부재 4 분기를 모두 BadRequestException 으로 거부해 Decision §3 fail-fast 를 코드로 강제한다, route 는 `src/llm/difficulty-mapping.controller.ts` 58 행 `@Controller("api/llm/difficulty-mappings")` 아래 75 행 `@Get()` 과 92 행 `@Patch(":difficulty")` 이며 둘 다 94~95 행 `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles("Admin")` 로 Admin+ 로 좁혀져 있고, 영속은 `prisma/schema.prisma` 441 행 `model DifficultyMapping` 의 443 행 `difficulty String` · 444 행 `llmProviderConfigId String?` (nullable FK, `onDelete: Restrict`) · 451 행 `@@unique([difficulty])` 다. 난이도 → model routing 소비처도 실재한다 — `src/llm/llm-http-gateway.service.ts` 112~116 행이 `options.difficulty` 가 주어지면 `resolveModel(...)` 의 `configId` 로 routing 하고 미제공이면 `options.modelId` 를 그대로 쓴다. **미충족은 항목→난이도 결정 규칙 축** 이다 — 평가 단위 1 건의 난이도를 실제로 정하는 지점은 `src/assessment-evaluation/domain/evaluation-prompt.ts` 155 행 `export function classifyNarrative(narrative: string)` 인데, 판정 근거는 **규칙 기반이 아니라 LLM 응답 파싱 기반** 이다 — 50 행 `DIFFICULTY_MARKER` regex 로 LLM 이 뱉은 `difficulty` marker 토큰을 뽑아 163 행 `isDifficulty` 멤버십만 확인하고, marker 부재 · 미인식 값이면 32 행 `DEFAULT_DIFFICULTY` 상수의 중앙값으로 환원한다. 즉 "어떤 항목이 어떤 난이도를 가지는지" 는 109 행 prompt instruction 으로 LLM 에 위임돼 있을 뿐, 항목 종류 · 규모 · metadata 를 난이도로 사상하는 결정 규칙이 코드에도 ADR-0011 에도 없다 (ADR-0011 은 49 행에서 3 슬롯 cardinality · 값 집합만 확정하고 28 행에서 routing 의 정적 매핑 backbone 만 자기 범위로 선언). 검증 위치 `policy + unit` 의 실 근거는 policy 축 = 위 ADR-0011 실재 · `ACCEPTED`, unit 축 = 행두 `it(` 기준 5 spec 105 it 이다 (`src/llm/difficulty.spec.ts` 3 it · `difficulty-mapping.service.spec.ts` 20 it · `difficulty-mapping.repository.spec.ts` 21 it · `difficulty-mapping.controller.spec.ts` 24 it · `src/assessment-evaluation/domain/evaluation-prompt.spec.ts` 37 it). 한계 — 분류 결과가 model 선택으로 되먹임되지 않는다: `src/assessment-evaluation/evaluation-scoring.service.ts` 99~101 행은 `gateway.generate(prompt, { modelId })` 로 `difficulty` 를 주입하지 않고 (97 행 주석 "difficulty 미주입 (narrative 산물이라 사전 미상)") 104 행에서 사후 분류만 하므로, ADR-0032 56 행의 "이 값이 generate 호출 전 `options.difficulty` 로 주입되어 난이도 모델 routing 을 driving 한다" 서술과 어긋나 gateway 의 routing 분기가 평가 경로에서 미발화다 (production 전수에서 `options.difficulty` 를 넘기는 호출 0). 또한 Decision §1 · §3 운영 함의가 전제한 3 row seed 경로가 부재하고 (`prisma/` 에 seed script 0, `difficultyMapping.create` 참조는 repository 와 그 spec 뿐) `test/e2e/` 의 difficulty-mappings 참조도 0 이라 (perf 축 `test/perf/difficulty-mapping-read.perf-spec.ts` 만 존재) 셋업 전 fail-fast 4xx 의 실경로 검증이 unit 밖에 없다) |
| REQ-051 | 99 | custom LLM (OpenAI 호환, 내부 서버, proxy, 3 model 슬롯) | FR | P4 | unit | DONE (adapter·gateway 배선, live 는 env-gated) |
| REQ-052 | 100 | Azure OpenAI provider | FR | P4 | unit | DONE (adapter·gateway 배선, live 는 env-gated) |
| REQ-053 | 101 | Anthropic provider | FR | P4 | unit | DONE (adapter·gateway 배선, live 는 env-gated) |
| REQ-054 | 102 | Google Gemini provider | FR | P4 | unit | DONE (adapter·gateway 배선, live 는 env-gated) |
| REQ-055 | 103 | OpenAI provider | FR | P4 | unit | DONE (adapter·gateway 배선, live 는 env-gated) |
| REQ-056 | 108 | Well-known library / 중복 import 금지 / version mismatch 방지 | Constraint | P0 + 모든 phase | policy + CI | IN_PROGRESS (정책 축 · 중복 library 축 · version pin 축 실재 / CI 중복·mismatch 전용 판정 step 부재: **정책 축** 은 충족이다 — `CLAUDE.md` 246 행 (§5 BLOCKED 처리) "새 외부 dependency 추가 (package.json에 새 패키지 / 새 외부 서비스 호출)" 와 310 행 (§9 안전장치) "새 dependency 추가는 BLOCKED. 사용자 승인 후 ADR 작성 → 추가" 가 신규 library 도입을 사람 승인 게이트로 묶고, 31 행 §1 기술 스택 확정 표가 Backend / Frontend / DB / Test / CI / Package manager 6 축의 채택 library 를 단일 목록으로 고정하며, `.claude/agents/reviewer.md` 82 행 "변경된 코드가 새 ADR 을 필요로 하는가? (모듈 경계 변경, library 추가, schema 변경 등)" 가 PR 단계의 2 차 검문을 건다 — 스택 결정 자체는 `docs/decisions/ADR-0001-stack.md` 4 행 `status: ACCEPTED` · `docs/decisions/ADR-0040-frontend-stack.md` 4 행 `status: ACCEPTED` 로 박제돼 있다. **중복 library 축** 도 충족이다 — root `package.json` 의 `dependencies` **19 개** (25~44 행) · `devDependencies` **25 개** (46~71 행) 를 기능군별로 대조한 결과 동일 기능을 두 library 가 중복 제공하는 사례는 **0 건** 이다 (HTTP client 0 · ORM 은 `prisma` / `@prisma/client` / `@prisma/adapter-pg` 단일 계열이며 `pg` 는 그 adapter 가 쓰는 드라이버 · validation 의 `class-validator` 와 `class-transformer` 는 검증 / 변환으로 역할이 갈리는 상보 쌍 · scheduler 의 `@nestjs/schedule` 과 `cron` 은 wrapper 와 엔진 관계 · 테스트는 `jest` 단일이고 `supertest` 는 HTTP assertion 보조), version 을 강제 재작성하는 `overrides` / `resolutions` 필드는 root 와 `web/package.json` (deps 2 · devDeps 6) 양쪽 모두 **부재** 다. **version pin / mismatch 방지 축** 도 충족이다 — `package.json` 7 행 `"packageManager": "pnpm@9.12.0"` 이 패키지 매니저 version 을 고정하고 (8~9 행 `engines.node` `>=20.11.0`), lockfile 은 root `pnpm-lock.yaml` **단일** 이며 `ls web/pnpm-lock.yaml` 은 No such file 로 하위 lockfile 이 없고, `pnpm-workspace.yaml` 3~4 행 `packages:` / `- web` 이 backend (root) 와 frontend (`web`) 를 한 lockfile 아래 해석시켜 (1 행 주석 "ADR-0040 §4 — 단일 lockfile pnpm workspace") 두 manifest 의 공통 항목이 실제로 같은 version 으로 정렬돼 있다 (`typescript` = root · web 모두 `5.6.2`). **미충족은 CI 자동 검증 축** 이다 — `.github/workflows/ci.yml` 179~182 행이 `pnpm/action-setup@v4` 로 `version: 9.12.0` 을 pin 하고 184~188 행 `actions/setup-node@v4` 가 `cache: 'pnpm'` 로 묶은 뒤 190~191 행 `- name: 의존성 설치` / `run: pnpm install --frozen-lockfile` 이 lockfile 정합을 강제하지만, 이는 **lockfile 과 manifest 의 불일치를 막을 뿐 중복 library 나 version mismatch 를 판정(fail)하지 않는다** — `.github/workflows/ci.yml` 전체를 `grep -rn` 으로 dedupe · depcheck · npm ls · why · licenses 5 패턴에 대해 훑은 결과 매치 **0 건**, `package.json` 의 `scripts` 블록에도 dedupe · depcheck · npm ls · licenses · audit 계열 script **0 건** 이라 전용 판정 step 은 전무하다. 따라서 검증 위치 컬럼은 `policy` 축 **충족** (위 CLAUDE.md · reviewer.md · ADR 인용) / `CI` 축 **부분 충족** (lockfile 정합 강제만 존재, 중복 판정 0) 이다. 한계 — 본 재판정은 top-level manifest 기준 정적 실측 (파일 · 행 · 개수 · grep) 이라 `pnpm-lock.yaml` 전수 분석을 하지 않아 transitive dependency 의 복수 version 유입 여부는 판정 불가이고, `pnpm install` / `dedupe` / `why` 는 lockfile 재작성 위험 때문에 실행하지 않았으며, README 108 행이 요구하는 "well-maintained" 여부 (유지보수 활성도) 는 외부 조회 없이는 정적 판정이 불가하다. 또한 backend `jest` 와 web `vitest` 는 서로 다른 workspace package 의 런타임 stack 분리 (ADR-0040) 라 중복 건수에 세지 않았다) |
| REQ-057 | 109 | 한 commit = 한 주제 | Constraint | (정책) CLAUDE.md §3 | policy | DONE |
| REQ-058 | 110 | commit/PR 후 코드 검토 + test 작성 + test 수행 | Constraint | CLAUDE.md §3.2 R-110 + agents | policy | DONE |
| REQ-059 | 111 | 모든 test → CI 자동 실행, fail → CI error | Constraint | CLAUDE.md §3.2 R-111 + ci.yml | policy + CI | DONE (T-0005 후 active) |
| REQ-060 | 112 | unit test (기능 + 예외 + flow + negative) | Constraint | CLAUDE.md §3.2 R-112 + planner | policy + CI (T-0007/T-0008) | DONE |
| REQ-061 | 113 | smoke + e2e 도 CI 에서 수행 | Constraint | CLAUDE.md §3.2 R-113 + T-0009/T-0010 | CI | DONE (T-0009/T-0010) |
| REQ-062 | 114 | 활동 후 test 수행 + 종료 전 CI 수행 | Constraint | CLAUDE.md §3.2 R-114 + LOOP §1 [5] | policy | DONE |
| REQ-063 | 115 | PR 만들면 다른 agent 가 review | Constraint | integrator → reviewer | policy | DONE |
| REQ-064 | 116 | Reviewer + Committer 합의로 merge, 7 round | Constraint | CLAUDE.md §3.3 + integrator | policy | DONE |
| REQ-065 | 117-128 | Reviewer 8 check | Constraint | reviewer.md | policy | DONE |
| REQ-066 | 133 | 코드 commit = PR / 진행상황 doc = direct | Constraint | CLAUDE.md §3.1 | policy | DONE |

## 매핑 표 갱신 룰

- 새 task 가 만들어질 때 task 파일 frontmatter 에 `coversReq: [REQ-NNN, REQ-MMM]` 명시.
- task merge 시 integrator 가 본 표의 해당 REQ row 상태를 `IN_PROGRESS` → `DONE` 으로 갱신 (직접 또는 follow-up doc-only direct commit).
- README 가 변경되면 planner 가 다음 호출에서 본 표를 동기화 (새 row 추가 또는 기존 row 갱신).
- 본 표의 row 가 phase 의 PLAN.md bullet 과 1:N 또는 N:1 일 수 있다. 그 경우 "구현 위치" 컬럼에 PLAN bullet 위치를 명시.

## 누락 검사 (정기 수행)

- planner 는 P 단위 phase 진입 시 본 표를 grep 하여 해당 phase 에 매핑된 REQ row 중 `PLANNED` 상태로 남은 것이 있는지 확인. 있으면 task 생성 후보로.
- reviewer 의 8 check (1) "주어진 주제 해결" 점검 시 PR 의 task frontmatter `coversReq` 가 본 표의 REQ 와 일치하는지 검증.

> **T-1470 (`§ 12.68`) pointer 판정 각주 — 본 파일 소관 README 행 번호 pointer 요약.** 본 파일이 담은 README pointer 는 **산문 13 지점 + 매핑 표 `README 행` 컬럼 66 지점 = 79** 이며, 그중 **산문 13 지점을 전수 판정** 했다 (표 컬럼 66 은 census · 구조 검사까지만 — 어구 대조는 다음 batch 이월).
> 판정 결과는 **참 11 · 부분참 2 · 거짓 0** 이다. 부분참 2 는 모두 **범위 표기의 끝 좌표** 에서 났다 — **20 행** 의 `136~139 행` (끝 139 는 빈 줄이고 인용 어구 `pnpm install` 은 **140** 행) 과 **39 행** 의 `19~22 행` (주제가 맞는 행은 **20** 단독이고 19 · 22 는 빈 줄).
> 표 컬럼 66 값은 구조 검사를 통과했다 — 전부 **1 ≤ N ≤ 151** 범위 안이고 REQ-001 → REQ-066 순으로 **엄격 증가** 한다 (spot-check 6 건 전량 대응).
> **본 각주는 append-only 기록이며 REQ 본문 · REQ ID · 기존 pointer 표기를 정정하지 않는다** ([REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) `§ 12.15`). 어긋난 pointer 2 건은 **pointer 정정 batch** 후보로만 이월한다.
> 상세 판정표 · 전수 census (**126** 지점 / **11** 파일) · 표기 변종 **10** 종은 [REQ-COVERAGE-AUDIT.md](use-cases/REQ-COVERAGE-AUDIT.md) **`§ 12.68`** (T-1470) 에 있다.
