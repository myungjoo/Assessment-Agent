# Architecture documents — INDEX

본 디렉토리는 Assessment-Agent 의 architecture document 들을 모은 곳이다. P1 (Architecture) phase 의 각 task 가 본 디렉토리에 문서를 신설·갱신한다. 모두 living document — 이후 phase 진행 중 architect agent 가 ADR 와 함께 갱신.

## 문서 목록

| 문서 | 책임 | 생성 task | 상태 |
| --- | --- | --- | --- |
| [requirements.md](../requirements.md) | FR/NFR/Constraint 분리된 REQ-NNN 매핑 표 | T-A1 (P1) | 부분 (P1-Entry 가 kind 채움) |
| [deployment.md](deployment.md) | Deployment view — monolith vs worker / DB / secrets / scheduler / 네트워크 boundary | T-A2 (P1) | 완료 (T-0014 + T-0015) |
| [components.md](components.md) | Component view — 시스템을 component 단위 + 외부 시스템 + 상호 contract | T-A3 (P1) | 완료 (T-0016) |
| [modules.md](modules.md) | Module view — NestJS module 구조 + 의존성 방향 (acyclic) | T-A4 (P1) | 완료 (T-0017) |
| [api.md](api.md) | API contract — HTTP endpoint 목록 + schema | T-0030 (P2) | 완료 (T-0030) |
| [data-model.md](data-model.md) | Conceptual data model — entity + 관계 (구체 schema 는 P3) | T-0031 (P2) | 완료 (T-0031) |
| [directory.md](directory.md) | 디렉토리 구조 정의 — NestJS 표준 + module view mapping | P2 (T-0021) | 완료 (T-0021) |
| [p3-implementation-plan.md](p3-implementation-plan.md) | P3 task 시퀀스 매핑 — 10 PLAN bullet ↔ T-NNNN task 8 row + ADR 후보 + 의존성 graph + 인간 승인 게이트 | T-0032 (P3-Entry) | 완료 (T-0032) |
| [p3-to-p4-transition.md](p3-to-p4-transition.md) | P3 → P4 전이 checkpoint — 진척 status quo (entity 5/11 / module 2/5 / ADR 1/4 / test-quality 4/4 9-cell closure) + 3 trigger option (eager / strict / hybrid) + 권장 hybrid-parallel | T-0063 (P3 → P4 transition) | 완료 (T-0063) |
| [estimate-model.md](estimate-model.md) | planner estimate calibration — 7 회차 cap-bend case study (T-0055/56/57/58/61/62/63) + 4 카테고리 multiplier (× 1.5 / × 1.6 / × 1.3 / × 1.0) + planner 적용 절차 | T-0064 (estimate model 박제) | 완료 (T-0064) |
| [race-patterns.md](race-patterns.md) | race pattern 7+7=14 회차 누적 박제 — gh pr merge worktree race (T-0048~T-0062) + reviewer-gate race-fix (T-0036~T-0047 + T-0061 `gh run rerun` SUCCESS) + integrator race-aware 평가 절차 cross-ref | T-0065 (race lessons doc) | 완료 (T-0065) |
| [concurrency.md](concurrency.md) | fine-grained driver concurrency 운영 view — claim registry 저장 위치(단일 CAS 평면) + claims.json schema + select+claim 원자성(이중 claim 0 정확성 게이트) + 동시 claimable 조건 + staleness/PR-resume(slice 2+ forward-looking) | T-0327 (ADR-0036 stage 2 slice 1) | 부분 (slice 1 — schema + select+claim primitive) |
| [frontend-api-contract.md](frontend-api-contract.md) | P6 frontend ↔ backend API 소비 계약 prep — 4 화면별 소비 endpoint 맵 + 인증 cookie 소비 패턴 + R-78 배너 데이터 소스 + RBAC↔가시성 + backend 선행 gap 5종 (ADR-0040 §2 순수 소비자 경계의 화면 단위 구체화, stack flip 비의존 dependency-free prep) | T-0352 (P6 prep) | 완료 (T-0352) |

## 갱신 룰

- 각 문서는 architect agent 가 책임. ADR 신설 시 관련 view 문서 동시 갱신 (architect.md 참조).
- 큰 변경 (예: monolith → worker 분리 전환) 은 새 ADR 로 박제 + 기존 view 문서 갱신. 이전 결정은 SUPERSEDED ADR 로 보존.
- view 문서 자체는 한 commit 안에서 너무 커지면 split (예: components.md 가 비대해지면 components/overview.md + components/<name>.md 로 분리). 단 INDEX.md 는 항상 동기.

## ADR 매핑

| ADR | 영향받는 view 문서 | 상태 |
| --- | --- | --- |
| ADR-0001 stack (NestJS / TS / pnpm / Jest / GHA) | modules.md (NestJS choice 반영) | ACCEPTED (T-0002, 8c6defe) |
| ADR-0002 DB (PostgreSQL + Prisma) | deployment.md + data-model.md | ACCEPTED (T-0014) |
| ADR-0003 Deployment (monolith vs worker, secret, scheduler, network) | deployment.md + components.md | ACCEPTED (T-0015) |
| [ADR-0011](../decisions/ADR-0011-difficulty-model-assignment.md) 3 난이도 모델 할당 정책 (3 row 고정 + FK 참조 + fail-fast fallback) | data-model.md (DifficultyMapping) | ACCEPTED (T-0136) |
| [ADR-0012](../decisions/ADR-0012-cross-cutting-field-policy.md) Cross-cutting field policy (UTC 저장 / mutable-only updatedAt / Person-only soft delete / createdBy = AuditLog event-stream) | data-model.md (§5 cross-cutting field) | ACCEPTED (T-0144) |
| [ADR-0013](../decisions/ADR-0013-confluence-space-traversal-policy.md) Confluence SPACE 탐색 정책 (page List 기반 탐색 / SPACE allowlist 순회 + (page, version) raw-transient / 4xx skip-and-continue) | modules.md (ConfluenceModule) | ACCEPTED (T-0145) |
| [ADR-0014](../decisions/ADR-0014-llm-api-key-encryption-at-rest.md) LLM API key encryption-at-rest 정책 (application-layer AES-256-GCM envelope / `LLM_APIKEY_ENC_KEY` env / write-only never-read-back / KMS 전환 친화) | data-model.md (LlmProviderConfig apiKey) | ACCEPTED (T-0146) |
| [ADR-0016](../decisions/ADR-0016-github-adapter-http-transport-contract.md) GithubAdapter HTTP transport 계약 (내장 fetch + injectable `FetchLike` / 3 host variant base URL 라우팅 (public `api.github.com` vs Enterprise `/api/v3`) / `Authorization: Bearer` + `X-GitHub-Api-Version` / non-2xx 도메인 매핑 + 4xx→PermissionDeniedEvent / Link rel=next pagination) | modules.md (GithubModule) | ACCEPTED (T-0173) |
| [ADR-0017](../decisions/ADR-0017-github-instance-config-source.md) GithubModule instance sub-config source (env 기반 — `process.env` instance-keyed config `GITHUB_INSTANCES` + per-key `GITHUB_<KEY>_HOST`/`_ORG`/`_TOKEN_ENC` / DB table·`@nestjs/config` 미채택 / token encrypted-at-rest + JIT decrypt / env→config 순수 함수 변환) | modules.md (GithubModule) | ACCEPTED (T-0177) |
| [ADR-0018](../decisions/ADR-0018-confluence-adapter-http-transport-contract.md) ConfluenceAdapter HTTP transport 계약 (내장 fetch + injectable `ConfluenceFetchLike` / Cloud `/wiki/rest/api` vs Server `/rest/api` 풀 base URL 박제 / Cloud Basic `email:api_token` vs Server `Bearer <pat>` (AUTH_USER 존재 여부로 분기) / non-2xx 도메인 매핑 + 4xx→PermissionDeniedEvent (adapter throw / service skip-and-continue) / `_links.next` body cursor pagination + `CONFLUENCE_MAX_PAGES` cap / 4 단 경계: builder→adapter.request→requestAllPages→SpaceTraversalService) | modules.md (ConfluenceModule) | ACCEPTED (T-0183) |
| [ADR-0019](../decisions/ADR-0019-same-host-auth-restriction-for-pagination.md) pagination cursor 의 same-host Authorization 전송 제약 (cursor URL 의 (scheme + host case-insensitive + port default 정규화) 가 instance base URL 과 strict equal — subdomain 불허 / mismatch 시 순회 abort + 도메인 error throw (Authorization drop·partial 반환 미채택) / GitHub `Link` header cursor·Confluence `_links.next` body cursor 양쪽 adapter 공통, fetch 직전 게이트로 수렴 / host-check 실패에도 평문 token·full cursor URL 미노출 invariant / Node 내장 `URL` 비교, dep 0) | modules.md (GithubModule + ConfluenceModule) | ACCEPTED (T-0196) |
| [ADR-0021](../decisions/ADR-0021-github-confluence-live-integration-test-contract.md) GitHub·Confluence live-integration TEST CONTRACT (env-gated skip-unless-credentialed live smoke — GitHub per-host token `GITHUB_LIVE_TEST`/`_TOKEN_PUBLIC`/`_SEC`/`_ECODE` + Confluence `CONFLUENCE_LIVE_TEST`/`_BASE_URL`/`_AUTH_USER`/`_TOKEN` / 부재 시 `describe.skip` → public CI green / live = happy round-trip only (non-2xx 재현은 layer 2 stub 위임) / adapter 별 3-layer (mocked unit / localhost-stub roundtrip / live-gated) 경계 표 / 실값 0 env 이름만) | modules.md (GithubModule + ConfluenceModule) | ACCEPTED (T-0203) |
| [ADR-0022](../decisions/ADR-0022-permission-denied-record-data-model.md) PermissionDeniedRecord 데이터 모델·영속화 설계 (권한 거부 audit entity — `provider` discriminator + `instanceRef`(GitHub host / Confluence baseUrl 정규화) + `resourceRef` + `principal?`(현 null) + `httpStatus` + `createdAt` immutable / token·본문 컬럼 부재 (§9 schema-level) / 영구 보존 + append-only (dedup·TTL 미적용) / `@@index([instanceRef, createdAt])`·`@@index([provider, httpStatus, createdAt])` audit 조회 / standalone relation 부재 (audit 독립성) / `PermissionDeniedEmitter` port → 영속화 emitter 패턴 (adapter 결합도 0) / prisma schema·migration·repo·service·wiring·test 는 후속 chain) | modules.md (GithubModule + ConfluenceModule) + data-model.md | ACCEPTED (T-0207) |
| [ADR-0023](../decisions/ADR-0023-permission-denied-audit-query-rbac-contract.md) PermissionDeniedRecord audit 조회 RBAC/audience 모델 + instance-scoping 필터 계약 (`GET /api/permission-denied-records` audience — 미인증 401 / Admin·SuperAdmin 전체 bypass / non-Admin authenticated 자기 instance 범위 / instance binding 부재 시 빈 결과 (403 아님) / "자기 instance" = User entity allowed-instance server-side lookup (JwtPayload 비확장) + exact-match (prefix/substring 미채택, host-spoofing 차단) / own-instance 필터는 service-layer actor-aware 분기 (신규 guard 미신설, 단일 강제 지점) / 타 instance = 빈-필터 (403 아님, audit 노출 최소화) / `@Roles("User")` + `@UseGuards(JwtAuthGuard, RolesGuard)` dependency 0 / User↔instance binding schema·controller slice·R-112 4종+negative 는 후속 chain) | modules.md (GithubModule + ConfluenceModule) | ACCEPTED (T-0213) |
| [ADR-0024](../decisions/ADR-0024-user-instance-binding-data-model.md) User↔instance binding 데이터 모델 + own-instance 필터 계약 (non-Admin audit 조회의 실 필터 — binding 대상 = record `instanceRef`(ADR-0022 GitHub host / Confluence base URL) exact-match / 데이터 모델 = `UserInstanceAccess` join table { userId / instanceRef } + `@@unique([userId, instanceRef])` + `onDelete: Cascade` (PersonGroupMembership 패턴, array 컬럼·group 파생 기각) / 필터 계약 = `actor.sub` → allowlist server-side lookup → `instanceRef in (allowlist)` service-layer 강제 (ADR-0023 §3) + query.instanceRef ∩ allowlist 교집합 (합집합 기각) / 경계 = 미인증 401 / binding 0 → 200 빈 배열 / 타 instance 빈-필터 / host case·trailing-slash 정규화 exact-match (ADR-0019 strict-equal) / migration = ADR-0004 migrate-deploy + binding 0 시작 breaking-change 0 (§5 게이트 Q-0021 OPEN) / JwtPayload 비확장 / schema+migration·repository·service 필터 결선·R-112 4종+negative+regression 은 후속 chain) | modules.md (GithubModule + ConfluenceModule) + data-model.md | ACCEPTED (T-0220) |
| [ADR-0029](../decisions/ADR-0029-assessment-collection-orchestrator.md) Assessment collection orchestrator 설계 (기존 GitHub·Confluence adapter 위 활동 수집 → Activity → Contribution 영속화, 결정 전용 0 LOC — 신규 `AssessmentCollectionModule` 신설 (수집 P4 / 평가 P5 module 분리, GithubModule·ConfluenceModule·PersistenceModule import) / typed `Activity` discriminated union (base + `GithubActivity`/`ConfluenceActivity`: externalId·sourceType·instanceKey·author·timestamp·metadata) + raw `unknown`→typed mapper layer 단독 책임 (REQ-032/059 raw-not-stored 보존, raw 본문 매핑 후 폐기) / orchestration = `collectForPerson(person, since?)` instance×org×repo (GithubInstanceClient 재사용) + instance×SPACE (ConfluenceSpaceTraversalService 재사용) + per-source skip-and-continue 기존 emit 재사용 / dedup = commit SHA earliest-timestamp wins (REQ-009/031) + Confluence page-id+version latest / incremental since = 직전 Assessment→since 도출 (결정만, 구현 deferred) / Activity→기존 Contribution entity 1:1 (sourceType/sourceUrl/sourceRef, difficulty·score·volume 은 P5) / testing = mocked-adapter unit 필수 R-112, live·e2e 수집 UI 이후 deferred (Q-0025) / 새 dep·credential·migration 0 / 구현 slice i~vii 는 후속 chain) | modules.md (AssessmentModule + GithubModule + ConfluenceModule) + data-model.md (Contribution) | ACCEPTED (T-0247) |
| [ADR-0030](../decisions/ADR-0030-assessment-collection-enumerate.md) Assessment collection enumerate(v-b2) 설계 (Person → `CollectionSpec` 산출 + repo source 정책, 결정 전용 0 LOC — repo source = 모드 B 지정 repo env allowlist(`GITHUB_<KEY>_REPOS`, ADR-0017 패턴, `GithubInstanceConfig.repos` 추가) 우선 / 모드 A org 전체 = `requestAllPagesForInstance("orgs/{org}/repos")` 런타임 enumerate fallback / DB allowlist(§5 schema) 기각 / Person→instance 매핑 = `ServiceIdentity.service` ↔ `GITHUB_INSTANCES` key, 매칭 부재 skip / org·repo scope 는 instance config(env) 소유, Person 차이는 author 귀속 = `Activity.author === ServiceIdentity.externalId` post-collection 필터 (collection service 시그니처 불변, API-side `?author=` 는 deferred 최적화) / Confluence = 활성 instance 전체 enumerate, SPACE 는 traversal 책임 / since = enumerate 주입받음(도출 안 함, slice vi) / 진입 = `collectForPerson(person, since?): Promise<Contribution[]>` + `buildCollectionSpec` 순수 분리 + cap 초과 시 3 slice 분할 / testing = mocked-adapter R-112, live UI 이후 deferred (Q-0025) / 새 dep·credential·migration 0 / 구현 slice 는 후속) | modules.md (AssessmentCollectionModule + GithubModule) + ADR-0017 (env config) | ACCEPTED (T-0256) |
| [ADR-0040](../decisions/ADR-0040-frontend-stack.md) Frontend stack 결정 (React + Vite + TypeScript SPA / 기존 `/api/*` REST + ADR-0008 JWT HttpOnly cookie 순수 소비자 + CORS 0 (same-origin) / 개발 Vite dev proxy ↔ 운영 NestJS `@nestjs/serve-static` static serve (monolith 정합) / repo root `web/` + pnpm workspace 단일 lockfile / 실 패키지 추가는 ACCEPTED 후 별도 task — §5 new-dep 게이트 / R-78 배너 = 실행 상태 polling + 전역 배너 컴포넌트) | deployment.md + directory.md + modules.md + components.md | ACCEPTED (T-0377 flip) |
| [ADR-0041](../decisions/ADR-0041-frontend-composition-wiring.md) Frontend composition-wiring 전환 (App.tsx 조립 구조·무라우터 view 전환·fetch hook 경계·R-78 보호 배선·non-parallel single-claim stream) | modules.md + components.md + directory.md | ACCEPTED (T-0376/T-0377) |

## MVA 원칙

본 디렉토리의 문서는 **Minimum Viable Architecture** 수준 — over-design 회피.

- 구체적 schema 컬럼 / API endpoint signature / service 메서드 시그니처는 본 문서 범위 밖. P3+ 의 task 에서 구체화.
- "어떤 component 가 있고 어떤 boundary 로 분리되어 있는가" + "deployment 구조" + "NFR 충족 계획" 까지만 P1 에서.
- 나머지는 ADR + 코드와 함께 진화.
