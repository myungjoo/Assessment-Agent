# Journal — 2026-09-10 (UTC)

- 00:55 driver: **T-2007 DONE (pr, PR [#1574](https://github.com/myungjoo/Assessment-Agent/pull/1574) → main [`9391201e`](https://github.com/myungjoo/Assessment-Agent/commit/9391201e))** — 난이도 슬롯 seed 응답 `{created, existing}` 을 화면 요약 1 줄로 표면화했다. [T-2000](../tasks/T-2000-difficulty-unassigned-slot-notice.md) `## Follow-ups` (b) 의 유일한 미집행 항목으로, 지금까지 러너가 200 응답 body 를 버려 첫 실행과 멱등 재실행이 사람 눈에 구분되지 않았다. 순수 파서 `summarizeSeedSlots`(계약 shape 일 때만 요약, 그 외 `undefined`) 신설 + `runSeedSlots` 배선(발사 시 비움 · 실패 시 미설정 · 기존 POST 호출 계약 문자 무변경) + hook `seedSummary` 노출(공개 표면 42→43) + [AdminView](../../web/src/views/AdminView.tsx) `role="status"` 렌더. backend · [api.md](../architecture/api.md) `139 행` 계약 무접촉, 새 dep 는 optional 로 두어 T-2001 계약 guard 파일을 건드리지 않고 5 파일 cap 을 지켰다. 5 파일 +241/-9. R-112 4 축 cover — 러너 spec 에 파서 8 · 배선 7 케이스, hook 심볼 guard 42→43. 로컬 `pnpm lint && pnpm build && pnpm test`(484 suite · 14069 test) + `test:cov` 임계 통과, web 148 파일 4357 케이스 green. 4-게이트 전부 충족, round 1 에 squash merge + 브랜치 삭제.
- 00:55 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건(`e321041f` T-2006 머지 · `ecef4f61` bookkeeping · `20c74263` planner) 모두 main run `success` 로 정산, `consecutiveFails` 0 유지. 본 fire 의 PR head run 은 첫 실행에서 "reviewer agent approval 검증" step 만 red 였고(approve comment 게시 전이라 정상), comment 게시 후 `gh run rerun --failed` 로 green 확인했다. squash 머지 commit `9391201e` 의 main run 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `9391201e` + 본 bookkeeping + planner commit).
- 00:55 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T00:13:12Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`c0c5ed33`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 00:13:12Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 32 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2007 1574` 은 executor 가 대신 수행(exit 0, `prNumber` 1574 · `PR_OPEN` 박제 확인). 머지 후 driver 가 lock 재획득(`b44a38ff`) + `claims.json` prune(`[]`, lock.json 동거 엔트리 보존 확인). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 00:55 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 는 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-2007 을 소비) 직전 task 가 `pr` 이라 ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · `claims.json` / lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 AC 10 의 "PR diff 를 `touchesFiles` 5 개와 정확히 일치" 요구 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `ecef4f61` 동일).
- planner: queued T-2008 — ADR-0066 신설: 사전 난이도 routing 의 운영 발화 스위치(source · 적용 진입점 · 기본값 경계) 결정 (`pr`, ADR 신설 + [ADR-0065](../decisions/ADR-0065-difficulty-routing-activation.md) pointer 2 파일 · est 240 LOC · 코드 0 LOC). issue-still-relevant pre-check(origin/main `01c0c4b4`): `ADR-0066` 파일 0 · `useInputDifficultyRouting` production 히트 3 개가 전부 선언/주석/읽기(`evaluation-scoring.service.ts` `54`·`81`·`108 행`)이고 `true` 세팅 호출자 0 · `DIFFICULTY_ROUTING` env 키 0 → REQ-050 잔여 "운영 발화 0 축" 미해소 확인. ADR-0065 `## Follow-ups` 확장 지점(운영 설정화)이 후속 ADR 선행을 지정.
- 01:16 driver: **T-2008 DONE (pr, PR [#1575](https://github.com/myungjoo/Assessment-Agent/pull/1575) → main [`ccf6e25b`](https://github.com/myungjoo/Assessment-Agent/commit/ccf6e25b))** — 사전 난이도 routing 의 운영 발화 스위치를 [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) 신설로 결정했다. [ADR-0065](../decisions/ADR-0065-difficulty-routing-activation.md) `§ Decision 3` 이 opt-in · 기본 OFF 를 확정했으나 그 스위치를 켜는 경로가 어디에도 없어 REQ-050 잔여가 "운영 발화 0 축" 하나로 남아 있던 상태다. 결정: 스위치 source = 요청 DTO 의 선택 boolean(`useInputDifficultyRouting`), 배선 좌표 2 곳([assessment-evaluation.controller.ts](../../src/assessment-evaluation/assessment-evaluation.controller.ts) `291 행` · [build-fill-run-scoring-options.ts](../../src/assessment-evaluation/dto/build-fill-run-scoring-options.ts) `108~131 행`), 3 슬롯 자동 감지는 [ADR-0011](../decisions/ADR-0011-difficulty-model-assignment.md) 이 기각한 silent fallback 재도입이라 명시 기각, 요약 경로는 ADR-0065 `§ Decision 4` 대로 범위 밖 유지. ADR-0065 는 `## Follow-ups` 아래 pointer 1 줄 add-only(결정문 문자 무변경). 2 파일 +145/-0, 코드 0 LOC(`src/`·`web/`·`test/`·`package.json`·워크플로 diff 0)라 신규 spec 0 이 정당하며 R-112 4 축은 Follow-ups 집행 slice 소관으로 ADR Status 에 박제했다. R-110 이행 — `pnpm lint && pnpm build && pnpm test` 484 suite / 14069 case green. 4-게이트 전부 충족, round 1 에 squash merge + 브랜치 삭제.
- 01:16 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건 전부 `success` 로 정산했다: `9391201e`(T-2007 머지) · `01c0c4b4`(T-2007 bookkeeping) · `5996ea32`(T-2008 planner, run 2 건 모두 success). `consecutiveFails` 0 유지. 본 fire 의 PR #1575 head CI 는 "기본 검사" · "배포 산출물 검증" 둘 다 pass 로 4-게이트 CI green 을 충족했고, squash 머지 commit `ccf6e25b` 의 main run 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `ccf6e25b` + 본 bookkeeping + planner commit).
- 01:16 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T00:57:13Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone(`{"holder":null}`) · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`dde9322e`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 00:57:35Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 18 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2008 1575` 는 executor 가 대신 수행(exit 0, `SYNC taskId=T-2008 prNumber=1575`). 머지 후 driver 가 lock 재획득(`de9d99b0`) + `claims.json` prune. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 01:16 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 는 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-2008 을 소비) 직전 task 가 `pr` 이라 ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · `claims.json` / lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 AC 10 의 "PR diff 를 `touchesFiles` 2 개와 정확히 일치" 요구 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `01c0c4b4` 동일).
- planner: queued T-2009 — `POST /evaluate` 에 사전 난이도 routing opt-in 스위치(`useInputDifficultyRouting`) 배선: DTO 선택 boolean 필드 + controller `291 행` 전사 1 줄 + 양쪽 spec ([ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (a)) (`pr`, est 220 LOC · 4 파일). issue-still-relevant pre-check(origin/main `fc02206e`): `useInputDifficultyRouting` 전수 7 히트 중 production 3 개가 전부 선언(`evaluation-scoring.service.ts` `54 행`)·주석(`81 행`)·읽기(`108 행`)뿐이고 `true` 세팅 호출자 0(나머지 4 개는 spec), `EvaluateActivitiesDto` 에 필드·`IsBoolean` import 부재, controller `291 행` 리터럴이 여전히 `modelId` 단일 키 → ADR-0066 Follow-ups (a) 0% 안착 확인.
- 01:57 driver: **T-2009 DONE (pr, PR [#1576](https://github.com/myungjoo/Assessment-Agent/pull/1576) → main [`f37a315d`](https://github.com/myungjoo/Assessment-Agent/commit/f37a315d))** — `POST /evaluate` 에 사전 난이도 routing opt-in 스위치를 배선했다. [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (a) 의 첫 좌표로, 지금까지 `useInputDifficultyRouting` 은 선언·읽기만 있고 `true` 로 세팅하는 호출자가 0 이라 REQ-050 잔여가 "운영 발화 0 축" 으로 남아 있었다. [EvaluateActivitiesDto](../../src/assessment-evaluation/dto/evaluate-activities.dto.ts) 에 선택 boolean 1 개(`@IsOptional` + `@IsBoolean`, coercion decorator 미부여 — ADR-0066 `§ Decision 1`) + [controller](../../src/assessment-evaluation/assessment-evaluation.controller.ts) 조립 리터럴 전사 1 줄만 추가하고, scoring service · fill-run · period bridge · 요약 경로는 전부 무접촉으로 뒀다. 4 파일 +268/-1(cap 300 LOC / 5 파일 이내). R-112 4 축 cover — happy 3 갈래(true / false / 미지정=OFF) + error path(ON 요청 reject raw 전파 + `RunStatus.end` 1 회) + 비-boolean · 오타 필드명 400 negative. `pnpm lint && pnpm build && pnpm test` 484 suite / 14092 case green, `test:cov` line 99.94% · function 100%. round 1 reviewer nit(주석-실측 불일치) 1 건은 §3 nit-in-PR closure (3) 으로 같은 PR 다음 commit `3fa251a4` 에서 해소, 4-게이트 전부 충족 후 squash merge + 브랜치 삭제.
- 01:57 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 정산했다: `ccf6e25b`(T-2008 머지) · `01e5371e`(T-2009 planner) 두 main run `success`, `fc02206e`(T-2008 bookkeeping) 는 다음 push 에 superseded 되어 `cancelled`(0-job) — 양성 취소라 fail 로 세지 않는다. `consecutiveFails` 0 유지. 본 fire 의 PR #1576 head(`3fa251a4`) CI 는 "기본 검사"(6m4s) · "배포 산출물 검증"(1m43s) 둘 다 pass 로 4-게이트 CI green 충족. squash 머지 commit `f37a315d` 의 main run 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `f37a315d` + 본 bookkeeping + planner commit).
- 01:57 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T01:37:22Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone(`{"holder":null}`) · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`97343754`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `validate-claim-candidate.sh T-2009` PASS → `select-claim.sh` atomic claim(`claimedAt` 01:37:22Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 19 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2009 1576` 은 executor 가 대신 수행(claim `PR_OPEN` · `prNumber` 1576 박제 확인). 머지 후 driver 가 lock 재획득(`015cb68b`) + `claims.json` prune. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 01:57 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 는 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-2009 를 소비) 직전 task 가 `pr` 이라 ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · `claims.json` / lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 AC 11 의 "PR diff 가 `touchesFiles` 4 개와 일치" 요구 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `fc02206e` 동일).
- planner: queued T-2010 — fill-run 축 하단 2 층(순수 factory [buildFillRunScoringOptions](../../src/assessment-evaluation/dto/build-fill-run-scoring-options.ts) + 그 유일 소비처 [runUnevaluatedFillRunCore](../../src/assessment-evaluation/dto/run-unevaluated-fill-run-core.ts))에 사전 난이도 스위치 pass-through 배선 ([ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (b) 의 1/3 slice, `pr`, est 250 LOC · 4 파일). issue-still-relevant pre-check(origin/main `a768ca67`): `useInputDifficultyRouting` 전수 히트 중 fill-run 사슬 6 파일의 히트 0, helper `108~110 행` 시그니처가 여전히 2 인자 · 반환 `116 행`/`123 행` 이 `{ modelId }` 단일 키, core `116~122 행` 이 5 인자 → (b) 축 0% 안착 확인. split 근거: (b) 는 ADR 이 "5 파일" 로 적었으나 실측 축이 DTO·orchestrator·controller·core·helper 10 파일이라 §3 cap 초과 → 하단 4 파일만 본 slice 로 집행하고 상단 (b-2) 4 파일 · (b-3) 2 파일은 task Follow-ups 에 배선 단위로 명시(소비처 동반 의무는 본 slice 안에서 충족 — helper 확장 + 유일 소비처 core 동반).
- 02:57 driver: **T-2010 DONE (pr, PR [#1577](https://github.com/myungjoo/Assessment-Agent/pull/1577) → main [`ac2e5b8f`](https://github.com/myungjoo/Assessment-Agent/commit/ac2e5b8f))** — fill-run 축 하단 2 층에 사전 난이도 스위치 pass-through 를 배선했다. [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (b) 의 1/3 slice 로, T-2009 가 `POST /evaluate` 좌표를 닫은 뒤에도 fill-run 사슬 6 파일의 `useInputDifficultyRouting` 히트가 0 이라 REQ-050 잔여 "운영 발화 0 축" 의 나머지 절반이 남아 있던 상태다. 순수 factory [buildFillRunScoringOptions](../../src/assessment-evaluation/dto/build-fill-run-scoring-options.ts) 가 3 번째 선택 인자를 수용하되 반환 2 곳을 `composeScoringOptions` 로 모아 `=== true` 일 때만 스위치 키를 얹고(미지정 · `false` · 비-boolean 은 전부 `{ modelId }` 단일 키 그대로 — ADR-0066 `§ Decision 3` "미지정 = OFF = 문자 단위 동일"), 유일 소비처 [runUnevaluatedFillRunCore](../../src/assessment-evaluation/dto/run-unevaluated-fill-run-core.ts) 가 6 번째 선택 인자로 받아 해석 없이 전달한다. modelId 축의 `normalizeModelId` fail-fast 는 스위치에 미적용이라 비-boolean 은 throw 0 · OFF 환원이고, 기존 5 인자 호출자(orchestrator · spec 전량)는 무수정 컴파일된다. 4 파일 +271/-4(cap 300 LOC / 5 파일 이내). R-112 4 축 cover — 기존 spec 2 개에 describe 4 개 추가로 ON / 명시 OFF / 미지정 3 분기 · error path(스위치 ON 에서도 modelId 양쪽 빈 값 `TypeError` 그대로 전파) · negative 4 종(`null` · `"true"` · `1` · 객체) · 비변형 단언. `pnpm lint && pnpm build && pnpm test` 484 suite / 14114 case green, 변경 2 파일 statement · branch · function 100%, 전역 line 99.94% · function 100%. 4-게이트 전부 충족, round 1 에 squash merge + 브랜치 삭제.
- 02:57 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 **전부 `success`** 로 정산했다: `f37a315d`(T-2009 머지) · `a768ca67`(T-2009 bookkeeping) · `789fd8a5`(T-2010 planner). 직전 turn 이 "다음 fire 에서 재확인" 으로 이월한 `f37a315d` 가 여기서 green 확정됐다. `consecutiveFails` 0 유지. 본 fire 의 PR #1577 head CI 는 "기본 검사"(6m34s) · "배포 산출물 검증"(1m28s) 둘 다 pass 로 4-게이트 CI green 충족. squash 머지 commit `ac2e5b8f` 의 main run 은 turn 종료 시점 `pending` 이라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `ac2e5b8f` + 본 bookkeeping + planner commit).
- 02:57 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T02:37:12Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone(`{"holder":null}`) · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`528fbf3b`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 02:37:23Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 14 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2010 1577` 은 executor 가 대신 수행(exit 0). 머지 후 driver 가 lock 재획득(`18ca1730`) + `claims.json` prune. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 02:57 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 는 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-2010 을 소비) 직전 task 가 `pr` 이라 ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · `claims.json` / lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 AC 마지막 줄의 "PR diff 가 `touchesFiles` 4 개와 정확히 일치" 요구 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `a768ca67` 동일).
- planner: queued T-2011 — fill-run 난이도 스위치 DTO + orchestrator 배선 (ADR-0066 Follow-ups (b) 2/3). issue-still-relevant pre-check (origin/main `0a82d4b6` 실측): `git grep useInputDifficultyRouting -- src/ web/ test/` 히트 전수 중 `unevaluated-fill-run-request.dto.ts` · 그 spec · `unevaluated-fill-run-orchestrator.service.ts` · 그 spec 히트 **0** — DTO 는 80 행 전량에서 `54 행` 클래스가 rawBridges(`66~69 행`) + modelId(`76~79 행`) 2 축뿐이고, orchestrator `107~111 행` `run` 은 3 인자 · `136~142 행` core 위임은 5 인자라 T-2010 이 만든 core 6 번째 인자(`run-unevaluated-fill-run-core.ts` `126 행`)를 채우는 caller 가 아직 없다. 4 파일 / 예상 260 LOC (R-112 backbone × 1.5; 동형 실측 T-2009 268 · T-2010 271). controller `723 행` 전사는 동반 시 6 파일 · ~420 LOC 로 cap 초과라 §3 소비처 예외를 적용하고 Follow-ups (b-3) 에 파일 · 배선 단위로 남겼다.
- 03:55 driver: **T-2011 DONE (pr, PR [#1578](https://github.com/myungjoo/Assessment-Agent/pull/1578) → main [`a491d9fe`](https://github.com/myungjoo/Assessment-Agent/commit/a491d9fe))** — fill-run 축 중단 2 층(HTTP request DTO + orchestrator)에 사전 난이도 스위치를 배선해 T-2010 이 만든 core 6 번째 인자까지 사슬을 연결했다. [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (b) 의 2/3 slice 로, 직전까지 `unevaluated-fill-run-request.dto.ts` · `unevaluated-fill-run-orchestrator.service.ts` 의 `useInputDifficultyRouting` 히트가 0 이라 core 인자를 채우는 caller 가 없던 상태다. [UnevaluatedFillRunRequestDto](../../src/assessment-evaluation/dto/unevaluated-fill-run-request.dto.ts) 에 선택 boolean 1 개(`@IsOptional` + `@IsBoolean`, `IsBoolean` import 알파벳 순 삽입, coercion decorator 미부여)와 [orchestrator](../../src/assessment-evaluation/unevaluated-fill-run-orchestrator.service.ts) `run` 의 4 번째 선택 인자 + core 위임 전사 1 줄만 추가하고, 변환 · 정규화 · `=== true` 판정은 전부 T-2010 helper 단독 책임으로 남겼다. 기존 3 인자 호출자는 무수정 컴파일. 4 파일 +237/-2(233 LOC, cap 300 LOC / 5 파일 이내). R-112 4 축 cover — 신규 13 케이스로 happy(양쪽) · error path(스위치 ON 에서도 `modelId` fail-fast 미우회) · 3 갈래 분기(ON · 명시 OFF 는 키 부재 단언 · 미지정) · negative(DTO 5 종 · orchestrator 3 종) · 축 독립성. `pnpm lint && pnpm build && pnpm test` 484 suite / 14130 case green, `test:cov` 임계(line · function ≥ 80%) 통과. 4-게이트 전부 충족, round 1 에 squash merge + 브랜치 삭제.
- 03:55 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 정산했다: `0a82d4b6`(T-2010 bookkeeping) · `cfea08ae`(T-2011 planner) 두 main run `success`, 직전 turn 이 이월한 `ac2e5b8f`(T-2010 머지) 는 다음 push 에 superseded 되어 `cancelled`(0-job, jobs 배열 빈 값 확인) — 양성 취소라 fail 로 세지 않는다. `consecutiveFails` 0 유지. 본 fire 의 PR #1578 head CI 는 "기본 검사"(6m25s) · "배포 산출물 검증"(1m21s) 둘 다 pass 로 4-게이트 CI green 충족했고, reviewer APPROVE comment 도 PR 외부에 실재함을 driver 가 직접 확인했다. squash 머지 commit `a491d9fe` 의 main run(`34434897782`)은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `a491d9fe` + 본 bookkeeping + planner commit).
- 03:55 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T03:37:28Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone(`{"holder":null}`) · `claims.json` `[]` 확인 → `validate-claim-candidate.sh T-2011` PASS → `select-claim.sh` atomic claim(`claimedAt` 03:37:28Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 13 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2011 1578` 은 executor 가 대신 수행(`PR_OPEN` · `prNumber` 1578 박제를 driver 가 재확인). 머지 후 driver 가 `claims.json` prune(`[]`, `lock.json` 동거 엔트리 보존 확인). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 03:55 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족이나 fine-grained 경로라 lock 이 critical section 밖에서 tombstone 이고(holder 부재) G2(`lock.holder == "cron"`) 가 거짓이라 본 step 을 통째로 skip 했다. 더해 chain 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-2011 을 소비). 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 AC 의 "PR diff 가 `touchesFiles` 4 개와 정확히 일치" 요구 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `0a82d4b6` 동일).
- planner: queued T-2012 — fill-run 난이도 스위치 controller 전사 (ADR-0066 Follow-ups (b) 3/3, `pr`, est 200 LOC · 2 파일). issue-still-relevant pre-check (origin/main `cea89400` 실측): `git grep -c useInputDifficultyRouting -- src/ web/ test/` 15 파일 중 [controller](../../src/assessment-evaluation/assessment-evaluation.controller.ts) 히트가 **정확히 1 개**(`297 행`, T-2009 의 `/evaluate` 축)뿐이고 fill-run 핸들러(`723 행`)의 orchestrator 위임(`773~777 행`)은 여전히 **3 인자**, controller spec 스위치 히트 26 개는 행 번호가 전량 `702~1365` 라 fill-run describe 4 군(`3695`·`3942`·`4219`·`4346 행`) 안 히트 **0** → (b-3) 0% 안착 확인. T-2011 이 만든 orchestrator `116 행` 4 번째 인자를 채우는 HTTP caller 가 0 이라 DTO `102 행` 필드가 지금은 버려진다. 실측 제약 박제: 무조건 전사 시 `run` arity 가 3→4 로 바뀌어 기존 `runSpy` 3 인자 `toHaveBeenCalledWith` 9 곳(`3714`·`3741`·`3858`·`3876`·`3896`·`3934`·`3959`·`4068`·`4079 행`)이 jest 인자-길이 비교로 fail 하므로 같은 slice 에서 arity 정합이 필수 — AC 에 명시했다. 본 slice 머지 시 ADR-0066 (a)·(b) 전량 종료 → (e) REQ-050 재판정(once-rule) 조건 성립.
- 05:00 driver: **T-2012 DONE (pr, PR [#1579](https://github.com/myungjoo/Assessment-Agent/pull/1579) → main [`94d8abef`](https://github.com/myungjoo/Assessment-Agent/commit/94d8abef))** — fill-run 축 최상단 controller 전사로 HTTP 요청 → helper `=== true` 게이트 사슬을 닫았다. [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (b) 의 3/3 slice 로, 직전까지 controller fill-run 핸들러의 orchestrator 위임이 3 인자라 T-2011 이 만든 DTO `102 행` 필드가 버려지던 상태다. [controller](../../src/assessment-evaluation/assessment-evaluation.controller.ts) 위임에 4 번째 인자 `dto.useInputDifficultyRouting` 전사 1 줄 + doc comment · 위임 주석의 "3 인자 / 3 축" 서술 정합만 넣고, 해석 · 정규화는 전부 하위 helper 책임으로 뒀다(`/evaluate` 축 `297 행` 형태 mirror, try/finally · resolver fail-fast 무변경). 2 파일 +228/-15(cap 300 LOC / 5 파일 이내). R-112 4 축 cover — happy(4 인자 정확 1 회 + 반환 deep-equal) · error path(503 유지 시 run 미호출 / raw 전파 + `end` 1 회) · 3 갈래 분기(true · 명시 false · 미지정) · negative(문자열 · 숫자 · 객체 · 배열 400, `null` 흡수, 오타 필드명 400) · 축 독립성. 기존 `runSpy` 3 인자 단언 9 개는 arity 정합만 반영(계약 변경 아님). `pnpm lint && pnpm build && pnpm test` 484 suite / 14144 case green, line 99.94% · function 100%. 4-게이트 전부 충족, round 1 에 squash merge + 브랜치 삭제. 본 slice 로 ADR-0066 (a) · (b) 가 전량 닫혀 (e) REQ-050 재판정(once-rule) 조건이 성립한다.
- 05:00 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 **전부 `success`** 로 정산했다: `a491d9fe`(T-2011 머지, 직전 turn 이 `in_progress` 로 이월한 run `34434897782`) · `cea89400`(T-2011 bookkeeping) · `e8230a32`(T-2012 planner). `consecutiveFails` 0 유지. 본 fire 의 PR #1579 head CI 는 1 회 rerun 이 필요했다 — 첫 run 이 "reviewer agent approval 검증"(게이트 2) 단독 red 였고 나머지 step 은 전부 green, reviewer comment 를 PR 외부에 게시한 뒤 `gh run rerun --failed` 로 green 이 됐다. 게이트 2 의 정상 동작(외부 comment 부재 → red)이라 `consecutiveFails` 에 세지 않는다. squash 머지 commit `94d8abef` 의 main run 2 건("기본 검사" · "배포 산출물 검증")은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `94d8abef` + 본 bookkeeping + planner commit).
- 05:00 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T04:37:13Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`50f4909d`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 04:37:13Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 23 분, claim TTL 60 분 이내). PR-open 직후 `sync-claim-pr.sh T-2012 1579` 는 executor 가 대신 수행(`PR_OPEN` · `prNumber` 1579 박제를 driver 가 재확인). 머지 후 driver 가 lock 재획득(`16601985`) + `claims.json` prune. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 05:00 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족이나 fine-grained 경로라 chain 판정 시점 lock 이 critical section 밖 tombstone 이고 G2(`lock.holder == "cron"`) 가 거짓이며, `STATE.nextTask` 도 비어 있다(본 fire 가 T-2012 를 소비). 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · lock 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status:` 플립은 PR diff 를 `touchesFiles` 2 개와 정확히 일치시키라는 AC 때문에 PR 에 넣지 않고 본 bookkeeping commit 이 처리한다(선례 `cea89400` 동일).
- planner: queued T-2013 — REQ-050 재판정: 난이도 routing 스위치 발화 경로 실측 반영 (ADR-0066 Follow-ups (e) 집행, `direct` doc-only 1 파일, est 40 LOC). issue-still-relevant pre-check (origin/main `0cb07ddc`): requirements.md 최신 commit `e321041f`(T-2006) 는 본 arc 4 건 머지 이전이고, `69 행` 의 "운영 발화 0 · production caller 0" 자인이 controller `297 행`(evaluate) · `786 행`(fill-run) 2 종 전사로 뒤집혀 재판정이 유효.
- 05:52 driver: **T-2013 DONE (direct, main [`ed954ca0`](https://github.com/myungjoo/Assessment-Agent/commit/ed954ca0))** — [ADR-0066](../decisions/ADR-0066-input-difficulty-routing-activation.md) `## Follow-ups` (e) 를 집행해 REQ-050 을 once-rule 대로 **1 회 재판정** 했다. (a) T-2009 · (b) T-2010~T-2012 가 전량 머지된 뒤에도 [requirements.md](../requirements.md) `69 행` 은 "잔여는 운영 발화 0 축 하나" · "production caller 는 0" 을 자인하고 있었고(요구표 최신 commit `e321041f` 가 본 arc 4 건 이전), 실측은 그 반대였다 — HTTP 요청이 스위치를 켜는 경로가 [controller](../../src/assessment-evaluation/assessment-evaluation.controller.ts) `263 → 297 행`(`POST /evaluate`) · `725 → 786 행`(`POST /unevaluated-fill-run`) 2 종 실재. 그 이동을 경로별 배선 사슬 좌표 · 기본 OFF 불변(미지정 OFF · 비-boolean 400 · `null` OFF 환원) · arc 신규 `it` 53 과 함께 박제하고, status 는 e2e 축 1 개 미충족으로 **IN_PROGRESS 유지** 했다(DONE 승격은 미리 정해 두지 않고 축별 실측이 결정). `docs/requirements.md` 69 행 1 행만 +2/-2 로 정정하는 add-only 수정이라 seed 4 축 · 기존 e2e · perf 서술은 보존됐고, 표 구조는 `grep -c '^| REQ-'`=84 · `sed -n 69p` 파이프 8 · `wc -l`=121 로 기계 검증했다. direct doc-only(`src` · `web` · `test` · `prisma` diff 0)라 R-110 tester 호출과 R-112 4 축은 면제이며 그 사유를 commit body 에 박제했다.
- 05:52 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 **전부 `success`** 로 정산했다: `94d8abef`(T-2012 머지, 직전 turn 이 `in_progress` 로 이월한 run `34439305077`) · `0cb07ddc`(T-2012 bookkeeping) · `c4f5455c`(T-2013 planner). `consecutiveFails` 0 유지. 본 fire 는 `direct` 이라 PR · reviewer · 4-게이트 경로가 없고, main push `ed954ca0` 의 run(`34442145133`)은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(이월 예상 3 건: `ed954ca0` + 본 bookkeeping + planner commit). 짧은 sha 로는 `gh run list --commit` 이 빈 배열을 돌려주므로 정산은 full sha 로 조회했다.
- 05:52 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T05:37:30Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) 확보 → lock tombstone(`{"holder":null}`) · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`ec0372e8`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) 단독 claim 조건 충족 → `validate-claim-candidate.sh T-2013` PASS → `select-claim.sh` atomic claim(`claimedAt` 05:37:30Z, 즉시성 확인) + 같은 commit tombstone release → lock-free executor 진행(약 4 분, claim TTL 60 분 이내). direct task 라 PR 이 없어 `sync-claim-pr.sh` 는 해당 없음. commit 후 driver 가 lock 재획득(`04c956f6`) + bookkeeping + `claims.json` prune. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, double-claim 정황 0.
- 05:52 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족이나 fine-grained 경로라 chain 판정 시점 lock 이 critical section 밖 tombstone 이고 G2(`lock.holder == "cron"`) 가 거짓이며, `STATE.nextTask` 도 비어 있다(본 fire 가 T-2013 을 소비). 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 편차 0 — STATE · journal · lock · `claims.json` 무접촉, `git reset --hard` 미사용(`git pull --ff-only` 로 main 복귀), force-push 0. task 파일 `status: DONE` 플립은 direct 1-commit 이라 executor 의 `ed954ca0` 에 함께 들어갔다.
- planner: queued T-2014 — 난이도 routing 스위치 ON 경로 e2e 잠금 (REQ-050 잔여 e2e 축, `pr` test-only, est 230 LOC · 2 파일: [assessment-evaluation-evaluate.e2e-spec.ts](../../test/e2e/assessment-evaluation-evaluate.e2e-spec.ts) · [unevaluated-fill-run.e2e-spec.ts](../../test/e2e/unevaluated-fill-run.e2e-spec.ts)). issue-still-relevant pre-check (origin/main `fef5d7fa` 실측): `git grep -n useInputDifficultyRouting origin/main -- test/` **0 hit** (전체 15 파일 전부 `src/assessment-evaluation/`), `test/e2e/` 최신 commit `c8c7c5cc`(T-1998) 라 스위치 arc 4 commit 무접촉 → T-2013 이 특정한 잔여 축 0 % 안착. 관측 근거: evaluate e2e 가 `LOAD_TEST_STUB=1` 로 부팅하고 [LlmStubGateway](../../src/llm/llm-stub-gateway.service.ts) `94~97 행` 이 `difficulty` 제공 시에만 narrative 에 ` difficulty=<값>` 을 붙이므로 ON → 주입 → 응답까지 HTTP 로 관측 가능(commit `titleLength` 12 → medium · 100 → hard). fill-run 은 좌표 0 이라 HTTP 계약 축(200 · 비-boolean 400 · 503 fail-fast 비우회)만 잠근다.
- 06:55 driver: **T-2014 DONE (pr, PR [#1580](https://github.com/myungjoo/Assessment-Agent/pull/1580) → main [`4738186d`](https://github.com/myungjoo/Assessment-Agent/commit/4738186d))** — 난이도 routing 스위치(`useInputDifficultyRouting`) ON 경로를 e2e 로 잠갔다. [evaluate e2e](../../test/e2e/assessment-evaluation-evaluate.e2e-spec.ts) 13 it · [fill-run e2e](../../test/e2e/unevaluated-fill-run.e2e-spec.ts) 8 it 신설(2 파일 +256/-2, production 0 LOC, 기존 it 무수정). R-112 4 축 cover — happy(unitId 대응 difficulty 반영) · error(scope bogus 500 · provider 부재 503) · 분기(false · 미지정 · null) · negative(비-boolean 400 · 오타 필드 400 · 401 · 403). 로컬 lint · build · test:cov green, e2e 는 로컬 DB 부재로 CI 에서 693→714 total green 확인. reviewer round 1 APPROVE(MINOR 2 건 — 변경 요청 아님), 4-게이트 충족 후 squash + 브랜치 삭제.
- 06:55 driver: **CI 정산 (R-114)** — turn 시작 이월 3 건을 **전부 `success`** 로 정산했다: `ed954ca0`(run `34442145133`) · `fef5d7fa`(`34442263636`) · `b33a07a8`(`34442790900`). `consecutiveFails` 0 유지. PR #1580 은 push run `34446530996` · approve-comment rerun `34446639538` 둘 다 success. 머지 commit `4738186d` 의 main run `34447163753` 은 turn 종료 시점 `in_progress` 라 다음 fire 로 이월한다(이월 예상 3 건).
- 06:55 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T06:36:57Z`(`gh api -i rate_limit` `Date` 헤더, midnight fallback 아님) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`9141e11c`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim → `select-claim.sh` atomic claim(`claimedAt` 06:37:18Z) + tombstone release → lock-free executor(약 15 분). `sync-claim-pr.sh T-2014 1580` 은 executor 가 수행(`PR_OPEN` · `prNumber` 1580 을 driver 가 재확인). 머지 후 lock 재획득(`b691d727`) + claim prune. `concurrencyIncidents` 임계 미달, double-claim 정황 0.
- 06:55 driver: **[7.5] multi-task chain 미진입** — fine-grained 경로라 G2(`lock.holder == "cron"`) 거짓 + `STATE.nextTask` 비어 있음. 이어서 planner dispatch. executor 편차: nested sub-agent 불가로 implementer · tester · reviewer 를 inline 수행(reviewer comment 도 동일 에이전트 작성). STATE · journal 무접촉, `git pull --ff-only` 복귀, force-push 0. task 파일 `status:` 플립은 본 bookkeeping commit 이 처리.
- planner: queued T-2015 — REQ-050 재판정: 스위치 ON e2e 축(T-2014) 머지 실측 반영 (`direct` doc-only 1 파일 [requirements.md](../requirements.md) `69 행`, est 40 LOC). issue-still-relevant pre-check(origin/main `d94a96fe`): 요구표 최신 commit `ed954ca0`(T-2013)가 T-2014 머지 `4738186d` 이전이고, `69 행` 이 아직 "`test/` 0 hit · e2e 축 미충족" 을 단언하지만 실측 `git grep -c useInputDifficultyRouting -- test/` 는 e2e 2 파일(evaluate `308 행` · fill-run `302 행` describe)이다. once-rule: T-2014(`coversReq: [REQ-050]`) post-merge 1 회분이며 T-2014 Out of Scope `81 행` 이 사전 등록했다. 본 재판정을 stream 종결 판정으로 두고, 실 gateway e2e 는 잔여 / 범위 밖 분류만 한다.
- 10:45 driver: **T-2015 DONE (direct, main [`dcad667c`](https://github.com/myungjoo/Assessment-Agent/commit/dcad667c))** — [requirements.md](../requirements.md) `69 행` REQ-050 을 DONE 으로 재판정했다. T-2014(PR #1580, `4738186d`) 머지로 스위치 ON e2e 축이 채워졌고, 실 LlmHttpGateway e2e 는 T-2005 unit 근거로 범위 밖 hardening 으로 분류했다. 1 hunk +2/-2 (task 파일 `status: DONE` 플립 동반). executor 메모: T-2014 trail 의 "12 it · 7 it" 와 달리 선언 7 · 5, `it.each` 전개 13 · 8 로 실측값만 기재.
- 10:45 driver: **CI 정산 (R-114)** — 이월 3 건 전부 `success`: `4738186d`(run `34447163753`) · `d94a96fe`(`34447354685`) · `44120d90`(`34447919424`). `consecutiveFails` 0 유지. `dcad667c` 의 main run `34467371232` 는 `in_progress` 라 다음 fire 로 이월한다.
- 10:45 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T10:37:03Z`(`gh api -i rate_limit` `Date` 헤더) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`cea12df0`) → reclaim 대상 0 → 활성 claim 0 → `select-claim.sh T-2015` atomic claim(`claimedAt` 10:37:29Z) + tombstone release → lock-free executor(약 4 분). commit 후 lock 재획득(`f881068e`) + bookkeeping + claim prune. `concurrencyIncidents` 임계 미달, double-claim 정황 0.
- 10:45 driver: **[7.5] multi-task chain 미진입** — fine-grained 경로라 G2(`lock.holder == "cron"`) 거짓 + `nextTask` 비어 있음. 이어서 planner dispatch. executor 편차 0 — STATE · journal · lock 무접촉, force-push 0.
- planner: queued T-2016 — REQ-004 재판정: 기간 상한 arc(T-1939~T-1942, PR #1521~#1524) 머지 실측 반영 + [modules.md](../architecture/modules.md) `40 행` `period-window-filter` 인덱스 등록 + [UC-09](../use-cases/UC-09-user-defined-period-evaluation.md) §7.5 해소 주석 (`direct` doc-only 3 파일, est 45 LOC). T-1942 `## Follow-ups` (b) 가 등록한 arc 종단 재판정이 T-1943(REQ-009 arc) 전환으로 미집행된 것을 회수한다. issue-still-relevant pre-check(origin/main `fe434fd7`): REQ-004 셀에서 `filterActivitiesByPeriodWindow` · `until` · `T-1939`~`T-1942` · `T-1981` **0 hit**(인용 task 는 T-1936~T-1938 뿐), `docs/architecture/` 의 `period-window-filter` 0 hit, UC-09 최신 commit `fb5c8f3c`(T-1421)는 arc 이전. once-rule: T-1939~T-1942 + T-1981(이월 명시) post-merge 합산 1 회이며, status 는 AC 의 축별 실측이 결정한다(상한 e2e 단언 0 hit 을 잔여로 볼지 hardening 으로 볼지 명시 분류).
- 11:50 driver: **T-2016 DONE (direct, main [`fbc41cba`](https://github.com/myungjoo/Assessment-Agent/commit/fbc41cba))** — [requirements.md](../requirements.md) REQ-004 를 IN_PROGRESS 에서 DONE 으로 재판정했다(기간 상한 arc T-1939~T-1942 실측 · 6 축 좌표 · hardening 근거). [modules.md](../architecture/modules.md) `40 행` `period-window-filter` 등록, [UC-09](../use-cases/UC-09-user-defined-period-evaluation.md) add-only 주석 5 곳. 4 파일 +10/-7(task 파일 `## Follow-ups` 2 항목 동반 — (a) modules.md 그래프 `assessmentEvaluation --> assessmentCollection` edge 누락, (b) REQ-004 셀 · UC-09 의 arc 이전 stale 행 좌표). executor 메모: commit 중 git spawn EPERM 1 회 자가 해소, 커밋 내용 무결 확인.
- 11:50 driver: **CI 정산 (R-114)** — 이월 3 건 전부 `success`: `dcad667c`(run `34467371232`) · `fe434fd7`(`34467515825`) · `6438245c`(`34468257228`). `consecutiveFails` 0 유지. `fbc41cba` 의 main run `34473065274` 는 `in_progress` 라 다음 fire 로 이월한다.
- 11:50 driver: **동시성 (ADR-0036)** — server-time `2026-09-10T11:36:54Z`(`gh api -i rate_limit` `Date` 헤더) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`167c7f62`) → reclaim 대상 0(스크립트 내 grep 1 회 EPERM 경고, 빈 배열이라 판정 무영향) → 활성 claim 0 → `select-claim.sh T-2016` atomic claim(`claimedAt` 11:37:25Z) + tombstone release → lock-free executor(약 9 분). commit 후 lock 재획득(`480d206f`) + bookkeeping + claim prune. `concurrencyIncidents` 임계 미달, double-claim 정황 0.
- 11:50 driver: **[7.5] multi-task chain 미진입** — fine-grained 경로라 G2(`lock.holder == "cron"`) 거짓 + `nextTask` 비어 있음. 이어서 planner dispatch. executor 편차 0 — STATE · journal · lock 무접촉, force-push 0.
- planner: queued T-2017 — REQ-045 재판정 + REQ-046 · REQ-073 drift 정정 (`direct` doc-only 1 파일 [requirements.md](../requirements.md) `64` · `65` · `92 행`, est 60 LOC). issue-still-relevant pre-check(origin/main `0bff79a9`): REQ-045 칸은 근거 없는 맨 `IN_PROGRESS`, REQ-046 은 맨 `DONE` 인데 `src/user/` 의 person · group · part controller 3 파일에 `UseGuards` · `@Roles` 0 hit — 20 route(편집 포함)가 무인증이고 persons · groups · parts e2e 의 cookie · 403 도 0 hit 이다. REQ-073 DONE 셀은 평가 대상 시스템 guard 만 근거로 들어 인원 축이 빠졌다. REQ-045 는 T-1980 · T-1981 · T-1982 · T-1989 가 이월한 post-merge 1 회이고 나머지 2 행은 arc 무관 drift 예외다. guard 실 배선은 §5 오너 승인 대상이라 범위 밖이며, humanQuestion 이 필요하다는 점을 task `## Follow-ups` (a) 에 적었다. T-2016 Follow-ups (a)(b) 는 후순위 doc 정정이라 보류했다.
- 12:45 driver: **T-2017 DONE (direct, main [`218658b3`](https://github.com/myungjoo/Assessment-Agent/commit/218658b3))** — [requirements.md](../requirements.md) REQ-045 는 IN_PROGRESS 유지(6 축 중 인원 · Group/파트 편집 2 축 guard 0), REQ-046 · REQ-073 은 DONE 에서 IN_PROGRESS 로 정정했다. 1 파일 +3/-3. 잔여는 인원 · 그룹 · 파트 20 route guard 배선 하나이며 §5 오너 승인 대상이다(task Follow-ups (a)). executor 실측 차이 3 건을 task `## Follow-ups` (b)~(d) 로 옮겼다.
- 12:45 driver: **CI 정산 (R-114)** — 이월 3 건 전부 `success`: `fbc41cba`(run `34473065274`) · `0bff79a9`(`34473269621`) · `5d405b93`(`34474065523`). `consecutiveFails` 0 유지. `218658b3` 의 main run `34478311708` 는 `in_progress` 라 다음 fire 로 이월한다.
- 12:45 driver: **동시성 (ADR-0036)** — server-time `12:36:45Z` → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`0fbf1a7c`) → reclaim 0 → `select-claim.sh T-2017`(`533ff835`). 이때 스크립트 내 grep 이 EPERM 으로 실패해 `claims.json` 이 `[,{...}]` 로, tombstone 이 `holder:""` 로 박제됐다. `lock_tree_cas_push` 로 즉시 정상값 복구(`23c572b5`). bookkeeping lock 재획득은 bash fork EPERM 으로 PowerShell plumbing CAS(`a80ec80d`). double-claim 정황 0.
- planner: queued T-2018 — [modules.md](../architecture/modules.md) 의존성 표 · 그래프를 실 imports 배열과 정합 (`direct` doc-only 1 파일, est 30 LOC). [T-2016](../tasks/T-2016-req004-period-window-arc-readjudication.md) `## Follow-ups` (a) 회수. issue-still-relevant pre-check(origin/main `e2e9a6a5`): 평가 module `80 행` imports 4 개 중 Collection · User · RunStatus 가 `41 행` deps 칸과 그래프에 없고, 그래프에 수집 → auth · 스케줄링 → 수집 edge 도 없으며, modules.md 의 `RunStatus` 히트는 0 이다. add-only 로 진행하고 표 row 신설 · conceptual edge 삭제는 범위 밖이다. 남은 IN_PROGRESS REQ 7 건 중 REQ-043 · 045 · 046 · 073 은 인원 · 그룹 · 파트 20 route guard 배선([T-2017](../tasks/T-2017-req045-046-073-rbac-person-group-part-readjudication.md) Follow-ups (a), §5 오너 승인)에, REQ-047 은 실 자격증명에, REQ-048 web 축은 브라우저 측정 신규 dep 에 걸려 있다. 그래서 오너 humanQuestion 이 필요하다.
- 17:50 driver: **동시성 · CI 정산** — server-time `17:42:00Z` → lock tip 이 `cron@aa-local-408d`(since 12:47Z, 직전 fire 가 release 누락) 이라 stale(≥ 60 분) 판정, `acquire-lock.sh` 로 탈취(`3a6ed055`, stole stale lock from cron@aa-local-408d). `claims.json` `[]` → reclaim 0 · 활성 claim 0. 이월 2 건 `218658b3`(run `34478311708`) · `e2e9a6a5`(`34478592931`) 전부 `success`, `consecutiveFails` 0. currentTask · nextTask 모두 null 이라 planner dispatch → T-2018 큐잉(`direct`). planner 가 권고한 guard 배선 오너 HQ 는 T-2018 을 멈추지 않도록 이번 fire 에서 올리지 않았다 — 게이트 없는 작업이 소진되는 다음 planner 시점에 notifier 로 올린다.
- 18:42 driver: T-2018 DONE — modules.md 표 deps 칸 2 곳 · 그래프 edge 4 줄 · Topological 괄호 · 각주를 실 imports 에 맞춰 add-only 정정(`b3c73cdf`, direct 1 파일 +15/-7). Follow-ups (a) 실 imports 에 없는 scheduler · evaluation edge 판정 (b) 153 행 괄호 누락.
- 18:42 driver: **동시성 · CI** — server-time `18:37:10Z` → lock tombstone · `claims.json` `[]` → `acquire-lock.sh` 획득(`2e0f2f27`) → reclaim 0 → `select-claim.sh T-2018`(`405e2784`, claimedAt 18:37:21Z 정상). bookkeeping lock 재획득 `0efd5494`. 이월 `397eed39`(run `34510625376`) success, `b3c73cdf` run `34515706153` in_progress 이월. [7.5] chain 은 nextTask 부재로 미진입.
- planner: queued T-2019 — REQ-002 재판정 + PLAN P6 deferred 잔여 stale 정정 (`direct` doc-only 2 파일 [requirements.md](../requirements.md) `21 행` · [PLAN.md](../PLAN.md) `124` · `126` · `127 행`, est 30 LOC). issue-still-relevant pre-check(origin/main `f837d5ba`): REQ-002 는 T-0401(2026-06-14) 이후 근거 없는 맨 `IN_PROGRESS` 인데, 그 사유였던 잔여 3 건(SchedulePanel T-0885 · ReEval T-0886 · 자동 polling T-1849)이 전부 머지됐고 web vitest CI 도 T-0405 로 배선됐다(`ci.yml` `222~224 행`). PLAN `126 행` · `127 행` 은 아직 이를 미해소로 적는다. once-rule 상 잔여 머지 후 1 회분이며, guard 부재 등 타 REQ 잔여는 중복 계상하지 않는다. 20 route guard 배선 오너 HQ 는 이 task 와 독립이라 병행 제기 가능하다.
- 19:43 driver: **동시성 · CI 정산** — server-time `19:36:50Z` → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`e574ba09`) → reclaim 0 · 활성 claim 0. 이월 2 건 `b3c73cdf`(run `34515706153`) · `f837d5ba`(`34515876247`) 전부 `success`, `consecutiveFails` 0. currentTask · nextTask 모두 null 이라 planner dispatch → T-2019 큐잉(`direct`). planner 가 T-2019 이후 게이트 없는 후보는 좌표 · 괄호 수준 doc 정정뿐이라며 다음 planner 시점 오너 HQ(20 route guard 배선)를 권고했다 — 이번 fire 는 T-2019 를 막지 않도록 HQ 를 올리지 않는다.
- 20:42 driver: T-2019 DONE — REQ-002 를 6 축(SPA · backend serve · 잔여 해소 · smoke · e2e · web CI) 실측 근거로 `DONE` 재판정, PLAN `124` · `126` · `127 행` stale 정정(`db00acc2`, direct 2 파일 +4/-4). Follow-ups (a) PLAN `119 행` "실 defer 잔여 1 항목" (b) `123 행` pointer 좌표.
- 20:42 driver: **동시성 · CI** — server-time `20:37:06Z` → lock tombstone · `claims.json` `[]` → `acquire-lock.sh` 획득(`c9cdf853`) → reclaim 0 → `select-claim.sh T-2019`(claimedAt 20:37:06Z 정상). bookkeeping lock 재획득 `6ddd6bf2`. 이월 `d34f6f2e`(run `34522139065`) success, `db00acc2` run `34527814645` in_progress 이월. [7.5] chain 은 nextTask 부재로 미진입.
- 21:40 notifier: HQ Q-0056 — `src/user/` person · group · part controller 20 route 무인증 guard 배선 승인 요청 (security, T-2017 Follow-ups (a)); ① slice 3 개 승인 / ② 좌표 정정 1 건 후 대기 / ③ 위험 수용 종결.
- 21:40 driver: **동시성 · CI 정산 · 오너 HQ** — server-time `21:36:42Z` → lock tombstone · `claims.json` `[]` → `acquire-lock.sh` 획득(`b3d50eee`) → reclaim 0 · 활성 claim 0. 이월 `db00acc2`(run `34527814645`) · `78fc896e`(`34527927966`) 전부 `success`, `consecutiveFails` 0. currentTask · nextTask 모두 null 이라 planner dispatch → planner 가 게이트 없는 실질 후보 소진으로 HQ-NEEDED 반환, 직전 두 fire 가 미룬 20 route guard 배선 오너 질문을 notifier 로 Q-0056 제기했다. task 큐잉 없음, Q-0056 해소 전까지 driver 는 no-op 종료한다.
