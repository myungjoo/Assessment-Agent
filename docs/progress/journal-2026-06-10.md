# Journal — 2026-06-10 (KST)

- 00:22 driver(/loop loop@AKIHA-707106 t7, 로컬): T-0306 DONE — PR #257 squash 97b253e 머지. aggregateMetricScore(avg 난이도 ordinal + avg 기여도 ordinal + log1p(Σvolume), 6자리 round — REQ-036 상대 비교 보존) + isPeriodEvaluable(now≥periodEnd, day/week/month, unknown throw) 순수 함수 2개 + 56 R-112 테스트(신규 2파일 line/func/branch 100% cov) + mapper DIFFICULTY_ORDER export (ADR-0035 §Decision 1·3 구현 slice). implementer→tester(architect 불요). reviewer round1 APPROVE(2 NIT — month day-overflow 계약 의존·unknown difficulty silent vs contribution throw 비대칭, 둘 다 비차단) + 4-게이트 PASS + CI green(run 27215304186). **CI 주의**: 첫 pull_request run 이 approve-comment 의 issue_comment 이벤트와 동일 concurrency 그룹이라 cancel(failure 표기) → gh run rerun 으로 clean green. reviewRounds[T-0306]=1. tasksCompleted 302→303. next: planner T-0307 큐잉(aggregate write service).
- 운영 피드백(사용자, 06-10): driver lock 은 공유 상태(STATE/main/select+claim) 접근 시에만 점유하고 implement/test/review/CI-wait 중엔 비점유 권장. 즉시 적용분 = 긴 CI/verify idle 중 inline hold 금지, claim 박제 후 release+resume(§1[2]). 전면 fine-grained(task당 1 driver, N 동시)는 ADR-0009 "활성 driver 1개" supersede 라 별도 ADR 필요(순차 chain·단일 진입점 현 시점 병렬 이득 ≈0, multi-machine/cron 동시엔 의미). memory lock-only-critical-sections 박제.
- planner(inline, 로컬 t8): queued T-0307 — Summary batch 정성 narrative 생성 서비스(한 person-period unit 묶음 → typed surface batch prompt → LlmHttpGateway.generate 1회 mocked-LLM → narrative, ADR-0035 §Decision 5). commitMode=pr. write service slice 를 cap(300/5) 초과 회피로 2 분할한 첫 조각 — DB write·metricScore 결합·reset-and-recreate 는 T-0308. 단위평가 chain prompt+classify(T-0290) 분리 동형. cross-person 금지·raw 0(typed surface). architect 불요(ADR §Decision 5 박제)→implementer→tester. dep0/credential0(기존 LlmHttpGateway + mocked-fetch). nextTask=T-0307.
- 00:36 driver(/loop loop@AKIHA-707106 t9, 로컬): **세션 graceful 종료(사용자 option 1)**. 사용자가 multi-machine/cron 병렬 throughput(implement/test 대기 단축) 위해 Option B(fine-grained concurrency) 진입 결정. 그 설계 ADR 을 fresh session 에서 먼저 작성하기로 해, T-0308(ADR-0036 fine-grained concurrency — critical-section-only lock + claim registry, ADR-0009 "활성 driver 1개" supersede 검토) 큐잉. ADR 결정사항으로 우려 0~7 박제: (0)독립 task 없으면 병렬 이득 0(현 chain 순차)→planner 독립-stream 분해 정책 동반 필요 / (1)claim 원자성+staleness 회수+PR-resume / (2)코드 merge 충돌 경계 / (3)의존성 위반 방지 / (4)cron@cloud feature-push/PR/merge credential 한계 / (5)multi-machine clock skew→staleness 오판 / (6)N 동시 PR CI 비용+per-PR concurrency group / (7)관측성. nextTask=T-0308. T-0307(Summary narrative) deferred — ADR 후 재큐. context 큼+cap 근접으로 loop 종료(reschedule X). 재개: /clear 후 새 /loop.
