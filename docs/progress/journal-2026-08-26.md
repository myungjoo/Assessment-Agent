# Journal — 2026-08-26

- planner: queued T-1707 — PLAN R-92 mega-narrative bullet prune (`direct`, 2 파일). T-1706 으로 부하계획 `§5` 잔여 축이 닫힌 뒤 그 밖에서 고른 축이며, **아직 미착수인 오너 직접 지시** [PLAN](../PLAN.md) `146 행`(🟡 2026-07-30 "PLAN R-92 bullet prune — 문서 smell") 의 집행이다. 현 `142 행` 은 **46,156 자 단일 줄** mega-narrative 로 남아 있고(오너 관측 ~67,000 자 대비 줄었으나 압축 미집행), slice 1~29 상세 · 체크인 게이트 상세는 이미 [test/perf/README.md](../../test/perf/README.md) `## 실 DB round-trip baseline (slice 목록)`(`618 행` ~) · `## 체크인 baseline 게이트`(`108 행` ~) 로 외화돼 있으므로 **새 정보를 쓰지 않고 PLAN 쪽 서술만** ≤ 10 줄 요약본 + 정본 pointer 로 대체한다. 요약본 필수 사실 축 **6 개** — ① R-92 정의 · 임계 `p95 3000ms`(REQ-048) ② harness 규모(perf-spec **63 개** / read **51 개** + primitive **4 파일**) ③ CI 강제(`ci.yml` `perf test` step 의 `pnpm test:perf`, T-0878) ④ 실 DB round-trip **slice 29** 도달 + README 정본 pointer ⑤ 체크인 게이트([ADR-0056](../decisions/ADR-0056-perf-baseline-checkin-ci.md) · `PERF_CHECKIN_BASELINE: "1"` T-1584 · baseline JSON **5 route** · 상대 회귀 관찰만 · `§Follow-ups (c)` 미완) ⑥ 오너 상한 승계(`145 행` 신규 per-route slice 큐잉 금지 · `140 행` checkbox `[ ]` 유지 근거). 6 축 · README 어느 쪽에도 없는 사실은 **삭제 금지**(요약본 잔류 또는 Follow-ups). 함께 `146 행` checkbox 를 `[x]` 로 닫고, prune 으로 밀린 행 좌표 때문에 [ADR-0057](../decisions/ADR-0057-s1-batch-load-io-isolation.md) `251 행` 의 `` `144 행` `` pointer 를 재계산 정정한다(§3.1 rule 5 비-결정 pointer 정정 = `direct`; `docs/tasks/*` · `docs/progress/*` 는 §12 범위 밖이라 소급 정정 금지). **범위 불변** — `141 행`(R-91 bullet) · `140 행` checkbox · `144` · `148` · `156 행` 본문 문자 단위 무변경, `test/` · `src/` · `web/` · `.github/workflows/` · `package.json` diff **0 파일**(README 는 `pr` 컬럼이라 읽기만), 새 perf-spec · 임계 재조정 · dispatch · 실측 **0**. doc-only 2 파일 est 15 LOC → `commitMode: direct`, R-110 면제. `STATE.nextTask=T-1707`, counters · lock · ci 불변.
- 00:47 driver: T-1707 DONE — PLAN `142 행` R-92 mega-narrative 46,156 자 단일 줄을 6 사실 축 보존 **7 줄 요약본 + [test/perf/README.md](../../test/perf/README.md) 정본 pointer** 로 압축(direct `9cba851f`, +9/-3, 2 파일). 오너 지시 checkbox(현 `152 행`) `[x]` 마감 + 집행 문장 append, 행 밀림 +6 반영해 [ADR-0057](../decisions/ADR-0057-s1-batch-load-io-isolation.md) `251 행` pointer `144 행`→`150 행` 정정. `ADR-0056` 의 `142 행` pointer 는 머리 행 불변으로 stale 아님(무변경). 사라진 서술 전량이 README `618`/`108`/`1244 행~` 에 기존 외화 — 신규 정보 손실 0. `test/`·`src/`·`web/`·CI·`package.json` diff 0 파일, `pnpm lint` 무경고. 본 fire 는 planner 큐잉(`9336cda5`) + 집행(`9cba851f`) chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1707` claimedAt 00:42:10Z(server-time 정상), executor 는 lock-free 진행 후 bookkeeping 전 lock 재획득. 두 main push CI 진행 중 — 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1696→1697, nextTask=null(다음 fire 는 planner dispatch).

- planner: queued T-1708 — realdata devset 정본의 stale "실 run 0 회" 문장 정정 (`direct`, 2 파일). 오너 최우선 R-91 축([PLAN](../PLAN.md) `150 행` 🔴🔴 · `154 행` 🟢)의 정본 [realdata-scale-devset.md](../ops/realdata-scale-devset.md) `## seed 실행 경로` 마지막 bullet 이 T-1662 배선 직후 시점의 **`실 dataset 을 태운 run 은 아직 0 회`** 를 그대로 들고 있어, 정본 문서가 자기 축 진척을 0 으로 보고하는 문서 결함이었다. 실제로는 run `32665014391` 첫 성공 이후 [부하계획](../ops/load-resilience-test-plan.md) `§3.1` `16 회차` 기준 seed step **연속 9 회 성공**. 새 측정 · 새 dispatch · 코드 변경 0 인 doc-only slice 로 큐잉. `STATE.nextTask=T-1708`, counters · lock · ci 불변.
- 01:50 driver: T-1708 DONE — devset 정본 실측 상태 bullet 을 기박제 4 사실(run `32652307813` fail · `32665014391` 첫 성공 person/serviceIdentity 133 · `32677333740` 표본 133 명 회수 · `§3.1` 16 회차 연속 9 회) 인용으로 교체하고, 같은 bullet 에 실 수집 왕복 0([ADR-0057](../decisions/ADR-0057-s1-batch-load-io-isolation.md) D1 stub) + T-1706 결론 분포 + `§5` item 5 잔여 ① pointer 를 동거(direct `8bbb9376`, +18/-2, 2 파일). PLAN `154 행` 은 꼬리 pointer 1 문장 append 만 — 기존 문장 삭제 0, checkbox `[ ]` 유지. `test/` · `src/` · `web/` · CI · `package.json` diff 0 파일, `pnpm lint` 무경고. 본 fire 는 planner 큐잉(`d0f0caef`) + 집행(`8bbb9376`) chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1708` claimedAt 01:43:58Z(server-time 정상), executor lock-free 진행 후 bookkeeping 전 lock 재획득. push 중 오너 commit `2783f24b`(사내 발표자료) 가 끼어 rebase 1 회 후 성공 — 코드 영역 충돌 0, `2783f24b` main run 은 다음 push 로 superseded cancelled(정상). 본 fire 두 main run(`d0f0caef` in_progress · `8bbb9376` pending) conclusion 은 다음 fire 가 재확인(R-114 이월). counters.tasksCompleted 1697→1698, nextTask=null(다음 fire 는 planner dispatch).

- planner: queued T-1709 — 오너 신규 지시 2 건을 requirements.md REQ row 로 동기 (`direct`, 1 파일). 오너가 `9485c923` 로 직접 push 한 [README](../../README.md) `154~168 행` 신규 섹션(계정 생성 UX · 평가 대상 관리 UI)과 [PLAN](../PLAN.md) `129~130 행` 🔴 bullet 2 건이 공통으로 "requirements.md REQ row 동기 후 task 분해" 를 선행 step 으로 명시 → 분해보다 동기가 먼저인 slice 로 큐잉. planner 실측으로 `R-158~R-160` / `R-164~R-168` 이 REQ ID 가 아니라 **README 행 번호** 표기임을 확정(README 총 168 행 기준 위치 일치), 매핑 표 기존 66 row 에 대응 row 0 개라 `REQ-067`~`REQ-073` 7 row 로 충돌 없이 채번. `STATE.nextTask=T-1709`, counters · lock · ci 불변.
- 02:50 driver: T-1709 DONE — [requirements.md](../requirements.md) 매핑 표에 `REQ-067`~`REQ-073` 7 row 를 7 컬럼 schema 로 append(067~072 FR / 073 NFR · 상태 PLANNED · 구현 위치에 PLAN bullet 좌표) 하고, 부수 stale 2 곳을 정정(direct `b3ed3670`, +9/-2, 1 파일). `12 행` 은 "P1-Entry 시점 66 row + 이후 REQ-067 승계" 로 inline amend, T-1470 각주 census 는 `66 지점`→`73 지점` · 합 `79`→`86` 으로 정정하되 산문 13 지점 판정은 불변. README 원문 대조로 `158`·`159`·`160`(계정 생성 UX 3 문장) · `164~168`(평가 대상 관리 UI 5 문장) 일치 실측, `grep -c "^| REQ-"` = 73 · 신규 row 전부 파이프 8 개로 표 구조 무손. `src/` · `web/` · `test/` · CI · `package.json` diff 0 파일이라 coverage 무영향. 본 fire 는 planner 큐잉(`59b726f5`) + 집행(`b3ed3670`) chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1709` claimedAt 02:41:56Z(server-time 정상), executor lock-free 진행 후 bookkeeping 전 lock 재획득. 본 fire 두 main run(`59b726f5` in_progress · `b3ed3670` pending) conclusion 은 다음 fire 가 재확인(R-114 이월). counters.tasksCompleted 1698→1699, nextTask=null(다음 fire 는 planner dispatch — 오너 지시 2 건의 task 분해가 최우선 후보).

- planner: queued T-1710 — SuperAdmin 초기 셋업 폼 아이디·암호 조건 사전 안내 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX)의 선행 step 인 requirements 동기가 T-1709 로 닫혀 남은 **task 분해** 에 착수 — 그 첫 slice 로 [REQ-067](../requirements.md) `86 행`(조건 사전 안내)만 떼어냈다. 현 [SuperAdminSetupForm.tsx](../../web/src/components/SuperAdminSetupForm.tsx) 는 label 2 + input 2 뿐이라 아이디 email 형식도 비밀번호 최소 8 자([add-user.dto.ts](../../src/user/dto/add-user.dto.ts) `PASSWORD_MIN_LENGTH`)도 화면에 없어 제출 실패 후에야 조건을 추측하는 상태. presentational 컴포넌트 1 개 + spec 1 개로 한정해 안내 문구 2 축 + `aria-describedby` 배선 + 기존 14 개 it 회귀 보존. REQ-068/069(구체 실패 사유 · 중복 vs 형식 구분)는 `signup` 반환 계약(400·409 → `null` 흡수) 변경을 동반해 표면이 달라 **후속 slice** 로 분리, Admin 사용자 추가 화면 · REQ-070~073(평가 대상 UI) · requirements 상태 갱신(doc-only `direct`)도 Out of Scope. `src/` · `test/` · CI · `package.json` diff 0 파일 목표. `STATE.nextTask=T-1710`, counters · lock · ci · humanQuestions 불변.
- 03:55 driver: T-1710 DONE — [SuperAdminSetupForm.tsx](../../web/src/components/SuperAdminSetupForm.tsx) 에 아이디(email 형식) · 비밀번호(최소 8 자) 조건을 **입력 전에 항상 보이는** 안내로 박제(pr, PR [#1341](https://github.com/myungjoo/Assessment-Agent/pull/1341) round 1 APPROVE → merge `4924bc8c`, 2 파일 +131/-1). 오너가 `9485c923` 로 직접 등록한 지시(PLAN `129 행` 🔴 계정 생성 UX) 의 분해 첫 slice 이자 [REQ-067](../requirements.md) 집행 — T-1709 가 마친 requirements 동기 다음 step 이다. `PASSWORD_MIN_LENGTH` · 힌트 문구를 컴포넌트 상단 상수로 올리고 backend [add-user.dto.ts](../../src/user/dto/add-user.dto.ts) 동명 상수가 정본임을 주석으로 명시(web 은 별도 package 라 값 동기), 안내 `<p>` 는 label **바깥** 배치로 입력의 접근 가능 이름 오염을 피하고 `aria-describedby` 로 연결. 조건 분기 추가 0(무조건 렌더). spec 은 기존 14 it 유지 + 7 it 추가로 R-112 4 종 cover(happy · error alert 동시 렌더 · branch loading/error 2×2 · negative 3 종 — 빈 입력 · loading 차단 · 비밀번호 값 노출 0). web 70 files / 2089 test green, backend 453 suite / 13009 test green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. 본 fire 는 planner 큐잉(`79bbd80c`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1710` claimedAt 03:42:26Z(server-time 정상), executor lock-free 진행 후 bookkeeping 전 lock 재획득. `79bbd80c` main run success, merge commit `4924bc8c` run 은 in_progress — 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1699→1700, nextTask=null(다음 fire 는 planner dispatch — REQ-067 나머지 절반 Admin 사용자 추가 화면 안내, 또는 REQ-068~073 분해가 후보).
- planner: queued T-1711 — Admin 사용자 추가 폼 아이디·암호 조건 사전 안내 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX) 분해의 slice 2 — [REQ-067](../requirements.md) `86 행` 원문이 계정 생성 화면을 "첫 로긴 SuperAdmin 지정, **Admin 의 사용자 추가** 모두 포함" 으로 명시하는데 T-1710 이 앞 절반만 닫았기에 **나머지 절반**을 집행한다. 현 [AdminView.tsx](../../web/src/views/AdminView.tsx) `4544 행` 부근 사용자 생성 폼은 입력 2 + 버튼 1 뿐이라 동일 backend 계약([add-user.dto.ts](../../src/user/dto/add-user.dto.ts) `@IsEmail` + `@MinLength(PASSWORD_MIN_LENGTH=8)`) 을 쓰면서도 조건 안내가 0 줄. 문구는 T-1710 이 이미 export 한 `USERNAME_HINT_TEXT`/`PASSWORD_HINT_TEXT` 를 named import 로 **재사용**(두 화면 문구 drift 차단), AdminView 는 고유 hint id 상수 + `aria-describedby` 배선만 추가하고 무조건 렌더(분기 0). 기존 `9347 행` 대 정적 렌더 회귀 test(`not.toContain('role="alert"')`) 를 깨지 않는 것이 AC 1 항. REQ-068/069(구체 실패 사유 · 중복 vs 형식 구분)는 `runCreateUser` error 파생 계약 변경 동반이라 후속 slice, REQ-070~073(평가 대상 UI) · requirements 상태 갱신(doc-only `direct`)도 Out of Scope. `src/` · `test/` · CI · `package.json` diff 0 파일 목표. `STATE.nextTask=T-1711`, counters · lock · ci · humanQuestions 불변.
- 04:58 driver: T-1711 DONE — [AdminView.tsx](../../web/src/views/AdminView.tsx) 의 Admin 사용자 추가 폼에 아이디(email 형식) · 비밀번호(최소 8 자) 조건을 **입력 전에 항상 보이는** 안내로 박제(pr, PR [#1342](https://github.com/myungjoo/Assessment-Agent/pull/1342) round 1 APPROVE → merge `936e2786`, 2 파일 +194/-0). 오너 지시(PLAN `129 행` 🔴 계정 생성 UX) 분해 slice 2 이자 [REQ-067](../requirements.md) `86 행` 원문의 나머지 절반 — T-1710 이 SuperAdmin 셋업 폼만 닫았던 자리를 Admin 화면으로 확장했다. 문구는 T-1710 이 export 한 `USERNAME_HINT_TEXT`/`PASSWORD_HINT_TEXT` 를 named import 로 **재사용**(재정의 0 → 두 화면 drift 차단), AdminView 는 고유 hint id 상수 2 개 + `aria-describedby` 배선만 추가하고 조건 분기 없이 무조건 렌더. 안내 `<p>` 에 role 미부여로 매 렌더 경보 낭독을 피했다. spec 은 describe 1 + it 7 추가로 R-112 4 종 cover(happy 안내 2 축 + describedby · error alert 공존 · branch `creatingUser` 비의존/비-Admin fail-closed · negative 3 종 — 입력값 노출 0 · aria-label 유지 · role 부재) + 문구 drift guard. web 70 files / 2097 test green, `pnpm --dir web build` green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. 본 fire 는 planner 큐잉(`f103b0ba`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1711` claimedAt 04:42:16Z(server-time 정상), executor lock-free 진행 후 bookkeeping 전 lock 재획득. 직전 fire 의 R-114 이월 해소(`4924bc8c`·`ad8b8ec0` 둘 다 success), 본 fire 의 `f103b0ba` main run success · PR CI 2 checks pass · merge commit `936e2786` main run 은 in_progress 라 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1700→1701, nextTask=null(다음 fire 는 planner dispatch — REQ-068/069 실패 사유 구체화 또는 REQ-070~073 평가 대상 UI 분해가 후보). Follow-up: [requirements.md](../requirements.md) `86 행` REQ-067 상태 갱신은 doc-only `direct` 후속.

- planner: queued T-1712 — signup 실패 사유 분류 helper 박제 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX) 분해 slice 3 — T-1710·T-1711 이 [REQ-067](../requirements.md) `86 행`(사전 안내)을 두 화면 모두에서 닫았고 남은 축이 [REQ-068](../requirements.md) `87 행`(위반 조건별 구체 사유 · 포괄 문구 금지) · [REQ-069](../requirements.md) `88 행`(중복 vs 형식/길이 구분)이다. 현 [AppShell.tsx](../../web/src/AppShell.tsx) `99 행` 이 정확히 오너가 금지한 `'이미 등록된 사용자이거나 입력이 올바르지 않습니다.'` 를 쓰는 원인은 상류 [auth.ts](../../web/src/api/auth.ts) `signup` 이 409·400 을 **둘 다 `null` 로 흡수**해 사유를 버리기 때문 — 본 slice 는 그 정보를 되살리는 **순수 분류 함수**(`classifySignupFailure` / `formatSignupFailure` + `SignupFailure` 축별 배열)만 신설하고 `signup` 시그니처 · AppShell · AdminView 배선은 표면이 넓어 후속 slice 로 분리(§3 파일 cap). `ApiError.message` 가 비-2xx 응답 body 원문을 담는 실측을 근거로 400 의 class-validator 문구 **6 패턴만** 축 매핑, 미매핑 항목은 `other` 로 보존(정보 유실 0). planner 실측으로 enumeration-safe 흡수 정책이 [auth.ts](../../web/src/api/auth.ts) `70~72 행` **코드 주석**일 뿐 ADR 0 건임을 확인 — 오너 지시가 정면 대체하며 ADR 충돌 없음(본 task 는 `signup` 무변경이라 런타임 동작 변화 0). `src/` · `test/` · CI · `package.json` diff 0 파일 목표. `STATE.nextTask=T-1712`, counters · lock · ci · humanQuestions 불변.

- 06:02 driver: T-1712 DONE — signup 실패 응답을 위반 조건별 구체 사유로 분류하는 **순수 helper** 를 [web/src/api/signupError.ts](../../web/src/api/signupError.ts) 에 신설(pr, PR [#1343](https://github.com/myungjoo/Assessment-Agent/pull/1343) round 2 APPROVE → merge `04b112a9`, 2 파일 +289/-0). 오너 지시(PLAN `129 행` 🔴 계정 생성 UX) 분해 slice 3 — T-1710·T-1711 이 [REQ-067](../requirements.md) `86 행`(사전 안내)을 두 화면 모두에서 닫은 뒤 남은 [REQ-068](../requirements.md) `87 행`(위반 조건별 구체 사유) · [REQ-069](../requirements.md) `88 행`(중복 vs 형식/길이 구분)을 잇는다. `classifySignupFailure` / `formatSignupFailure` + `SignupFailure(Kind)` named export 로, 409 는 중복 전용 문구, 400 은 class-validator 6 패턴을 축 매핑, 미매핑·비정상 body 는 `other` 로 보존해 정보 유실 0. 비밀번호 최소 길이는 backend `AddUserDto` 정본과 동일 값 상수 + spec drift guard. colocated spec 이 R-112 4 종 + negative 5 종을 cover하고 오너가 금지한 포괄 문구가 생성되지 않는지도 guard 한다. web vitest 71 files / 2118 test green, `pnpm --dir web build` green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. reviewer round 1 이 Nit 2 건을 냈고 §3 Nit-in-PR closure 로 같은 PR 다음 commit 에서 닫아 round 2 APPROVE(남은 finding 0). 본 fire 는 planner 큐잉(`e9d1860d`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1712` claimedAt 05:43:14Z(server-time 정상, 단독 CLAIMED). **알려진 편차**: executor 가 PR flow 를 통째로 흡수해 PR open 시점 `sync-claim-pr.sh` 를 driver 가 호출할 window 가 없었다 — claim `prNumber` 는 null 로 남았고 bookkeeping 에서 prune. 단독 claim 이라 dup-PR risk 0. 직전 fire 의 R-114 이월 해소(`936e2786`·`f16ce0b5` 둘 다 success), 본 fire 의 `e9d1860d`·`04b112a9` main run 은 pending/in_progress 라 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1701→1702, nextTask=null(다음 fire 는 planner dispatch — 후속 slice 는 [auth.ts](../../web/src/api/auth.ts) `signup` 계약 변경 + [AppShell.tsx](../../web/src/AppShell.tsx) `99 행` 포괄 문구 교체 배선이 유력). Follow-up: [requirements.md](../requirements.md) `87~88 행` REQ-068/069 상태 갱신은 doc-only `direct` 후속.

- planner: queued T-1713 — signup 실패 사유를 보존하는 `signupDetailed` 계약 추가 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX) 분해 slice 4 — T-1712 가 순수 helper [signupError.ts](../../web/src/api/signupError.ts)(`classifySignupFailure`/`formatSignupFailure`)를 박제했지만, 정작 API 경로 [auth.ts](../../web/src/api/auth.ts) `signup` 이 409·400 을 **둘 다 `null` 로 흡수**해 사유를 버리므로 화면은 여전히 오너가 금지한 [AppShell.tsx](../../web/src/AppShell.tsx) `99 행` 포괄 문구밖에 못 쓴다. 본 slice 는 그 정보 손실 지점 **하나만** 열어 `signupDetailed` 를 신설(성공 `role` / 409·400 은 `SignupFailure` 보존 / 그 외는 `ApiError` throw 유지)하고, 기존 `signup` 은 얇은 wrapper 로 재구현해 반환 계약 `Promise<string|null>` 을 문자 그대로 유지한다 → 호출측([AppShell](../../web/src/AppShell.tsx)·[AdminView](../../web/src/views/AdminView.tsx)) diff 0, 런타임 동작 변화 0, 기존 `describe('auth.signup')` 8 test 무수정 통과가 회귀 게이트. [REQ-068](../requirements.md) `87 행`·[REQ-069](../requirements.md) `88 행` 의 최종 충족(포괄 문구 교체 배선)은 표면이 다른 후속 slice 로 분리, REQ-070~073(평가 대상 UI) · requirements 상태 갱신(doc-only `direct`)도 Out of Scope. `src/` · `test/` · CI · `package.json` diff 0 파일 목표. `STATE.nextTask=T-1713`, counters · lock · ci · humanQuestions 불변.

- 06:52 driver: T-1713 DONE — signup 실패 사유를 버리지 않고 보존하는 `signupDetailed` 계약을 [web/src/api/auth.ts](../../web/src/api/auth.ts) 에 신설(pr, PR [#1344](https://github.com/myungjoo/Assessment-Agent/pull/1344) round 1 APPROVE → merge `e5dbbfa5`, 2 파일 +189/-15). 오너 지시(PLAN `129 행` 🔴 계정 생성 UX) 분해 slice 4 — T-1712 가 순수 helper [signupError.ts](../../web/src/api/signupError.ts) 를 박제했지만 정작 API 경로 `signup` 이 409·400 을 둘 다 `null` 로 흡수해 사유를 버리던 정보 손실 지점을 연다. `signupDetailed` 는 성공 시 `role`, 409·400 은 `classifySignupFailure` 로 환원한 `SignupFailure` 를 반환하고 그 외 status 는 `ApiError` 를 그대로 throw 한다. 기존 `signup` 은 그 위의 얇은 wrapper 로 재구현해 반환 계약 `Promise<string|null>` 을 문자 그대로 유지 — 호출측([AppShell](../../web/src/AppShell.tsx)·[AdminView](../../web/src/views/AdminView.tsx)) diff 0, 런타임 동작 변화 0 이고 기존 `describe('auth.signup')` 8 test 무수정 통과가 회귀 게이트가 됐다. 신규 `describe('auth.signupDetailed')` 11 test 가 R-112 4 종을 cover(분기 5 종: 2xx+role / 2xx role 누락·비문자열 / 409 / 400 / 5xx·status 0 throw, negative 6 종: `@IsEmail` 축 분리 · 비-JSON body · 5xx 미조작 · null·빈 body · 오너 금지 문구 guard · wrapper 회귀). web vitest 71 files / 2130 test green, `pnpm --dir web build` green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. reviewer round 1 APPROVE(finding 0) 로 4-게이트 PASS → squash merge + branch delete. 본 fire 는 planner 큐잉(`f493ce13`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1713` claimedAt 06:40:47Z(server-time 정상, 단독 CLAIMED) → executor 가 `sync-claim-pr.sh` 로 `prNumber=1344`·PR_OPEN 동기(직전 fire 의 알려진 편차 해소) → bookkeeping 에서 prune + lock release. 직전 fire 의 R-114 이월 해소: `e9d1860d` success, `04b112a9` 는 cancelled 이나 직후 push `6e964953` run 이 success 라 supersede(benign) 판정. 본 fire 의 `f493ce13` main run success, merge commit `e5dbbfa5` main run 은 queued 라 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1702→1703, nextTask=null(다음 fire 는 planner dispatch — 후속 slice 는 [AppShell.tsx](../../web/src/AppShell.tsx) `99 행` 포괄 문구를 `formatSignupFailure` 결과로 교체하는 화면 배선이 유력). Follow-up: [requirements.md](../requirements.md) `87~88 행` REQ-068/069 상태 갱신은 doc-only `direct` 후속.

- planner: queued T-1714 — AppShell 셋업 실패 포괄 문구를 축별 구체 사유로 교체 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX) 분해 slice 5 — T-1712 가 순수 helper [signupError.ts](../../web/src/api/signupError.ts) 를, T-1713 이 사유 보존 API 계약 [auth.ts](../../web/src/api/auth.ts) `signupDetailed` 를 박제했으나 **화면은 아직 그 정보를 소비하지 않는다**. planner 실측으로 [AppShell.tsx](../../web/src/AppShell.tsx) `99 행` 이 여전히 오너가 금지한 포괄 문구 `'이미 등록된 사용자이거나 입력이 올바르지 않습니다.'` 를 그대로 들고 있음을 확인(main `e5dbbfa5` 기준) — 본 slice 는 그 소비 지점 하나만 `signupDetailed` + `formatSignupFailure` 로 교체해 [REQ-068](../requirements.md) `87 행` · [REQ-069](../requirements.md) `88 행` 을 셋업 화면에서 실제 충족시킨다. web 에 `@testing-library/react` 가 없어(§5 새-dep 게이트) 상호작용 렌더 test 가 불가하므로 변환 로직을 **named export 순수 함수**(`buildSetupErrorMessage`)로 분리해 R-112 4 종을 단위 검증하고, 기존 `describe('AppShell')` 8 it 무수정 통과를 회귀 게이트로 둔다. `SuperAdminSetupForm` 의 `error?: string` 제약은 그대로 두고(여러 줄 렌더 upgrade 는 Follow-up), [AdminView](../../web/src/views/AdminView.tsx)(다른 API 경로) · `signup` wrapper 정리 · requirements 상태 갱신(doc-only `direct`)은 Out of Scope. `src/` · `test/` · CI · `package.json` diff 0 파일 목표. `STATE.nextTask=T-1714`, counters · lock · ci · humanQuestions 불변.

- 07:53 driver: T-1714 DONE — SuperAdmin 초기 셋업 화면의 실패 문구를 오너가 금지한 포괄 문구에서 축별 구체 사유로 교체(pr, PR [#1345](https://github.com/myungjoo/Assessment-Agent/pull/1345) round 1 APPROVE → merge `2f4bd383`, 2 파일 +176/-14). 오너 지시(PLAN `129 행` 🔴 계정 생성 UX) 분해 slice 5 — T-1712 가 순수 helper [signupError.ts](../../web/src/api/signupError.ts) 를, T-1713 이 사유 보존 계약 `signupDetailed` 를 박제했으나 화면이 그 정보를 버리고 있던 마지막 소비 지점을 잇는다. [AppShell.tsx](../../web/src/AppShell.tsx) 의 `handleSetupSubmit` 이 `signup` 대신 `signupDetailed` 를 호출하고, `role` 이 문자열이면 종전대로 `setView('login')` 재진입(성공 동작 변화 0), 실패면 named export 순수 함수 `buildSetupErrorMessage(failure)` 가 `formatSignupFailure` 결과를 원문 보존해 합친 문자열을 폼 error 로 표시한다. 금지 문구 `'이미 등록된 사용자이거나 입력이 올바르지 않습니다.'` grep 0 건이고, 2xx 인데 `role`·`failure` 가 둘 다 없는 비정상 응답은 사유를 지어내지 않는 별도 fallback 문구(네트워크 catch 문구와 구분)로 처리한다. web 에 `@testing-library/react` 가 없어(§5 새-dep 게이트) 상호작용 렌더 test 가 불가하므로 변환 로직을 순수 함수로 분리한 것이 설계 제약의 귀결 — 신규 `describe('buildSetupErrorMessage')` 10 it 이 R-112 4 종을 cover(happy 중복 축 / error path 아이디+비밀번호 동시 위반 병합 금지 / 분기 4 종 1 줄·2+ 줄·빈 목록·null / negative 4 종 금지 문구 부재·비밀번호 값 미노출·중복 vs 형식 구분·`other` 원문 보존). 기존 `describe('AppShell')` 8 it 무수정 통과가 회귀 게이트(파일 총 18 it). web vitest 2140 test green, `pnpm --dir web build` green, `pnpm lint` green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. reviewer round 1 APPROVE(finding 0) 로 4-게이트 PASS → squash merge + branch delete. 본 fire 는 planner 큐잉(`5fb19d61`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1714` claimedAt 07:41:07Z(server-time 정상, 단독 CLAIMED) → executor 가 `sync-claim-pr.sh` 로 `prNumber=1345`·PR_OPEN 동기(exit 0) → bookkeeping 에서 prune + lock release. 직전 fire 의 R-114 이월 해소: `e5dbbfa5`·`4390c984` main run 둘 다 success. 본 fire 의 `5fb19d61` main run success, merge commit `2f4bd383` main run 은 in_progress 라 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1703→1704, nextTask=null(다음 fire 는 planner dispatch — 후속 slice 는 [AdminView.tsx](../../web/src/views/AdminView.tsx) 사용자 추가 경로의 같은 배선이 유력). Follow-up: [requirements.md](../requirements.md) `87~88 행` REQ-068/069 상태 갱신은 doc-only `direct` 후속.

- planner: queued T-1715 — Admin 사용자 추가 실패 문구를 축별 구체 사유로 교체 (`pr`, 2 파일). 오너 최우선 지시 [PLAN](../PLAN.md) `129 행` 🔴(계정 생성 UX) 분해 slice 6 — T-1712 helper · T-1713 API 계약 · T-1714 셋업 화면 배선에 이어 오너 지시가 대상으로 삼은 **두 번째 화면**(Admin 의 사용자 추가)이 남아 있었다. planner 실측으로 [AdminView.tsx](../../web/src/views/AdminView.tsx) 의 `runCreateUser` 비-409 분기가 `describeError(e)` 로 `HTTP 400: {"message":[...]}` 응답 원문을 그대로 노출함을 확인(main `3fcd577b` 기준). 교체 지점은 컨테이너 deps 1 줄 + 신규 순수 helper 뿐이라 기존 `AdminView.test.tsx` 회귀 0. `STATE.nextTask=T-1715`, counters · lock · ci 불변.

- 09:00 driver: T-1715 DONE — Admin 사용자 추가 화면의 400 실패를 축별 구체 사유로 교체(pr, PR [#1346](https://github.com/myungjoo/Assessment-Agent/pull/1346) round 2 APPROVE → merge `a9225679`, 2 파일 +215/-1 + nit round +20). [AdminView.tsx](../../web/src/views/AdminView.tsx) 에 named export 순수 함수 `describeCreateUserFailure` 와 구분자 상수 `CREATE_USER_ERROR_SEPARATOR`(`' / '`, AppShell 선례 동기) 를 신설하고 `handleCreateUser` 의 deps `describeError: toErrorMessage` 만 교체했다 — `CreateUserDeps` 타입 · `runCreateUser` 본문 · 409 전용 문구 `USER_DUPLICATE_ERROR` 는 무수정이라 기존 spec 회귀 0. 400 만 `classifySignupFailure`/`formatSignupFailure` 로 환원해 `아이디:` · `비밀번호:` 축별 한국어 사유를 원문 보존한 채 표시하고, 그 외 status 는 종전 `toErrorMessage` 로 흘려 동작 변화 0 · 신설 throw 0. 신규 spec 14 it 이 R-112 4 종 cover(happy 1 · error path 1 · 분기 4 종 400 분류·400 비-JSON·비-400·비-ApiError · negative 5 종 raw JSON 잔존 0·비-Error throw 0·비밀번호 값 미노출·409 어휘 분리·미매핑 원문 보존 · drift guard 3). web vitest 72 files / 2154 test green, `pnpm --dir web build` · lint green, `src/`·`test/`·워크플로·`package.json` diff 0 파일이라 backend coverage 불변. reviewer round 1 이 Nit-1(구분자 상수 값 동기 drift guard 부재) 을 지적해 CLAUDE.md §3 Nit-in-PR closure 로 같은 PR round 2(test-only)에서 마감, round 2 APPROVE 로 4-게이트 PASS → squash merge + branch delete. 본 fire 는 planner 큐잉(`3fcd577b`) + 집행 chain(multiTaskFire ON · holder=cron · 수행 task N=1). claim `T-1715` claimedAt 08:43:35Z(server-time 정상, 단독 CLAIMED) → executor 가 PR flow 를 흡수해 driver 의 `sync-claim-pr.sh` 시점이 없었으므로 `prNumber` 동기는 생략(알려진 편차) → bookkeeping 에서 prune + lock release. 직전 fire 의 R-114 이월 해소: `2f4bd383` · `86c959a6` main run 둘 다 success. 본 fire 의 `3fcd577b` main run success, merge commit `a9225679` main run 은 pending 이라 다음 fire 가 conclusion 재확인(R-114 이월). counters.tasksCompleted 1704→1705, nextTask=null(다음 fire 는 planner dispatch — 후속 slice 는 [requirements.md](../requirements.md) `87~88 행` REQ-068/069 상태 갱신 doc-only `direct` 가 유력). 오너 지시 `129 행` 🔴 의 두 소비 지점(셋업 · 사용자 추가)이 이로써 모두 배선 완료.
