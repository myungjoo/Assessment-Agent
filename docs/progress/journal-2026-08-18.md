# Journal — 2026-08-18

- 00:02 planner: queued **T-1588** — `acquire-lock.sh` holder 인자 `loop|cron|human` allowlist 검증 + 오인자 negative 회귀 가드 (`pr` mode, 2 파일 · 약 140 LOC). 2026-08-17 22:47 addendum 이 남긴 실사고 표면(`--release` 가 holder 로 해석돼 무효 lock 이 6 분간 박힘) 을 봉합 — 원격 접촉 전 검증 + `exit 2`, `scripts/acquire-lock.test.sh` 에 `B8` 분기와 negative 5 종(`--release` 회귀 가드 · 오타 · 대소문자 · holder 누락 · session 누락) 추가. ADR-0056 §Follow-ups (b) perf-spec 배선 확산보다 우선한 이유는 (1) 실제 사고 표면이라 재발 가능, (2) lock primitive 가 stage 5b 동시성 신뢰 기반, (3) 잔여 (b) 슬라이스는 균질 반복이라 순서 손실 0. CI 기존 leg 안에서 완결(`ci.yml`·`daily-test.sh` 미수정 — T-1122 파일 cap 전례 회피). `STATE.nextTask = T-1588`.

- 00:20 driver: **T-1588 DONE** — `pr` mode, PR [#1268](https://github.com/myungjoo/Assessment-Agent/pull/1268) squash merge `1f95ae3c` (2 파일 `+94/-6`). `acquire-lock.sh` acquire 경로에 holder allowlist(`loop|cron|human`) 정확매칭 검증을 **원격 접촉 이전** 에 넣어 오인자(`--release` 등)가 조용히 유효 lock 으로 박히던 표면을 봉합했다 — 불일치 시 입력값 · 허용값 · bare `release` 힌트 3 종 stderr + `exit 2`, release 경로 · env · exit 코드 계약 불변. `acquire-lock.test.sh` 에 `[T10]`(human 획득 + bare release 무회귀) · `[T11]`(negative 5 종: `--release` 회귀 가드 · 오타 `croon` · 대소문자 `Cron` · 빈 인자 · session 누락) + 분기표 `B8` 매핑을 추가해 T1~T11 exit 0(ok 23 건), 수정 전 코드에서 `[T11]` 이 실제 FAIL 함까지 확인(장식 test 아님). **4-게이트** — reviewer VERDICT=APPROVE + PR comment 외화 + integrator 자체 점검 + PR CI 2 check(기본 검사 4m57s · 배포 산출물 검증 1m24s) pass 로 4/4 충족, round 2 에서 머지. **LOOP `§7.5` chain 진입** — G1(`multiTaskFire` true) · G2(holder `cron`) 충족 + §2.5 (a) executor 1 회 호출 · SUMMARY + trail 만 수신 · (b) N=2 · (c) 직전 T-1587 무사고 · (d) lock `since` 00:00:05Z 로부터 6 분(< 45 분) · (e) pr + pr 동종 — 5 조건 전량 통과로 본 fire 의 **두 번째이자 마지막** task 로 진입. commit footer 에 `FIRE-BATCH: T-1587+T-1588` marker 박제, N=3 진입 금지에 따라 본 task 완료로 fire 종료. **R-114 정산** — `8de66603`(T-1587 머지) · `d03d73f5`(T-1587 bookkeeping) · `db18cdec`(T-1588 큐잉) main run 전부 **success**, 위임 잔량은 머지 commit `1f95ae3c`(in_progress) + 본 bookkeeping commit 으로 다음 turn 이관. **claim 경로** — `validate-claim-candidate.sh T-1588` PASS → `select-claim.sh` 단독 claim(server-time `00:05:47Z`) → executor 가 PR open 직후 `sync-claim-pr.sh` 로 `PR_OPEN`/`1268` 동기(exit 0) → 머지 후 driver prune, `claims.json` `[]` 복귀. humanQuestion **0** · blocker **0** · `concurrencyIncidents` 불변 · counters `tasksCompleted` **1578 → 1579**. **다음** — `nextTask` 비어 있음. 다음 fire 가 planner 를 dispatch 해 큐잉한다.

- 00:42 planner: queued **T-1589** — 체크인 baseline `absent` 국면에 candidate 지표 로그 박제 (`pr` mode, 4 파일 · 약 190 LOC). ADR-0056 `§Follow-ups (a)`(baseline JSON 최초 생성·commit) 가 착수 불가였던 구조적 이유 — `§Decision 2` 가 CI 자동 commit 을 비채택했는데 `runCheckinBaselineCheck` 의 `skip(absent)` 국면이 `path=` 한 줄만 내고 측정 수치를 하나도 싣지 않아, T-1584 로 토글이 켜진 CI 를 돌려도 `§Consequences (d)` 가 요구하는 "사람 눈 값 타당성 확인" 입력을 얻을 수 없다 — 를 봉합한다. 순수 포매터 `formatCheckinCandidateLine` 1 개 추가(label · concurrency · p50/p95/p99 · throughput · errorRate · count · pass) + `absent` 분기만 2 줄 로그로 확장, `disabled`/`compare` 분기 · 반환 union · exit code 불변. `*-read` 배선 확산(T-1586/T-1587 계열) 대신 이 slice 를 먼저 두는 근거는 확산 잔여 52 개가 균질 반복이라 순서 손실 0 인 반면 (a) 는 본 로그 없이는 영구 착수 불가라는 점. `test/perf/README.md` doc-sync 는 5 번째 파일이라 Follow-up 이월(T-1122 cap 전례). `STATE.nextTask = T-1589`.

- 01:12 driver: **T-1589 DONE** — `pr` mode, PR [#1269](https://github.com/myungjoo/Assessment-Agent/pull/1269) squash merge `2083b07b` (4 파일 `+283/-14`). 순수 포매터 `formatCheckinCandidateLine` 을 `checkin-baseline-report.ts` 에 추가하고 `runCheckinBaselineCheck` 의 `skip(absent)` 분기 로그를 정확히 2 줄로 확장했다 — `p50`·`p95`·`p99`·`throughput`·`errorRate`·`count`·`pass` + env label · concurrency 를 `key=value` 로 전사만 하며 재계산 · 반올림 · 임계 판정 0, 표본 0 국면의 `NaN` 은 가공 없이 노출. `disabled`·`compare` 분기와 반환 union · `absent` 의 compare 호출 0 회 계약은 불변. 대상 spec 48 → 82 test(+34), 전체 12540 pass, line 99.95% / function 100%. **4-게이트** — reviewer VERDICT=APPROVE(round 1) + PR comment 외화 + integrator 자체 점검 + PR CI 2 check(기본 검사 4m5s · 배포 산출물 검증 1m21s) pass 로 4/4 충족. round 1 안에서 nit(중복 property read) 1 건을 `a8e743a6` 으로 닫아 CLAUDE.md `§3` Nit-in-PR closure 준수. **R-114 정산** — 직전 fire 위임 잔량 `1f95ae3c`·`baff6ebc` 와 본 fire 의 `5d65ceb2`(큐잉)·`2083b07b`(머지) main run 전부 **success**, 위임 잔량은 본 bookkeeping commit 1 건. **claim 경로** — lock CAS 획득(`00:37Z`) → `validate-claim-candidate.sh T-1589` PASS → `reclaim-stale-claim.sh` 회수 대상 0 → `select-claim.sh` 단독 claim(server-time `00:45:14Z`, 활성 claim 0 이라 pr-mode 단독 조건 충족) → executor 가 PR open 직후 `sync-claim-pr.sh` 로 `PR_OPEN`/`1269` 동기(exit 0) → 머지 후 driver 가 `lock_tree_cas_push` 로 prune, `claims.json` `[]` 복귀. **LOOP `§7.5` chain 미진입** — G1·G2 는 충족하나 `nextTask` 가 비어 두 번째 task 진입에 planner 재dispatch 가 선행돼야 하고, 그 경우 pr-mode task 종료 예상 시각이 lock `since`(`00:37Z`) 기준 60 분 stale 임계를 넘길 위험이 있어 §2.5 (d) 의 취지대로 본 task 완료로 fire 종료. humanQuestion **0** · blocker **0** · `concurrencyIncidents` 불변 · counters `tasksCompleted` **1579 → 1580**. **다음** — `nextTask` 비어 있음. 다음 fire 가 planner 를 dispatch 해 큐잉한다. 후보: `test/perf/README.md` 문서 동기(T-1589 Follow-ups), ADR-0056 `§Follow-ups (a)` 본체(baseline JSON 최초 생성 — 본 task 로 관측 경로 확보됨), `§Follow-ups (c)` 임계 fix.


- 01:32 planner: queued **T-1590** — 체크인 baseline 게이트의 로그 · 토글 규약을 `test/perf/README.md` 에 doc-sync (`pr` mode, 1 파일 · 약 90 LOC). T-1589 `Follow-ups` 이월분 — 현재 README 1167 행에 `checkin-baseline` · `PERF_CHECKIN_BASELINE` · `formatCheckinCandidateLine` 언급이 **0 건** 이라, ADR-0056 `§Consequences (d)` 가 요구하는 "사람이 CI 로그 값을 읽고 승인" 절차의 입력 규약이 코드에만 있고 harness 문서에는 없다. 신규 절 1 개로 토글 값 규약(`"1"`/`"true"`/`"yes"`, 모호하면 off) · 3 국면(`disabled` 1 줄 · `absent` 2 줄 · `compared`) · 로그 키 순서 · 전사 전용/exit code 불변 계약 · write 국면 부재 이유(§Decision 2)를 박제한다. `§Follow-ups (a)` 본체보다 먼저 두는 근거는 (a) 의 승인자 입력이 본 문서라는 것. 코드 변경 0 (`test/perf/*.ts` 수정 금지), PLAN · requirements 갱신은 별도 `direct` slice 로 분리(T-1585 전례). `STATE.nextTask = T-1590`.
- 02:00 driver: T-1590 DONE — 체크인 baseline 게이트 로그·토글 규약 harness README doc-sync (PR #1270 squash ebc18af8, 1 파일 +96/-0, reviewer APPROVE round 1, 4-게이트 PASS). fineGrainedConcurrency ON claim-pickup fire.
- 02:45 planner: queued **T-1591** — person 실 DB 조회 perf-spec 에 실측 clock 기반 candidate 관찰 국면 추가 (`pr` mode, 1 파일 · 약 130 LOC). 체크인 baseline 배선 소비자 11 개가 전부 `registerCheckinBaselineWiringSuite` 의 `measure` 에 `createStepClock(stepMs)` 를 주입하고 있어 T-1584(CI 토글 ON) · T-1589(absent 국면 candidate 로그) 이후 CI 에 찍히는 candidate 수치가 **전량 합성값**이다. 따라서 ADR-0056 `§Consequences (d)` 가 요구하는 "사람이 값 타당성 확인 후 commit" 의 입력(실 p50/p95/p99)이 0 건이라 `§Follow-ups (a)`(baseline JSON 최초 생성) 착수가 막혀 있었다. 본 task 가 실측 축 관찰 국면 1 벌을 열어 그 마지막 선행 조건을 해소한다. `dependsOn` = T-1587 · T-1589, `touchesFiles` = `test/perf/person-read-realdb.perf-spec.ts` 1 개. `STATE.nextTask = T-1591`.
- 03:12 driver: **T-1591 DONE** — `pr` mode, PR [#1271](https://github.com/myungjoo/Assessment-Agent/pull/1271) squash merge `49bf4c7d` (1 파일 `+193/-0`). `test/perf/person-read-realdb.perf-spec.ts` 에 실측 축 전용 nested describe 1 개 추가 — label `ci-realdb-person-read` · 전용 상수 `REAL_CLOCK_ITER=3` · `repoRoot` 생략(실경로 바인딩) · 토글은 `processEnv` 주입 전용(전역 env 무접근). 국면 5 개(happy 1 · error 1 · 분기 1 · negative 2)로 R-112 충족, 기존 국면 · 상수 · hook 불변(추가만). 전체 12540 pass, line 99.95% / function 100%(`src/` 0 LOC). **성과** — 체크인 baseline 배선 소비자 11 개가 전부 `stepClock` 을 주입해 CI candidate 수치가 전량 합성값이던 상태를 깨고, ADR-0056 `§Consequences (d)` 의 승인 입력이 처음 실측값으로 CI 로그에 찍혔다: `p50=2.955ms p95=3.227ms p99=3.251ms throughput=333.3 errorRate=0 count=3 pass=true`. `§Follow-ups (a)`(baseline JSON 최초 생성 · commit)의 마지막 선행 조건 해소. **4-게이트** — reviewer VERDICT=APPROVE(round 1) + PR comment 외화 + integrator 자체 점검 + PR CI 전 step success 로 4/4. 첫 CI run 은 reviewer-approval 게이트만 red(코멘트 post 전) → comment 후 `--failed` 재실행 green. **R-114 정산** — 직전 fire 잔량 `5e8f55d4` main run success, 본 fire 의 `ca5a2f99`(큐잉) main run success, 머지 commit `49bf4c7d` main run 은 pending — 위임 잔량 1 건으로 다음 turn 이 conclusion 확인. **claim 경로** — lock CAS 획득(`02:38:07Z`) → `reclaim-stale-claim.sh` 회수 대상 0 → `validate-claim-candidate.sh T-1591` PASS → `select-claim.sh` 단독 claim(server-time `02:47:55Z`, 활성 claim 0 이라 pr-mode 단독 조건 충족) → executor 가 PR open 직후 `sync-claim-pr.sh` 로 `PR_OPEN`/`1271` 동기(exit 0) → 머지 후 driver 가 claim prune, `claims.json` `[]` 복귀. **관측 메모** — `CLAIM_NOW` 에 GitHub `Date` 헤더를 RFC 1123 형식 그대로 주입해 `claimedAt` 이 ISO 8601 이 아닌 `Tue, 18 Aug 2026 02:47:55 GMT` 로 박혔다. `reclaim-stale-claim.sh` 의 `to_epoch` 가 GNU `date -d` 로 정상 파싱함을 실측 확인(회수 판정 무영향)했고 본 fire 에서 claim 을 prune 했으므로 잔여 risk 0 — 다음 fire 는 ISO 로 정규화해 주입할 것. **LOOP `§7.5` chain 미진입** — G1 · G2 는 충족하나 `nextTask` 가 비어 두 번째 task 진입에 planner 재dispatch 가 선행돼야 하고, 이어질 task 가 pr-mode 면 lock `since` 기준 60 분 stale 임계를 넘길 위험이 있어 §2.5 (d) 취지대로 본 task 완료로 fire 종료. humanQuestion **0** · blocker **0** · `concurrencyIncidents` 불변 · counters `tasksCompleted` **1581 → 1582**. **다음** — `nextTask` 비어 있음. 다음 fire 가 planner 를 dispatch 한다. 최우선 후보 = ADR-0056 `§Follow-ups (a)` 본체(체크인 baseline JSON 최초 생성 — 본 task 로 실측 승인 입력 확보 완료), 차순위 = `§Follow-ups (b)` 본체 `ci.yml` perf step 토글 on 편입, `§Follow-ups (c)` 임계 fix.
- 03:30 planner: queued **T-1592** — 체크인 baseline JSON 최초 생성·commit (`pr` mode, 2 파일 · 약 210 LOC). ADR-0056 `§Follow-ups (a)` **본체** — `§Decision 1`(저장 위치) · `§Decision 2`(갱신 주체) · 경로/조립/로그 primitive 가 이미 전부 박제돼 남은 일이 "파일 1 개를 실제로 확정"뿐이고, 마지막 선행 조건이던 `§Consequences (d)` 승인 입력(실측 p50/p95/p99)은 T-1591 이 PR #1271 CI 로그로 열었다. 그 줄을 **전사만** 해 `test/perf/baselines/baseline-ci-realdb-person-read.json` 1 개를 확정하고, 정본 경로(`resolveCheckinBaselinePath`) · 직렬화 형태(`serializeBaselineReport` 단일 행) · 값 범위 · stale label 누적 0 을 감시하는 가드 spec `test/perf/checkin-baseline-file.spec.ts` 를 붙인다. 이로써 실측 축이 처음 `compared` 분기에 진입한다. `§Decision 3 (b)`(회귀 관찰만 · exit code 불변) 유지 — `count=3` 3 표본이라 이후 `regressed=true` 관측이 잦을 수 있음을 PR 본문에 승인 근거로 박제하되 tolerance 재산정·fail 승격은 20 run 절차 소관이라 범위 밖. `ci.yml` 편입(`(b)`) · 임계 fix(`(c)`) · doc-sync 는 별도 slice 로 분리해 5 파일 cap 회피. `dependsOn` = T-1591. `STATE.nextTask = T-1592`.
- 04:00 driver: **T-1592 DONE** — `pr` mode, PR [#1272](https://github.com/myungjoo/Assessment-Agent/pull/1272) squash merge `e9817f4f` (2 파일 `+161/-0`, `src/` 0 LOC). ADR-0056 `§Follow-ups (a)` **본체 완결** — T-1591 이 PR #1271 CI 로그에 남긴 실측 줄을 전사만 해 `test/perf/baselines/baseline-ci-realdb-person-read.json` 첫 체크인 baseline 을 확정하고, 정본 경로(`resolveCheckinBaselinePath`) · 직렬화 형태(`serializeBaselineReport` 단일 행) · 값 범위 · stale label 누적 0 을 감시하는 가드 spec `test/perf/checkin-baseline-file.spec.ts` 6 it(happy 1 · error 1 · 분기 2 · negative 3) 을 붙였다. **성과** — CI `perf test` step 에서 실측 축이 처음으로 `outcome=compared regressed=false`(base p50/p95/p99 = 3.0/3.2/3.3ms, throughput 333.33req/s) 로 떨어져 그동안 `skipped(absent)` 였던 비교 경로가 열렸다. ADR-0056 `§Consequences` 의 "회귀 탐지 전제" 성립. **4-게이트** — reviewer VERDICT=APPROVE(round 1) + PR comment 외화 + integrator 자체 점검 + PR CI 전 step success 로 4/4. 전체 438 suite · 12546 test pass, `src/` 무변경이라 coverage 변동 0. **claim 경로** — lock CAS 획득(`03:38Z`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 → `validate-claim-candidate.sh T-1592` PASS → `select-claim.sh` 단독 claim(server-time `03:44:23Z`) → executor 가 PR open 직후 `sync-claim-pr.sh` 로 prNumber=1272 동기(exit 0) → 머지 후 driver 가 `lock_tree_cas_push` 로 claim prune, `claims.json` `[]` 복귀. 직전 fire 의 관측 메모대로 `CLAIM_NOW` 를 **ISO 8601 로 정규화**해 주입(직전 fire 는 RFC 1123 그대로 박혔었음) — `claimedAt` 이 `2026-08-18T03:44:23Z` 로 정상 박제. **R-114 정산** — 직전 fire 잔량 `49bf4c7d` main run **success** 확인 완료, 본 fire 의 `00d8ca31`(큐잉) main run success, 머지 commit `e9817f4f` main run 은 bookkeeping 시점 `in_progress` — 위임 잔량 1 건으로 다음 turn 이 conclusion 확인. **LOOP `§7.5` chain 미진입** — 본 fire 는 이미 planner 큐잉 + task 1 개(pr-mode)를 소화했고 lock `since` 기준 경과가 §2.5 (d) 45 분 임계에 근접해 fire 종료. humanQuestion **0** · blocker **0** · `concurrencyIncidents` 불변 · counters `tasksCompleted` **1582 → 1583**. **다음** — `nextTask` 비어 있음. 다음 fire 가 planner 를 dispatch 한다. 최우선 후보 = ADR-0056 `§Follow-ups (b)`(본체 `ci.yml` perf step 토글 on 편입 — `daily-test.sh` leg 를 건드리면 drift-guard smoke spec 3 개 동반으로 5 파일 cap 초과 주의), 차순위 = `§Follow-ups (c)` 임계 재산정(현 baseline `count=3` 3 표본 한계), `*-realdb`/`*-read` 계열 perf-spec 의 factory 배선.

- 04:45 planner: queued **T-1593** — 체크인 baseline 실측 축 표본 수 상향 (`REAL_CLOCK_ITER` 3 → 20) + 표본 수 계약 가드 (`pr` mode, 1 파일 · 약 120 LOC). T-1592 가 확정한 첫 체크인 baseline 은 `count=3` 으로 만들어졌고 실측 국면도 여전히 3 표본이라 p95 · p99 가 사실상 최댓값 1 개와 동일해진다 — ADR-0056 `§Decision 3 (b)` 가 말한 공유 runner 의 wall-clock 비결정성이 그대로 지표에 실려 회귀 관찰이 잡음에 지배된다. 본 slice 는 그 잡음원을 먼저 제거해 `§Decision 5`(표본 축적) · `§Follow-ups (c)`(임계 재산정) 의 선행 조건을 만든다. tolerance 재산정 자체는 20 run 절차 소관이라 범위 밖, `ci.yml` · `daily-test.sh` 는 drift-guard smoke spec 3 개 동반으로 5 파일 cap 이 터지므로 명시적 Out of Scope(T-1122 전례). `dependsOn` = T-1591 · T-1592. `STATE.nextTask = T-1593`.

- 05:05 driver: **T-1593 DONE** — `pr` mode, PR [#1273](https://github.com/myungjoo/Assessment-Agent/pull/1273) squash merge `7979bb2b` (1 파일 `test/perf/person-read-realdb.perf-spec.ts` `+106/-7`, `src/` 0 LOC). 실측 축 반복수를 3 → 20 으로 올리고 근거 · 한계 · 비용 3 줄 주석을 박제, 표본 수 하한 상수 `REAL_CLOCK_ITER_MIN` 을 도입해 전 국면이 리터럴 대신 상수 기준으로 단언하게 했다. **성과** — CI `perf test` step 에 20 표본 실측 줄이 처음 찍혔다: `[ci-realdb-person-read] p50=2.1ms p95=3.5ms p99=5.5ms tput=400.00req/s err=0.00% count=20 pass=true`. 3 표본 degenerate p95/p99 국면이 해소돼 ADR-0056 `§Follow-ups (c)`(임계 재산정)의 입력이 의미를 갖게 됐다. **R-112** — happy(예외 0 · 로그 1 회 · `count === REAL_CLOCK_ITER`) · error(절반 reject 시 시도 표본 20 유지 + `errorRate` 전사) · 분기(`compared`/`skipped` 수용 + `disabled` 유지) · negative 3 종(표본 수 하한 가드 · 0/음수 입력 · `p50<=p95<=p99`). 전체 438 suite · 12546 test pass, `src/` 무변경이라 coverage 변동 0. **4-게이트** — reviewer VERDICT=APPROVE + PR comment 외화 + integrator 자체 점검 + PR CI green 으로 4/4. round 2 는 nit-in-PR closure(CLAUDE.md `§3`) — `compared` 분기 로그에 `count=` 가 실리지 않던 문제를 happy 국면 `formatBaselineLine` 1 줄로 닫았다. **claim 경로** — lock CAS 획득(`04:38:07Z`) → 활성 claim 0 → `validate-claim-candidate.sh T-1593` PASS → `select-claim.sh` 단독 claim(server-time `04:43:01Z`, ISO 정규화 주입, pr-mode 단독 조건 충족) → lock-free 실행 → 머지 후 driver 가 `lock_tree_cas_push` 로 prune, `claims.json` `[]` 복귀. **관측 메모** — executor 에게 `claims.json` 무접촉을 지시한 탓에 `sync-claim-pr.sh` 의 PR-open 시점 `prNumber` 동기가 생략됐다. 본 fire 는 executor 가 머지까지 마치고 driver 가 즉시 prune 했으므로 잔여 risk 0 이나, 도중 사망 시 `prNumber` 없는 claim 이 남아 resume 대신 중복 PR 위험이 생긴다 — 다음 fire 부터는 executor 에게 `sync-claim-pr.sh` **만** 예외 허용으로 지시할 것. **R-114 정산** — 직전 fire 잔량 `e9817f4f` main run **success** 확인 완료, 본 fire 의 `47fa06a1`(큐잉) main run 은 확인 위임, 머지 commit `7979bb2b` main run 은 bookkeeping 시점 pending — 위임 잔량 2 건으로 다음 turn 이 conclusion 확인. **LOOP `§7.5` chain 미진입** — 본 fire 는 이미 planner 큐잉 + task 1 개(pr-mode)를 소화했고 lock `since`(`04:38Z`) 기준 경과가 §2.5 (d) 45 분 임계에 근접해 fire 종료. humanQuestion **0** · blocker **0** · `concurrencyIncidents` 불변 · counters `tasksCompleted` **1583 → 1584**. **다음** — `nextTask` 비어 있음. 다음 fire 가 planner 를 dispatch 한다. 최우선 후보 = `baseline-ci-realdb-person-read.json` 을 20 표본 값으로 갱신하는 `pr` task(T-1593 Follow-up 예고 — ADR-0056 `§Decision 2 (b)/(c)` 측정 조건 변경 절차, `checkin-baseline-file.spec.ts` 의 `count` 단언 동반), 차순위 = `§Follow-ups (b)` 본체 `ci.yml` perf step 토글 on 편입, `*-realdb`/`*-read` 계열 perf-spec 의 factory 배선.
