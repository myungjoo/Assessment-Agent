# 2026-07-31 (UTC)

- 00:45 planner: queued **T-1337** — [api.md](../architecture/api.md) § 5 **125 행**(`POST /api/admin/import`)에 성공·실패 status 실측 보강. [T-1336](../tasks/T-1336-api-doc-status-code-413-row.md) Out of Scope 가 "§ 5 소관의 별도 slice" 로 명시 이월한 gap — 125 행은 `restoreSummary` 응답 조립만 적고 성공/실패 status 를 **한 개도** 적지 않아, 바로 아래 126 행(preview: "응답 200 … 실패 400/401/403/413")과 비대칭이다. 정작 실 복원을 수행해 실패 표면이 더 넓은 쪽이 비어 있는 역전. 채울 내용: 성공 **201**(`@HttpCode` 미부착 = `@Post` 기본값, 실 `ImportJob` row 생성) + 실패 **400**(파일 미첨부 `MISSING_DUMP_FILE_MESSAGE` · DTO 위반 · 복원 중 P2003) / **401**·**403**(guard 가 `FileInterceptor` 보다 먼저 → 파일 파싱 전 차단) / **409**(`evaluateImportRaceGuard` blocking + 복원 중 P2002·P2025 — **preview 에 없는 create 전용 status**) / **413**(50 MiB 상한, `MulterExceptionFilter`) + 매핑 표 밖 error 는 raw 전파 → **500**. doc-only `commitMode: direct`, 약 16 LOC / 1 파일, independentStream `api-status-code-table`, dependsOn T-1336. 126 행·§ 6 표(163~176) 재수정 · § 6 409 행 "구체 분기는 P3" stale 전수 감사 · `src/` 리팩터(`@HttpCode(201)` 명시 부착 등)는 Out of Scope.
- 00:50 driver: **T-1336 DONE** — direct-mode, main direct commit `5c1d7414`([api.md](../architecture/api.md) § 6 표 +1/-0, 1 파일). 409 행과 500 행 사이에 `413 Payload Too Large` 행을 신설해, 실측상 `src/import/multer-exception.filter.ts` 가 `MulterError(LIMIT_FILE_SIZE)` → `PayloadTooLargeException` 으로 매핑하고 그 filter 가 import 업로드 2 종(`POST /api/admin/import` 236 행 · `/api/admin/import/preview` 309 행)에 `@UseFilters` 로 붙어 있는데도 § 6 표에 row 가 없던 gap 을 닫았다. 한 행에 발화 조건 · 적용 범위 2 종 · 상한 근거([ADR-0055](../decisions/ADR-0055-import-multipart-file-upload.md) §Decision 3 = 50 MiB `MAX_IMPORT_FILE_SIZE_BYTES`, DoS 표면 차단) 를 담고, preview 의 `@HttpCode(200)` 보다 filter(413) 가 우선한다는 점도 1 구절 박제. 기존 165~174 행 · § 5 126 행은 글자 그대로 보존, `src/`·`test/`·`web/` 무수정. doc-only 라 R-110 tester 면제 — 실측 3 종 재확인 + 표 구조(전 행 파이프 4 · bold status 행 11) · 숫자 오름차순(200→201→202→204→400→401→403→404→409→**413**→500) self-check + 검증 grep(1 hit / api.md 총 2 hit) 으로 대체. 실측 nuance: `@UseFilters(MulterExceptionFilter)` raw grep 은 3 hit 이나 201 행은 설명 주석이라 실 부착은 2 종 — task 본문과 실질 일치. counters 1326 → **1327**(origin+1).
- 00:50 driver: lock/claim 규율 — server-time `2026-07-31T00:37:08Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 를 RFC-1123 → ISO 변환해 `RECLAIM_NOW`/`CLAIM_NOW` 에 주입(claim 박제 후 `claimedAt: "2026-07-31T00:37:08Z"` 직접 확인 — midnight fallback 아님) → `acquire-lock.sh` CAS(tip `00356627`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim 0 < 2) → `validate-claim-candidate.sh` T-1336 PASS → select-claim + lock tombstone 동일 commit release → lock-free executor 1 회. direct task 라 PR·`sync-claim-pr` 무관. **R-114 이월 0**: 직전 fire 이월분 `8e8dc934`(run 30591474847) main CI **success** 를 본 turn [1] 에서 확인했고, 본 fire 의 `5c1d7414`(run 30594153899) 도 fire 안에서 **success** 확인. bookkeeping commit 은 doc-only 로 종료 시점 in_progress → 다음 fire [1] 에서 확인. **[7.5] chain 미진입** — G1(`flags.multiTaskFire=true`)·G2(holder=cron) 은 충족하나, 두 번째 후보 T-1337 이 본 fire 의 planner 가 방금 생성한 task 라 bookkeeping commit 의 CI 확인 경계(R-114)를 깨끗이 유지하려고 N=1 로 fire 종료했다(직전 fire 와 동일 판단).
- 01:20 planner: queued **T-1338** — [api.md](../architecture/api.md) § 5 **124 행**(`POST /api/admin/export`)에 성공·실패 status 실측 보강. [T-1337](../tasks/T-1337-api-doc-import-create-failure-statuses.md) Out of Scope 가 이월한 § 5 export slice — UC-07 블록(123~140 행)에서 유일하게 status 를 **하나도** 적지 않고 문장도 조사에서 끊긴 행이다(대칭 쌍인 125 행 import create 는 방금 201 + 5 종 실패로 채워졌다). 채울 내용: 성공 **201**(`@HttpCode` 미부착 = `@Post` 기본값, 실 `ExportJob` row `status=PENDING` 생성) + 실패 **400** 3 계열(`CreateExportDto` 위반·`forbidNonWhitelisted` / `requestedById` 공백 / `validateExportScope` verdict → `buildExportScopeRejection` 메시지) / **401**·**403**(`@CurrentUser("sub")` → `requestedById` 결합이라 발화자 위장 불가, REQ-045) + **409·404 부재** 명시(import create 와의 비대칭) + scope 오류 400 의 **매핑 경로 차이**(create 는 `ScopeInputExceptionFilter` 미부착 verdict 기반 vs describe-scope·preview-selection 은 filter 매핑) + 매핑 밖 Prisma error(P2003 FK 등) raw 전파 → **500**. doc-only `commitMode: direct`, 약 14 LOC / 1 파일, independentStream `api-status-code-table`, dependsOn T-1337. 125·135·136 행 재수정 · § 6 표 수정 · `src/` 리팩터(`@HttpCode(201)` 부착 · create 에 filter 부착)는 Out of Scope.
- 01:46 driver: **T-1337 DONE** — direct-mode, main direct commit `dd8d1e82`([api.md](../architecture/api.md) § 5 **125 행** +1/-1, 1 파일). `POST /api/admin/import` 행이 `restoreSummary` 응답 조립만 적고 성공·실패 status 를 **한 개도** 적지 않아 바로 아래 126 행(preview: "응답 200 … 실패 400/401/403/413")과 비대칭이던 gap 을 닫았다. 채운 내용: 성공 **201**(`@HttpCode` 미부착 = `@Post` 기본값, 실 `ImportJob` row 생성) + 실패 **400**(파일 미첨부 `MISSING_DUMP_FILE_MESSAGE` · DTO 위반 · 복원 중 P2003) / **401**·**403**(guard 가 `FileInterceptor` 보다 먼저 → 파일 파싱 전 차단) / **409**(`evaluateImportRaceGuard` blocking + 복원 중 P2002·P2025) / **413**(50 MiB 상한 `MulterExceptionFilter`) + **409 는 create 전용** 이라는 비대칭 1 구절 + 매핑 표 밖 error raw 전파 → **500**. 기존 `T-1296 박제 (PR #1187)` 출처 표기와 응답 조립 서술은 문장 삭제 0 으로 보존, 126 행·§ 6 표(163~176)·`src/`·`test/`·`web/` 무수정. doc-only 라 R-110 tester 면제 — 실측 4 종 + 표 구조 self-check + 검증 grep 3 종(409@125 1 hit · `T-1296 박제` 1 hit · 126 행 diff 미등장)으로 대체. 실측 nuance: `@HttpCode` grep 이 주석 2 + decorator 1 = 3 hit 이나 **create 미부착** 사실은 동일. counters 1327 → **1328**(origin+1).
- 01:46 driver: lock/claim 규율 — server-time `2026-07-31T01:37:20Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 주입 → `acquire-lock.sh` CAS(tip `24ad608a`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim 0 < 2) → `validate-claim-candidate.sh` T-1337 PASS → select-claim(`claimedAt: "2026-07-31T01:37:48Z"` 직접 확인 — midnight fallback 아님) + lock tombstone 동일 commit release → lock-free executor 1 회 → 완료 후 `lock_tree_cas_push` 로 claim prune(claims.json `[]` 직접 write — jq 비의존, prune msg `=` 미포함). direct task 라 PR·`sync-claim-pr` 무관. **R-114**: 본 fire 의 `dd8d1e82`(run 30596926788) main CI **success** 를 fire 안에서 확인(이월 0), bookkeeping commit 은 다음 fire [1] 에서 확인. **[7.5] chain 미진입** — G1·G2 는 충족하나 두 번째 후보 T-1338 이 본 fire planner 산출물이라 R-114 경계 유지 목적으로 N=1 종료.
- 02:43 planner: queued **T-1339** — [api.md](../architecture/api.md) § 5 **136~137 행**(`POST /api/admin/backup` · `POST /api/admin/restore`)을 **conceptual placeholder(미구현)** 로 표기하고 합계를 동기. [T-1338](../tasks/T-1338-api-doc-export-create-statuses.md) Out of Scope 가 "나머지 UC-07 행 status 보강" 으로 이월했으나 실측 결과 **보강이 아니라 미구현 표기** 가 맞다 — `@Controller("api/admin` 은 export·import **2 종뿐**, backup/restore 는 controller·service·Prisma model 전부 부재이고 [requirements.md](../requirements.md) 49 행이 `REQ-030 … P7 / PLANNED` 로 못박았다. 그런데 두 행은 `DB backup 생성` 한 구절만 두어 shipped 행과 구별되지 않고, 그 탓에 153 행 합계 `shipped 기준 70` 과 155 행 집계 규칙 (3) `표 72 / shipped 70` 이 실제 **68** 과 2 만큼 어긋난다(T-1306 이 세운 규칙이 자기 숫자로 반증되는 상태). 채울 내용: 139~140 행 문체 정본(bold placeholder 토큰 + 사유 + 대체 경로 3 요소)에 맞춰 136 행에 "전량 dump 는 `scope=FULL` export + `:id/download` 로 이미 가능", 137 행에 "실 복원은 `POST /api/admin/import` `mode=REPLACE` 가 shipped" 를 병기 + 153 행(placeholder 2→4 행, 열거 2 개 추가, shipped 70→68, **72 endpoint 행은 불변**) · 155 행 (3) 항 숫자 동기. doc-only `commitMode: direct`, 약 10 LOC / 1 파일(변경 행 정확히 4), independentStream `api-status-code-table`, dependsOn T-1338. `src/` 구현(backup/restore controller 신설 — P7 소관) · § 7 191 행 cross-reference · 124~135 행 · § 6 표 · 139~140 행 재작성은 Out of Scope.
- 02:47 driver: **T-1338 DONE** — direct-mode, main direct commit `60206fa6`([api.md](../architecture/api.md) § 5 **124 행** +1/-1, 1 파일). UC-07 블록(123~140 행)에서 유일하게 status 를 **하나도** 적지 않고 문장도 조사에서 끊겨 있던 `POST /api/admin/export` 행을 대칭 쌍인 125 행(import create) 문체에 맞춰 채웠다. 채운 내용: 성공 **201**(`@HttpCode` 미부착 = `@Post` 기본값, 실 `ExportJob` row 생성) + 실패 **400** 3 계열(`CreateExportDto` 위반·`forbidNonWhitelisted` / `requestedById` 공백 / `validateExportScope` verdict → `buildExportScopeRejection` 메시지) / **401**·**403**(`@CurrentUser("sub")` → `requestedById` 결합, REQ-045) + **409·404 부재** 명시(import create 와의 비대칭) + scope 오류 400 의 **매핑 경로 차이**(create 는 `ScopeInputExceptionFilter` **미부착** verdict 기반 vs describe-scope·preview-selection 은 filter 매핑) + 매핑 밖 Prisma error(P2003 FK 등) raw 전파 → **500**. 기존 문구·UC-07 링크·`Admin+` 컬럼은 삭제 0 으로 보존, 125·132~133·166 행 및 `src/`·`test/`·`web/` 무수정. doc-only 라 R-110 tester 면제 — 실측 4 종 + 표 구조 self-check(124 행 파이프 6·총 229 행 편집 전후 동일, `"실패"` 20→21)로 대체. **실측 drift 1 건**: task 정의서가 describe-scope·preview-selection 을 135~136 행으로 적었으나 실제는 **132~133 행**(135 = `GET /api/admin/export/:id`, 136 = `POST /api/admin/backup`) — 실측을 정본으로 삼고 T-1338 Follow-ups 에 박제. counters 1328 → **1329**(origin+1).
- 02:47 driver: lock/claim 규율 — server-time `2026-07-31T02:37:06Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 주입 → `acquire-lock.sh` CAS(tip `e1f7eff6`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim 0 < 2) → `validate-claim-candidate.sh` T-1338 PASS → select-claim(`claimedAt: "2026-07-31T02:37:40Z"` 직접 확인 — midnight fallback 아님) + lock tombstone 동일 commit release → lock-free executor 1 회 → 완료 후 `lock_tree_cas_push` 로 claim prune(claims.json `[]` 직접 write — jq 비의존, prune msg `=` 미포함). direct task 라 PR·`sync-claim-pr` 무관. **R-114 이월 0**: 직전 fire 이월분 `64b2d531`(run 30597183026)·본 fire 의 `60206fa6`(run 30599564101) 둘 다 main CI **success** 를 fire 안에서 확인. bookkeeping commit 은 doc-only 로 다음 fire [1] 에서 확인. **[7.5] chain 미진입** — G1(`flags.multiTaskFire=true`)·G2(holder=cron) 은 충족하나 두 번째 후보 T-1339 가 본 fire planner 산출물이라 R-114 경계 유지 목적 N=1 종료(직전 3 fire 와 동일 판단).
- 03:45 driver: **T-1339 DONE** — direct-mode, main direct commit `f593116b`([api.md](../architecture/api.md) § 5 **136·137 행** + 153·155 행, +4/-4, 1 파일). `POST /api/admin/backup` · `POST /api/admin/restore` 두 행이 `DB backup 생성` 같은 한 구절만 두어 shipped endpoint 와 시각적으로 구별되지 않던 gap 을 139~140 행(UC-08) 문체 정본에 맞춰 닫았다. 채운 내용: 두 행에 **conceptual placeholder(미구현)** 3 요소(bold placeholder 토큰 + 미구현 사유 REQ-030 P7/PLANNED + 대체 경로) 표기 — 136 행에 "전량 dump 는 `scope=FULL` export + `:id/download` 로 이미 가능", 137 행에 "실 복원은 `POST /api/admin/import` `mode=REPLACE` 가 shipped" 병기 + 153 행 합계 선두 괄호 절(placeholder 2→4 행, shipped 70→**68**) · 155 행 집계 규칙 (3) 항(표 72 / shipped **68**) 동기. **T-1306 집계 규칙이 자기 숫자로 반증되던 2 만큼의 어긋남이 해소**됐다. 실측 4 종 재확인: `@Controller("api/admin` 은 export(`src/export/export.controller.ts:143`)·import(`src/import/import.controller.ts:175`) **2 종뿐**, backup/restore route 선언 **0**(spec fixture 문자열·주석뿐), `prisma/` backup 모델 **0**, METHOD 행 편집 전후 **72** 불변. 125·126·139·140·191 행 및 `src/`·`test/`·`web/` 무수정, 총 229 행 불변. doc-only 라 R-110 tester 면제 — 검증 grep 4 종(`conceptual placeholder` 4→6 · METHOD 72 불변 · `shipped 70` 0 hit · diff 변경 행 정확히 136·137·153·155)으로 대체. counters 1329 → **1330**(origin+1).
- 03:45 driver: lock/claim 규율 — server-time `2026-07-31T03:37:14Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 주입 → `acquire-lock.sh` CAS(tip `453d484a`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim **0** < 2 — 교집합 검사가 trivially 0, dependsOn T-1338 은 `64c52a87` bookkeeping 으로 머지 확인) → select-claim T-1339(`claimedAt: "2026-07-31T03:37:43Z"` 직접 확인 — midnight fallback 아님) + lock tombstone 동일 commit release → lock-free executor 1 회 → bookkeeping 위해 lock 재획득(tip `ee51d9a0`) 후 claim prune. direct task 라 PR·`sync-claim-pr` 무관. **R-114 이월 0**: 본 fire 의 `f593116b` main CI(run 30602120386) **success** 를 fire 안에서 확인. bookkeeping commit `ac8e4d5b` 의 run 은 doc-only 라 다음 fire [1] 에서 확인. **[7.5] chain 미진입** — G1(`flags.multiTaskFire=true`)·G2(holder=cron) 은 충족하나 두 번째 후보 T-1340 이 본 fire planner 산출물이고 (c) 직전 task CI 미확정이라 N=1 종료(직전 4 fire 와 동일 판단).
- 03:45 planner: queued **T-1340** — [api.md](../architecture/api.md) § 7 cross-reference 표 **191 행** UC-07 항목의 `POST /api/admin/backup` · `POST /api/admin/restore` 를 미구현 conceptual placeholder 로 표기해 § 5 136·137 행과 정합시킨다. T-1339 가 § 5 를 placeholder 로 바꾼 뒤 § 7 191 행만 미구현 표기 **0** 으로 남아 같은 endpoint 를 두 절이 다르게 서술하는 상태다. doc-only `commitMode: direct`, 약 4 LOC / 1 파일, independentStream `api-status-code-table`, dependsOn T-1339. 실측 baseline 박제: `conceptual placeholder` 6 hit → 편집 후 7, METHOD 행 72 불변, 총 229 행 불변, 191 행 pipe 4 불변.
- 03:48 driver: T-1339 코드 commit `f593116b` 의 main CI **success** 확인 → STATE.ci `running` → `green` 동기 (R-114 이월 0).
- 04:30 planner: queued **T-1341** — [api.md](../architecture/api.md) § 7 cross-reference 표 **192 행** UC-08 항목의 `GET /api/me/permission-denied` · `GET /api/admin/permission-denied` 를 미구현 conceptual placeholder 로 표기하고 실 shipped 대체 `GET /api/permission-denied-records` pointer 를 한 구절로 병기해 § 5 139~141 행과 정합시킨다. T-1340 이 191 행(UC-07)을 닫은 뒤 남은 § 7 마지막 gap 이며, 지금은 미구현 2 종만 나열돼 **UC-08 이 통째로 미구현인 것처럼 읽히는 오독**까지 생긴다. doc-only `commitMode: direct`, 약 4 LOC / 1 파일, independentStream `api-status-code-table`, dependsOn T-1340. 실측 baseline 박제: `conceptual placeholder` 7 hit → 편집 후 8, `permission-denied-records` 3 → 4, METHOD 행 72 불변, 총 229 행 불변, 192 행 pipe 4 불변, 153 행 `shipped 기준 68` 불변.
- 04:45 driver: **T-1340 DONE** — direct-mode, main direct commit `10715686`([api.md](../architecture/api.md) § 7 cross-reference 표 **191 행**, +1/-1, 1 파일). UC-07 행의 `POST /api/admin/backup` · `POST /api/admin/restore` 가 § 5 136·137 행에서는 **conceptual placeholder(미구현)** 로 표기됐는데 § 7 191 행은 미구현 표기 **0** 이라 같은 endpoint 를 두 절이 다르게 서술하던 gap 을 닫았다. 세 번째 셀 끝에 placeholder 식별 구절 + 대체 경로 pointer 1 구절만 추가(사유 전문은 § 5 유지). 첫 셀 UC-07 링크·둘째 셀 step 나열·나머지 6 endpoint 표기 불변, 136·137·139·140·153·155·192·194 행 무수정, 표 pipe 4·총 229 행 불변. doc-only 라 R-110 tester 면제 — 검증 grep(`conceptual placeholder` 6→7 · METHOD 행 72 불변 · `shipped 기준 68` 1 hit · 변경 행 정확히 191)으로 대체. counters 1330 → **1331**(origin+1).
- 04:45 driver: lock/claim 규율 — server-time `2026-07-31T04:37:06Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 주입 → `acquire-lock.sh` CAS(tip `ea5d2932`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim **0** < 2 — 교집합 검사 trivially 0, dependsOn T-1339 는 `ac8e4d5b` bookkeeping 으로 머지 확인) → select-claim T-1340(`claimedAt: "2026-07-31T04:37:26Z"` 직접 확인 — midnight fallback 아님) + lock tombstone 동일 commit release → lock-free executor 1 회 → bookkeeping 위해 lock 재획득 후 claim prune. direct task 라 PR·`sync-claim-pr` 무관. **R-114**: 직전 fire 이월분 `ac8e4d5b`·`179e6ebd` main CI 둘 다 **success** 확인, 본 fire 의 `10715686` run 은 push 직후 **in_progress** 라 다음 fire [1] 로 conclusion 확인 위임. **[7.5] chain 미진입** — G1(`flags.multiTaskFire=true`)·G2(holder=cron) 은 충족하나 두 번째 후보 T-1341 이 본 fire planner 산출물이고 (c) 직전 task CI 미확정이라 N=1 종료(직전 5 fire 와 동일 판단).
- 04:47 driver: T-1340 코드 commit `10715686` 의 main CI **success** 확인 → STATE.ci `running` → `green` 동기 (R-114 이월 0).
- 05:35 planner: queued **T-1342** — [api.md](../architecture/api.md) § 7 cross-reference 표 **185 행(UC-01)** · **190 행(UC-06)** 의 never-built route 4 종(`POST /api/assessments/run` · `DELETE /api/assessments` · `POST /api/assessments/reeval` · `POST /api/assessments/reset`)을 `**shipped 아님 (never-built)**` 로 표기하고 실 이관 경로 pointer 를 병기해 § 5 96~99 행과 정합시킨다. [T-1341](../tasks/T-1341-api-doc-uc08-crossref-placeholder.md) Out of Scope 가 "별개 부류 — 문구 자체가 달라야 한다" 로 명시 이월한 마지막 § 7 미shipped-표기 gap 이며, 두 행 모두 셀 전체가 미구현 경로뿐이라 **UC-01·UC-06 이 통째로 미구현인 것처럼 읽히는 오독** 이 191/192 행보다 크다(실제로는 둘 다 shipped). 실측 확인: `src/user/assessment.controller.ts` route 는 `@Get()`·`@Get(":id")`·`@Post()`·`@Delete(":id")` **4 개뿐**(run·reeval·reset·bulk delete 0), 대체 경로 4 종은 전부 shipped(`@Post("collect")` · `@Post("period")` · `@Post("unevaluated-fill-run")` · `@Post("recent-deletion/:personId")`). doc-only `commitMode: direct`, 약 4 LOC / 1 파일(변경 행 정확히 2), independentStream `api-status-code-table`, dependsOn T-1341. baseline: `never-built` 6 → 8, `conceptual placeholder` **8 불변**, METHOD 행 72 불변, 총 229 행 불변, 185/190 행 pipe 4/4 불변. § 5 96~99 행·191·192 행 재수정 · 153·155 합계 · 186~189 행 전수감사 · `src/` 실구현은 Out of Scope.
- 05:47 driver: **T-1341 DONE** — direct-mode, main direct commit `16d6e71b`([api.md](../architecture/api.md) § 7 cross-reference 표 **192 행**, +1/-1, 1 파일). UC-08 셀이 미구현 route 2 종(`GET /api/me/permission-denied` · `GET /api/admin/permission-denied`)만 표기 **0** 으로 나열해 § 5 139~140 행의 placeholder 표기와 어긋나고, 실 shipped 대체 경로가 표에 아예 없어 **UC-08 이 통째로 미구현인 것처럼 읽히는 오독**(191 행보다 비용이 큰 정반대 오독)까지 유발하던 gap 을 닫았다. T-1340 이 191 행에 확정한 문체를 그대로 따라 나열 끝에 한 구절만 append — 2 종이 **conceptual placeholder**(미구현)임 + 사유 정본은 § 5 139~140 행 pointer + 실 대체 수단은 shipped `GET /api/permission-denied-records`(단일 endpoint 가 `actor.role` 로 audience 차등, 두 route 를 하나로 흡수). RBAC 계약 전문 복제 0(§ 5 141 행·[ADR-0023](../decisions/ADR-0023-permission-denied-audit-query-rbac-contract.md) 유지). 첫 셀 UC-08 링크·둘째 셀 step 나열·기존 2 route 표기/순서 불변, 139·140·141·153·155·191·194 행 및 `src/`·`test/`·`web/`·`prisma/` 무수정, 표 pipe 4·총 229 행 불변. doc-only 라 R-110 tester 면제 — 검증 grep 6 종(`conceptual placeholder` 7→8 · METHOD 행 72 불변 · `permission-denied-records` 3→4 · `shipped 기준 68` 1 hit 불변 · 192 행 pipe 4 · diff hunk `@@ -192 +192 @@` 단 1 개)으로 대체. 실측 재확인 3 종도 task 본문과 일치(`api/me`·`api/admin` permission-denied controller **0 hit** — admin 은 export·import 2 종뿐 / `permission-denied-records` controller 1 hit). **§ 7 표의 placeholder 부류 미구현-표기 gap 은 이로써 전부 닫혔다**(never-built 부류 2 행은 T-1342 소관). counters 1331 → **1332**(origin+1).
- 05:47 driver: lock/claim 규율 — server-time `2026-07-31T05:37:17Z`(`gh api -i rate_limit` Date 헤더, MSYS leading-slash 회피) 주입 → `acquire-lock.sh` CAS(tip `5d06effc`) → `reclaim-stale-claim.sh` 회수 대상 0 → maxConcurrentClaims 게이트(활성 claim **0** < 2 — 교집합 검사 trivially 0, dependsOn T-1340 은 `10715686`·`90ae0d32` 로 머지 확인) → select-claim T-1341(`claimedAt: "2026-07-31T05:38:00Z"` 직접 확인 — midnight fallback 아님) + lock tombstone 동일 commit release → lock-free executor 1 회 → bookkeeping 위해 lock 재획득 후 claim prune(claims.json `[]` 직접 write — jq 비의존, prune msg `=` 미포함). direct task 라 PR·`sync-claim-pr` 무관. **R-114 이월 0**: 본 fire 의 `16d6e71b` main CI(run 30607408028) **success** 를 fire 안에서 확인. bookkeeping commit 의 run 은 doc-only 라 다음 fire [1] 에서 확인. **[7.5] chain 미진입** — G1(`flags.multiTaskFire=true`)·G2(holder=cron) 은 충족하나 두 번째 후보 T-1342 가 본 fire planner 산출물이라 R-114 경계 유지 목적으로 N=1 종료(직전 6 fire 와 동일 판단).
