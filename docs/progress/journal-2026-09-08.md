# Journal — 2026-09-08 (UTC)

- 00:53 driver: **T-1963 DONE (pr, PR [#1540](https://github.com/myungjoo/Assessment-Agent/pull/1540) → main [`3587309e`](https://github.com/myungjoo/Assessment-Agent/commit/3587309e))** — `POST /api/schedules/recent-deletion/:personId` e2e 계약 spec 을 신설해 요구표 `60 행` REQ-041 이 선언만 하고 실재하지 않던 e2e 축을 닫았다. `test/e2e/schedules-recent-deletion.e2e-spec.ts` 1 파일 +274/-0, production diff 0 — T-1958 backfill spec 패턴을 1:1 mirror 해 no-network(빈 `serviceIdentities` Person seed) 전략을 쓰고 기존 auth-e2e-helper · db-truncate 를 무수정 재사용, 신규 helper 0. R-112 4 축 전부 cover — happy 2(202 + 3 key + `deletedCount` 0 + `recollected:true` + 재수집 Assessment 1 건, `days:7` 명시 축) · error 2(부재 personId 404 raw forward · 쿠키 부재 401) · 분기 7(window 밖 instant · 빈 배열 no-op `recollected:false` + ValidationPipe 400 4 종) · negative 2(User 403 · SuperAdmin 202 escalation, 실패 경로 Assessment 0 건). 로컬 `pnpm lint && pnpm build && pnpm test`(480 suite · 13978 test) + `test:cov` 통과, 로컬 `pnpm test:e2e` 는 `DATABASE_URL` 부재로 불가해 CI e2e job(34 suite · 477 test, `PASS test/e2e/schedules-recent-deletion.e2e-spec.ts`)에 위임했다. 4-게이트 — reviewer APPROVE + PR comment 외부 존재 + integrator 자체 점검 + CI green 전부 충족, round 1 에 squash merge + 브랜치 삭제.
- 00:53 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1962 commit `ce6ab9d3` 의 main run(`34170864324`)은 `success` 로 정산했다. 본 fire 의 머지 commit `3587309e` main run(`34174660307`)은 turn 종료 시점 `pending` 이라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 00:53 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T00:37:46Z`(`gh api -i rate_limit` Date 헤더, 非midnight) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`ba726aa6`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 00:38:10Z, midnight fallback 없음) → 즉시 lock tombstone release → lock-free executor 진행(약 12 분, claim TTL 60 분 이내). bookkeeping 위해 lock 재획득 + `claims.json` prune. executor 가 PR flow 를 흡수해 반환하므로 PR-open 시점 `sync-claim-pr.sh` 는 driver 가 별도로 돌리지 못했다(기지 편차, MEMORY `executor-absorbs-pr-flow-syncclaim-gap`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동.
- 00:53 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-1963 을 소비) 직전 task 가 `pr` 이라 ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. T-1963 Follow-ups 2 건(① `days > 366` RangeError→500 현행 동작 적절성 판정 ② 머지 후 REQ-041 상태 칸 e2e 좌표 doc-sync 1 회)은 planner 후보로 남는다.
- 01:00 planner: queued **T-1964** — REQ-041 상태 칸 근거 공백 정정(recent-deletion unit·e2e 좌표 doc-sync, direct, `docs/requirements.md` 1 파일). T-1963(PR #1540 → main `3587309e`) 머지로 e2e 근거가 생겨 PLAN `183 행` once-rule 의 REQ-041 재판정 1 회 시점이 열렸다.
- 01:45 driver: **T-1964 DONE (direct commit `d60e74e6`)** — `docs/requirements.md` `60 행` REQ-041 상태 칸에 구현 4 축(controller `96~99 행` · runner `62 · 94 행` · window `19 · 21 · 48 행` · module `64 · 89 · 96 행`) + e2e 좌표(`test/e2e/schedules-recent-deletion.e2e-spec.ts`, T-1963/PR #1540/`3587309e` 출처)를 1 회 박제했다. 계수는 실행 시점 재검산(unit 5 spec 70 it, e2e describe 1 · it 8 + it.each 5 종). 1 파일 +1/-1, `src/`·`test/` diff 0 이라 R-110 · R-112 direct doc-only 면제.
- 01:45 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1963 머지 commit `3587309e` 의 main run(`34174660307`)은 `success` 로 정산했다. 본 fire 의 `d60e74e6` main run(`34177464354`)은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 01:45 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T01:37:44Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`a4e96413`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 01:37:57Z) → 즉시 lock tombstone release → lock-free executor 진행(약 3 분, claim TTL 60 분 이내). bookkeeping 위해 lock 재획득(`f0d82edd`) + `claims.json` prune(`8093f112`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동.
- 01:45 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) · G2(holder cron) 충족이나 chain 판정 시점 `STATE.nextTask` 가 비어 있어(본 fire 가 T-1964 를 소비) ADR-0020 Decision (2) 의 "nextTask 부재 → 종료" 로 두 번째 task 에 진입하지 않았다. 이어서 planner 를 dispatch 해 다음 fire 용 task 를 큐잉한다. executor 가 남긴 nit 후보(`test/e2e/schedules-recent-deletion.e2e-spec.ts` `206 행` 주석 "ValidationPipe 거부 4 종" 이 실제 table 5 종과 불일치)는 planner 후보로 남는다.
- 01:47 planner: queued **T-1965** — recent-deletion endpoint 입력 결함 500 → 400 매핑 filter 신설(pr, `src/scheduling/recent-deletion-input-exception.filter.ts` + colocated spec + controller `@UseFilters` 배선 + 배선 검증 it, 4 파일). T-1963 Follow-up ① 판정 결과 = 현행 결함 — `days > 366` 이 `assertValidDays` 의 `RangeError` 로 500 이 되는데 controller `27~30 행` 주석은 이미 400 을 선언한다. T-1328 `ScopeInputExceptionFilter` 패턴 1:1 mirror 라 ADR 신설 0 · domain throw 변경 0.
- 02:58 driver: **T-1965 DONE (pr, PR #1541 → main `9ecc7736`)** — `POST /api/schedules/recent-deletion/:personId` 의 입력 결함이 500 으로 새던 결함을 controller 경계 filter 로 닫았다. `src/scheduling/recent-deletion-input-exception.filter.ts` 신설(`@Catch()` + `toHttpException` 4 분기: HttpException passthrough / `RangeError`·`TypeError` → 400 / unknown → 500) + 소비처 동반 의무대로 같은 PR 에서 `recent-deletion.controller.ts` 에 `@UseFilters` 배선 + `27~30 행` raw forward 계약 주석 정정. T-1328 `ScopeInputExceptionFilter` 1:1 mirror 라 ADR 신설 0, domain layer(`-window`/`-plan`/`-runner`) · DTO 무접촉. 4 파일 +296/-4, 481 suite / 13996 test green(line 99.94% · function 100%), reviewer round 1 APPROVE.
- 02:58 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1964 direct commit `d60e74e6` 의 main run(`34177464354`)은 `success` 로 정산했다. 본 fire 의 PR CI run(`34181395907`)은 pass 로 4-게이트 충족(reviewer comment 외화 + approval 검증 step success). 머지 commit `9ecc7736` 의 main run 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 02:58 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T02:38:06Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`06c88753`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 조건(§D8 (e)) 충족 → `select-claim.sh` atomic claim(`claimedAt` 02:38:09Z) → 즉시 lock tombstone release → lock-free executor 진행(약 19 분, claim TTL 60 분 이내). `sync-claim-pr.sh T-1965 1541` exit 0 으로 claim `prNumber` 동기. bookkeeping 위해 lock 재획득(`cec577a8`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동.
- 03:12 planner: queued **T-1966** — recent-deletion `days` 상한 초과 400 e2e 계약 고정 + 거부 종수 주석 drift 정정(pr, `test/e2e/schedules-recent-deletion.e2e-spec.ts` 1 파일). T-1965 Follow-up (a)+(b) 를 한 slice 로 닫는다 — filter 의 400 매핑이 unit 축에만 있어 배선 회귀 시 500 재발을 잡을 실 HTTP 축이 없었다. 신규 축: `days: 400` → 400(안내 prefix + `366` 원문 결합 · Assessment 0) / `days: 366` 경계 → 202(정상 요청 downgrade 0) / User + `days: 400` → 403(guard 우선). pre-check(origin/main `a4d617bf`): e2e 상한 축 0 hit · controller `107 행` `@UseFilters` 배선 확인 · `206 행` "4 종" ↔ table 5 원소 drift 잔존. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web` 무접촉, 신설 helper 0).
- 03:52 driver: **T-1966 DONE (pr, PR #1542 → main `bf56722a`)** — T-1965 가 신설한 `RecentDeletionInputExceptionFilter` 의 400 매핑을 실 HTTP 왕복으로 고정했다. `test/e2e/schedules-recent-deletion.e2e-spec.ts` 1 파일 +74/-2 로 e2e 3 종 추가(`days: 400` → 400 + 안내 prefix·`366`·`400` 원문 결합 · Assessment 0 / `days: 366` 경계 → 202 정상 downgrade 0 / User + `days: 400` → 403 guard 우선) + `206 행` 주석 "ValidationPipe 거부 4 종" → 5 종 drift 정정 및 filter layer 구분 1 줄. `src/` 무접촉이라 전역 coverage 불변, 481 suite / 13996 test green. reviewer round 1 APPROVE, nit(상수 배치)은 §3 nit-in-PR closure 로 같은 PR 두 번째 commit 처리.
- 03:52 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1965 머지 commit `9ecc7736` 의 main run 은 `success` 로 정산했다(기본 검사 · 배포 산출물 검증 둘 다). 본 fire 의 PR CI run(head-sha `4bc82cb6`, run `34184507712`)은 success 로 4-게이트 충족(reviewer comment 외화 + approval 검증 step success). 첫 branch run 이 cancelled 인 것은 후속 push 에 의한 supersede 로 benign. 머지 commit `bf56722a` 의 main run 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 03:52 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T03:37:46Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `validate-claim-candidate.sh T-1966` PASS → `select-claim.sh` atomic claim(`claimedAt` 03:38:00Z, lock-하) → 즉시 lock tombstone release → lock-free executor 진행(약 12 분, claim TTL 60 분 이내). `sync-claim-pr.sh T-1966 1542` exit 0 으로 claim `prNumber` 동기. bookkeeping 위해 lock 재획득(`72184ef5`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 03:52 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있으며(본 fire 가 T-1966 을 소비), 머지 commit main run 이 `in_progress` 라 (c) fail-fast 회피 관점에서도 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 03:57 planner: queued **T-1967** — `GET /api/llm/providers` 목록 조회 e2e 계약 spec 신설(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 신규). 전 controller 22 개 중 e2e 참조가 0 인 둘(`llm-provider-config` · `cron-schedule`) 중 하나를 T-1960 동형으로 닫는다. 축: Admin 200 + 7 키 노출 + **`apiKey` 미노출**(ADR-0014 §3 never-read-back 회귀 감지) / 0 건 → 200 빈 배열(404 변환 안 함) / SuperAdmin 200 escalation / User 403 / 쿠키 부재 401 / 변조 JWT 401. pre-check(origin/main `c690aeaf`): `test/` 의 `api/llm/providers` hit 5 건이 전부 `test/perf`·`test/load` 라 **`test/e2e/` hit 0**, controller `77`·`95 행` route 실재. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web`·`requirements.md` 무접촉, 신설 helper 0). 단건 `:id` 축 · `isDefault` 분기 · 쓰기 4 축은 Out of Scope(후속 slice).
- 05:00 driver: **T-1967 DONE (pr, PR #1543 → main `b2a6ce5d`)** — `GET /api/llm/providers` 목록 조회 e2e 계약 spec 신설(`test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +237/-0, 7 케이스). Admin 200 + view 7 key / **`apiKey` 미노출**(키 부재 + 원문 미포함 이중 단언 — ADR-0014 §3 never-read-back 회귀 감지) / 0 건 200 빈 배열 / SuperAdmin 200 / User 403 / 쿠키 부재 401 / 변조 JWT 401. `afterEach` 는 truncateAll 뒤 `llmDefaultProvider` → `llmProviderConfig` 순 deleteMany 로 Restrict FK 를 자식→부모 순서 해소. `src` · `web` · 기존 spec 무접촉이라 전역 coverage 변동 0. reviewer APPROVE round 1/7, 4-게이트 PASS 후 squash 머지 + branch 삭제.
- 05:00 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1966 머지 commit `bf56722a` 의 main run `34184909020` 은 `success` 로 정산했다. 본 fire 의 PR #1543 CI 는 reviewer approval 검증 step 포함 green 이라 4-게이트 충족. 머지 commit `b2a6ce5d` 의 main run `34188687580` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 05:00 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T04:38:32Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 허용(§D8 (e)) → `select-claim.sh` atomic claim(`claimedAt` 04:38:32Z, lock-하) → 즉시 lock tombstone release → lock-free executor 진행(약 16 분, claim TTL 60 분 이내). claim `prNumber` 는 executor 내 integrator 가 1543 으로 동기(status PR_OPEN). bookkeeping 위해 lock 재획득(`6808eb6c`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 05:00 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있으며(본 fire 가 T-1967 을 소비), 머지 commit main run 이 `in_progress` 라 (c) fail-fast 회피 관점에서도 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 05:15 planner: queued **T-1968** — `/api/schedules` cron 스케줄 REST e2e 계약 spec 신설(pr, `test/e2e/schedules-cron.e2e-spec.ts` 1 파일 신규). T-1967 이 남긴 "e2e 참조 0 인 마지막 controller" 인 `CronScheduleController` 를 동형으로 닫는다. 축: PUT→GET→DELETE→GET lifecycle(200/200/204) · 동일 name 재등록 = 교체(중복 0) · 0 건 `GET` 200 `[]`(404 아님) · 유효하지 않은 cron 식/공백 name 400 · 부재 name DELETE 404 · `POST trigger` 202 · RBAC 4 route × (User 403 / 쿠키 부재 401 / 변조 JWT 401) `it.each` 압축 + SuperAdmin escalation. pre-check(origin/main `07cf952d`): `test/e2e/` 의 cron hit 은 무관 주석 1 건뿐이라 실 e2e 축 0, controller `70`·`90`·`103`·`120`·`140 행` route 실재, `@Cron` 데코레이터 0 이라 초기 `GET` 이 `[]` 보장. 요구표 `58 행` REQ-039 는 검증 방법에 e2e 를 선언하나 실재하지 않던 공백. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web`·`src`·`requirements.md` 무접촉, 신설 helper 0). REQ-039/040 상태 칸 재판정은 §3.1 규칙 6 대로 머지 후 1 회.
- 06:00 driver: **T-1968 DONE (pr, PR #1544 → main `92726756`)** — `/api/schedules` cron 스케줄 REST e2e 계약 spec 신설(`test/e2e/schedules-cron.e2e-spec.ts` 1 파일 +299/-0). PUT→GET→DELETE→GET lifecycle(200/200/204/200+`[]`) · 동일 name 재등록 = 교체(중복 0) · 등록 0 건 `GET` 200 빈 배열 · 유효하지 않은 cron 식/공백 name 400 · 부재 name DELETE 404 · `POST trigger` 202 · ValidationPipe `it.each` 6 케이스 · RBAC 4 route × (User 403 / 쿠키 부재 401 / 변조 JWT 401) 12 negative + SuperAdmin escalation + 실패 시 registry 불변. 기존 helper(`createAuthenticatedE2EApp` · `buildAuthCookie`)만 재사용해 신설 helper 0, cron 식이 매일 03:00/04:00 이라 spec 실행 중 발화 0 · sleep 0, `afterEach` registry 정리. `src` · `web` · 기존 spec 무접촉이라 전역 coverage 변동 0. **이로써 controller 22 개 중 e2e 참조 0 인 controller 가 소멸**했다. reviewer APPROVE round 1/7, 4-게이트 PASS 후 squash 머지 + branch 삭제. nit 1 건(`it.each` 배열 표를 `util.format` 이 test 이름에 route 객체째 덤프) 은 §3 nit-in-PR closure 로 같은 PR 에서 object 표 + `$routeLabel` 로 교정.
- 06:00 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1967 머지 commit `b2a6ce5d` 의 main run `34188687580` 은 `success` 로 정산했다. 본 fire 의 PR #1544 CI run `34192068726` 은 reviewer approval 검증 step 포함 green 이라 4-게이트 충족. 머지 commit `92726756` 의 main run `34192554584` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 06:00 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T05:37:55Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 pr-mode 단독 claim 허용(§D8 (e)) → `select-claim.sh` atomic claim(`claimedAt` 05:37:55Z, lock-하) → 즉시 lock tombstone release → lock-free executor 진행(약 18 분, claim TTL 60 분 이내). claim `prNumber` 는 executor 내 integrator 가 1544 로 동기(status PR_OPEN). bookkeeping 위해 lock 재획득(`4b72d3ba`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 06:00 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있으며(본 fire 가 T-1968 을 소비), 머지 commit main run 이 `in_progress` 라 (c) fail-fast 회피 관점에서도 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 06:07 planner: queued **T-1969** — REQ-039 · REQ-040 상태 칸 근거 공백 정정(`direct` doc-only, `docs/requirements.md` `58 행`·`59 행` 1 파일). T-1968 e2e 머지로 두 행이 선언만 하던 `unit + e2e` / `e2e` 축에 실 근거가 생겼으므로 §3.1 규칙 6(구현 후 REQ 당 1 회) 대로 좌표를 박제한다 — 구현 축(`cron-schedule.controller.ts` `90`·`103`·`120`·`140 행` route + `@HttpCode` + Admin tier · `cron-schedule.service.ts` SchedulerRegistry wrapper · `scheduling.module.ts` `CRON_TICK_HANDLER` provider) · e2e 축(`test/e2e/schedules-cron.e2e-spec.ts` describe/it 실계수 + lifecycle·교체·빈 배열·400/404/202·RBAC, 출처 T-1968 · PR #1544 · main `92726756`) · 한계 2 종(registry in-memory 라 재기동 시 소실 / tick handler 가 logging stub 이라 202 는 수락만). pre-check(origin/main `3fcda201`): e2e 파일 9 hit 실재 · 두 행 좌표 문자열 0 · REQ-039/040 언급 task 전부 DONE(중복 0) · T-1969 ID 미사용. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web`·`src`·워크플로 무접촉, helper 신설 0). REQ-051 재판정(T-1967 예약분)은 Out of Scope 로 별도 task 에 남긴다.
- 06:45 driver: **T-1969 DONE (direct, main `04f9b29d`)** — T-1968 e2e 머지로 실 근거가 생긴 `docs/requirements.md` `58 행`(REQ-039) · `59 행`(REQ-040) 상태 칸에 §3.1 규칙 6 대로 REQ 당 1 회 좌표를 박제했다. 구현 축(`cron-schedule.controller.ts` route + `@HttpCode` · `cron-schedule.service.ts` SchedulerRegistry wrapper · `scheduling.module.ts` `CRON_TICK_HANDLER` provider) · e2e 축(`test/e2e/schedules-cron.e2e-spec.ts`, 출처 T-1968 · PR #1544 · main `92726756`) · 한계 각 3 종을 함께 적었다. 계수는 task 파일 숫자를 베끼지 않고 실행 시점 재검산했고(unit 51 it + `it.each` 8 행 · e2e 8 it + `it.each` 18 행), task 파일이 예상한 "`web/` 노출 0" 은 실측에서 거짓이라 한계 문구 대신 소비처 좌표(`web/src/views/adminScheduleRunners.ts` `48`·`154 행`)로 정정해 적었다. `docs/requirements.md` +2/-2 (+ task `status:` 1 행) 로 코드 축 diff 0 — R-110 면제 요건 충족.
- 06:45 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1968 머지 commit `92726756` 의 main run `34192554584` 는 `success` 로 정산했다(뒤이은 `3fcda201` · `8e857330` main run 도 success). 본 fire 의 direct commit `04f9b29d` 의 main run `34195856892` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 06:45 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T06:37:52Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`b4285d53`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) 단독 claim 조건 충족 → `select-claim.sh` atomic claim(`claimedAt` 06:37:52Z) → 즉시 lock tombstone release → lock-free executor 진행(약 5 분, claim TTL 60 분 이내). direct task 라 PR 0 이므로 `sync-claim-pr.sh` 미호출. bookkeeping 위해 lock 재획득(`71150ae3`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 06:45 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있으며(본 fire 가 T-1969 를 소비), direct commit 의 main run 이 `in_progress` 라 (c) fail-fast 회피 관점에서도 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 06:52 planner: queued **T-1970** — LLM provider config 단건 조회 · 삭제 e2e 계약 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 describe 추가). T-1967 이 목록 축만 닫아 controller 6 handler 중 `:id` 경로 e2e 가 0 이었다. 축: `GET :id` 200 + view 7 key + **apiKey 미노출**(키 부재 + 원문 미포함 이중 단언, ADR-0014 §3) · `isDefault` false/true 2 분기 · 부재 id 404 2 종(GET/DELETE) · `DELETE` 204 + 후속 GET 404 + count 0 · **기본 provider 슬롯 점유 config DELETE → 409 + config·슬롯 둘 다 잔존**(ADR-0062 §Decision 2 의 `onDelete: Restrict` 회귀 감지 — 현재 unit P2003 매핑 축만 존재) · RBAC 2 route × (User 403 / 쿠키 부재 401 / 변조 JWT 401) `it.each` 6 + SuperAdmin escalation 200. pre-check(origin/main `5c15e1f5`): `test/e2e/` 의 `llm/providers` hit 3 건 전부 목록 축이라 `:id` e2e 0 · controller `141~144`·`199~203 행` route 실재 · service `156~159`·`297~309 행` 404/409 분기 실재 · schema `LlmDefaultProvider` 의 `@unique` + `onDelete: Restrict` 실재. 암호화 경로를 타는 `POST`·`PATCH`·`PUT default` 는 `LLM_APIKEY_ENC_KEY` 주입 결정 선행이라 Out of Scope. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web`·`src`·`requirements.md` 무접촉, 신설 helper 0). REQ-051(+REQ-043) 재판정은 본 slice 머지 후 1 회로 예약.
- 08:11 driver: T-1970 **DONE** — LLM provider config 단건 조회 · 삭제 e2e 계약 고정(pr, PR #1545 → main `5e05de31`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +259/-1). 기존 목록 describe 무수정으로 파일 하단에 `:id` 축 top-level describe 1 개(케이스 13, `it.each` 6 포함)를 append 했다 — `GET :id` 200 + view 7 key + apiKey 미노출 이중 단언 · `isDefault` false/true 2 분기 · 404 2 종(null 반환 / P2025) · `DELETE` 204 + 후속 GET 404 + count 0 · 기본 provider 슬롯 점유 config DELETE → 409 + config · 슬롯 둘 다 잔존(ADR-0062 `onDelete: Restrict` 회귀 감지) · RBAC 2 route × 3 조건 6 케이스 + SuperAdmin escalation 200. helper · fixture · dependency 신설 0, `src/` · `prisma/` 무접촉이라 전역 coverage 불변(line 99.94% · function 100%), 로컬 481 suite / 13996 test green, CI e2e 36 suite / 526 case green.
- 08:11 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1969 direct commit `04f9b29d` 의 main run `34195856892` 는 `success` 로 정산했다(뒤이은 planner commit `834ecef0` main run `34201804449` 도 success; `5c15e1f5` 의 run `34195979561` cancelled 는 다음 push 로 superseded 된 무해 케이스). 본 fire 의 PR #1545 는 head `30b35b1b` 에서 기본 검사 · 배포 산출물 검증 · reviewer approval 게이트 전부 green(approval 게이트는 reviewer comment 게시 후 `gh run rerun --failed` 로 green, rollup CLEAN) 확인 후 squash merge 했다. merge commit `5e05de31` 의 main run `34202998388` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 08:11 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T07:38:07Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`6c4b2b2c`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족 → `validate-claim-candidate.sh` PASS → `select-claim.sh` atomic claim(`claimedAt` 07:38:07Z) → 즉시 lock tombstone release → lock-free executor 진행(약 31 분, claim TTL 60 분 이내). PR-open 시점 `sync-claim-pr.sh` 는 executor 가 PR flow 를 흡수한 구조상 executor 안에서 호출됐고 exit 0(`SYNC taskId=T-1970 prNumber=1545`) 확인. bookkeeping 위해 lock 재획득(`5c89080f`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 08:11 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있으며(본 fire 가 T-1970 을 소비), (c) 관점에서도 merge commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 08:24 planner: queued **T-1971** — REQ-049 · REQ-051 상태 칸 근거 공백 정정(`direct` doc-only, `docs/requirements.md` `68 행`·`70 행` 1 파일). T-1967(PR #1543 → main `b2a6ce5d`) + T-1970(PR #1545 → main `5e05de31`) 의 LLM provider config e2e arc 가 머지돼 PLAN `183 행` once-rule 의 "구현 후 REQ 당 1 회" 시점이 열렸다 — REQ-049 는 검증 방법을 `e2e` 로 선언만 하고 상태 칸 좌표가 0 개였고 REQ-051 도 서술만 있었다. 박제 축: 구현(`llm-provider-config.controller.ts` `77 행` `@Controller` + 6 handler route + `@Put("default")` 의 `:id` 선행 제약 · service · repository · resolver) · unit(`llm-provider-config.controller.spec.ts` · `.service.spec.ts` 실계수) · e2e(`test/e2e/llm-provider-configs.e2e-spec.ts` `60 행`·`256 행` 두 describe, 계수는 실행 시점 재검산) · 한계 2 종(쓰기 3 route 는 `LLM_APIKEY_ENC_KEY` 주입 결정 선행이라 e2e 미고정 / custom 3 model 슬롯 미구현). pre-check(origin/main `3334cfc5`): `grep -c "llm-provider-configs.e2e-spec" docs/requirements.md` = 0 이라 중복 0, 두 행 좌표 문자열 0, T-1971 ID 미사용. REQ-043(`62 행`)은 74 route 재측정이 필요해 Out of Scope. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`src`·`test`·`web`·`prisma`·워크플로 무접촉).
- 08:45 driver: T-1971 **DONE** — REQ-049 · REQ-051 상태 칸 좌표 박제(`direct` doc-only, main `5f4a0f12`, `docs/requirements.md` + task 파일 2 파일 +7/-4). `68 행`(REQ-049) · `70 행`(REQ-051) 의 상태 칸만 교체하고 표 7 칸 구조 · 검증 방법 원값(e2e / unit) · ADR-0062 링크 · 기존 참 서술은 보존했다. 박제 좌표는 모두 실행 시점 재검산값 — 구현 `src/llm/llm-provider-config.controller.ts` `77 행` `@Controller("api/llm/providers")` + handler 6 개(`95`·`123`·`141`·`159`·`180`·`199 행`, 전부 `@Roles("Admin")`) · unit provider-config 계열 8 spec 합계 `it(` 170 개 + `it.each` 13 블록 · e2e `test/e2e/llm-provider-configs.e2e-spec.ts` 495 행 / describe 2 개(`60 행` 목록 · `256 행` `:id`) / 실행 20 케이스. 한계 축 2 종(쓰기 3 route 는 `LLM_APIKEY_ENC_KEY` 주입 결정 선행이라 e2e 미고정 / custom 3 model 슬롯 미구현)도 함께 명시했다. `src/` · `test/` · `web/` · `prisma/` · 워크플로 diff 0 이라 R-110 · R-112 면제.
- 08:45 driver: **CI 정산 (R-114)** — 직전 fire 이월 pending 이던 T-1970 merge commit `5e05de31` 의 main run `34202998388` 은 `success` 로 정산했다. 본 fire 의 direct commit `5f4a0f12` 의 main run `34206177739` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다. `consecutiveFails` 0 유지.
- 08:45 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T08:37:55Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`fae18d92`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) 단독 claim 조건 충족 → `select-claim.sh` atomic claim(`claimedAt` 08:37:55Z, 재검증 OK) → 즉시 lock tombstone release → lock-free executor 진행(약 7 분, claim TTL 60 분 이내). direct-mode 라 `sync-claim-pr.sh` 는 해당 없음. bookkeeping 위해 lock 재획득(`7e833d62`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동.
- 08:45 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 가 비어 있으며(본 fire 가 T-1971 을 소비), (c) 관점에서도 direct commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 09:05 planner: queued **T-1972** — LLM provider config 생성(`POST /api/llm/providers`) e2e 계약 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 세 번째 describe append). T-1967(목록) · T-1970(`:id`) 로 읽기 축은 닫혔으나 쓰기 축 e2e 는 0 이라 `service.create` 의 **암호문 저장**(ADR-0014 §1) 이 unit mock 축에만 존재했다. 선행 slice 가 미룬 `LLM_APIKEY_ENC_KEY` 주입은 실측으로 해소 — cipher `48 행` `resolveKey()` 가 호출 시점마다 `process.env` 를 읽으므로 spec-local 일회용 키(`randomBytes(32)`, `afterAll` 복원)로 충분하고 CI env · `.env` · 워크플로 무접촉(새 dep · credential · 커밋되는 secret 0, `confluence-token-decrypt.spec.ts` `33~46 행` `withEnvKey` mirror). 축: Admin 201 + view 7 key · `apiKey` 미노출 이중 단언 · **저장 row `!==` 평문 + `cipher.decrypt` 왕복 일치** · 생성 후 목록 1 건 반영 · 미지원 provider 400 · ValidationPipe `it.each` 4(누락/빈값/wrong type/allow-list 밖) · 키 부재 500 fail-fast(평문 저장 0) · RBAC `it.each` 3(403/401/401, row 0) · SuperAdmin 201. pre-check(origin/main `fb4a19a3`): `.post(` hit 0 · `api/llm/providers` 참조 7 파일 중 `test/e2e/` 는 1 개뿐(나머지 perf 3 · load 1 · smoke 1 · README) · T-1972 ID 미사용. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`web`·`src`·`prisma`·워크플로·`requirements.md` 무접촉, 신설 helper 0). `PATCH /:id` · `PUT /default` 와 REQ 재판정은 Out of Scope(후속 1 회).
- 10:12 driver: T-1972 **DONE** — LLM provider config 생성(`POST /api/llm/providers`) e2e 계약 고정(pr, PR #1546 → main squash `40a757c4`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +292/-2). 읽기 축(T-1967 목록 · T-1970 `:id`)만 닫혀 있던 상태에서 쓰기 축 e2e 0 을 해소해, `service.create` 가 `cipher.encrypt` 를 거쳐 **암호문**을 영속한다는 ADR-0014 §1 invariant 를 실 HTTP 왕복으로 red-able 하게 만들었다. 미뤄져 있던 `LLM_APIKEY_ENC_KEY` 주입은 spec-local 일회용 키(`randomBytes(32)` → `process.env`, `afterAll` 원값 복원)로 해결 — cipher `resolveKey()` 가 호출 시점마다 env 를 읽는 성질 덕에 CI env · `.env` · 워크플로 · `src/` 무접촉이고 새 dep · credential · 커밋되는 secret 0. 고정 축 9 케이스: Admin 201 + view 7 key(`isDefault` false) · `apiKey` 키 부재 + 본문 원문 부재 이중 단언 · 저장 row `!==` 평문 + `cipher.decrypt` 왕복 일치 · 생성 직후 목록 GET 1 건 반영(소비처 배선) · 미지원 provider 400 + row 0 · ValidationPipe `it.each` 4 종 400 + row 0 · 키 부재 500 fail-fast + row 0(try/finally 복원) · RBAC `it.each` 3 종(403/401/401) + count 0 · 과차단 없음(SuperAdmin 201). 로컬 `pnpm lint && build && test` 481 suite / 13996 case green, e2e 는 실 DB 필요라 CI `pnpm test:e2e`(R-113) 에서 green 확인. Out of Scope 였던 `PATCH /:id` · `PUT /default` 쓰기 축과 REQ 재판정(once-rule 1 회)은 후속 slice 로 남는다.
- 10:12 driver: **CI 정산 (R-114)** — 이월분 3 건 전부 `success` 로 정산했다: 직전 fire 가 pending 으로 넘긴 T-1971 direct commit `5f4a0f12` 의 main run `34206177739`, 그리고 그 뒤 main 에 얹힌 `fb4a19a3`(T-1971 bookkeeping) · `e1726fee`(T-1972 큐잉) 도 check-run 2 종(기본 검사 · 배포 산출물 검증) 모두 success. 본 fire 의 PR #1546 은 push-run `34211895150` 에서 `reviewer agent approval 검증` 단계만 red(리뷰 코멘트 게재 전 상태, 그 외 e2e 포함 전 단계 success) 였고, reviewer APPROVE 코멘트 후 rerun `34212540697`(event=issue_comment) 가 success, merge 후 main run `34213227339` 도 success 다. `consecutiveFails` 0 유지.
- 10:12 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T09:37:40Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`71863ac2`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족 → `validate-claim-candidate.sh` PASS → `select-claim.sh` atomic claim(`claimedAt` 09:37:40Z) → 즉시 lock tombstone release → lock-free executor 진행(약 24 분, claim TTL 60 분 이내). PR-open 시점 `sync-claim-pr.sh` 는 executor 가 PR flow 를 흡수한 구조상 executor 안에서 호출됐고 exit 0(`SYNC taskId=T-1972 prNumber=1546`) 확인. bookkeeping 위해 lock 재획득(`d2c79586`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동. dup-PR 0.
- 10:12 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1972 를 소비). ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 10:30 planner: queued **T-1973** — LLM provider config 부분 갱신(`PATCH /api/llm/providers/:id`) e2e 계약 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 네 번째 describe append). T-1972 가 남긴 쓰기 축 잔여 2 route 중 `PATCH /:id` 만 잇는다(`PUT /default` 는 cap 상 후속). pre-check(origin/main `fe25f429`): `.patch(` × providers 매칭 **0**, `api/llm/providers` 참조 test 7 파일 중 `test/e2e/` 는 1 개뿐(perf 3 · load 1 · smoke 1 · README 1), 그 spec 은 `785 행` · describe 3 개(`65`·`261`·`574 행`)로 PATCH 없음, 구현 축은 controller `180 행` `@Patch(":id")` + service `228~271 행` `update` 로 이미 main 에 실재(= 미구현 신설 아님, 계약 고정). 고정 가치는 `update` `253~255 행` **apiKey 분기**(명시 → 재암호화 / 부재 → 기존 ciphertext 바이트 동일 유지, ADR-0014 §1·§3)가 unit mock 축에만 있어 평문 저장·ciphertext 덮어쓰기 회귀가 실 HTTP 왕복에서 red 가 되지 않는 점. 축: 부분 갱신 200 + view 7 key + 미명시 필드 불변 · 직후 GET `:id` 반영(소비처 배선) · 재암호화 왕복 · 미명시 ciphertext 불변 · 미지원 provider 400 · 부재 id 404(P2025) · ValidationPipe `it.each` 3 · RBAC `it.each` 3 + SuperAdmin 과차단 0. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`src`·`prisma`·`web`·워크플로·`requirements.md` 무접촉, 신설 helper 0, REQ 재판정 once-rule 상 T-1971 이 이미 수행).
- 11:05 driver: T-1973 **DONE** — LLM provider config 부분 갱신(`PATCH /api/llm/providers/:id`) e2e 계약 고정(pr, PR #1547 → main squash `333edf53`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +298/-2). T-1972 가 쓰기 축의 `POST` 만 닫고 남긴 잔여 2 route 중 `PATCH /:id` 를 잇는다 — 구현은 이미 main 에 있던 controller `@Patch(":id")` + service `update` 라 신설이 아니라 **계약 고정**이고, 핵심 가치는 `update` 의 apiKey 분기(명시 → 재암호화 / 미명시 → 기존 ciphertext 바이트 유지, ADR-0014 §1·§3)가 unit mock 축에만 존재해 평문 회귀·ciphertext 덮어쓰기가 실 HTTP 왕복에서 red 가 되지 않던 공백을 메운 점이다. 기존 describe 3 개는 무수정이고 파일 하단에 네 번째 top-level describe(it 13 개) 만 append — happy 200 + view 7 key + 미명시 필드 불변 · 직후 GET `:id` 반영(소비처 배선) · 재암호화 왕복 · 미명시 시 ciphertext 동일 · 미지원 provider 400 + row 불변 · 부재 id 404(P2025) + row 불변 · ValidationPipe 3 종 · RBAC 3 종 + SuperAdmin 200(과차단 0). 로컬 `pnpm lint && build && test` 481 suite / 13996 case green, e2e 는 실 DB 필요라 CI `pnpm test:e2e`(R-113) 에서 해당 spec PASS 확인. Follow-ups 로 `PUT /api/llm/providers/default`(쓰기 축 마지막 route) 와 `isDefault: true` 파생 유지 e2e 가 남는다.
- 11:05 driver: **CI 정산 (R-114)** — 직전 fire 가 이월한 건 없음(T-1972 정산 완료). 본 fire 의 PR #1547 은 push-run `34217160053`(pull_request) 과 reviewer APPROVE 후 rerun `34217253582`(event=issue_comment) 가 모두 success 로 4-게이트 green. merge squash `333edf53` 의 main run `34217839833` 은 종료 시점 `in_progress` 라 다음 turn 첫 단계에서 재확인한다(이월 1 건). `consecutiveFails` 0 유지.
- 11:05 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T10:38:01Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`86cd34aa`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족 → `select-claim.sh` atomic claim(`claimedAt` 10:38:07Z, 즉시성 확인) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 15 분, claim TTL 60 분 이내). PR-open 시점 `sync-claim-pr.sh` 는 executor 가 PR flow 를 흡수한 구조상 executor 안에서 호출됐고, driver 가 prune 직전 claim 을 직독해 `status: PR_OPEN` · `prNumber: 1547` 동기 완료를 실측 확인했다. 완료 후 `lock_tree_cas_push` 로 claim prune(`87080566`, `claims.json` `[]` byte 확인). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, dup-PR 0.
- 11:05 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1973 을 소비). ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 11:35 planner: queued **T-1974** — LLM 기본 provider 지정(`PUT /api/llm/providers/default`) e2e 계약 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 다섯 번째 describe append). T-1973 Follow-up 의 쓰기 축 **마지막** route 이며 `isDefault: true` 파생도 PUT 응답·후속 GET 범위 안에서 함께 닫는다. pre-check(origin/main `fd2908ea`): `.put(` × providers 매칭 **0** · `providers/default` 매칭 **0**(test 트리 전체) · `api/llm/providers` 참조 test 7 파일 중 `test/e2e/` 는 1 개뿐, 그 spec 은 `1081 행` · describe 4 개(`65`·`261`·`574`·`845 행`)로 PUT 없음, 구현은 controller `123 행` `@Put("default")` + service `333 행` `setDefault` 로 이미 main 에 실재(= 계약 고정), `setDefault` 검증은 controller.spec 33 · service.spec 10 의 **unit mock 축 전량**. 축: 200(201 아님) + view 7 key + `isDefault:true` · 직후 `GET :id`/목록 반영(소비처 배선) · 슬롯 교체 후 true 정확히 1 개 + `llmDefaultProvider.count()` 1 · 멱등 재지정 200 · 라우트 순서 회귀 가드(`default` 가 `:id` 로 오매칭 0) · 부재 id 404 + 슬롯 0 · ValidationPipe `it.each` 4 · RBAC `it.each` 3 + SuperAdmin 200. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`src`·`prisma`·`web`·워크플로·`requirements.md` 무접촉, 신설 helper 0, REQ 재판정 once-rule 상 T-1971 이 이미 수행).
- 11:12 driver: **CI 재확인 (R-114 이월 정산)** — T-1973 merge squash `333edf53` 의 main run `34217839833` 이 `success` 로 마감돼 이월분 0. bookkeeping commit `fd2908ea` 의 run `34218053080` 은 job 0 개로 `cancelled` 인데, 곧이은 planner push `f80f7e46` 이 같은 concurrency group 을 차지한 benign supersede 이므로 fail 로 세지 않는다(`consecutiveFails` 0 유지). `f80f7e46` 의 run `34218352910` 은 `queued` 상태라 다음 turn 첫 단계에서 확인한다.
- 12:00 driver: T-1974 **DONE** — LLM 기본 provider 지정(`PUT /api/llm/providers/default`) e2e 계약 고정(pr, PR #1548 → main squash `08918950`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +298/-2). T-1972(POST) · T-1973(PATCH) 에 이어 쓰기 축 **마지막** route 를 닫았다 — 구현은 이미 main 에 있던 controller `@Put("default")` + service `setDefault` 라 신설이 아니라 계약 고정이고, 그 검증이 controller.spec 33 · service.spec 10 의 unit mock 축 전량이던 공백을 실 HTTP 왕복으로 메웠다. 기존 top-level describe 4 개는 무수정, 파일 하단에 다섯 번째 describe(it 14 개) 만 append — happy 200(201 아님) + view 7 key + `isDefault:true` + `apiKey` 미노출 · 직후 단건 GET · 목록 GET 파생 반영(소비처 배선) · 슬롯 교체 후 true 정확히 1 개 + 슬롯 count 1 · 멱등 재지정 200 + count 1 · 라우트 순서 가드(`default` 가 `:id` 로 오매칭 0, `GET /default` 404) · 부재 id 404 + 슬롯 count 0 · ValidationPipe 4 종 400 + 슬롯 0 · RBAC 3 종 + SuperAdmin 200(과차단 0). 로컬 `pnpm lint && build && test` 481 suite / 13996 case green(line 99.94% · function 100%), CI `pnpm test:e2e` 36 suite / 567 case green(R-113). 이로써 provider config 6 route 중 읽기 2 · 쓰기 3 축이 e2e 로 닫혔고, PATCH 대상 row 의 `isDefault` 파생 유지는 후속 slice 로 남는다.
- 12:00 driver: **CI 정산 (R-114)** — 직전 fire 이월분 2 건 모두 `success` 로 마감: planner push `f80f7e46` 의 run `34218352910`, bookkeeping `86652fe0` 의 run `34218427483`. 본 fire 의 PR #1548 은 push-run `34222777694` 의 두 job(기본 검사 · 배포 산출물 검증)이 모두 success 이고 reviewer APPROVE comment 외화(#issuecomment-5584689402)로 approval 게이트도 green — 4-게이트 충족 후 squash merge. merge commit `08918950` 의 main run `34223379871` 은 turn 종료 시점 `pending` 이라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건). `consecutiveFails` 0 유지.
- 12:00 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T11:37:51Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`9b0a6ec4`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족 → `select-claim.sh` atomic claim(`claimedAt` 11:37:51Z, 즉시성 확인) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 19 분, claim TTL 60 분 이내). PR-open 시점 `sync-claim-pr.sh` 는 executor 가 PR flow 를 흡수한 구조상 executor 안에서 호출됐고, driver 가 prune 직전 claim 을 직독해 `status: PR_OPEN` · `prNumber: 1548` 동기 완료를 실측 확인했다. bookkeeping 위해 lock 재획득(`dac6498a`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, dup-PR 0.
- 12:00 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1974 를 소비). (c) 관점에서도 merge commit 의 main run 이 `pending` 이라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 12:00 driver: **reviewer nit** — round 1 APPROVE(BLOCKER 0 / MAJOR 0 / MINOR 1). MINOR 1 건은 seed 상수 주석 위치 표기라 CLAUDE.md §3 nit-in-PR closure (3) 항목에 해당해 follow-up task 를 만들지 않았다. commit body trail 의 `it 12 개` 표기는 실제 14 개의 오기로, 계약 · CI 영향 0 이며 본 journal 이 정본이다.
- 12:30 planner: queued **T-1975** — 기본으로 지정된 row 의 `PATCH /api/llm/providers/:id` × `isDefault` 파생 유지 e2e 계약 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 여섯 번째 describe append). T-1973 · T-1974 가 둘 다 Follow-up 으로 넘긴 **route 교차** 잔여 1 축이다 — 단일 route 계약은 6 route 전부 닫혔고 남은 것은 "기본 슬롯이 걸린 row 를 수정할 때 파생·슬롯이 유지되는가" 뿐. pre-check(origin/main `b3b12266`): 파일 전체 `isDefault` 29 매칭이지만 PATCH describe 구간(`846~1082 행`)의 매칭은 **1 줄뿐이고 그것도 `1127 행` PUT describe 헤더 주석** 이라 본문 단언 **0**, 같은 구간 `llmDefaultProvider` 매칭도 `904 행` `afterEach` deleteMany 1 줄뿐이라 **슬롯 seed 0**(모든 케이스가 `isDefault:false` 경로만 밟음). 구현은 service `228 행` `update` 의 `270 행` `sanitize(row, await this.readDefaultConfigId())` 로 이미 main 에 실재(= 계약 고정), 그 검증은 `service.spec` 14 · `controller.spec` 8 의 unit mock 축 전량. 인접 후보였던 기본 config `DELETE` 409 는 `427 행` 이 이미 고정해 중복 slice 로 큐잉하지 않았다. 축: happy 200 + 7 key + `isDefault:true` 유지 + 슬롯 count 1 · 대상 A 불변 · 직후 `GET :id`/목록 반영(소비처 배선) · apiKey 재암호화 경로에서도 파생 유지 · 비기본 B PATCH 는 `false` 이고 승격 0(반대 방향) · 슬롯 부재 null 분기 · 부재 id 404 + 슬롯 불변 · 미지원 provider 400 · ValidationPipe `it.each` 2 · RBAC `it.each` 3 + SuperAdmin 200. 오너 게이트 `157`·`158`·`182`·`183 행` 미침범(`test/perf`·`test/load`·`src`·`prisma`·`web`·워크플로·`requirements.md` 무접촉, 신설 helper·상수 0, REQ 재판정 once-rule 상 T-1971 이 이미 수행).
- 13:18 driver: T-1975 **DONE** — 기본으로 지정된 row 의 `PATCH /api/llm/providers/:id` × `isDefault` 파생 유지 e2e 계약 고정(pr, PR #1549 → main squash `70d20c18`, `test/e2e/llm-provider-configs.e2e-spec.ts` +284/-1 · task 파일 Follow-ups 갱신 포함 2 파일). T-1973 · T-1974 가 둘 다 Follow-up 으로 넘긴 **route 교차** 잔여 1 축을 닫았다 — 단일 route 계약은 6 route 전부 e2e 로 고정돼 있었고 공백은 "기본 슬롯이 걸린 row 를 수정할 때 파생 · 슬롯이 유지되는가" 뿐이었다. 기존 top-level describe 5 개 무수정 · 신설 상수 0 으로 여섯 번째 describe 만 append(it 선언 8 + `it.each` 전개 포함 12 case). seed 는 prisma 직접 create 로 config A · B + A 를 가리키는 기본 슬롯 1 건(쓰기 route 경유 0). 축: happy 200 + view 7 key + `isDefault:true` 유지 + 슬롯 count 1 · 직후 단건 `GET :id` 와 목록에서 true 정확히 1 개(소비처 배선) · `apiKey` 재암호화 왕복에서도 파생 · 슬롯 불변 · 비기본 row B 는 `false` 이고 승격 0(반대 방향) · 슬롯 부재 시 `readDefaultConfigId` null → `false` · 부재 id 404 + row · 슬롯 불변 · ValidationPipe 3 종 400 · RBAC 3 종(403/401/401) + 평문 미노출. 로컬 `pnpm lint && build && test` 481 suite / 13996 case green + `test:cov` 임계 유지, CI `pnpm test:e2e` 에서 해당 spec PASS(10.2 s) · e2e 합계 36 suite / 579 case green(R-113).
- 13:18 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건 마감: T-1974 merge squash `08918950` 의 main run `34223379871` **success**. 본 fire 는 PR push-run `34228151311` success 이나 "reviewer agent approval 검증" step 이 comment 외화 이전 실행분이라 red → reviewer APPROVE comment 외화 후 `--failed` rerun 및 issue_comment rerun `34228819906` success 로 게이트 green, 4-게이트 충족 후 squash merge. merge commit `70d20c18` 의 main run `34230211322` 도 driver 가 종료 전 직접 조회해 **success** 확인 — 이번 fire 는 **이월 0**. `consecutiveFails` 0 유지.
- 13:18 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T12:37:48Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`df00ee61`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족 → `validate-claim-candidate.sh` PASS(`touchesFiles` 교집합 0 · `dependsOn: [T-1974]` 머지됨) → `select-claim.sh` atomic claim(`claimedAt` 12:37:48Z, 즉시성 확인) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 31 분, claim TTL 60 분 이내). `sync-claim-pr.sh T-1975 1549` 는 executor 가 PR flow 를 흡수하는 구조상 executor 안에서 reviewer 호출 이전에 수행돼 exit 0. bookkeeping 위해 lock 재획득(`709f04d9`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, dup-PR 0.
- 13:18 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1975 를 소비). (d) 관점에서도 lock `since` 로부터 40 분 경과라 45 분 임계에 근접했다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 13:18 driver: **reviewer nit · cap 축소** — round 1 APPROVE. AC 중 2 건(기본 row A 에 대한 미지원 provider 400, SuperAdmin 과차단 없음 200)은 §3 300 LOC cap 을 지키려 축소돼 `pending` 으로 남았고 task 파일 `Follow-ups` 에 파일 · 단언 단위로 박제됐다(예상 +30 LOC · 1 파일) — 다음 slice 후보.
- 13:45 planner: queued **T-1976** — 기본 슬롯이 존재하는 상태의 `POST` 생성 · 비기본 row `DELETE` 교차 계약 + T-1975 가 cap 으로 넘긴 잔여 2 건 e2e 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 · 2 site). 절단면을 diff 크기가 아니라 상태 축(**기본 슬롯 seed 된 fixture**)으로 잡아 30 LOC 짜리 잔여 slice 단독 큐잉을 피했다(PLAN `182 행` 과분할 차단 정합). pre-check(origin/main `de636b79`): `llmDefaultProvider.create` 는 파일 전체 **3 hit**(`390`·`429`·`1461 행`)뿐이라 POST describe(`575~845 행`)의 슬롯 매칭은 `621 행` `deleteMany` **1 줄** = 슬롯 존재 상태의 생성 조합 **0**, GET/DELETE describe 의 `.delete(` **4 곳**(`341`·`370`·`416`·`434 행`) 중 슬롯이 다른 row 를 가리키는 상태의 **비기본 row 삭제 0**(`434 행` 은 기본 row 409 — 재작성 금지), T-1975 describe(`1396~1660 행`)에 `SuperAdmin` 매칭 **0**(actor 는 `1421~1422 행` User·Admin 2 명)이고 허용 집합 밖 provider **값** 축 **0**(`INVALID_PATCH_BODIES` 는 키 축 3 종), `T-1976` ID 미사용. 구현은 service `203 행` `create` 의 `sanitize(row, await this.readDefaultConfigId())` + `295~310 행` `remove` 의 P2025→404 / P2003→409 로 이미 main 에 실재(= 계약 고정). 축: POST 201 + 신규 row `isDefault:false` + 슬롯 count 1 · target A · 목록 true 정확히 1 개(소비처 배선) · POST 400 슬롯 불변 · 비기본 B DELETE 204 + A·슬롯 잔존 + 목록 반영 · RBAC `it.each` 3 종 row·슬롯 불변 · site 1 SuperAdmin PATCH 200 과차단 0 + 미지원 provider 400. 오너 게이트 `156~157`·`182~183 행` 미침범(`src`·`web`·`prisma`·`test/perf`·`test/load`·워크플로·`requirements.md` 무접촉, 신규 helper 0, REQ 재판정 once-rule 상 T-1971 이 이미 수행).
- 14:02 driver: T-1976 **DONE** — 기본 슬롯이 존재하는 상태의 `POST` 생성 · 비기본 row `DELETE` 교차 계약 + T-1975 가 §3 cap 으로 넘긴 잔여 2 건 e2e 고정(pr, PR #1550 → main squash `e4510bb8`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +256/-0 · 2 site). 절단면을 diff 크기가 아니라 상태 축(기본 슬롯 seed 된 fixture)으로 잡아 30 LOC 잔여 slice 단독 큐잉을 피했다. site 1 은 T-1975 describe 에 SuperAdmin actor 1 명 + case 2 건(기본 row A 의 SuperAdmin PATCH 200 과차단 0 · 허용 집합 밖 provider **값** 400 + row · 슬롯 불변)을 append 하고, site 2 는 슬롯 A 가 걸린 fixture 위에 POST · DELETE 교차 describe 를 신설(8 case)했다 — POST 201 + view 7 key + 신규 row `isDefault:false` + 슬롯 count 1 · target A · 목록 true 정확히 1 개(소비처 배선) · POST 400 시 config 2 건 · 슬롯 불변 · 비기본 B DELETE 204 빈 body + B 부재 · A 잔존 · 슬롯 불변 + 목록 반영 · RBAC 3 종 row · 슬롯 불변 · 평문 미노출. 기존 describe 1~5 무수정 · 신규 상수 0(`VALID_CREATE_PAYLOAD` · `DEFAULT_RBAC_CASES` · `SECRET_DEFAULT_A/B` · `VIEW_FIELDS` 재사용) · production 0 LOC. 로컬 `pnpm lint && build && test` 481 suite / 13996 case green + `test:cov` line 99.94% · function 100%(임계 80/80), CI `pnpm test:e2e` 에서 본 spec PASS(10.5 s) · e2e 합계 36 suite / 580→590 case(+10) 실측(R-113).
- 14:02 driver: **CI 정산 (R-114)** — 직전 fire 이월 0 으로 시작. 본 fire 의 PR #1550 은 push-run `34233651406` 두 job(기본 검사 6m10s · 배포 산출물 검증 1m27s) 모두 success 이고, reviewer APPROVE comment 외화(#issuecomment-5586122367) 후 issue_comment rerun `34233873268` 도 success 라 approval 게이트 green — 4-게이트 충족 후 squash merge. merge commit `e4510bb8` 의 main run `34234356608` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건). `consecutiveFails` 0 유지.
- 14:02 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T13:33:38Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`6d9becbe`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: [T-1975]` 는 `70d20c18` 로 머지됨, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 13:33:38Z, 즉시성 확인) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 16 분, claim TTL 60 분 이내). `sync-claim-pr.sh T-1976 1550` 은 executor 가 PR flow 를 흡수하는 구조상 executor 안에서 reviewer 호출 이전에 수행돼 exit 0. bookkeeping 위해 lock 재획득(`a491982e`). `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, dup-PR 0.
- 14:02 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1976 을 소비). (c) 관점에서도 merge commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 14:30 planner: queued **T-1977** — 난이도 슬롯(`DifficultyMapping`)이 참조하는 provider config 의 `DELETE` 409 + 해소 경로 e2e 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 여덟 번째 describe append). provider config 6 route 의 단일 route 계약과 **기본 슬롯** 교차 축은 T-1967~T-1976 이 전부 닫았고, 같은 `delete` 의 P2003→409 를 트리거하는 **두 번째 FK 경로**(ADR-0011 §2 난이도 슬롯)만 남아 있었다. pre-check(origin/main `ec90aa38`): `difficultyMapping` 매칭이 provider spec 에서 **0**, 역방향 `api/llm/providers` 매칭이 difficulty spec 에서 **0** = 교차 e2e 양방향 0 이고 `test/e2e/` 전체에서도 `difficulty-mappings.e2e-spec.ts` 밖 참조 0. 해당 spec 은 `1916 행` · top-level describe 7 개(`66`·`262`·`575`·`846`·`1131`·`1396`·`1715 행`)이며 `409` 매칭은 `427 행` 기본 슬롯 1 건뿐. 구현은 service `296~311 행` `delete` 의 P2025→404 / P2003→409 + schema `452 행` `onDelete: Restrict` 로 이미 main 에 실재(= 계약 고정)이고 그 검증은 service.spec P2003 **18** · controller.spec **6** 의 unit mock 축 전량. 축: 참조 중 A DELETE 409 + A·슬롯 잔존 · 409 본문 apiKey 미노출 · 비참조 B 204(과차단 확산 0) · **해소 경로**(`PATCH /difficulty-mappings/easy` 로 B 재지정 200 → A DELETE 204, controller 2 개 교차 배선) · 다중 슬롯 참조 409 · FK 전부 null 이면 204(Restrict 미발동) · SuperAdmin 도 409(과허용 0) · RBAC `it.each` 3 종 403/401/401 + row·슬롯 불변. 오너 게이트 `156~157`·`182~183 행` 미침범(`src`·`web`·`prisma`·`test/perf`·`test/load`·워크플로·`requirements.md` 무접촉, 신규 helper·상수 0, REQ 재판정 once-rule 상 T-1971 이 이미 수행).
- 15:05 driver: T-1977 **DONE** — 난이도 슬롯(`DifficultyMapping`)이 참조하는 provider config 의 `DELETE` 409 + 해소 경로 e2e 고정(pr, PR #1551 → main squash `3035291d`, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일 +256/-0, 여덟 번째 top-level describe append). 같은 `delete` 의 P2003→409 를 트리거하는 **두 번째 FK 경로**(ADR-0011 §2 난이도 슬롯)를 실 HTTP 왕복으로 닫았다 — 참조 중 A `DELETE` 409 + A 잔존 · easy FK 불변 · config 2 건, 409 본문 `apiKey` 원문 미노출, 비참조 B `DELETE` 204 빈 body + 슬롯 3 건 불변(과차단 확산 0), **해소 경로**(`PATCH /difficulty-mappings/easy` 로 B 재지정 200 → A `DELETE` 204 · easy FK=B, controller 2 개 교차 배선), 다중 참조(easy·medium→A) 409 + 두 FK 불변, FK 전부 null 이면 204(`onDelete: Restrict` 미발동), SuperAdmin 도 409(과허용 0), RBAC `it.each` 3 종 403/401/401 + 409 미누출. 기존 describe 7 개 무수정 · production `src/` · `prisma/` · 워크플로 0 LOC. 로컬 `pnpm lint && build && test` 481 suite / 13996 case green + `test:cov` line 99.94% · function 100%(임계 80/80), CI `pnpm test:e2e` 36 suite / 590→600 case(+10, 신규 case 수와 정확히 일치) 실측(R-113).
- 15:05 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(`e4510bb8` 의 main run `34234356608`)을 본 fire 첫 단계에서 `success` 로 확인해 정산 완료. 본 fire 의 PR #1551 은 pull_request run `34240260526` success. 최초 run 은 reviewer comment 게시 **이전** 타이밍이라 `reviewer agent approval 검증` step 만 red 였고, APPROVE comment 외화(#issuecomment-5587154438) 후 `--failed` rerun 으로 green — 코드 결함 fail 0 이라 `consecutiveFails` 미증가(0 유지). 4-게이트(reviewer APPROVE · PR comment 외화 · integrator 자체 점검 #issuecomment-5587258605 · CI green) 충족 후 `--squash --delete-branch` merge. merge commit `3035291d` 의 main run `34242285512` 는 turn 종료 시점 `pending` 이라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 15:05 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T14:38:02Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`6ff17dff`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: [T-1976]` 는 `e4510bb8` 로 머지됨, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 14:38:02Z, 즉시성 확인) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 25 분, claim TTL 60 분 이내). `sync-claim-pr.sh T-1977 1551` 은 executor 가 PR flow 를 흡수하는 구조상 executor 안에서 수행돼 exit 0. merge 후 `lock_tree_cas_push` 로 `claims.json` `[]` prune(`769e8cea`, tree 엔트리 2 개 유지 · 중복 0). bookkeeping 은 lock 재획득 없이 doc-only direct commit 으로 수행. `concurrencyIncidents` 4 유형 전부 임계(2) 미달로 회로 차단기 미발동, dup-PR 0.
- 15:05 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1977 을 소비). (c) 관점에서도 merge commit 의 main run 이 `pending` 이라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 를 큐잉한다.
- 15:40 planner: queued **T-1978** — 기본 슬롯(`LlmDefaultProvider`)과 난이도 슬롯(`DifficultyMapping`)이 **동시에** 같은 provider config 를 가리키는 상태의 `DELETE` 409 · **2 단 해소** e2e 고정(pr, `test/e2e/llm-provider-configs.e2e-spec.ts` 1 파일에 아홉 번째 describe append). T-1977 이 Out of Scope 로 명시 이월한 마지막 잔여 축이다. pre-check(origin/main `0f5c72db`, 실측): 해당 spec `2172 행` · top-level describe 8 개(`66`·`262`·`575`·`846`·`1131`·`1396`·`1715`·`1933 행`), T-1976 구간(`1715~1932 행`)의 `difficultyMapping` 매칭 **0** · T-1977 구간(`1933 행` 이후)의 `llmDefaultProvider.create|upsert` 매칭 **0**(cleanup `2002 행` 뿐) → 두 FK 동시 seed 케이스 전무. 기존 409 는 전부 단일 FK(`427`·`2026`·`2098 행`). service `297~311 행` 의 P2003→409 는 FK 종류를 구분하지 않는 단일 분기라 "한 쪽만 재지정하면 아직 409" 라는 부분 해소 의미가 unit mock 축으로는 원리적으로 검증 불가. AC 는 이중 참조 409 / 부분 해소 2 종(난이도만 · 기본만) 409 유지 / 완전 해소 204 / 누출 0 / 비참조 B 204 / SuperAdmin 409 / guard 3 종을 잠근다. 오너 게이트 침범 0(perf slice 0 — PLAN `156~157 행`, helper 신설 0 — `182 행`, REQ 재판정 0 — `183 행`).
- 16:05 driver: T-1978 **DONE** — 기본 슬롯(`LlmDefaultProvider`)·난이도 슬롯(`DifficultyMapping`) 두 Restrict FK 가 **동시에** 같은 provider config 를 가리키는 상태의 `DELETE` 409 와 **2 단 해소** 경로를 e2e 로 고정. `test/e2e/llm-provider-configs.e2e-spec.ts` 에 아홉 번째 top-level describe append(+282/-0, 1 파일, cap 300 LOC / 5 파일 이내). production · prisma · CI 워크플로 변경 0 — 순수 계약 고정 slice. PR #1552 round 1 APPROVE → squash merge `c029d1e5`.
- 16:05 driver: **테스트 (R-110 · R-112)** — lint · build · unit(481 suite / 13996 case) · `test:cov` 전부 green(line 99.94% / function 100%, 임계 line 80% · function 80% 유지). CI e2e leg 는 PR #1551 기준 `599 passed / 600 total` · suite 35 에서 `609 passed / 610 total` · suite 36 으로 증가(+10 case, +1 suite) — 신설 case 10 건은 happy · 분기 4, error path 1, negative 5(무참조 B 의 204 로 과차단 확산 0, SuperAdmin 409 로 과허용 0, guard 선행 3 종) 구성.
- 16:05 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `3035291d` 의 main run `34242285512`)은 본 fire 첫 단계에서 확인: 후속 push 로 superseded 된 0-job `cancelled` 이고 같은 계열 최신 main run `34243191937`(`8c00d66d`) 가 `success` 라 정산 완료 — 코드 결함 fail 아니므로 `consecutiveFails` 미증가(0 유지). 본 fire 의 PR #1552 는 pull_request run `34246915543` success(기본 검사 6m27s · 배포 산출물 검증 1m21s · `e2e test` · `reviewer agent approval 검증` 전부 green). 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `c029d1e5` 의 main run `34248382135` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 16:05 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T15:38:00Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`3306be56`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: [T-1977]` 는 `3035291d` 로 머지됨, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 15:38:00Z 즉시성 확인, 중복 claim 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 22 분, claim TTL 60 분 이내). **편차 1 건**: driver 가 executor 에게 lock/claims 미조작을 지시해 `sync-claim-pr.sh T-1978 1552` 가 수행되지 않아 claim 의 `prNumber` 가 null 로 남았다 — merge 는 이미 완료됐으므로 bookkeeping 에서 orphan claim 을 prune 하는 것으로 정리하고, 다음 fire 부터는 executor 에게 sync-claim-pr 수행을 명시 위임한다. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 16:05 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1978 을 소비). (c) 관점에서도 merge commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 만 큐잉한다.
- 16:10 planner: queued **T-1979** — `GET /api/assessment-evaluation/relative-comparison` 좌표 상대 비교 조회의 e2e 계약 고정(pr, `test/e2e/assessment-evaluation-relative-comparison.e2e-spec.ts` 신설 1 파일). PLAN `166 행` e2e 커버리지 축에서 이 route 만 실 부팅 왕복이 0 이고, `docs/requirements.md` `55 행` REQ-036 이 한계 (a) 로 "실 부팅 왕복 e2e 는 0" 을 스스로 자인한 지점이다. pre-check(origin/main `2dde2b00`, 실측): `git grep -c "relative-comparison" -- test/` 히트 **0** · `test/e2e/` 34 spec 중 본 route 호출 **0** · 신설 대상 파일명 부재 · T-1979 ID 미사용, 구현은 controller `845~855 행`(`@Roles("Admin")`)로 이미 main 에 실재(계약 고정이지 신설 아님). e2e 로만 잡히는 축 2 개 — ① `metricScore` 가 `Decimal` 이라 reader `54~69 행` `toNumber()` 분기는 실 왕복에서만 타고, ② domain `181~188 행` 의 동점 tie-break 결정성은 repository `137~145 행` `orderBy: personId asc` 와 결합해야 관찰된다. AC 는 happy 3-cohort + 좌표 격리(잡음 2 건) · `typeof number` · 동점 `1,1,3` · 빈 좌표 200 `{0,0,[]}` · `invalid period` 400 · ValidationPipe `it.each` 4 · guard `it.each` 3(401/401/403, 본문 누출 0) · SuperAdmin 200 을 잠근다. 오너 게이트 침범 0(perf·load slice 0 — `157~158 행`, helper 신설 0 — `182 행`, REQ 재판정은 머지 후 1 회로 이월 — `183 행`).
- 17:05 driver: T-1979 **DONE** — `GET /api/assessment-evaluation/relative-comparison` 좌표 상대 비교 조회의 e2e 계약을 신설 spec 1 파일로 고정(`test/e2e/assessment-evaluation-relative-comparison.e2e-spec.ts` +296/-0, cap 300 LOC / 5 파일 이내). production · prisma · CI 워크플로 변경 0 — 실 부팅 왕복이 0 이던 route 를 계약으로만 잠근 slice. PR #1553 round 1 APPROVE → squash merge `932f1e69`.
- 17:05 driver: **테스트 (R-110 · R-112)** — lint · build · unit(481 suite / 13996 case) · `test:cov` 전부 green(임계 line 80% · function 80% 유지). CI e2e leg 는 suite 36 → 37 · case 610 → 623(+1 suite / +13 case). 신설 it 13 건 = happy 3(좌표 격리 · `typeof number` Decimal 축 · cohort 통계) + 분기 2(동점 tie-break `rank 1,1,3` · 빈 좌표 200 `{0,0,[]}`) + error path 1(`period=quarter` 400) + negative 7(ValidationPipe `it.each` 4 · guard `it.each` 3 로 401/401/403 · SuperAdmin 200 으로 과차단 0 확인).
- 17:05 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `c029d1e5` 의 main run `34248382135`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 본 fire 의 PR #1553 는 pull_request run `34253306188` success(기본 검사 · 배포 산출물 검증 · `e2e test` · `reviewer agent approval 검증` 전부 green). 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `932f1e69` 의 main run `34254275656` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 17:05 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T16:37:56Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`8652b9d8`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 16:37:56Z 즉시성 확인, 중복 claim 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 21 분, claim TTL 60 분 이내). 직전 fire 편차(sync-claim-pr 미수행)를 교정해 이번엔 executor 가 `sync-claim-pr.sh T-1979 1553` 을 수행(exit 0, `prNumber` 1553 박제)했다. bookkeeping 을 위해 lock 재획득(`71650ee8`) 후 claim prune + tombstone release. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 17:05 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 판정 시점 `STATE.nextTask` 가 비어 있고(본 fire 가 T-1979 를 소비), merge commit 의 main run 이 `in_progress` 라 (c) 관점의 진입 근거도 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 만 큐잉한다.
- 17:20 planner: queued **T-1980** — export job 진행 조회 3 route(`GET /api/admin/export/running` · `:id/status-view` · `:id`)의 e2e 계약 고정(pr, `test/e2e/export-job-status-read.e2e-spec.ts` 신설 1 파일). PLAN `166 행` e2e 커버리지 축에서 이 3 route 만 실 부팅 왕복이 0 이다. pre-check(origin/main `3f9f5017`, 실측): `test/e2e/` 35 spec 중 `running`·`status-view` route 호출 **0**(export 계열 e2e 2 개는 `POST /` 생성 + `GET :id/download` 만 침), 구현은 controller `183`·`464`·`479 행`(전부 `@Roles("Admin")`)로 이미 main 에 실재(= 계약 고정이지 신설 아님). 기존 커버는 perf-spec 뿐인데 `export-running-read.perf-spec.ts` `1~40 행` 이 자인하듯 `ExportJobService` 를 `useValue` mock 으로 갈고 `overrideGuard(JwtAuthGuard/RolesGuard)` 로 가드를 무력화해 **실 DB·실 RBAC·실 route matching 을 어느 spec 도 통과시키지 않는다**. e2e 로만 잡히는 축 3 개 — ① `running`/`:id/status-view` 가 `:id` 동적 segment 보다 먼저 선언돼야 하는 route ordering(뒤집히면 `GET /running` 이 404, controller `178~180`·`459~461 행` 자기 주석이 근거), ② `createdAt`/`startedAt`/`finishedAt` 의 ISO 직렬화, ③ service `298~300 행` `where: {status:"RUNNING"}` 4 값 enum 필터와 `JOB_STATUS_TO_VIEW`(`136~141 행`)→`describeExportJobStatus` 파생의 실 row 결합. AC 는 happy 3 route + 분기(필터 0/1/다건 · `RUNNING`/`SUCCEEDED` 파생 불변식) + route-ordering 2 + 404 2 + guard `it.each` 3 route × 3 actor + SuperAdmin 과차단 0 + read-only(count·status 불변)를 잠그고, `truncateAll` 이 지우는 actor User 의 `ExportJob.requestedById` Restrict FK 재 seed 순서를 `export-download.e2e-spec.ts` `118~135 행` 패턴으로 명시했다. 오너 게이트 침범 0(perf·load 무접촉 — `157`·`158 행`, 신설 helper 0 + 절단면을 읽기 축 3 route 전량으로 잡아 잔여 slice 0 — `182 행`, `requirements.md` 무접촉 — `183 행`).
- 18:05 driver: T-1980 **DONE** — export job 진행 조회 3 route(`GET /api/admin/export/running` · `:id/status-view` · `:id`)의 e2e 계약을 신설 spec 1 파일로 고정(`test/e2e/export-job-status-read.e2e-spec.ts` +300/-0, cap 300 LOC / 5 파일 경계 내). 기존 커버는 `ExportJobService` 를 `useValue` mock 으로 갈고 guard 를 override 한 perf-spec 뿐이라 실 Postgres 왕복 · 실 RBAC · 실 route matching 이 어느 spec 도 통과하지 않던 지점이다. production · prisma · 워크플로 0 LOC · 신규 helper 0(기존 `auth-e2e-helper` · `db-truncate` 재사용). PR #1554 round 1 APPROVE → squash merge `75463897`.
- 18:05 driver: **테스트 (R-110 · R-112)** — lint · build · unit(481 suite / 13996 case) · `test:cov` 전부 green(`src/` 0 LOC 라 전역 coverage 불변, 임계 line 80% · function 80% 유지). CI `e2e test` leg 에서 신규 suite PASS 확인(R-113). 잠근 축: happy 3 route(파생 view · raw job 필드) · 분기(`RUNNING` 필터 0/1/다건 · status-view 파생 불변식) · **route ordering**(`running` · `:id/status-view` 가 `:id` 동적 segment 에 미포착) · error path(부재 id → 404 + 내부 정보 미노출) · negative 9(3 route × 401/401/403) + SuperAdmin 과차단 0 · read-only 계약(count · status · finishedAt 불변).
- 18:05 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `932f1e69` 의 main run `34254275656`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 본 fire 의 PR #1554 는 pull_request run `34259081879` success(기본 검사 5m52s · 배포 산출물 검증 · `e2e test` · `reviewer agent approval 검증` 전부 green). 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `75463897` 의 main run `34260166965` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 18:05 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T17:37:44Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`375756d6`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 17:37:54Z 즉시성 확인, 중복 claim 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 20 분, claim TTL 60 분 이내). **편차 1 건**: driver 가 executor 에게 lock/claims 무접촉을 지시해 `sync-claim-pr.sh T-1980 1554` 가 수행되지 않아 claim 의 `prNumber` 가 null 로 남았다 — merge 완료 후 `lock_tree_cas_push` 로 orphan claim prune(`3c1542b4`, tree 엔트리 2 개 유지 · 중복 0)으로 정리했고, 다음 fire 부터는 executor dispatch 문구에 sync-claim-pr 수행을 명시 위임한다. bookkeeping 은 lock 재획득 없이 doc-only direct commit. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 18:05 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1980 을 소비). (c) 관점에서도 merge commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 만 큐잉한다.
- 18:20 planner: T-1981 큐잉 — `POST /api/assessment-evaluation/summary` 의 LLM-무의존 계약면 e2e 신설(pr, 신규 spec 1 파일). pre-check 실측: `test/` 전체에서 이 route 히트 0(controller unit spec 25 건은 mock 경유), 같은 controller 의 다른 route 는 e2e 히트 보유(period 9 · fill-run 3 · fill-plan 3 · reset 3 · relative-comparison 2) — summary 만 남은 공백. T-1980 이 이월한 import 대칭 3 route 는 `import-read-realdb`(slice 12) · `import-detail-read-realdb`(slice 21) perf-spec 이 mock 0 · override 0 으로 이미 덮어 중복이라 받지 않고 축을 옮겼다(PLAN 182 행). 절단면은 시점 게이트 skip 200 `{evaluated:false}` · 400 매트릭스 6 종 · 미허용 period 500 · 401/403 · SuperAdmin 과차단 0 · `GET /api/run-status` 로 본 RunStatus 짝 불변식. PLAN 157/158(perf·load 무접촉) · 182(helper 신설 0) · 183(requirements.md 무접촉) 미침범.
- 19:20 driver: T-1981 **DONE** — `POST /api/assessment-evaluation/summary` 의 LLM-무의존 계약면을 신설 e2e spec 1 파일로 고정(`test/e2e/assessment-evaluation-summary-aggregate.e2e-spec.ts` +275/-0, cap 300 LOC / 5 파일 경계 내). 기존 커버는 controller unit spec 25 건뿐이라 전부 mock 경유였고 실 Postgres 왕복 · 실 RBAC · 실 route matching 이 이 route 를 한 번도 통과하지 않았다. production · prisma · web · 워크플로 0 LOC, 신규 helper 0(`auth-e2e-helper` · `db-truncate` 재사용). PR #1555 round 1 APPROVE → squash merge `f7c885ac`.
- 19:20 driver: **테스트 (R-110 · R-112)** — lint · build · tsc(src+test 0 error) · unit(481 suite / 13996 case) · `test:cov` 전부 green(`src/` 0 LOC 라 전역 coverage 불변, 임계 line 80% · function 80% 유지). CI `e2e test` leg 에서 신규 suite(13 it / 케이스 21) PASS 확인(R-113). 잠근 축: happy 시점 게이트 skip 200 `{evaluated:false}`(201 아님 · `result` 키 부재) · 분기 granularity 3(day · week · month) × mode 2(fill · reeval) · DB write 0(`prisma.summary.count` 불변) · error path 미허용 `period="year"` 500 에서 내부 메시지 · 스택 · SQL 비노출 · negative 9 종(401 본문 되비침 0 포함) · SuperAdmin 과차단 0 · `GET /api/run-status` 로 본 RunStatus 짝 불변식(400 · skip 200 · 500 직후 `active === false`).
- 19:20 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `75463897` 의 main run `34260166965`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 본 fire 의 PR #1555 는 pull_request run `34264893776` success(기본 검사 · 배포 산출물 검증 · `e2e test` · perf · Docker 런타임 smoke · `reviewer agent approval 검증` 전부 green). 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `f7c885ac` 의 main run `34267590901` 은 turn 종료 시점 `pending` 이라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 19:20 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T18:37:51Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`558a905d`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 18:38:30Z 즉시성 확인, 중복 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 34 분, claim TTL 60 분 이내). 직전 fire 의 편차를 반영해 executor dispatch 문구에 `sync-claim-pr.sh` 수행을 명시 위임했고 실제로 PR-open 직후 `prNumber=1555` 동기가 박혔다(orphan claim 0). merge 후 `lock_tree_cas_push` 로 claim prune(`2861b376`, tree 엔트리 2 개 유지 · 중복 0). bookkeeping 은 lock 재획득 없이 doc-only direct commit. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 19:20 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 G2(`lock.holder == "cron"`) 가 거짓이고, 판정 시점 `STATE.nextTask` 도 비어 있다(본 fire 가 T-1981 을 소비). (c) 관점에서도 merge commit 의 main run 이 `pending` 이라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 만 큐잉한다.
- 19:25 planner: queued **T-1982** — `POST /api/assessment-evaluation/evaluate` e2e 계약 spec 신설(pr, `test/e2e/assessment-evaluation-evaluate.e2e-spec.ts` 신설 1 파일 / est 295 LOC). issue-still-relevant pre-check(origin/main `4409b6b3`, 실측): controller route decorator **102 개**를 `test/e2e/` 37 spec 의 경로 리터럴과 전수 대조해 e2e 히트 **0 인 route 가 3 개**(`evaluate` · `import/modes` · `import/running`)임을 확인했고, 뒤 2 개는 T-1981 `§Why` 가 realdb perf-spec 중복으로 명시 이월 금지한 축이라 **남은 유일한 공백이 `evaluate`** 다(`git grep -c "assessment-evaluation/evaluate" -- test/` → 0, k6 `s1-batch.js` 도 `unevaluated-fill-run` 만 침). 기존 커버는 controller unit spec 의 mock 축 346 매칭뿐이라 ValidationPipe · guard · 실 Postgres unique 좌표 분기를 어느 spec 도 통과하지 않는다. LLM 왕복 회피는 DI override 가 아니라 module `176~181 행` 의 `LLM_GATEWAY` useFactory 가 읽는 프로덕션 env 분기(`LOAD_TEST_STUB="1"`, ADR-0057 D1 · load-k6.yml `93 행` 선례)로 해결 — guard · pipe · Prisma · orchestrator · persist 는 전부 실물 유지. 절단면: stub 왕복 200 + row 1/2 · fill 재호출 `contributionCount: 0` idempotency · reeval 의 assessmentId 교체(row 1 불변) · 미허용 `scope` 500 누출 0 · 400 매트릭스 5 종 + 401/401/403 · SuperAdmin 과차단 0 · `GET /api/run-status` 짝 불변식. 오너 게이트 침범 0(`157`·`158 행` perf·load 무접촉, `182 행` helper 신설 0, `183 행` requirements.md 무접촉).
- 19:58 driver: T-1982 **DONE** — `POST /api/assessment-evaluation/evaluate` 의 계약면을 신설 e2e spec 1 파일로 고정(`test/e2e/assessment-evaluation-evaluate.e2e-spec.ts` +298/-0, cap 300 LOC / 5 파일 경계 내). route 인벤토리 전수 대조에서 실 부팅 왕복이 0 으로 남은 마지막 route 였고, 기존 커버는 controller unit spec 의 mock 축뿐이라 ValidationPipe · guard · 실 Postgres unique 좌표 분기를 어느 spec 도 통과하지 않았다. LLM 왕복은 DI override 가 아니라 부팅 전 `LOAD_TEST_STUB="1"` 프로덕션 env 분기(ADR-0057 D1)로 회피 — guard · pipe · Prisma · orchestrator · persist 는 전부 실물 유지, 네트워크 0 · override 0. production · prisma · web · 워크플로 0 LOC, 신규 helper 0. PR #1556 round 1 APPROVE → squash merge `aa9020b9`.
- 19:58 driver: **테스트 (R-110 · R-112)** — lint · build · unit(481 suite / 13996 case) 전부 green(`src/` 0 LOC 라 전역 coverage 불변, 임계 line 80% · function 80% 유지). CI `e2e test` leg 에서 신규 suite PASS 확인(R-113). 잠근 축: stub 왕복 happy 200 + DB row 1/2 + 좌표 일치 + `[load-test-stub]` 접두 · fill 재호출의 `contributionCount: 0` idempotency(assessmentId 동일 · row 불변) · reeval 재호출의 row 교체(assessmentId 변경 · count 2 · Assessment 1 유지 · Contribution cascade 재생성) · 미허용 `scope` 의 500 에서 내부 메시지 · 스택 · SQL 비노출(Assessment 0) · negative 8 종(400 5 · 401 2 · 403 1, row 0) · SuperAdmin 과차단 0 · `GET /api/run-status` 로 본 RunStatus 짝 불변식(400 · 200 · 500 직후 `active === false`).
- 19:58 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `f7c885ac` 의 main run `34267590901`)은 job 0 개 `cancelled` 로, 뒤이은 main push(`4409b6b3` run `34267828091` success · `ffa2de29` run `34271091945` success)에 의해 대체된 **양성 supersede** 다 — runner 장애도 fail 도 아니므로 `consecutiveFails` 0 유지하고 정산 종료. 본 fire 의 PR #1556 은 pull_request run 전 leg green(기본 검사 · 배포 산출물 검증 · `e2e test` · perf · Docker 런타임 smoke · `reviewer agent approval 검증`). 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `aa9020b9` 의 main run `34271673279` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계에서 재확인하도록 이월한다(1 건).
- 19:58 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T19:38:06Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) 확보 → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`cb42ac5e`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 19:38:06Z 즉시성 확인, 중복 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 16 분, claim TTL 60 분 이내). executor 가 PR-open 직후 `sync-claim-pr.sh` 로 `prNumber=1556` 을 박아 orphan claim 0. merge 후 bookkeeping 은 lock 재획득(`02964508`) 하에 수행하고 claim prune + release 로 마감. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 19:58 driver: **[7.5] multi-task chain 미진입** — G1(`flags.multiTaskFire` true) 은 충족하나 fineGrainedConcurrency ON 경로라 claim 직후 lock 을 tombstone 해제해 판정 시점 G2(`lock.holder == "cron"`) 가 거짓이었고, `STATE.nextTask` 도 비어 있다(본 fire 가 T-1982 를 소비). (c) 관점에서도 merge commit 의 main run 이 `in_progress` 라 진입 근거가 없다. ADR-0020 Decision (2) 대로 두 번째 task 에 진입하지 않고 planner dispatch 로 다음 fire 용 task 만 큐잉한다.
- 20:10 planner: queued **T-1983** — 전 route 인증 guard 적용률 census drift smoke spec 신설(pr, `test/smoke/route-auth-guard-coverage-census-drift.smoke-spec.ts` 신설 1 파일 / est 290 LOC). 출처는 [T-1379](../tasks/T-1379-requirements-auth-protection-coverage-status-rejudge.md) `## Follow-ups` 가 별도 slice 로 명시 이월한 "전 route 보호 적용률을 강제하는 drift-guard spec". pre-check(origin/main `37da6882`, 실측): `src/**/*.controller.ts` **23 개** · route decorator **89 개** · 보호 **64** / 미보호 **25**(app 1 · auth 3 · users signup 1 · group 9 · part 6 · person 5), 클래스 레벨 `@UseGuards` 0(전부 메서드 레벨), `APP_GUARD` 히트 **0**(전역 등록 없음), `test/smoke/` 의 guard census spec **0**. T-1379 시점(20 controller / 74 route / 미보호 25) 대비 route 가 +15 늘었는데 그 증가분이 전부 보호된 것은 강제 장치 없이 우연이었다 — 본 spec 이 미보호 full-path 집합을 allowlist(`public-by-design` 5 / `known-gap-REQ-043` 20)와 **정확 일치**로 고정해 신규 미보호 route 유입·기존 guard 제거를 red 로 잡는다. guard 실 배선(group·part·person 20 route)은 §5 인증 변경이라 오너 승인 대상 — 본 task 는 `src/` 0 LOC, 관측만. 오너 게이트 침범 0(perf/load 신규 slice 0 · 공유 helper 신설 0 · `docs/requirements.md` 무접촉).
- 20:59 driver: **T-1983 DONE (pr, PR #1557 → main squash `23a26f01`)** — 전 route 인증 guard 적용률 census drift smoke spec 신설(`test/smoke/route-auth-guard-coverage-census-drift.smoke-spec.ts` 1 파일 +300/-0, `src/` 0 LOC). [T-1379](../tasks/T-1379-requirements-auth-protection-coverage-status-rejudge.md) `## Follow-ups` 가 별도 slice 로 이월한 관측 축을 계약으로 바꿨다 — controller 발견 · `@Controller` prefix 추출 · route/guard census 3 함수를 spec 파일 안에 두고 같은 파일에서 소비(공유 helper 신설 0). guard 실 배선(group · part · person 20 route)은 CLAUDE.md §5 인증 변경이라 오너 승인 대상으로 그대로 남긴다.
- 20:59 driver: **잠근 축** — happy 3 축 하한(controller 23+ · route 89+ · 보호 64+) + 미보호 full-path 집합을 allowlist(`public-by-design` 5 / `known-gap-REQ-043` 20 = 25)와 **양방향 정확 일치**로 고정 + `APP_GUARD` 히트 0 전제 고정(전역 guard 등록이 생기면 census 판정 규칙 자체가 뒤집히므로 함께 박음) + error path 2(경로 부재 throw · `@Controller` 부재 throw) + 분기 4 + 주석 오염 방지 1 + 회귀 감지 능력 자체 검증. 신규 미보호 route 유입 · 기존 guard 제거가 이제 red 로 잡힌다.
- 20:59 driver: **테스트 (R-110 · R-112 · R-113)** — lint · build · unit(481 suite / 13996 case) · `test:cov` 전부 green(`src/` 0 LOC 라 전역 coverage 불변, 임계 line 80% · function 80% 유지). 신규 suite 14/14 PASS. 로컬은 DB 부재로 globalSetup 을 우회했고 `test:smoke` 정본은 CI leg 에서 green 확인.
- 20:59 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `aa9020b9` 의 main run `34271673279`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 그 뒤 main push 2 건(`37da6882` run `34271899420`, `e4fcb650` run `34272855367`)도 success. PR #1557 은 전 leg green(기본 검사 · 배포 산출물 검증 · e2e · perf · Docker 런타임 smoke · `reviewer agent approval 검증`) → 4-게이트 충족 후 `--squash --delete-branch` merge. merge commit `23a26f01` 의 main run `34277861673` 은 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계로 이월(1 건).
- 20:59 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T20:38:12Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`c1893c44`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 20:38:12Z 즉시성 확인, 중복 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 20 분, claim TTL 60 분 이내). executor 가 PR-open 직후 `sync-claim-pr.sh` 로 `prNumber=1557` 을 박아 orphan claim 0(claim status `PR_OPEN` 확인). bookkeeping 은 lock 재획득(`a3d3b796`) 하에 수행하고 claim prune + release 로 마감. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동, dup-PR 0.
- 21:20 planner: queued **T-1984** — REQ-043 상태 칸 적용률 축 drift 정정(direct, `docs/requirements.md` 1 파일 / est 20 LOC). 상태 칸의 census 가 [T-1379](../tasks/T-1379-requirements-auth-protection-coverage-status-rejudge.md) 실측 시점(2026-08-02) 값에 멈춰 있어 네 수치가 틀렸다 — pre-check(origin/main `c0715e47`, 주석 제거 실측): controller **23**(문서 20) · `@UseGuards` 보유 controller **19**(문서 16) · route decorator **89**(문서 74) · 보호 route **64**(문서 49). 세 값은 방금 머지된 census spec 의 `MIN` 상수(`19 행`)와 정확히 일치한다. 또 문단 끝 "group · part · person 의 미보호를 fail 로 잡는 test 는 없다" 는 [T-1983](../tasks/T-1983-route-auth-guard-coverage-census-smoke.md) 머지(`23a26f01`)로 **거짓**이 됐다 — `test/smoke/route-auth-guard-coverage-census-drift.smoke-spec.ts` 가 미보호 집합을 allowlist 25 건(`public-by-design` 5 · `known-gap-REQ-043` 20)과 양방향 정확 일치로 고정해 이미 red 로 잡는다. 등급은 `IN_PROGRESS` **불변**(20 route 실 배선은 CLAUDE.md §5 오너 승인 대상) — 재판정 왕복이 아니라 stale 수치 · 거짓 문장의 drift 정정 1 회(§3.1 예외). 이미 참인 서술(guard 미참조 controller 4 · 미보호 21 route)은 무접촉. `src/` · `test/` 0 LOC, 오너 게이트 침범 0(PLAN `157`·`158`·`182 행`).
- 21:39 driver: **T-1984 DONE (direct, main `30b197a3`)** — [docs/requirements.md](../requirements.md) REQ-043 상태 칸 적용률 축의 stale census 수치 4 개를 실측값으로 교체(controller 20→**23** · `@UseGuards` 보유 16→**19** · route decorator 74→**89** · 보호 route 49→**64**). 세 하한은 [T-1983](../tasks/T-1983-route-auth-guard-coverage-census-smoke.md) 이 머지한 census spec 의 `MIN` 상수(`19 행`)와 정확히 일치한다. 1 파일 +1/-1, `src/` · `test/` · `web/` 0 LOC.
- 21:39 driver: **거짓 문장 교체** — "group · part · person 의 미보호를 fail 로 잡는 test 는 없다" 는 T-1983 머지(`23a26f01`)로 거짓이 됐으므로 census smoke spec 좌표(`19 행` 하한 · `21~57 행` allowlist 25 건 · `213 행` 태그 건수) + 정적 추출 기반이라는 한계 서술로 교체했다. 등급은 `IN_PROGRESS` **불변** — 20 route 실 배선은 여전히 CLAUDE.md §5 오너 승인 대상이라 재판정이 아니라 §3.1 예외의 drift 정정 1 회다. 이미 참인 서술(guard 미참조 controller 4 · 미보호 21 route · 공개 의도)은 무접촉.
- 21:39 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(merge commit `23a26f01` 의 main run `34277861673`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 그 뒤 main push 2 건(`c0715e47` run `34278084903`, `3eff9dc9` run `34278889172`)도 success. 본 fire 는 direct doc-only 라 PR·reviewer 없음(R-110 면제) — main push `30b197a3` 의 run `34281853455` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계로 이월(1 건).
- 21:39 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T21:38:13Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`512ed8cf`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) 단독 claim 조건 충족(`dependsOn: [T-1983]` 머지됨, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 21:38:22Z 즉시성 확인, 중복 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 1 분, claim TTL 60 분 이내). bookkeeping 은 lock 재획득(`acd85216`) 하에 수행하고 claim prune + release 로 마감. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동.
- 21:45 planner: queued **T-1985** — 전 route e2e 왕복 커버리지 census drift smoke spec 신설(pr, `test/smoke/route-e2e-coverage-census-drift.smoke-spec.ts` 신설 1 파일 / est 290 LOC). PLAN `166 행` e2e 커버리지 축은 T-1960~T-1982 40 여 slice 로 사실상 닫혔지만 그 성과를 지키는 장치가 0 이라, 새 route 가 e2e 없이 들어와도 red 가 나지 않는다. issue-still-relevant pre-check(origin/main `9e1a0bb4`, 실측): `test/smoke/` 153 개 중 census 계열은 T-1983 의 guard 축 1 개뿐(e2e 축 중복 0) · controller **23** · route decorator **89** · e2e spec **38** · e2e 왕복 0 인 route 는 정확히 **2 건**(`GET /api/admin/import/running` · `/modes`)이고 둘 다 T-1981 이 realdb perf-spec 중복으로 명시 이월한 축이라 새 e2e 를 만들지 않고 allowlist 로만 박는다. 판정 비대칭은 T-1983 승계 — 4 축 하한 + 미커버 집합 정확 일치.
- 22:56 driver: **T-1985 DONE (pr, PR #1558 → main `c328d5c9`)** — 전 route e2e 왕복 커버리지 census drift smoke spec 신설([test/smoke/route-e2e-coverage-census-drift.smoke-spec.ts](../../test/smoke/route-e2e-coverage-census-drift.smoke-spec.ts) 1 파일 +285/-0, `src/` · `web/` · e2e · prisma · 워크플로 0 LOC). T-1960~T-1982 40 여 slice 가 닫은 route 별 e2e 왕복 공백을 스냅샷이 아니라 **불변식**으로 바꾼다 — 순수 함수 3 개(`censusRoutes` · `isCoveredBy` · `uncoveredLabels`) + fs IO helper 로 Nest 부팅 0 · DB 0 · 네트워크 0. 4 축 하한(controller 23 · route decorator 89 · e2e spec 38 · 커버 route 87)과 **미커버 집합 == allowlist 2 건**(`GET /api/admin/import/running` · `/modes`, 둘 다 T-1981 이 realdb perf-spec 중복으로 명시 이월한 축이라 reason 태그로 박제) 양방향 정확 일치. 직전 [T-1983](../tasks/T-1983-route-auth-guard-coverage-census-smoke.md) guard 축 census 의 판정 비대칭 골격을 승계했고 공통 helper 추출은 task 가 별도 slice 로 이월.
- 22:56 driver: **검증 (R-110 · R-112 · R-113)** — implementer 후 tester 필수 호출, `pnpm lint` · `pnpm build` · `pnpm test` 481 suite / 13996 test green, `test:cov` line **99.94%** · function **100%**(임계 80/80 충족). it 14 개가 4 축 하한 · 집합 정확 일치 · 분기 (a)~(d) · error path · negative (1)~(5) 를 각 1+ 로 cover. CI smoke leg 에서 신규 suite PASS(147 passed / 154 total) 확인.
- 22:56 driver: **4-게이트 (README 116 행)** — reviewer round 1/7 APPROVE, reviewer comment 외부 존재(CI "reviewer agent approval 검증" step green), integrator 자체 점검 통과, PR CI green → `--squash --delete-branch` 머지. reviewer MINOR 3 건(정적 suffix segment 인접성 미검사 · 동적 segment 정규식 관대 · 토크나이저 T-1983 과 동형 중복)은 머지 차단 아님 — 공통 추출은 task 가 이미 이월한 축이라 follow-up 후보로만 남긴다.
- 22:56 driver: **CI 정산 (R-114)** — 직전 fire 이월 1 건(main push `30b197a3` 의 run `34281853455`)은 본 fire 첫 단계에서 `success` 확인 → 정산 완료, `consecutiveFails` 0 유지. 본 fire 머지 commit `c328d5c9` 의 main run `34288259722` 는 turn 종료 시점 `in_progress` 라 다음 fire 첫 단계로 이월(1 건).
- 22:56 driver: **동시성 (ADR-0036)** — server-time `2026-09-08T22:37:58Z`(`gh api -i rate_limit` Date 헤더, midnight fallback 아님) → lock tombstone · `claims.json` `[]` 확인 → `acquire-lock.sh` 획득(`7148fbcb`) → `reclaim-stale-claim.sh` 회수 대상 0 → 활성 claim 0 이라 §D8 (e) pr-mode 단독 claim 조건 충족(`dependsOn: []`, `touchesFiles` 교집합 0) → `select-claim.sh` atomic claim(`claimedAt` 22:37:58Z 즉시성 확인, 중복 0) → 같은 commit 으로 lock tombstone release → lock-free executor 진행(약 18 분, claim TTL 60 분 이내). PR open 직후 `sync-claim-pr.sh T-1985 1558` exit 0. bookkeeping 은 lock 재획득(`72e40d11`) 하에 수행하고 claim prune + release 로 마감. `concurrencyIncidents` 4 유형 전부 임계(2) 미달 — 회로 차단기 미발동.
- 23:05 planner: queued **T-1986** — route census 정적 추출 공통 helper 추출 + 두 census smoke spec 동시 배선(pr, `test/helpers/route-census.ts` + colocated spec 신설 · 소비처 2 spec 배선, 4 파일 / est 280 LOC). T-1983(guard 축) · T-1985(e2e 축) 두 census 가 같은 토크나이저를 각자 복제해, 추출기 결함을 한쪽만 고치면 두 계약이 서로 다른 route 모수를 세게 된다. issue-still-relevant pre-check(origin/main `7d08e8da`, 실측): `test/helpers/` 에 census helper 부재(히트 0) · `git grep TOKEN_RE origin/main` 히트는 그 smoke spec 2 개뿐 · 두 파일의 동일 trimmed 행 **69 개**이고 `findFiles` 는 guard `84~92 행` = e2e `144~152 행` **8 행 byte-identical**, 정규식 4 종도 내용 동일. 소비처 동반 의무(§3) 충족 — helper + 소비처 2 개 같은 PR. `censusRoutes` 본체 통합(반환 타입 상이)은 Out of Scope 로 이월.
