# 2026-08-01 (UTC)

- 00:44 driver: T-1360 DONE — requirements.md 53·54 행 REQ-034·REQ-035 (일별/주간·월간 요약 평가) 상태 컬럼을 실측 기반 `PLANNED` → DONE 으로 재판정. 2 파일 +18/-12, main direct commit `0079040e`, CI run 30676074011 success.
- 실측 근거: `computePeriodEnd` / `isPeriodEvaluable` / `enumerateSummaryDueCoordinates` / `SummaryBatchOrchestratorService` / `evaluateAndPersist` 각 1 hit + spec 4 파일 존재, day/week/month 3 분기 cover 확인.
- 잔여 `PLANNED` row 34 → 32. requirements-status-resync 축 계속.
- 다음: planner 가 T-1361 (requirements.md 42~44 행 REQ-023~025 서비스 ID 매핑 상태 재판정, commitMode direct) 을 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.
- 01:45 driver: T-1361 DONE — requirements.md 42~44 행 REQ-023·REQ-024·REQ-025 (서비스별 ID 매핑 · primary 지정 · NULL 허용) 상태 컬럼을 실측 기반 `PLANNED` → DONE 으로 재판정. 2 파일 +25/-11, main direct commit `54fca341`, CI run 30678429138 success.
- 실측 근거: `model ServiceIdentity` 1 hit + `@@unique([personId, service])` 선언 1 + `isPrimary` 필드 1 + repository `setPrimary` (`$transaction` updateMany→update) 1, `src/user/service-identity.*` 2 파일 (service·controller 부재). REQ-024 는 "정확히 1 primary" service-layer 강제·전용 ADR 부재를 상태 문자열에 한계로 부기.
- 잔여 `PLANNED` row 34 → 31 (REQ row 32 → 29). requirements-status-resync 축 7 번째 slice.
- 다음: planner 가 T-1362 (requirements.md 45·47 행 REQ-026 인원 CRUD+deactivate · REQ-028 Group 정책 상태 재판정, commitMode direct) 를 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.
- 02:47 driver: T-1362 DONE — requirements.md 45 · 47 행 REQ-026(평가 대상 인원 CRUD + deactivate/activate) · REQ-028(Group 정책 — 다중 group 소속 + 조직도 part) 상태 컬럼을 실측 기반 `PLANNED` → DONE 으로 재판정. 2 파일 +24/-9, main direct commit `db390657`.
- 실측 근거: `src/user` 의 person · group · person-group-membership · part 축 모듈과 대응 e2e spec 을 직접 확인해 CRUD · deactivate/activate · 다중 group 소속 · 조직도 part 1 개 배선을 실재 확인. REQ-028 은 nullable 한계를 상태 문자열에 부기.
- 잔여 `PLANNED` row 31 → 29 (REQ row 29 → 27). requirements-status-resync 축 8 번째 slice.
- 본 fire 는 fineGrainedConcurrency 경로(lock CAS 획득 → server-time 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 → validate PASS → select-claim + 동일 commit lock release) 로 진행. N=1 fire.
- CI: main run(headSha db390657, 02:40:17Z)은 fire 종료 시점 in_progress — 다음 fire 가 conclusion 재확인. 직전 main CI(84613294) success.
- 다음: planner 가 T-1363 (requirements.md 48 · 51 행 REQ-029 비휘발 저장 · REQ-032 raw 저장 금지 상태 재판정, commitMode direct) 를 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.
planner: queued T-1364 — requirements.md 49 행 REQ-030 Export/backup + Restore 상태를 실측 기반 재판정 (requirements-status-resync 10 번째 slice, commitMode direct)

## 12:45 driver turn (cron@aa-local-fee7d19d-1243) — T-1363 DONE

- T-1363 (requirements.md 48 · 51 행 REQ-029 비휘발 저장 · REQ-032 raw 저장 금지 상태 실측 재판정) DONE — main direct commit `d3d610f0`.
- 잔여 `PLANNED` row 29 → 27. requirements-status-resync 축 9 번째 slice.
- 본 fire 는 fineGrainedConcurrency 경로(lock CAS 획득 tip fead4b49 → server-time 03:37:35Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 → validate PASS → select-claim + 동일 commit lock release) 로 진행. N=1 fire.
- CI: 직전 fire 가 남긴 in_progress run 2 건(db390657 · fee7d19d) 모두 success 확인. 본 turn 의 main run(headSha d3d610f0, 03:40:35Z)은 fire 종료 시점 in_progress — 다음 fire 가 conclusion 재확인.
- 다음: planner 가 T-1364 (requirements.md 49 행 REQ-030 Export/backup + Restore 상태 재판정, commitMode direct) 를 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.

## 17:45 driver turn (cron@aa-local-c5384745-24563) — T-1364 DONE

- T-1364 (requirements.md 49 행 REQ-030 Export/backup + Restore 상태 실측 재판정) DONE — main direct commit `3f414f53`.
- 근거 실측: export/import controller 경로 + `ExportJob`(614 행)·`ImportJob`(649 행) model + e2e 5 spec 56 it. 미확인 사항(restore reset 범위 · MERGE 모드 병존)은 "한계 —" 로 부기. requirements-status-resync 축 10 번째 slice.
- 본 fire 는 fineGrainedConcurrency 경로(lock CAS 획득 tip 322c42a2 → server-time 08:38:52Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 → validate PASS → select-claim + 동일 commit lock release → 종료 시 claims `[]` prune tip f8afeb75) 로 진행. N=1 fire.
- CI: 직전 fire 가 남긴 in_progress run(headSha c5384745) success 확인. 본 turn 의 main run(headSha 3f414f53, 08:41:48Z)은 fire 종료 시점 in_progress — 다음 fire 가 conclusion 재확인.
- 다음: planner 가 T-1365 (requirements.md 50 행 REQ-031 재수집 중복 방지 상태 재판정, commitMode direct) 를 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.
planner: queued T-1365 — requirements.md 50 행 REQ-031 재수집 중복 방지 + 최근 1주 재수집 상태를 실측 기반 재판정 (requirements-status-resync 11 번째 slice, commitMode direct)

## 09:45 driver: T-1365 DONE — requirements.md REQ-031 재수집 중복 방지·최근 1주 window 상태 실측 재판정

- `docs/requirements.md` 50 행 REQ-031 상태를 `PLANNED` → `DONE (implemented-on-main)` 로 재판정. 근거: `commit-dedup.ts`(`dedupGithubActivities`, commit 은 `commit:<sha>` earliest-wins / pr·issue 는 `<kind>:<repoRef>:<externalId>`) · `page-dedup.ts`(page-id + 최대 `version` latest-wins) · `recollection-window.ts`(`applyRecollectionWindow` + `RECOLLECTION_WINDOW_DAYS = 7`) + 배선 4 곳 + spec 4 개 76 it. "한계 —" 로 window 밖 late-arriving 보정 부재 · `CRON_TICK_HANDLER` no-op stub 부기. direct commit `4d4166b6`.
- 본 fire 는 fineGrainedConcurrency 경로(lock CAS 획득 tip c5567717 → server-time 09:37:58Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 → validate PASS → select-claim + 동일 commit lock release → 종료 시 claims prune) 로 진행. N=1 fire.
- CI: 직전 fire 가 남긴 in_progress run(headSha 3f414f53) success 확인. 본 turn 의 main run(headSha 4d4166b6, id 30694215314)은 fire 종료 시점 in_progress — 다음 fire 가 conclusion 재확인.
- 다음: planner 가 T-1366 (requirements.md 52 행 REQ-033 건별 기여도·난이도·양 상태 재판정, commitMode direct) 를 nextTask 로 dispatch.
- blocker 없음. 열린 humanQuestion 0.

planner: queued T-1366 — requirements.md 52 행 REQ-033 (건별 기여도·난이도·양 보유) 상태를 `Contribution` model + 평가 산정/영속 경로 실측 기반으로 재판정 (requirements-status-resync 12 번째 slice, commitMode direct)


planner: queued T-1367 — requirements.md 55 행 REQ-036 (상대 비교 + LLM 정성 + Metric 수치) 상태를 `Summary` model narrative/metricScore + summary-narrative/aggregate 경로 실측 기반으로 재판정 (requirements-status-resync 13 번째 slice, commitMode direct)

10:47 driver: T-1366 DONE — requirements.md 52 행 REQ-033 (건별 기여도·난이도·양 보유) 상태를 PLANNED → DONE (implemented-on-main) 으로 실측 재판정. 근거 = schema.prisma 329 행 Contribution 의 difficulty/contributionScore/volume 3 축 + 348 행 @@unique([assessmentId, sourceRef]) 건별 키, evaluation-scoring.service 90 행 scoreUnit() 산정 경로, evaluation-result-persist.service 213·216~217 행 nested create 영속 배선, unit 5 spec 107 it. 한계 — sourceType 이 buildUnitId prefix 와 KNOWN_SOURCE_TYPES 미일치로 "" placeholder 인 점 부기. 2 파일 +27/-12, spec commit 61edcc1c(direct→main).
- CI: run 61edcc1c push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. blocker 없음, 열린 humanQuestion 0.
- 다음: T-1367 (requirements-status-resync 13 번째 slice — REQ-036 재판정).

11:43 driver: T-1367 DONE — requirements.md 55 행 REQ-036 (상대 비교·LLM 정성·Metric 수치 보유) 상태를 PLANNED → IN_PROGRESS 로 실측 재판정. 근거 = schema.prisma 361~380 행 Summary 의 narrative/metricScore 두 축 + (personId, period, periodStart) 좌표 키, summary-narrative.service 54/96/106 행 LLM 정성 산정 경로, summary-aggregate 84 행 metricScore 수식, summary-persist 105/110/119/192 행 영속 배선, unit 4 spec 110 it. 미충족 축 — 순위·백분위·person 간 비교 산출 심볼이 src/ 전체에 부재(summary.controller 100~105 행 findAll 은 personId/period filter + periodStart desc 만) → DONE 아닌 IN_PROGRESS. 2 파일 +4/-2, spec commit 5ba64189(direct→main).
- CI: run 5ba64189 push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 61edcc1c·711f9e04 run 은 둘 다 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 다음: T-1368 (requirements-status-resync 14 번째 slice — REQ-037 평가 없는 부분 일괄 평가 + Reset & Reeval 재판정).

12:52 driver: T-1368 DONE — requirements.md 56 행 REQ-037 (평가 없는 부분 일괄 평가 + Reset & Reeval) 상태를 PLANNED → IN_PROGRESS 로 실측 재판정. 충족 축 = 일괄 평가 endpoint + service 심볼, "평가 결과 없음" 판별 로직, persistInTransaction 의 delete→create 재평가 경로, e2e 3 spec 27 it. 미충족 축 = 명시적 Reset endpoint 부재(deleteMany 2 건은 내부 경로일 뿐 controller wiring·Admin gate 없음) + Reset 축 e2e 0 → DONE 아닌 IN_PROGRESS. 2 파일 +18/-3, spec commit 16bbdff0(direct→main).
- CI: run 16bbdff0 push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 5ba64189·c89ac54d run 은 둘 다 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 다음: T-1369 (requirements-status-resync 15 번째 slice — REQ-013 저성과자 식별 재판정).

13:45 driver: T-1369 DONE — requirements.md 32 행 REQ-013 (저성과자 식별 signal/adjust) 상태를 PLANNED → 실측 재판정. 로직 축(signature·판정식·FLOOR 비교 대상) + wiring 축(두 pipeline + orchestrator 158/177 행 호출) + 노출 축(narrative marker only, 전용 컬럼 0) 3 축을 소스 인용으로 확정, 미확인 부분은 "한계 —" 부기. 검증 위치는 unit 2 spec 38 it / e2e 0. 2 파일 +24/-11, spec commit ae761351(direct→main).
- CI: run ae761351 push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 16bbdff0·3ac36b55 run 은 둘 다 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 다음: T-1370 (requirements-status-resync 16 번째 slice — REQ-009 Fork/Rebase/Meld 중복 제거 + 시간적 중복 earlier-date 우선 재판정).

14:50 driver: T-1370 DONE — requirements.md 28 행 REQ-009 (Fork/Rebase/Meld 중복 제거 + 시간적 중복) 상태를 PLANNED → IN_PROGRESS 로 실측 재판정. 시간적 중복 축(earlier date 우선)·fork 동일 SHA 축·wiring 축은 실재 확인, rebase/meld 로 SHA 가 재작성된 내용물 중복은 dedupKey 가 SHA 기반이라 미검출(내용 hash/유사도 dedup 은 src 전체 부재) — "한계 —" 부기. 2 파일 +24/-11, spec commit c503b1bf(direct→main).
- CI: run c503b1bf **success 확인 완료**(gh run watch, consecutiveFails 0). 직전 fire 의 ae761351·ae01f625 run 도 둘 다 success. blocker 없음, 열린 humanQuestion 0.
- 다음: T-1371 (requirements-status-resync 17 번째 slice — REQ-014 Issue 평가 본인 follow-up 소비 제외 재판정).

15:45 driver: T-1371 DONE — requirements.md 33 행 REQ-014 (Issue 작성 평가, 본인 follow-up 자가 소비 제외) 상태 컬럼을 실측 기반 `PLANNED` → `DONE (implemented-on-main)` 으로 재판정. 2 파일 +25/-11, main direct commit `1962fa40`.
- 실측 근거: `excludeSelfFollowUps` (evaluation-dedup.ts) 의 제외 판정 3 단계 + `evaluation-orchestrator.service.ts` 148 행 wiring 전수 grep + spec 14 it. 한계 — comment thread 미수집으로 issue 단위 author 동일성 휴리스틱, e2e 검증 부재를 상태 문자열에 부기.
- CI: run `1962fa40` push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 `80cffb3e` run 은 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 다음: T-1372 (requirements-status-resync 18 번째 slice — REQ-018 단순 보고·copy-paste 로그 zero-contribution 재판정).

planner: queued T-1372 — requirements.md 37 행 REQ-018 (단순 보고·copy-paste 로그 zero-contribution) 상태를 quality-signal/quality-adjust 심볼 쌍 + pipeline wiring 실측 기반으로 재판정 (requirements-status-resync 18 번째 slice, commitMode direct)

16:45 driver: T-1372 DONE — requirements.md 37 행 REQ-018 (단순 보고성 자료·copy-paste 로그 zero-contribution) 상태 컬럼을 실측 기반 `PLANNED` → `DONE` 으로 재판정. 2 파일 +26/-12, main direct commit `7a5ff1e9`.
- 실측 근거 3 축: `computeContributionQualitySignal` (evaluation-quality-signal.ts, TITLE_FLOOR=1 판정식) + `applyContributionQualityFloor` (evaluation-quality-adjust.ts, `CONTRIBUTION_QUALITY_FLOOR_LEVEL="zero"` 3 조건 AND · 멱등 비파괴) + detection/adjustments 두 pipeline wiring 전수 grep (dead code 아님), spec 44 it. 한계 — copy-paste 내용 비교 부재·임계 1 튜닝 근거·오탐 해제 경로 부재·e2e 부재를 상태 문자열에 부기.
- CI: run `7a5ff1e9` push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 `1962fa40`·`123921f2` run 은 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 다음: T-1373 (requirements-status-resync 19 번째 slice — REQ-011 중요·어려운 기여 높은 점수 재판정).

planner: queued T-1373 — requirements.md 30 행 REQ-011 (중요·어려운 기여 → 높은 점수) 상태를 notable-contribution 심볼 쌍 + pipeline wiring 실측 기반으로 재판정 (requirements-status-resync 19 번째 slice, commitMode direct). 직전 T-1372 Out of Scope 가 지목한 REQ-019 는 오지정 — `evaluation-notable-contribution-*.ts` 상단 주석이 스스로 R-25/REQ-011 을 책임 범위로 명시.

17:45 driver: T-1373 DONE — requirements.md 30 행 REQ-011 (중요·어려운 기여 → 높은 점수) 상태 컬럼을 실측 기반 `PLANNED` → `IN_PROGRESS` 로 재판정. 2 파일 +27/-12, main direct commit `7da39724`.
- 실측 근거 2 축 분리: 식별 축(`computeNotableContributionSignal` + `NOTABLE_RELATIVE_CEILING`, detection/adjustments 두 pipeline + orchestrator wiring 전수 확인, spec 38 it) 은 실재하나, **높은 점수 부여 축은 부재** — `applyNotableContributionAnnotation` 이 점수 필드(difficulty/contribution/volume) 를 올리지 않고 `narrative` marker 접두만 붙임 → README 25 행 뒷절 미충족이라 `DONE` 아닌 `IN_PROGRESS` 판정. 한계 — 점수 상향 부재·임계 calibration 근거 부재·manual 검증 절차 부재를 상태 문자열에 부기, Follow-ups 4 건 박제.
- CI: run `7da39724` push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 `7a5ff1e9`·`ebe9394d` run 은 둘 다 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup fire — lock CAS 획득(tip `74abf440`) → server-time 17:37:16Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 게이트 통과 → T-1373 claim(17:37:38Z) + 같은 commit 으로 lock tombstone release → executor lock-free 수행. 회로 차단기 4 유형 모두 2회 임계 미만(double-claim 1) — 강등 없음.
- 다음: T-1374 (requirements-status-resync 20 번째 slice — REQ-019 새 알고리즘·외부 연구 소개 = 높은 contribution 재판정).

planner: queued T-1374 — requirements.md 38 행 REQ-019 (새 알고리즘·외부 연구 소개 → 높은 contribution) 상태를 quality 심볼 쌍 실측 기반으로 재판정 (requirements-status-resync 20 번째 slice, commitMode direct)

18:45 driver: T-1374 DONE — requirements.md 38 행 REQ-019 (새 알고리즘·외부 연구 소개 → 높은 contribution) 상태 컬럼을 실측 기반 `PLANNED` → `IN_PROGRESS` 로 재판정. 2 파일 +17/-3, main direct commit `8561b43e`.
- 실측 근거 2 축 분리: 식별 축은 LLM marker 단일 경로로 실재(quality 심볼 쌍 + detection/adjustments wiring 전수 확인)하나, **높은 contribution 상향 축의 결정적 코드는 부재** → `DONE` 아닌 `IN_PROGRESS` 판정. 한계 — 상향 helper 부재·marker 미인식 시 default `low`(evaluation-prompt.ts 33 행 `DEFAULT_CONTRIBUTION`, task 가설의 `medium` 과 상이)·e2e/smoke 0 을 상태 문자열에 부기, Follow-ups 3 건 박제.
- CI: run `8561b43e` push 시점 in_progress — 다음 fire [1] 에서 conclusion 재확인. 직전 fire 의 `7da39724`·`5a13d66c` run 은 둘 다 success 확인(consecutiveFails 0). blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup fire — lock CAS 획득(tip `bfab0927`) → server-time 18:37:11Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 게이트 통과 → T-1374 claim(18:37:26Z) + 같은 commit 으로 lock tombstone release → executor lock-free 수행. 회로 차단기 4 유형 모두 2회 임계 미만(double-claim 1) — 강등 없음.
- 다음: T-1375 (requirements-status-resync 21 번째 slice — REQ-020 조직 기여 큰 인원 → 높은 점수·평가 코멘트 재판정).

planner: queued T-1375 — requirements.md 39 행 REQ-020 (조직 기여 큰 인원 → 높은 점수·평가 코멘트) 상태를 실측 기반으로 재판정 (requirements-status-resync 21 번째 slice, commitMode direct)

## 04:52 driver: T-1375 DONE — REQ-020 조직 문서 기여 점수·코멘트 상향 상태 실측 재판정 (direct, commit 21710266)

- `docs/requirements.md` 39 행 REQ-020 상태를 `PLANNED` → `IN_PROGRESS` 로 갱신. requirements-status-resync stream 21 번째 slice (+18/-2, 2 파일).
- 실측 근거 3 축 분리: (a) **식별 축 부재** — `src` 전수에서 `R-39`/`REQ-020` 자기 책임 심볼 0, 최근접 `computeNotableContributionSignal` 은 150~152 행이 `contributionKind === "code"` 만 계수하고 21~23·77 행 주석이 document 제외를 자인. 대칭 하향 `evaluation-underperformer-signal.ts` 146 행도 동형. (b) **결정적 점수 상향 축 부재** — 5-adjuster 중 상향 0, `aggregateMetricScore` 입력 `EvaluationResult` 에 `contributionKind` 필드 자체가 없어 kind 구분 불가 (간접 합산 효과만 실재). (c) **문서 기반 코멘트 상향 축 부재** — `applyNotableContributionAnnotation` 은 `[중요기여] ` marker 접두까지고 대상 판정이 code 단위 수 기반이라 문서 기여만 많은 인원은 구조적 제외.
- 한계 3 절 부기: `manual` 검증 절차 문서 부재 / ADR-0003·T-0015 의 `REQ-020` 언급은 옛 번호 체계 잔재 (권한 부족 통지) 라 본 행 근거 아님 / 본 축 e2e·smoke 0. 검증 위치 실 근거 spec it 19+19+15.
- CI: run `30715383921` (head `21710266`) **success 확인 완료** — `gh run watch` 로 conclusion 까지 대기해 R-114 "종료 전 CI 수행" 충족. 직전 fire 의 `8561b43e`·`aa9e17cd` run 도 success (consecutiveFails 0 유지). blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup fire — lock CAS 획득(tip `f1bea5c0`) → server-time 19:37:23Z 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 게이트 통과 → T-1375 claim(19:37:23Z) + 같은 commit 으로 lock tombstone release → executor lock-free 수행 → bookkeeping 재-lock(tip `465d4381`). 회로 차단기 4 유형 모두 2회 임계 미만(double-claim 1) — 강등 없음.
- 다음: T-1376 (requirements-status-resync 22 번째 slice — REQ-010 코드 기여 양·질 평가 상태 재판정).

planner: queued T-1376 — requirements.md 29 행 REQ-010 (코드 기여 양·질 평가) 상태를 volume/quality 심볼 쌍 + wiring 실측 기반으로 재판정 (commitMode direct)

## 05:52 driver: T-1376 DONE — REQ-010 코드 기여 양·질 평가 상태 실측 재판정 (direct, commit a84cab40)

- `docs/requirements.md` 29 행 REQ-010 상태를 `PLANNED` → `DONE (implemented-on-main)` 으로 갱신. requirements-status-resync stream 22 번째 slice (+13/-3, 2 파일). 코드·schema 변경 0.
- 실측 근거 5 축 분리: (a) **양 축 실재** — `evaluation-volume.ts` 30 행 `calculateEvaluationVolume`, 31 행이 `metadata.titleLength` 단일 신호를 세며 코드 규모(diff LOC 등) 는 미참조. (b) **질 축 2 층 실재** — `evaluation-prompt.ts` 155 행 `classifyNarrative` (+ 32/33 행 default) 와 `evaluation-quality-signal.ts` 115 행 (52 행 임계 1) · `evaluation-quality-adjust.ts` 119 행 (62 행 zero 강등). (c) **둘 모두 병존** — `evaluation-result.ts` 54 행 5 필드 + `schema.prisma` 329 행 `Contribution` 335~337 행 3 컬럼이 양·질을 합성 없이 병렬 보존. (d) **wiring 실재** — detection-pipeline 108 행 · adjustments-pipeline 211 행 (5-adjuster 중 3 번째) · orchestrator 158/164/177 행. (e) **영속·노출** — REQ-010 전용 컬럼·플래그는 부재.
- 한계 부기: 질 축이 **zero 강등만 있고 상향은 부재** 인 비대칭, 양 축이 `titleLength` 프록시라 코드 규모 미반영. 검증 위치 실 근거 — domain unit 5 spec 123 it + e2e contributions 19 it (노출 축만) + live smoke 2 종 env-gated skip.
- CI: run `30717585904` (head `a84cab40`) **success 확인 완료** — `gh run watch` 로 conclusion 까지 대기해 R-114 "종료 전 CI 수행" 충족. 직전 fire 의 `21710266`·`6f223f02` run 도 success (consecutiveFails 0 유지). blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup fire — lock CAS 획득(tip `a60c162f`) → server-time 20:37:09Z 주입 reclaim 회수 0 → validate-claim-candidate PASS → 활성 claim 0 < maxConcurrentClaims 2 게이트 통과 → T-1376 claim(20:37:09Z) + 같은 commit 으로 lock tombstone release → executor lock-free 수행 → bookkeeping 재-lock. 회로 차단기 4 유형 모두 2회 임계 미만 — 강등 없음.
- 다음: T-1377 (requirements-status-resync 23 번째 slice — REQ-004 수치 지표 + 사용자 지정 기간 LLM 평가 코멘트 상태 재판정).

planner: queued T-1377 — requirements.md 23 행 REQ-004 (수치 지표 + 사용자 지정 기간 LLM 평가 코멘트) 상태를 scoring/period-bridge/narrative 3 축 실측으로 재판정 (commitMode direct)

## 21:52 driver: T-1377 DONE — requirements.md REQ-004 상태 실측 재판정 (PLANNED → IN_PROGRESS)

- fire: `cron@aa-local-7c3d` (매시 30분대 로컬 스케줄), N=1 task. commitMode `direct` (doc-only) — main direct push `6381588d`.
- 결과: `docs/requirements.md` 23 행 REQ-004 (수치 지표 + 사용자 지정 기간 LLM 평가 코멘트) 를 4 축 실측으로 재판정. 수치 축 (`evaluation-scoring.service.ts`) · 기간 축 (`period-bridge.dto.ts` + `POST /api/assessment-evaluation/period` 의 ephemeral / admin-persist 두 경로) · LLM 코멘트 축 (`summary-narrative.service.ts` + `evaluation-prompt.ts`, fallback 포함) · API 노출 축 실재 확인.
- 한계 부기: 프런트 (`DashboardView.tsx`) 렌더 · 기간 지정 UI 부재, 기간 **종료** 경계 입력 부재, summary batch narrative chain 의 HTTP 진입점 0 → "보여주고" 축 미완이라 DONE 이 아닌 IN_PROGRESS 판정.
- CI: run `30719777996` (head `6381588d`) **success 확인 완료** — `gh run watch` 로 conclusion 까지 대기해 R-114 "종료 전 CI 수행" 충족. consecutiveFails 0 유지. blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup fire — lock CAS 획득(tip `a0ce58f9`) → server-time 21:37:11Z 주입 reclaim 회수 0 → validate-claim-candidate PASS → 활성 claim 0 < maxConcurrentClaims 2 통과 → T-1377 claim + 같은 commit 으로 lock tombstone release → executor lock-free 수행 → bookkeeping 재-lock. 회로 차단기 4 유형 모두 2회 임계 미만 — 강등 없음.
- 관측: `select-claim.sh` 에 `CLAIM_NOW` 를 GitHub `Date` 헤더 원문(RFC-2822)으로 주입하면 `claimedAt` 이 그 포맷 그대로 박혀 reclaim 의 ISO 파서가 읽지 못한다(보수적 보존 → orphan 이 영구 잔류할 수 있음). 본 fire 는 `lock_tree_cas_push` 로 ISO 재-stamp 해 해소. 후속으로 script 측 정규화 검토 여지.
- 다음: T-1378 (requirements-status-resync 24 번째 slice — REQ-003 기여 양·질 평가 / 저장 / 표시 상태 재판정).

planner: queued T-1378 — requirements.md 22 행 REQ-003 (기여 양·질 평가 / 저장 / 표시) 상태를 평가·저장·표시·wiring 4 축 실측으로 재판정 (commitMode direct)

## 22:05 driver: T-1378 DONE — requirements.md REQ-003 상태 실측 재판정 (PLANNED → IN_PROGRESS)

- fire: `cron@aa-local-7c3d` — **본 fire 의 두 번째이자 마지막 task** (multiTaskFire chain N=2). commitMode `direct` (doc-only) — main direct push `95f31117`.
- chain 게이트: `flags.multiTaskFire=true` + holder=cron 이중 게이트 통과, CLAUDE.md §2.5 (a)~(e) 재평가 전 조건 충족 — (a) executor 로부터 SUMMARY + trail blob 만 수신, (b) N=2 상한 준수, (c) 직전 task 실패·CI fail·push contention·merge conflict 0, (d) lock 경과 14분 < 45분 임계, (e) direct+direct 동종. 두 번째 commit trail 에 `FIRE-BATCH: T-1377+T-1378` marker 박제 (ADR-0020 Decision (3)).
- 결과: `docs/requirements.md` 22 행 REQ-003 (기여 양·질 평가 / 저장 / 표시) 을 평가·저장·표시·wiring 4 축 실측으로 재판정. 평가 축 (양·질 심볼 분리) · 저장 축 (persist → Prisma model → 컬럼 경로) · 표시 축의 API 응답 shape 실재 확인. 검증 위치 실 근거로 spec 3 종 172 it 실측.
- 한계 부기: 표시 축의 **프런트 렌더 부재** — 직전 slice T-1377 (REQ-004) 과 동일한 미충족 사유라 두 REQ 모두 DONE 이 아닌 IN_PROGRESS 판정.
- CI: run `30720242891` (head `95f31117`) **success 확인 완료** — `gh run watch` 로 conclusion 까지 대기해 R-114 충족. 본 fire 의 4 run (`334be22e`·`6381588d`·`f6800efc`·`95f31117`) 전부 success, consecutiveFails 0 유지. blocker 없음, 열린 humanQuestion 0.
- 동시성: task2 도 claim-pickup 경로 — acquire-lock CAS(tip `f035606c`) → server-time 21:51:14Z 주입 reclaim 회수 0 → validate-claim-candidate PASS → 활성 claim 0 < maxConcurrentClaims 2 → T-1378 claim + lock tombstone release → executor lock-free → bookkeeping 재-lock. 회로 차단기 4 유형 모두 2회 임계 미만 — 강등 없음.
- 다음: T-1379 (requirements-status-resync 25 번째 slice — REQ-043 모든 기능 ID/Password 보호 상태 재판정. 하위 REQ-044/045/046 은 이미 판정 완료인데 상위만 PLANNED 로 남은 표-코드 drift 해소).

planner: queued T-1379 — requirements.md 62 행 REQ-043 (모든 기능 ID/Password 보호) 상태를 인증·적용률·프런트 3 축 실측으로 재판정 (commitMode direct)

## 22:44 driver: T-1379 DONE — requirements.md REQ-043 상태 실측 재판정 (PLANNED → IN_PROGRESS)

- fire: `cron@aa-local-cb281818` (매시 30분대 로컬 스케줄) — **N=1 단일 task**. commitMode `direct` (doc-only) — main direct push `b8a53c03` (+18/-3, 2 파일).
- 결과: `docs/requirements.md` 62 행 REQ-043 (모든 기능 ID/Password 보호) 을 자격증명 · guard wiring · 프런트 진입 차단 · 전 기능 보호 적용률 4 축 실측으로 재판정. 자격증명 축은 login DTO 2 필드 (email + password — README 의 "ID" 가 별도 식별자가 아니라 email 로 구현됨) 와 bcrypt 비교 · JWT 2 종 httpOnly cookie 발급까지 행 인용으로 확정.
- 한계 부기: **적용률 축 미완** — controller 20 개 중 guard 보유 16 개, route 74 개 중 보호 49 개. `APP_GUARD` 전역 등록 0 건이라 미참조 controller 4 개 (`app` · `group` · `part` · `person`) 의 21 route 는 실제 미보호이며, 그중 `group` · `part` · `person` 3 개는 주석이 "후속 task 책임" 으로 미완임을 자인. 상위 REQ-043 만 PLANNED 로 남아 있던 표-코드 drift 해소 (하위 REQ-044/045/046 은 이미 판정 완료).
- CI: run `30721835353` (head `b8a53c03`) push 직후 **in_progress** — R-114 "종료 전 CI 시작" 충족, conclusion 은 다음 fire [1] 단계에서 최우선 재확인. 직전 run `30720550383` (head `cb2881e4`) success, consecutiveFails 0 유지. blocker 없음, 열린 humanQuestion 0.
- 동시성: fineGrainedConcurrency ON(stage 5b) claim-pickup 경로 — acquire-lock CAS(tip `755164107e`) → server-time `2026-08-01T22:37:21Z` (gh api rate_limit `Date` 헤더) 주입 reclaim 회수 0 → 활성 claim 0 < maxConcurrentClaims 2 통과 → validate-claim-candidate PASS → T-1379 claim (`claimedAt` `22:37:29Z` — midnight fallback 없음 확인) + 같은 commit 으로 lock tombstone release → executor lock-free 수행 → bookkeeping 재-lock(tip `3f13fcb6d4`). 회로 차단기 4 유형 (`double-claim`=1 · 나머지 0) 모두 2회 임계 미만 — 강등 없음.
- 관측: `multiTaskFire=true` 이나 executor 소요가 길어 §2.5 (c) 안전측 판단으로 두 번째 task 진입 없이 N=1 종료.
- 다음: T-1380 (requirements-status-resync 26 번째 slice — REQ-008 접근 권한 부족 인식·통지 상태 재판정).

planner: queued T-1380 — requirements.md 27 행 REQ-008 (접근 권한 부족 인식·통지) 상태를 감지 · 기록 · 인식 경로 · 프런트 4 축 실측으로 재판정 (commitMode direct)
