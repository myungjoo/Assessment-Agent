# Journal 2026-06-24

02:06 driver: T-0604 DONE — PR #517 squash merged (5ea0b9f). triggerCollection since 도출을 R-58 backoff variant(deriveSinceWithRecollectionWindow) 로 wiring. reviewer r1 APPROVE + CI green 4-게이트 충족. fineGrainedConcurrency claim path(claim 5ea0b9f… select-claim atomic, server-time 17:06:09Z) — concurrency incident 0.

planner: queued T-0605 — 재수집 정책 R-58 chain manual-trigger windowDays 동적 전달 (P5 PLAN 100행 R-58/REQ-031/REQ-040). T-0604 가 deferred 한 caller-configurable 재수집 window 를 닫는다 — CollectTriggerDto 에 optional `windowDays?` 형식-검증 필드(@IsOptional+@IsInt, 새 dependency 0) 추가 + CollectionTriggerService.triggerCollection 이 deriveSinceWithRecollectionWindow(dto.personId, dto.windowDays) 로 thread. 미전달 시 applyRecollectionWindow 기본 7일 backoff 유지(non-breaking). since-derivation/recollection-window 변경 0(호출만). controller route·api.md doc-sync·scheduling(P7) 은 별도 follow-up. commitMode=pr, R-112 backbone(DTO+service+spec) est ~130 LOC/4 파일, R-112 4종 + negative cases 충분. independentStream: p5-recollection-window, dependsOn [].
