# Journal — 2026-05-30 (KST)

## 00:08 cron driver (KST 00:05 scheduled fire, Anthropic 클라우드 fresh checkout): **T-0096 doc-amend WORK DONE BUT REROUTED via PR (harness branch policy)**. T-0096 task spec `commitMode: direct` (main 직접 push) 이나 본 cron 환경의 harness wrapper 가 `claude/affectionate-babbage-hfhSC` branch 만 push 허용 → feature branch + draft PR 경로로 reroute. doc edits 자체는 T-0096 Acceptance Criteria A~C 정확히 충족: api.md L70 POST /api/users row 응답 description 갱신 ("응답 201 + User row body" → "응답 201 + `UserResponseDto` body (5 readonly 필드 `id` / `email` / `role` / `createdAt` / `updatedAt` — `hashedPassword` 응답 누출 차단, T-0095 박제 — `UserResponseDto.fromEntity(user)` static factory 매핑, private constructor + whitelist 5 필드 securing). defence in depth 2 layer 박제 — DB-level bcrypt 10 rounds (T-0092 박제) + HTTP-layer UserResponseDto whitelist (T-0095 박제), ADR-0008 §6 application-layer last-mile 완결."); api.md L71 PATCH /api/users/:id/role row 응답 description 갱신 ("응답 200 + user body" → "응답 200 + `UserResponseDto` body (5 readonly 필드 ... POST /api/users 응답과 동일 shape)"); modules.md L34 UserModule row 책임 description 에 `+ UserResponseDto (T-0095 박제 — 응답 shape whitelist DTO, private constructor + fromEntity static factory + 5 readonly 필드 — POST signup + PATCH changeRole 두 endpoint 응답 매핑 동일 박제, hashedPassword 응답 누출 차단 application-layer last-mile)` 추가 + follow-up chain "password reset / list / fromEntities 배열 helper (GET /api/users list endpoint 박제 시점) / ClassSerializerInterceptor 도입 ADR 등 후속 endpoint chain 은 follow-up task" 갱신. **grep D1~D9 검증 all pass**: D1 `grep -c "T-0095" docs/architecture/api.md` = 2 (≥2 ✓) / D2 `grep -c "UserResponseDto" docs/architecture/api.md` = 2 (≥2 ✓) / D3 `grep -c "hashedPassword" docs/architecture/api.md` = 2 (≥2 ✓) / D4 `grep -c "defence in depth" docs/architecture/api.md` = 1 (≥1 ✓) / D5 `grep -c "T-0095" docs/architecture/modules.md` = 1 (≥1 ✓) / D6 `grep -c "UserResponseDto" docs/architecture/modules.md` = 1 (≥1 ✓) / D7 `grep -c "fromEntity" docs/architecture/modules.md` = 1 (≥1 ✓) / D8 markdown table row count 유지 (api.md 92 / modules.md 32) / D9 변경 파일 = docs/architecture/api.md + docs/architecture/modules.md 2 파일만 (그 외 src/test/package.json 등 변경 0). **STATE counters / mostRecentTasks / nextTask 변동 0** — T-0096 가 PR 머지 후에야 project state 에서 진정 DONE 판정. STATE 변경은 lastActivity + ci.note 만 (cron fire 박제 + reroute 사유 박제). **harness branch policy reroute 첫 박제** — local Windows /loop env (D:/Assessment-Agent) 의 main 직접 push 정공법과 Anthropic 클라우드 cron 의 feature branch + PR 우회 경로 의 책임 분담 backbone 박제. 다음 cron 또는 local /loop driver 가 PR merge 후 task file frontmatter `status: DONE` + `completedAt` + `actualDiff` + `actualFiles` + counters 94→95 + mostRecentTasks prepend T-0096 + nextTask=null 박제 책임. CI 검증은 본 PR 의 GitHub Actions run 으로 진행 (doc-only path 라 CI guard 작동 시 zero-cost). 잔여 P3 진척 ≈ 95% (T-0095 + T-0096 contract source 정합 박제 path 위).
